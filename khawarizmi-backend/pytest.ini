[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
norecursedirs = manual
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*:UserWarning
markers =
    nightly: tests nécessitant un appel LLM réel (exécutés hors CI, sur schedule)
