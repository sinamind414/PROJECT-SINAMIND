# Configuration Railway pour Khawarizmi Backend
[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
startCommand = "python scripts/precompute_embeddings.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
