comment regle ce probleme j'ai commenc e vce agent mode et me dit The AI took too long to respond. Please try again.


Ce message “The AI took too long to respond. Please try again.” veut dire que l’agent a dépassé le délai de réponse. Ce n’est pas forcément une erreur de ta part. Voici quoi faire :

Réessaie simplement

Clique sur Try again ou renvoie ton message.
Parfois c’est juste un timeout temporaire.
Raccourcis ta demande

Si tu as envoyé une longue consigne, divise-la en plusieurs étapes.
Exemple : au lieu de “crée toute une app complète”, commence par “crée la structure du projet”.
Évite les gros fichiers au début

Si tu as joint plusieurs fichiers lourds, commence avec un seul fichier.
Ou explique d’abord ce que tu veux faire, puis ajoute les fichiers ensuite.
Rafraîchis la page

Recharge l’onglet Arena.ai.
Puis relance Agent Mode.
Vérifie ta connexion

Une connexion instable peut provoquer ce timeout.
Essaie aussi depuis un autre réseau si possible.
Change de navigateur

Essaie avec Chrome, Edge, Firefox ou Safari.
Désactive temporairement les extensions type bloqueur de pub/VPN si tu en utilises.
Vide le cache du site

Déconnecte-toi/reconnecte-toi.
Ou ouvre Arena.ai en navigation privée.
Formule une demande plus directe
Par exemple :

“Analyse ce projet et dis-moi seulement les 3 premières étapes à faire.”

Puis ensuite :

“Fais l’étape 1.”

Si ça continue

Le service peut être surchargé.
Attends quelques minutes et réessaie.
Si le problème revient souvent, contacte le support Arena.ai avec une capture du message.
Tu peux aussi me copier ici exactement ce que tu avais demandé à l’agent, et je peux t’aider à reformuler la requête pour éviter le timeout.


https://www.dropbox.com/scl/fo/a90lv21vuoldsnr4vw93b/AKbS74EfqgxywhSQc1yLz_8?rlkey=4j8pcslciwd9b7na9bq1ck83d&st=w4zzrhu9&dl=0


Fetched https://www.dropbox.com/scl/fo/a90lv21vuoldsnr4vw93b/AKbS74EfqgxywhSQc1yLz_8?rlkey=4j8pcslciwd9b7na9bq1ck83d&st=w4zzrhu9&dl=0
J’ai ouvert le lien : c’est un dossier Dropbox nommé workspace-019ee520... qui contient au moins un dossier annales_svt.

Si ton problème est que Agent Mode bloque avec “The AI took too long to respond”, c’est probablement parce qu’il essaie de lire/analyser trop de fichiers depuis Dropbox.

Essaie comme ça :

Télécharge le dossier Dropbox en ZIP

Clique sur Download dans Dropbox.
Récupère le fichier .zip.
Ne donne pas seulement le lien Dropbox à l’agent

Les liens Dropbox peuvent être lents ou difficiles à parcourir automatiquement.
Mieux : téléverse directement le .zip ou quelques fichiers importants.
Découpe la demande
Au lieu de demander :

“Analyse tout le dossier annales_svt”

Demande d’abord :

“Regarde la structure du dossier et dis-moi quels fichiers sont importants.”

Puis :

“Analyse seulement ces 3 fichiers.”

Si le dossier contient beaucoup de PDF/images

Envoie seulement 2 ou 3 fichiers à la fois.
Les gros PDF ou scans peuvent faire dépasser le temps de réponse.
Tu peux me dire ce que tu veux faire avec ce dossier annales_svt :

résumer les annales ?
créer des fiches de révision ?
extraire les exercices ?
faire une app/site avec les documents ?
organiser les fichiers ?

télécharge dan ston workspace



Ran commands
4
C’est fait ✅

J’ai téléchargé le dossier Dropbox dans le workspace ici :

downloads/annales_svt

Taille : environ 89 MB

Il contient notamment :

README.md
MANIFESTE.md
input/
KHELIFA1_VOLUME_01.pdf
KHELIFA1_VOLUME_02.pdf
KHELIFA1_VOLUME_03.pdf
KHELIFA1_VOLUME_04.pdf
plusieurs fichiers .ocr.txt
output/questions_taggees.json
referentiel/micro_concepts.json
scripts/ocr_pipeline.py
scripts/validate_tags.py
J’ai supprimé le fichier ZIP après extraction pour éviter de dépasser la limite du workspace.


Vous avez partagé un dossier Dropbox. Je tente d'y accéder — Dropbox est généralement plus accessible que Google Drive :

Dropbox est plus accessible que Google Drive ! Je peux voir la structure. Il y a 4 sous-dossiers + un fichier prompt annales.txt* (5.83 KB). Je vais explorer les sous-dossiers et récupérer le fichier texte en parallèle :

Excellente nouvelle ! Dropbox fonctionne beaucoup mieux que Google Drive. J'ai pu :

✅ Télécharger prompt annales.txt — c'est le prompt de mission (déjà pris en compte).

✅ Découvrir la structure complète avec sous-dossiers VOLUMES_*.

Je descends maintenant dans les sous-dossiers VOLUMES_* pour trouver les fichiers réels :

J'ai maintenant la cartographie complète du dossier. Dropbox est accessible ! Voici l'inventaire :

| Dossier | Fichiers PDF |

|---|---|

| FINAL BAC | 6 volumes (18-20 MB chacun) |

| KHELIFA 1 | 12+ volumes (20-23 MB) |

| KHELIFA 2 | 12+ volumes (15-18 MB) |

| SCIENCES MORAFIK | 3 volumes (14-21 MB) |

Total estimé : ~33+ fichiers, ~600 MB. C'est massif. Le test critique maintenant : puis-je télécharger et lire le contenu des PDF ? Je teste le téléchargement d'un volume FINAL BAC :

Téléchargement réussi ! 19 MB, 40 pages. Je vérifie maintenant si le PDF contient du texte extractible ou des images scannées :

Aucun outil PDF installé. J'installe PyMuPDF (le plus performant pour extraire texte ET images) :

PyMuPDF installé. J'analyse maintenant le PDF : est-il textuel ou scanné (images) ?

Confirmation : les PDF sont des images scannées (1 image par page, aucun texte extractible). C'est typique des annales.

La solution : convertir les pages en images, puis les lire visuellement. Je teste sur les premières pages :

Maintenant je lis les pages pour vérifier la qualité et la lisibilité du contenu :

Les images ne sont pas lisibles par vision directe. Je teste l'OCR avec Tesseract (support arabe) comme alternative :

Tesseract + arabe (ara) + anglais (eng) installés ! Je teste l'OCR sur les premières pages :

L'OCR fonctionne ! Je peux extraire l'arabe des scans. La page 2 est une table des matières parfaitement reconnaissable :

> الوحدة الأولى : تركيب البروتين (Unité 1 : Synthèse des protéines)

> الوحدة الثانية : العلاقة بين بنية البروتين و وظيفته (Structure-fonction)

> الوحدة الثالثة : النشاط الإنزيمي للبروتينات (Activité enzymatique)

> الوحدة الرابعة : دور البروتينات في الدفاع عن الذات (Immunité)

> الوحدة الخامسة : دور البروتينات في الإتصال العصبي (Neurotransmission)

Le pipeline complet fonctionne : Dropbox → PDF → Image → OCR arabe. Je vais maintenant examiner le contenu des pages de questions et construire le pipeline automatisé. D'abord, regardons quelques pages de contenu :

L'OCR produit des résultats excellents pour l'arabe ! Je peux lire clairement le contenu du cours (acides nucléiques, nucléotides, bases azotées, etc.).

Le pipeline complet fonctionne : Dropbox → curl → PDF → PyMuPDF → Image → Tesseract ara+eng → texte.

Maintenant je construis le pipeline automatisé complet pour traiter efficacement un volume entier, puis je l'exécute sur le premier volume :

Maintenant j'exécute le pipeline OCR sur tout le volume 1 (40 pages) :

OCR terminé pour les 40 pages (71136 caractères). Je lis maintenant le résultat pour identifier les questions et comprendre la structure du contenu :

J'ai maintenant le contenu complet de FINALBAC_VOLUME_1 (40 pages). Voici mon analyse critique :

FINALBAC_VOLUME_1 est un livre de COURS (résumé théorique), pas un recueil de questions d'examen. Il couvre :

Pages 5-14 : Unité 1 — Synthèse des protéines (ADN, ARN, transcription, traduction, code génétique…)

Pages 15-19 : Unité 2 — Structure-fonction des protéines

Pages 20-22 : Unité 3 — Activité enzymatique

Pages 23-39 : Unité 4 — Immunité (anticorps, lymphocytes, VIH/SIDA…)

Page 40 : Unité 5 — Communication nerveuse (début)

C'est un excellent matériel de référence pour le vocabulaire scientifique arabe, mais il ne contient pas de questions d'annales à tagger. Les sujets d'examen sont probablement dans d'autres volumes. Je vais vérifier rapidement d'autres volumes pour localiser les vraies questions :

Trois volumes téléchargés. J'OCR les premières pages de chacun pour identifier où sont les questions d'annales :

Découverte clé ! J'ai identifié où se trouvent les vraies questions :

| Volume | Type de contenu |

|---|---|

| FINALBAC_VOLUME_3 | ✅ Sujets d'examen (مواضيع نموذجية مقترحة لامتحان البكالوريا) |

| FINALBAC_VOLUME_1 | Cours théorique (résumés) |

| KHELIFA1_VOLUME_01 | Livre d'exercices |

| MORAFIK_VOLUME_01 | Cours + exercices + sujets (BAC 2020) |

FINALBAC_VOLUME_3 contient les vraies questions d'annales ! Je peux déjà voir des sujets complets : photosynthèse, synthèse des protéines, immunité, système nerveux. Je vais maintenant OCRiser le volume complet et extraire les questions. Je lance le traitement :

OCR complet ! Je dois maintenant combiner les pages 1-5 (déjà OCRisées précédemment) avec les pages 6-40. Je relis le fichier complet et combine :

Maintenant j'ai les 40 pages complètes de FINALBAC_VOLUME_3 (69427 caractères). Je lis tout le contenu pour identifier et extraire chaque question :

J'ai maintenant le contenu complet de FINALBAC_VOLUME_3 (40 pages). C'est un recueil d'environ 20 sujets d'examen modèle couvrant tous les domaines du programme SVT. L'OCR est de bonne qualité mais nécessite correction.

Je vais maintenant traiter les questions : transcription corrigée + tagging. Je commence par un échantillon représentatif pour valider la qualité, puis je continuerai avec le reste. Je crée le JSON avec les premières questions traitées :

50 questions traitées ! Je lance la validation automatique :

✅ Validation parfaite : 0 erreur, 0 avertissement ! Voici la distribution des 50 questions traitées :

text


Domaine              Questions  Concepts couverts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌿 Photosynthèse        9      photo_01, 02, 03

🧬 Synthèse protéines   8      prot_01, 02, 03

🛡️ Immunité            12      imm_01, 03, 04, 05

⚡ Système nerveux       9      nerv_01, 02, 03, 04

⚗️ Enzymes              4      enz_01, 02, 03, 05

📐 Structures            4      struc_01, 03, 05

💨 Respiration           3      resp_01, 05

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TOTAL                  50      23 micro-concepts distincts
Je nettoie le workspace et crée un manifeste de suivi :

📊 Récapitulatif complet
✅ Ce qui a été accompli
Pipeline technique complet validé de bout en bout :

text


Dropbox → curl → PDF → PyMuPDF (200 DPI) → Tesseract ara+eng → OCR → Correction arabe → Tagging (42 concepts) → Validation
50 questions traitées à partir de FINALBAC_VOLUME_3 (sujets d'examen modèles), couvrant 23 micro-concepts sur les 42. Le JSON de sortie est dans annales_svt/output/questions_taggees.json et a passé la validation avec 0 erreur.

🔍 Observations importantes
FINALBAC_VOLUME_1 = cours théorique (40 pages) → précieux comme référence terminologique mais pas de questions à tagger.

FINALBAC_VOLUME_3 = sujets d'examen modèles (40 pages, ~20 sujets) → c'est ici que se trouvent les vraies questions d'annales.

KHELIFA et MORAFIK = probablement un mélange de cours + exercices + sujets (à confirmer en traitant les volumes).

📋 Prochaines étapes proposées
Le volume restant est massif : ~33 PDFs × ~40 pages. Voici comment je propose de procéder :

Parfait ! Vos choix sont clairs :

✅ Qualité validée → poursuivre

✅ Granularité : par question numérotée

✅ Priorité : tout traiter dans l'ordre (FINALBAC → KHELIFA 1 → KHELIFA 2 → MORAFIK)

Je continue avec les volumes restants. Je télécharge les 4 derniers volumes FINALBAC et vérifie leur type de contenu :

Tous téléchargés (40, 40, 40, 39 pages). Je vérifie le type de contenu des premiers pages de chaque volume :

Voici l'analyse des 4 volumes :

| Volume | Type détecté |

|---|---|

| Vol 2 | 📖 Cours théorique (suite du Vol 1 — système nerveux) |

| Vol 4 | ❓ Probablement sujets d'examen (à confirmer) |

| Vol 5 | ✅ Corrigés/Solutions (تحليل وتفسير) |

| Vol 6 | ✅ Corrigés/Solutions (مواضيع نموذجية + إجابات) |

Les Vols 5 et 6 sont des corrigés qui contiennent les questions auxquelles ils répondent. Je vais OCRiser le Vol 4 entièrement (s'il contient des questions) et examiner plus en détail les Vols 5-6 pour extraire les questions référencées. Je lance le traitement complet du Vol 4 :

FINALBAC_VOLUME_4 contient des sujets d'examen ET leurs corrigés. J'y trouve un nouveau domaine non couvert : la tectonique des plaques (mc_tec_*) + des questions sur la chaîne respiratoire et le cycle de Krebs. Je vais extraire ces nouvelles questions et les ajouter au JSON. Focus sur les concepts encore manquants :

80 questions au total. Je valide :

Une erreur : le champ difficulte est mal nommé pour la question 67. Je corrige :

✅ 80 questions validées — 0 erreur, 0 avertissement ! 30 micro-concepts couverts.

Je mets à jour le manifeste et fais le point :

📊 Bilan de la session
80 questions traitées et validées à partir de FINALBAC Volumes 3 et 4, couvrant 30 des 42 micro-concepts :

text


Synthèse protéines      9  █████████

Structures              8  ████████

Enzymes                 9  █████████

Immunité               17  █████████████████

Système nerveux        12  ████████████

Photosynthèse          13  █████████████

Respiration             7  ████████

Tectonique              5  █████

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TOTAL                  80  (0 erreur de validation)
🗂️ Structure du workspace
text


annales_svt/

├── output/

│   └── questions_taggees.json    ← 80 questions (JSON validé)

├── input/

│   ├── FINALBAC_VOLUME_1.ocr.txt ← cours (référence)

│   ├── FINALBAC_VOLUME_3.ocr.txt ← sujets d'examen

│   └── FINALBAC_VOLUME_4.ocr.txt ← sujets + corrigés

├── referentiel/

│   └── micro_concepts.json       ← les 42 concepts

├── scripts/

│   ├── ocr_[pipeline.py](http://pipeline.py)           ← PDF → OCR ara+eng

│   └── validate_[tags.py](http://tags.py)          ← validation automatique

└── [MANIFESTE.md](http://MANIFESTE.md)                  ← suivi d'avancement
📋 Prochaines étapes
Il reste à traiter : ~25 volumes KHELIFA + 3 volumes MORAFIK (~1000 pages, estimation ~400-500 questions supplémentaires). Le pipeline est entièrement opérationnel — dites-moi simplement « continue » et je traiterai les volumes suivants par lots (KHELIFA 1 → KHELIFA 2 → MORAFIK).



Explored
2 reads
Read
MANIFESTE.md
Read
README.md
Oui, j’ai bien compris : tu m’as copié le compte-rendu d’un précédent Agent Mode qui avait déjà avancé sur le projet.

De mon côté, dans mon workspace actuel, j’ai bien le dossier :

downloads/annales_svt

Et j’ai vérifié que l’état correspond au résumé :

✅ output/questions_taggees.json existe
✅ Il contient 80 questions
✅ Validation relancée : 0 erreur, 0 avertissement
✅ 30 micro-concepts utilisés sur 42
✅ Le manifeste indique bien : FINALBAC volumes 3 et 4 traités

Le rapport de validation confirme :

text

Total questions : 80
Concepts distincts utilisés : 30
Erreurs : 0
Avertissements : 0
Par contre, précision importante : dans le workspace que j’ai téléchargé ici, je vois seulement ces PDF présents :

text

KHELIFA1_VOLUME_01.pdf
KHELIFA1_VOLUME_02.pdf
KHELIFA1_VOLUME_03.pdf
KHELIFA1_VOLUME_04.pdf
Les autres volumes mentionnés dans ton message, par exemple KHELIFA 1 Vol 05-12, KHELIFA 2, MORAFIK Vol 2-3, ne sont pas présents en PDF dans ce workspace actuel. Il y a cependant déjà plusieurs fichiers OCR .txt.

Donc maintenant on peut faire une des deux choses :

Continuer avec les fichiers déjà présents

Traiter KHELIFA1_VOLUME_01.pdf à KHELIFA1_VOLUME_04.pdf
Extraire les questions
Ajouter au JSON existant
Valider
Télécharger le dossier Dropbox complet original

Si le lien Dropbox contient encore tous les volumes
Je peux récupérer les PDFs manquants par lots
Puis continuer dans l’ordre : FINALBAC → KHELIFA 1 → KHELIFA 2 → MORAFIK
Vu ton objectif, la prochaine étape logique est :

traiter les volumes KHELIFA1_VOLUME_01 à 04 déjà présents dans input/, puis enrichir questions_taggees.json.

Si tu veux, dis simplement : continue avec KHELIFA1.


https://www.dropbox.com/scl/fo/m0lfrgjfc2qwm7wpf9bi5/AGeavvjhzRPAaX3SQ5LalBk?rlkey=53hhohf694y48jbe5w4hs1ktf&st=7u5vpk78&dl=0



Searched the web
Write
downloads/dropbox_full_inventory/INVENTAIRE_DOSSIER_ANNALES_KHELIFA.md
65 lines
open
Inventaire — DOSSIER ANNALES KHELIFA
MD
Inventaire — DOSSIER ANNALES KHELIFA
Lien source : https://www.dropbox.com/scl/fo/m0lfrgjfc2qwm7wpf9bi5/AGeavvjhzRPAaX3SQ5LalBk?rlkey=53hhohf694y48jbe5w4hs1ktf&st=7u5vpk78&dl=0

Racine
FINAL BAC/
KHELIFA 1/
KHELIFA 2/
SCIENCES MORAFIK/
prompt annales.txt — téléchargé dans ce dossier
FINAL BAC / VOLUMES_FINALBAC
FINALBAC_VOLUME_1.pdf — 18.92 MB
FINALBAC_VOLUME_2.pdf — 20.15 MB
FINALBAC_VOLUME_3.pdf — 18.5 MB
FINALBAC_VOLUME_4.pdf — 18.92 MB
FINALBAC_VOLUME_5.pdf — 18.03 MB
FINALBAC_VOLUME_6.pdf — 18.18 MB
KHELIFA 1 / VOLUMES_KHELIFA1
KHELIFA1_VOLUME_01.pdf — 22.59 MB
KHELIFA1_VOLUME_02.pdf — 23.02 MB
KHELIFA1_VOLUME_03.pdf — 20.61 MB
KHELIFA1_VOLUME_04.pdf — 21.31 MB
KHELIFA1_VOLUME_05.pdf — 21.09 MB
KHELIFA1_VOLUME_06.pdf — 21.03 MB
KHELIFA1_VOLUME_07.pdf — 20.06 MB
KHELIFA1_VOLUME_08.pdf — 20.38 MB
KHELIFA1_VOLUME_09.pdf — 22.63 MB
KHELIFA1_VOLUME_10.pdf — 22.72 MB
KHELIFA1_VOLUME_11.pdf — 22.39 MB
KHELIFA1_VOLUME_12.pdf — 21.04 MB
KHELIFA1_VOLUME_13.pdf — 20.89 MB
KHELIFA1_VOLUME_14.pdf — 21.36 MB
KHELIFA1_VOLUME_15.pdf — 13.56 MB
KHELIFA 2 / VOLUMES_KHELIFA2
KHELIFA2_VOLUME_01.pdf — 15.39 MB
KHELIFA2_VOLUME_02.pdf — 17.84 MB
KHELIFA2_VOLUME_03.pdf — 18.1 MB
KHELIFA2_VOLUME_04.pdf — 17.19 MB
KHELIFA2_VOLUME_05.pdf — 16.54 MB
KHELIFA2_VOLUME_06.pdf — 17.3 MB
KHELIFA2_VOLUME_07.pdf — 16.05 MB
KHELIFA2_VOLUME_08.pdf — 16.71 MB
KHELIFA2_VOLUME_09.pdf — 17.26 MB
KHELIFA2_VOLUME_10.pdf — 17.75 MB
KHELIFA2_VOLUME_11.pdf — 15.4 MB
KHELIFA2_VOLUME_12.pdf — 15.81 MB
KHELIFA2_VOLUME_13.pdf — 16.55 MB
KHELIFA2_VOLUME_14.pdf — 16.83 MB
KHELIFA2_VOLUME_15.pdf — 13.56 MB
SCIENCES MORAFIK
VOLUMES_MORAFIK
MORAFIK_VOLUME_1.pdf — 19.75 MB
MORAFIK_VOLUME_2.pdf — 20.74 MB
MORAFIK_VOLUME_3.pdf — 14.28 MB
ANNALES_SVT
MORAFIK_VOLUME_3.pdf — 14.28 MB
Note opérationnelle
Le dossier complet pèse environ 700 MB. Pour éviter les timeouts et la limite de snapshot du workspace, il vaut mieux traiter par lots : télécharger 1 à 3 PDF, OCR/extraction, écrire le JSON, puis supprimer les PDF temporaires si nécessaire.

Open
J’ai bien ouvert le nouveau lien Dropbox ✅

Il correspond au dossier complet :

DOSSIER ANNALES KHELIFA

J’ai pu voir la structure complète :

text

DOSSIER ANNALES KHELIFA/
├── FINAL BAC/
│   └── VOLUMES_FINALBAC/
│       ├── FINALBAC_VOLUME_1.pdf
│       ├── FINALBAC_VOLUME_2.pdf
│       ├── FINALBAC_VOLUME_3.pdf
│       ├── FINALBAC_VOLUME_4.pdf
│       ├── FINALBAC_VOLUME_5.pdf
│       └── FINALBAC_VOLUME_6.pdf
├── KHELIFA 1/
│   └── VOLUMES_KHELIFA1/
│       ├── KHELIFA1_VOLUME_01.pdf
│       ├── ...
│       └── KHELIFA1_VOLUME_15.pdf
├── KHELIFA 2/
│   └── VOLUMES_KHELIFA2/
│       ├── KHELIFA2_VOLUME_01.pdf
│       ├── ...
│       └── KHELIFA2_VOLUME_15.pdf
├── SCIENCES MORAFIK/
│   ├── VOLUMES_MORAFIK/
│   │   ├── MORAFIK_VOLUME_1.pdf
│   │   ├── MORAFIK_VOLUME_2.pdf
│   │   └── MORAFIK_VOLUME_3.pdf
│   └── ANNALES_SVT/
│       └── MORAFIK_VOLUME_3.pdf
└── prompt annales.txt
J’ai aussi téléchargé le fichier :


prompt annales.txt

Et j’ai créé un inventaire ici :


INVENTAIRE_DOSSIER_ANNALES_KHELIFA.md

⚠️ Le dossier complet fait environ 700 MB. Je ne vais pas tout télécharger d’un coup, sinon on risque :

timeout Agent Mode,
workspace trop lourd,
fichiers non sauvegardés correctement.
La bonne méthode est de traiter par lots.

Prochaine étape logique :

Continuer avec KHELIFA 1
Télécharger quelques volumes à la fois
OCR
Extraire les questions
Ajouter dans 
questions_taggees.json
Valider
Dans le workspace actuel, on a déjà :

text

KHELIFA1_VOLUME_01.pdf
KHELIFA1_VOLUME_02.pdf
KHELIFA1_VOLUME_03.pdf
KHELIFA1_VOLUME_04.pdf
Donc je peux commencer par traiter KHELIFA1 volumes 01 à 04 déjà présents, puis télécharger les volumes suivants après.


ok phase par phase je suis d'accord



Explored
2 reads
Read
ocr_pipeline.py
Read
micro_concepts.json

Ran commands
5
Write
downloads/annales_svt/scripts/ocr_pipeline_robust.py
84 lines
open
ocr_pipeline_robust.py
PYTHON
#!/usr/bin/env python3
"""
Pipeline OCR robuste pour PDF scannés arabe/français.
- PyMuPDF -> PNG à DPI contrôlé
- Tesseract ara+eng
- Timeout configurable
- Peut reprendre par plage de pages

Usage:
  python scripts/ocr_pipeline_robust.py input/KHELIFA1_VOLUME_01.pdf --dpi 180 --timeout 120
"""
import argparse
import os
import subprocess
import tempfile
from pathlib import Path

import fitz


def ocr_pdf(pdf_path: str, dpi: int = 180, start: int = 1, end: int | None = None,
            timeout: int = 120, psm: int = 3, suffix: str = ".ocr.txt") -> str:
    pdf = Path(pdf_path)
    doc = fitz.open(str(pdf))
    total = doc.page_count
    if end is None or end > total:
        end = total
    start = max(1, start)
    out_path = pdf.with_suffix(suffix)
    mat = fitz.Matrix(dpi / 72, dpi / 72)

    parts = []
    errors = []
    for page_no in range(start, end + 1):
        page = doc[page_no - 1]
        pix = page.get_pixmap(matrix=mat, alpha=False)
        tmp_path = None
        try:
            with tempfile.NamedTemporaryFile(suffix=".png", delete=False) as tmp:
                tmp_path = tmp.name
            pix.save(tmp_path)
            result = subprocess.run(
                ["tesseract", tmp_path, "-", "-l", "ara+eng", "--psm", str(psm)],
                capture_output=True,
                text=True,
                timeout=timeout,
            )
            text = result.stdout.strip()
            if result.returncode != 0:
                errors.append((page_no, result.stderr.strip()[:300]))
        except Exception as e:
            text = f"[OCR ERROR: {e}]"
            errors.append((page_no, str(e)))
        finally:
            if tmp_path and os.path.exists(tmp_path):
                os.unlink(tmp_path)

        header = f"\n{'='*70}\nPAGE {page_no}\n{'='*70}\n"
        parts.append(header + text)
        print(f"Page {page_no}/{end}: {len(text)} chars")

    doc.close()
    full = "\n".join(parts)
    out_path.write_text(full, encoding="utf-8")
    print(f"\nOCR terminé: {out_path} ({len(full)} chars)")
    if errors:
        print(f"Avertissements OCR: {len(errors)} page(s)")
        for p, err in errors[:10]:
            print(f"- page {p}: {err}")
    return str(out_path)


if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("pdf_path")
    ap.add_argument("--dpi", type=int, default=180)
    ap.add_argument("--start", type=int, default=1)
    ap.add_argument("--end", type=int, default=None)
    ap.add_argument("--timeout", type=int, default=120)
    ap.add_argument("--psm", type=int, default=3)
    ap.add_argument("--suffix", default=".ocr.txt")
    args = ap.parse_args()
    ocr_pdf(args.pdf_path, args.dpi, args.start, args.end, args.timeout, args.psm, args.suffix)
Open

Ran commands
2
Parfait. On avance phase par phase pour éviter les timeouts.

✅ Phase 1 terminée — Préparation + OCR du premier volume
J’ai fait :

Réinstallé les outils OCR nécessaires

PyMuPDF
Tesseract
langues OCR : ara + eng
Créé un script OCR plus robuste

Fichier créé :

text

downloads/annales_svt/scripts/ocr_pipeline_robust.py
Il gère mieux :

les gros scans,
les timeouts,
les pages lentes,
l’arabe + anglais.
OCR complet de KHELIFA1_VOLUME_01.pdf

PDF traité :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.pdf
Résultat OCR complet :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_full.txt
Résultat :

28 pages traitées
environ 50 KB de texte OCR
l’ancien OCR était partiel, donc je n’ai pas écrasé le fichier original ; j’ai créé un fichier séparé .ocr_full.txt.
Observation rapide
KHELIFA1_VOLUME_01 contient surtout des exercices sur :

ADN / ARN
transcription
traduction
code génétique
mutation
relation gène → protéine → caractère
synthèse des protéines
Donc il correspond principalement aux micro-concepts :

text

mc_prot_01  Transcription de l'ADN
mc_prot_02  Traduction de l'ARNm
mc_prot_03  Code génétique
mc_prot_04  ARNm
mc_prot_05  ARNt
mc_prot_06  Ribosome
mc_prot_07  Initiation de la traduction
mc_prot_08  Élongation et terminaison
mc_struc_01 Structure primaire
mc_struc_05 Relation structure-fonction
Prochaine phase
Phase 2 — Extraction des questions de KHELIFA1_VOLUME_01
Je vais maintenant :

découper le texte OCR en questions/exercices,
corriger la transcription arabe,
préparer les entrées JSON,
ne pas encore tagger définitivement avant vérification.
Ensuite, en Phase 3, je ferai :

tagging micro-concepts,
ajout dans questions_taggees.json,
validation automatique.
Dis-moi simplement : continue phase 2.


please essaie ce script #!/usr/bin/env python3
"""
Pipeline OCR robuste — arabe (principal) + français.
Supporte : PDF scannés, images (PNG, JPG, TIFF, BMP, WEBP).

Corrections vs version précédente :

ara+fra au lieu de ara+eng
300 DPI par défaut
Pré-traitement image (deskew, binarisation, débruitage)
Vrai mode reprise (append + pages déjà traitées ignorées)
Erreurs OCR marquées dans l'output
Parallélisation (ProcessPoolExecutor)
Vérification des prérequis au démarrage
Context manager pour fitz
Support images directes (PNG, JPG, TIFF, BMP, WEBP)
OEM explicite
Compatible Python 3.8+
Usage PDF :
python ocr_pipeline.py input/doc.pdf --dpi 300 --workers 4

Usage image unique :
python ocr_pipeline.py input/scan.jpg

Usage dossier d'images :
python ocr_pipeline.py input/scans/ --workers 4

Reprise (pages déjà faites ignorées) :
python ocr_pipeline.py input/doc.pdf --resume
"""

import argparse
import logging
import os
import re
import shutil
import subprocess
import sys
import tempfile
from concurrent.futures import ProcessPoolExecutor, as_completed
from pathlib import Path
from typing import List, Optional, Tuple

import cv2
import numpy as np

---------------------------------------------------------------------------
Logging
---------------------------------------------------------------------------
logging.basicConfig(
level=logging.INFO,
format="%(asctime)s [%(levelname)s] %(message)s",
datefmt="%H:%M:%S",
)
log = logging.getLogger(name)

---------------------------------------------------------------------------
Vérification des prérequis (une seule fois au démarrage)
---------------------------------------------------------------------------
def check_requirements() -> None:
"""Vérifie que tesseract est accessible et que les langues ara/fra sont installées."""
if not shutil.which("tesseract"):
log.error("Tesseract introuvable dans le PATH. Installez-le : apt install tesseract-ocr")
sys.exit(1)

text

result = [subprocess.run](http://subprocess.run)(  
    ["tesseract", "--list-langs"],  
    capture_output=True, text=True  
)  
installed = result.stdout + result.stderr  
missing = [lang for lang in ("ara", "fra") if lang not in installed]  
if missing:  
    log.error(  
        "Langues Tesseract manquantes : %s\n"  
        "  Ubuntu/Debian : apt install %s",  
        missing,  
        " ".join(f"tesseract-ocr-{l}" for l in missing),  
    )  
    sys.exit(1)  
---------------------------------------------------------------------------
Pré-traitement image
---------------------------------------------------------------------------
def deskew(image: np.ndarray) -> np.ndarray:
"""Corrige l'inclinaison du scan via la transformée de Hough."""
gray = cv2.cvtColor(image, cv2.COLORBGR2GRAY) if image.ndim == 3 else image
edges = cv2.Canny(gray, 50, 150, apertureSize=3)
lines = cv2.HoughLinesP(edges, 1, np.pi / 180, threshold=100,
minLineLength=100, maxLineGap=10)
if lines is None:
return image

text

angles = []  
for line in lines:  
    x1, y1, x2, y2 = line[0]  
    if x2 != x1:  
        angles.append(np.degrees(np.arctan2(y2 - y1, x2 - x1)))  

if not angles:  
    return image  

median_angle = np.median(angles)  
if abs(median_angle) &lt; 0.5:   # Inclinaison négligeable  
    return image  

h, w = image.shape[:2]  
M = cv2.getRotationMatrix2D((w / 2, h / 2), median_angle, 1.0)  
return cv2.warpAffine(image, M, (w, h),  
                      flags=cv2.INTER_LINEAR,  
                      borderMode=cv2.BORDER_REPLICATE)  
def preprocess_image(img_array: np.ndarray) -> np.ndarray:
"""
Pipeline de pré-traitement pour OCR arabe/français sur scans.
Ordre : deskew → niveaux de gris → débruitage → binarisation adaptative.
"""
# 1. Correction d'inclinaison
img = deskew(imgarray)

text

# 2. Niveaux de gris  
if img.ndim == 3:  
    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)  
else:  
    gray = img  

# 3. Débruitage non-local (conserve bien les fins détails arabes)  
denoised = cv2.fastNlMeansDenoising(gray, h=10, templateWindowSize=7, searchWindowSize=21)  

# 4. Binarisation adaptative (meilleure que Otsu sur scans non uniformes)  
binary = cv2.adaptiveThreshold(  
    denoised, 255,  
    cv2.ADAPTIVE_THRESH_GAUSSIAN_C,  
    cv2.THRESH_BINARY,  
    blockSize=31,   # Large bloc : bon pour les variations d'éclairage sur scans  
    C=10,  
)  

# 5. Légère érosion pour renforcer les liaisons de l'écriture arabe cursive  
kernel = np.ones((1, 1), np.uint8)  
processed = cv2.erode(binary, kernel, iterations=1)  

return processed  
---------------------------------------------------------------------------
OCR d'une page (worker — appelé depuis les sous-processus)
---------------------------------------------------------------------------
def ocrsingle(args: Tuple) -> Tuple[int, str, Optional[str]]:
"""
Worker isolé pour ProcessPoolExecutor.
Retourne (page_no, text, error_or_None).
"""
page_no, img_array, psm, oem, timeout = args
error = None
text = ""
tmp_path = None

text

try:  
    processed = preprocess_image(img_array)  

    fd, tmp_path = tempfile.mkstemp(suffix=".png")  
    os.close(fd)  
    cv2.imwrite(tmp_path, processed)  

    result = [subprocess.run](http://subprocess.run)(  
        [  
            "tesseract", tmp_path, "-",  
            "-l", "ara+fra",  
            "--psm", str(psm),  
            "--oem", str(oem),  
            "-c", "preserve_interword_spaces=1",   # Crucial pour l'arabe  
            "-c", "textord_arabic_right_to_left=1",  
        ],  
        capture_output=True,  
        text=True,  
        timeout=timeout,  
    )  
    text = result.stdout.strip()  
    if result.returncode != 0:  
        error = result.stderr.strip()[:400]  
        text = f"[TESSERACT ERROR page {page_no}: {error}]"  

except subprocess.TimeoutExpired:  
    error = f"Timeout ({timeout}s) dépassé"  
    text = f"[TIMEOUT ERROR page {page_no}]"  
except Exception as e:  
    error = str(e)  
    text = f"[OCR ERROR page {page_no}: {e}]"  
finally:  
    if tmp_path and os.path.exists(tmp_path):  
        os.unlink(tmp_path)  

return page_no, text, error  
---------------------------------------------------------------------------
Extraction des numéros de pages déjà traitées (mode reprise)
---------------------------------------------------------------------------
def alreadydone_pages(out_path: Path) -> set:
"""Parse le fichier de sortie existant pour trouver les pages déjà traitées."""
if not out_path.exists():
return set()
pattern = re.compile(r"^={10,}\nPAGE (\d+)\n={10,}", re.MULTILINE)
content = out_path.read_text(encoding="utf-8", errors="ignore")
return {int(m.group(1)) for m in pattern.finditer(content)}

---------------------------------------------------------------------------
Conversion source → liste de (page_no, np.ndarray)
---------------------------------------------------------------------------
IMAGE_EXTENSIONS = {".png", ".jpg", ".jpeg", ".tiff", ".tif", ".bmp", ".webp"}

def loadpages(source: Path, dpi: int, start: int, end: Optional[int]) -> List[Tuple[int, np.ndarray]]:
"""
Retourne une liste ordonnée de (page_no, img_array BGR).
Gère : PDF, image unique, dossier d'images.
"""
pages = []

text

if [source.is](http://source.is)_dir():  
    # Dossier d'images — trié alphabétiquement  
    files = sorted(  
        f for f in source.iterdir()  
        if f.suffix.lower() in IMAGE_EXTENSIONS  
    )  
    for idx, f in enumerate(files, start=1):  
        img = cv2.imread(str(f))  
        if img is not None:  
            pages.append((idx, img))  
        else:  
            log.warning("Image illisible ignorée : %s", [f.name](http://f.name))  

elif source.suffix.lower() in IMAGE_EXTENSIONS:  
    # Image unique  
    img = cv2.imread(str(source))  
    if img is None:  
        log.error("Impossible de lire l'image : %s", source)  
        sys.exit(1)  
    pages.append((1, img))  

else:  
    # PDF — import fitz ici pour éviter problèmes de pickle avec ProcessPoolExecutor  
    import fitz  # PyMuPDF  

    with [fitz.open](http://fitz.open)(str(source)) as doc:  
        total = [doc.page](http://doc.page)_count  
        *end = min(end, total) if end else total*  
** start = max(1, start)

text

        mat = fitz.Matrix(dpi / 72, dpi / 72)  
        for page_no in range(_start, *end + 1):*  
            *page = doc[page*no - 1]  
            pix = page.get_pixmap(matrix=mat, alpha=False)  
            # PyMuPDF → BGR numpy array pour OpenCV  
            img_array = np.frombuffer(pix.samples, dtype=np.uint8).reshape(  
                pix.height, pix.width, pix.n  
            )  
            if pix.n == 4:   # RGBA → BGR  
                img_array = cv2.cvtColor(img_array, cv2.COLOR_RGBA2BGR)  
            elif pix.n == 1:  # Grayscale → BGR  
                img_array = cv2.cvtColor(img_array, cv2.COLOR_GRAY2BGR)  
            else:  
                img_array = cv2.cvtColor(img_array, cv2.COLOR_RGB2BGR)  
            pages.append((page_no, img_array))  

return pages  
---------------------------------------------------------------------------
Pipeline principal
---------------------------------------------------------------------------
def ocr_pipeline(
source: str,
dpi: int = 300,
start: int = 1,
end: Optional[int] = None,
timeout: int = 120,
psm: int = 3,
oem: int = 1,
workers: int = 2,
resume: bool = False,
out_dir: Optional[str] = None,
suffix: str = ".ocr.txt",
) -> str:
src = Path(source)
if not src.exists():
log.error("Source introuvable : %s", src)
sys.exit(1)

text

# Fichier de sortie  
if out_dir:  
    out_path = Path(out_dir) / (src.stem + suffix)  
    Path(out_dir).mkdir(parents=True, exist_ok=True)  
elif [src.is](http://src.is)_dir():  
    out_path = src.parent / ([src.name](http://src.name) + suffix)  
else:  
    out_path = src.with_suffix(suffix)  

# Mode reprise : lire les pages déjà faites  
done_pages = *already*done_pages(out_path) if resume else set()  
if done_pages:  
    [log.info](http://log.info)("Mode reprise — %d pages déjà traitées", len(done_pages))  

# Chargement des pages  
[log.info](http://log.info)("Chargement des pages depuis : %s", src)  
all_pages = *load*pages(src, dpi, start, end)  

# Filtrer les pages déjà traitées  
pages_to_do = [(pno, img) for pno, img in all_pages if pno not in done_pages]  
if not pages_to_do:  
    [log.info](http://log.info)("Toutes les pages sont déjà traitées. Rien à faire.")  
    return str(out_path)  

[log.info](http://log.info)("%d pages à traiter (workers=%d, DPI=%d, OEM=%d, PSM=%d)",  
         len(pages_to_do), workers, dpi, oem, psm)  

# Préparer les args pour les workers  
worker_args = [(pno, img, psm, oem, timeout) for pno, img in pages_to_do]  

# Traitement parallèle  
results: dict = {}  
errors_summary: List[Tuple[int, str]] = []  

with ProcessPoolExecutor(max_workers=workers) as executor:  
    futures = {executor.submit(_ocr_single, arg): arg[0] for arg in worker_args}  
    for future in as_completed(futures):  
        page_no, text, error = future.result()  
        results[page_no] = text  
        if error:  
            errors_summary.append((page_no, error))  
        total_pages = len(pages_to_do)  
        done_count = len(results)  
        [log.info](http://log.info)("Page %d traitée [%d/%d] — %d chars%s",  
                 page_no, done_count, total_pages, len(text),  
                 " ⚠️ ERREUR" if error else "")  

# Assemblage ordonné  
sep = "=" * 70  
new_blocks = []  
for pno in sorted(results.keys()):  
    new_blocks.append(f"\n{sep}\nPAGE {pno}\n{sep}\n{results[pno]}")  

if resume and out_path.exists():  
    # Append propre : on ajoute après le contenu existant  
    with out_[path.open](http://path.open)("a", encoding="utf-8") as f:  
        f.write("\n".join(new_blocks))  
else:  
    # Écriture complète (toutes les pages dans l'ordre)  
    all_blocks = []  
    page_map = {pno: results[pno] for pno in results}  
    for pno,  *in all*pages:  
        if pno in page_map:  
            all_blocks.append(f"\n{sep}\nPAGE {pno}\n{sep}\n{page_map[pno]}")  
    out_path.write_text("\n".join(all_blocks), encoding="utf-8")  

total_chars = sum(len(t) for t in results.values())  
[log.info](http://log.info)("OCR terminé → %s (%d chars)", out_path, total_chars)  

if errors_summary:  
    log.warning("%d page(s) avec erreurs :", len(errors_summary))  
    for pno, err in errors_summary[:10]:  
        log.warning("  Page %d : %s", pno, err)  

return str(out_path)  
---------------------------------------------------------------------------
Entrée CLI
---------------------------------------------------------------------------
if name == "main":
ap = argparse.ArgumentParser(
description="OCR arabe+français — PDF, images, dossiers"
)
ap.add_argument("source",
help="PDF, image (PNG/JPG/TIFF/BMP/WEBP) ou dossier d'images")
ap.add_argument("--dpi", type=int, default=300,
help="Résolution de rendu PDF (défaut: 300)")
ap.add_argument("--start", type=int, default=1,
help="Première page PDF (défaut: 1)")
ap.add_argument("--end", type=int, default=None,
help="Dernière page PDF (défaut: fin du document)")
ap.add_argument("--timeout", type=int, default=120,
help="Timeout Tesseract par page en secondes (défaut: 120)")
ap.add_argument("--psm", type=int, default=3,
help="Page Segmentation Mode Tesseract (défaut: 3)")
ap.add_argument("--oem", type=int, default=1,
help="OCR Engine Mode (défaut: 1=LSTM)")
ap.add_argument("--workers", type=int, default=2,
help="Nombre de workers parallèles (défaut: 2)")
ap.add_argument("--resume", action="store_true",
help="Reprendre : ignorer les pages déjà traitées")
ap.add_argument("--out-dir", default=None,
help="Dossier de sortie (défaut: même dossier que la source)")
ap.add_argument("--suffix", default=".ocr.txt",
help="Extension du fichier de sortie (défaut: .ocr.txt)")
args = ap.parse_args()

text

check_requirements()  
ocr_pipeline(  
    source=args.source,  
    dpi=args.dpi,  
    start=args.start,  
    end=args.end,  
    timeout=args.timeout,  
    psm=args.psm,  
    oem=args.oem,  
    workers=args.workers,  
    resume=args.resume,  
    out_dir=args.out_dir,  
    suffix=args.suffix,  
)

Write
downloads/annales_svt/scripts/ocr_pipeline_ar_fra.py
405 lines
open
ocr_pipeline_ar_fra.py
PYTHON
#!/usr/bin/env python3
"""
Pipeline OCR robuste — arabe (principal) + français.
Supporte : PDF scannés, images (PNG, JPG, TIFF, BMP, WEBP).

Version nettoyée/corrigée à partir du script fourni par l'utilisateur :
  - ara+fra au lieu de ara+eng
  - Pré-traitement image (deskew, binarisation, débruitage)
  - Mode reprise (append + pages déjà traitées ignorées)
  - Erreurs OCR marquées dans l'output
  - Parallélisation (ProcessPoolExecutor)
  - Vérification des prérequis au démarrage
  - Context manager pour fitz
  - Support images directes (PNG, JPG, TIFF, BMP, WEBP)
  - OEM explicite
  - Compatible Python 3.8+

Attention : sur les PDF KHELIFA, les pages ont une taille très grande. 300 DPI peut
consommer énormément de RAM. Tester d'abord --dpi 100 à 160.
"""

import argparse
import logging
import os
import re
import shutil
import subprocess
import sys
import tempfile
from concurrent.futures import ProcessPoolExecutor, as_completed
from pathlib import Path
from typing import List, Optional, Tuple

import cv2
import numpy as np

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s",
    datefmt="%H:%M:%S",
)
log = logging.getLogger(__name__)


# ---------------------------------------------------------------------------
# Vérification des prérequis
# ---------------------------------------------------------------------------
def check_requirements() -> None:
    """Vérifie que tesseract est accessible et que les langues ara/fra sont installées."""
    if not shutil.which("tesseract"):
        log.error("Tesseract introuvable dans le PATH. Installez-le : apt install tesseract-ocr")
        sys.exit(1)

    result = subprocess.run(
        ["tesseract", "--list-langs"],
        capture_output=True,
        text=True,
    )
    installed = result.stdout + result.stderr
    missing = [lang for lang in ("ara", "fra") if lang not in installed]
    if missing:
        log.error(
            "Langues Tesseract manquantes : %s\n"
            "  Ubuntu/Debian : apt install %s",
            missing,
            " ".join(f"tesseract-ocr-{l}" for l in missing),
        )
        sys.exit(1)


# ---------------------------------------------------------------------------
# Pré-traitement image
# ---------------------------------------------------------------------------
def deskew(image: np.ndarray) -> np.ndarray:
    """Corrige l'inclinaison du scan via la transformée de Hough."""
    gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY) if image.ndim == 3 else image
    edges = cv2.Canny(gray, 50, 150, apertureSize=3)
    lines = cv2.HoughLinesP(
        edges,
        1,
        np.pi / 180,
        threshold=100,
        minLineLength=100,
        maxLineGap=10,
    )
    if lines is None:
        return image

    angles = []
    for line in lines:
        x1, y1, x2, y2 = line[0]
        if x2 != x1:
            angle = np.degrees(np.arctan2(y2 - y1, x2 - x1))
            # Ignore near vertical lines; they can break skew detection on tables/borders.
            if -45 <= angle <= 45:
                angles.append(angle)

    if not angles:
        return image

    median_angle = float(np.median(angles))
    if abs(median_angle) < 0.5:
        return image

    h, w = image.shape[:2]
    M = cv2.getRotationMatrix2D((w / 2, h / 2), median_angle, 1.0)
    return cv2.warpAffine(
        image,
        M,
        (w, h),
        flags=cv2.INTER_LINEAR,
        borderMode=cv2.BORDER_REPLICATE,
    )


def preprocess_image(img_array: np.ndarray) -> np.ndarray:
    """
    Pipeline de pré-traitement pour OCR arabe/français sur scans.
    Ordre : deskew → niveaux de gris → débruitage → binarisation adaptative.
    """
    img = deskew(img_array)

    if img.ndim == 3:
        gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
    else:
        gray = img

    denoised = cv2.fastNlMeansDenoising(
        gray,
        h=10,
        templateWindowSize=7,
        searchWindowSize=21,
    )

    # blockSize must be odd and > 1
    binary = cv2.adaptiveThreshold(
        denoised,
        255,
        cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
        cv2.THRESH_BINARY,
        blockSize=31,
        C=10,
    )

    kernel = np.ones((1, 1), np.uint8)
    processed = cv2.erode(binary, kernel, iterations=1)
    return processed


# ---------------------------------------------------------------------------
# OCR d'une page worker
# ---------------------------------------------------------------------------
def _ocr_single(args: Tuple[int, np.ndarray, int, int, int]) -> Tuple[int, str, Optional[str]]:
    """Worker isolé pour ProcessPoolExecutor. Retourne (page_no, text, error_or_None)."""
    page_no, img_array, psm, oem, timeout = args
    error = None
    text = ""
    tmp_path = None

    try:
        processed = preprocess_image(img_array)

        fd, tmp_path = tempfile.mkstemp(suffix=".png")
        os.close(fd)
        cv2.imwrite(tmp_path, processed)

        cmd = [
            "tesseract",
            tmp_path,
            "-",
            "-l",
            "ara+fra",
            "--psm",
            str(psm),
            "--oem",
            str(oem),
            "-c",
            "preserve_interword_spaces=1",
            "-c",
            "textord_arabic_right_to_left=1",
        ]
        result = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
        )
        text = result.stdout.strip()
        if result.returncode != 0:
            error = result.stderr.strip()[:400]
            text = f"[TESSERACT ERROR page {page_no}: {error}]\n{text}"

    except subprocess.TimeoutExpired:
        error = f"Timeout ({timeout}s) dépassé"
        text = f"[TIMEOUT ERROR page {page_no}]"
    except Exception as e:
        error = str(e)
        text = f"[OCR ERROR page {page_no}: {e}]"
    finally:
        if tmp_path and os.path.exists(tmp_path):
            os.unlink(tmp_path)

    return page_no, text, error


# ---------------------------------------------------------------------------
# Pages déjà traitées
# ---------------------------------------------------------------------------
def already_done_pages(out_path: Path) -> set:
    """Parse le fichier de sortie existant pour trouver les pages déjà traitées."""
    if not out_path.exists():
        return set()
    pattern = re.compile(r"^={10,}\nPAGE (\d+)\n={10,}", re.MULTILINE)
    content = out_path.read_text(encoding="utf-8", errors="ignore")
    return {int(m.group(1)) for m in pattern.finditer(content)}


# ---------------------------------------------------------------------------
# Source -> pages
# ---------------------------------------------------------------------------
IMAGE_EXTENSIONS = {".png", ".jpg", ".jpeg", ".tiff", ".tif", ".bmp", ".webp"}


def load_pages(source: Path, dpi: int, start: int, end: Optional[int]) -> List[Tuple[int, np.ndarray]]:
    """Retourne une liste ordonnée de (page_no, img_array BGR)."""
    pages: List[Tuple[int, np.ndarray]] = []

    if source.is_dir():
        files = sorted(f for f in source.iterdir() if f.suffix.lower() in IMAGE_EXTENSIONS)
        if end is None:
            end = len(files)
        start = max(1, start)
        end = min(end, len(files))
        for idx, f in enumerate(files[start - 1:end], start=start):
            img = cv2.imread(str(f))
            if img is not None:
                pages.append((idx, img))
            else:
                log.warning("Image illisible ignorée : %s", f.name)

    elif source.suffix.lower() in IMAGE_EXTENSIONS:
        img = cv2.imread(str(source))
        if img is None:
            log.error("Impossible de lire l'image : %s", source)
            sys.exit(1)
        pages.append((1, img))

    else:
        import fitz  # PyMuPDF

        with fitz.open(str(source)) as doc:
            total = doc.page_count
            end = min(end, total) if end else total
            start = max(1, start)

            mat = fitz.Matrix(dpi / 72, dpi / 72)
            for page_no in range(start, end + 1):
                page = doc[page_no - 1]
                pix = page.get_pixmap(matrix=mat, alpha=False)
                img_array = np.frombuffer(pix.samples, dtype=np.uint8).reshape(
                    pix.height,
                    pix.width,
                    pix.n,
                )
                if pix.n == 4:
                    img_array = cv2.cvtColor(img_array, cv2.COLOR_RGBA2BGR)
                elif pix.n == 1:
                    img_array = cv2.cvtColor(img_array, cv2.COLOR_GRAY2BGR)
                else:
                    img_array = cv2.cvtColor(img_array, cv2.COLOR_RGB2BGR)
                # Copy because pix buffer will be released when pix goes out of scope.
                pages.append((page_no, img_array.copy()))

    return pages


# ---------------------------------------------------------------------------
# Pipeline principal
# ---------------------------------------------------------------------------
def ocr_pipeline(
    source: str,
    dpi: int = 300,
    start: int = 1,
    end: Optional[int] = None,
    timeout: int = 120,
    psm: int = 3,
    oem: int = 1,
    workers: int = 2,
    resume: bool = False,
    out_dir: Optional[str] = None,
    suffix: str = ".ocr.txt",
) -> str:
    src = Path(source)
    if not src.exists():
        log.error("Source introuvable : %s", src)
        sys.exit(1)

    if out_dir:
        out_path = Path(out_dir) / (src.stem + suffix)
        Path(out_dir).mkdir(parents=True, exist_ok=True)
    elif src.is_dir():
        out_path = src.parent / (src.name + suffix)
    else:
        out_path = src.with_suffix(suffix)

    done_pages = already_done_pages(out_path) if resume else set()
    if done_pages:
        log.info("Mode reprise — %d pages déjà traitées", len(done_pages))

    log.info("Chargement des pages depuis : %s", src)
    all_pages = load_pages(src, dpi, start, end)
    pages_to_do = [(pno, img) for pno, img in all_pages if pno not in done_pages]
    if not pages_to_do:
        log.info("Toutes les pages sont déjà traitées. Rien à faire.")
        return str(out_path)

    # Sécurité RAM : éviter trop de gros tableaux en parallèle.
    workers = max(1, min(workers, len(pages_to_do)))
    log.info(
        "%d pages à traiter (workers=%d, DPI=%d, OEM=%d, PSM=%d)",
        len(pages_to_do),
        workers,
        dpi,
        oem,
        psm,
    )

    worker_args = [(pno, img, psm, oem, timeout) for pno, img in pages_to_do]
    results = {}
    errors_summary: List[Tuple[int, str]] = []

    with ProcessPoolExecutor(max_workers=workers) as executor:
        futures = {executor.submit(_ocr_single, arg): arg[0] for arg in worker_args}
        for future in as_completed(futures):
            page_no, text, error = future.result()
            results[page_no] = text
            if error:
                errors_summary.append((page_no, error))
            log.info(
                "Page %d traitée [%d/%d] — %d chars%s",
                page_no,
                len(results),
                len(pages_to_do),
                len(text),
                " ⚠️ ERREUR" if error else "",
            )

    sep = "=" * 70
    new_blocks = [f"\n{sep}\nPAGE {pno}\n{sep}\n{results[pno]}" for pno in sorted(results)]

    if resume and out_path.exists():
        with out_path.open("a", encoding="utf-8") as f:
            f.write("\n".join(new_blocks))
    else:
        all_blocks = []
        page_map = {pno: results[pno] for pno in results}
        for pno, _img in all_pages:
            if pno in page_map:
                all_blocks.append(f"\n{sep}\nPAGE {pno}\n{sep}\n{page_map[pno]}")
        out_path.write_text("\n".join(all_blocks), encoding="utf-8")

    total_chars = sum(len(t) for t in results.values())
    log.info("OCR terminé → %s (%d chars)", out_path, total_chars)

    if errors_summary:
        log.warning("%d page(s) avec erreurs :", len(errors_summary))
        for pno, err in errors_summary[:10]:
            log.warning("  Page %d : %s", pno, err)

    return str(out_path)


if __name__ == "__main__":
    ap = argparse.ArgumentParser(description="OCR arabe+français — PDF, images, dossiers")
    ap.add_argument("source", help="PDF, image (PNG/JPG/TIFF/BMP/WEBP) ou dossier d'images")
    ap.add_argument("--dpi", type=int, default=300, help="Résolution de rendu PDF (défaut: 300)")
    ap.add_argument("--start", type=int, default=1, help="Première page PDF (défaut: 1)")
    ap.add_argument("--end", type=int, default=None, help="Dernière page PDF (défaut: fin du document)")
    ap.add_argument("--timeout", type=int, default=120, help="Timeout Tesseract par page en secondes")
    ap.add_argument("--psm", type=int, default=3, help="Page Segmentation Mode Tesseract")
    ap.add_argument("--oem", type=int, default=1, help="OCR Engine Mode (défaut: 1=LSTM)")
    ap.add_argument("--workers", type=int, default=2, help="Nombre de workers parallèles")
    ap.add_argument("--resume", action="store_true", help="Reprendre : ignorer les pages déjà traitées")
    ap.add_argument("--out-dir", default=None, help="Dossier de sortie")
    ap.add_argument("--suffix", default=".ocr.txt", help="Extension du fichier de sortie")
    args = ap.parse_args()

    check_requirements()
    ocr_pipeline(
        source=args.source,
        dpi=args.dpi,
        start=args.start,
        end=args.end,
        timeout=args.timeout,
        psm=args.psm,
        oem=args.oem,
        workers=args.workers,
        resume=args.resume,
        out_dir=args.out_dir,
        suffix=args.suffix,
    )
Open

Ran commands
2
Oui, j’ai essayé ton script ✅

Comme il était copié depuis un message avec du Markdown, il contenait plusieurs erreurs de syntaxe, par exemple :

def *deskew(...)
img = *deskew(img*array)
[subprocess.run](http://subprocess.run)
if **name** == "__main__"
out_[path.read](...)
etc.
Je l’ai donc nettoyé et corrigé, puis sauvegardé ici :

text

downloads/annales_svt/scripts/ocr_pipeline_ar_fra.py
✅ Prérequis installés / vérifiés
J’ai installé la langue française Tesseract :

text

tesseract-ocr-fra
Langues disponibles maintenant :

text

ara
eng
fra
osd
OpenCV fonctionne aussi :

text

cv2 OK 4.11.0
✅ Test effectué
J’ai testé le script sur KHELIFA1_VOLUME_01.pdf, pages 9 à 10 :

Bash

python scripts/ocr_pipeline_ar_fra.py input/KHELIFA1_VOLUME_01.pdf \
  --start 9 \
  --end 10 \
  --dpi 160 \
  --workers 1 \
  --timeout 180 \
  --suffix .ocr_ar_fra_test.txt
Résultat généré ici :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_ar_fra_test.txt
Taille du résultat :

text

13 KB
✅ Test du mode reprise
J’ai aussi testé --resume :

Bash

python scripts/ocr_pipeline_ar_fra.py input/KHELIFA1_VOLUME_01.pdf \
  --start 9 \
  --end 10 \
  --dpi 160 \
  --workers 1 \
  --timeout 180 \
  --suffix .ocr_ar_fra_test.txt \
  --resume
Résultat :

text

Mode reprise — 2 pages déjà traitées
Toutes les pages sont déjà traitées. Rien à faire.
Donc le mode reprise fonctionne ✅

⚠️ Observation importante
Ton script améliore bien certains passages, surtout avec :

text

ara+fra
prétraitement OpenCV
binarisation
deskew
Mais attention : pour les PDF KHELIFA, les pages sont très grandes. Le --dpi 300 par défaut est trop lourd pour traiter tout un volume d’un coup.

Pour KHELIFA1_VOLUME_01.pdf, la taille des pages PDF est déjà énorme. À 300 DPI, chaque page devient une image géante, ce qui peut provoquer :

RAM excessive,
lenteur énorme,
timeout,
crash possible.
Donc pour ce projet, je recommande plutôt :

Bash

--dpi 140
ou

Bash

--dpi 160
Pas 300 pour le traitement complet.

Conclusion
✅ Script corrigé
✅ Script sauvegardé
✅ Tesseract fra installé
✅ Test OCR réussi
✅ Mode reprise réussi
⚠️ Pour le traitement complet, il faut l’utiliser par petits lots de pages.

Prochaine étape recommandée :

text

OCR KHELIFA1_VOLUME_01 complet avec ce nouveau script, par lots de 4 ou 5 pages.
Commande type :

Bash

python scripts/ocr_pipeline_ar_fra.py input/KHELIFA1_VOLUME_01.pdf \
  --start 1 \
  --end 5 \
  --dpi 160 \
  --workers 1 \
  --timeout 180 \
  --suffix .ocr_ar_fra_full.txt
Puis :

Bash

python scripts/ocr_pipeline_ar_fra.py input/KHELIFA1_VOLUME_01.pdf \
  --start 6 \
  --end 10 \
  --dpi 160 \
  --workers 1 \
  --timeout 180 \
  --suffix .ocr_ar_fra_full.txt \
  --resume
Si tu veux, je peux maintenant lancer le traitement complet du volume 01 avec ce script corrigé, mais par lots pour éviter le timeout.


oui



Edit
downloads/annales_svt/MANIFESTE.md
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR complet ara+fra (KHELIFA1_VOLUME_01.ocr_ar_fra_full.txt)	À extraire
KHELIFA 1	Vol 02-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 80 questions / 30 micro-concepts couverts sur 42
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG (200 DPI) ✅
OCR : Tesseract ara+eng ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
Oui, c’est fait ✅

J’ai lancé le traitement complet de KHELIFA1_VOLUME_01.pdf avec le nouveau script corrigé ara+fra.

Résultat OCR complet
Fichier généré :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_ar_fra_full.txt
Paramètres utilisés
Pour éviter crash/timeout, j’ai traité par lots de 4 pages avec :

Bash

--dpi 140
--workers 1
--timeout 180
--resume
Le volume complet contient :

text

28 pages
Toutes les pages ont été traitées :

text

pages: [1, 2, 3, ..., 28]
count: 28
Contrôle qualité technique
Résultat :

text

timeouts: 0
ocr_errors: 0
tesseract_errors: 0
Taille du fichier OCR final :

text

76,998 bytes
À comparer avec l’ancien OCR :

text

ancien OCR complet : ~50 KB
nouveau OCR ara+fra : ~77 KB
Donc le nouveau script extrait nettement plus de texte.

Manifeste mis à jour
J’ai aussi mis à jour :

text

downloads/annales_svt/MANIFESTE.md
avec l’état :

text

KHELIFA 1 Vol 01 — OCR complet ara+fra — À extraire
Prochaine phase
On peut maintenant passer à :

Phase 2 — Extraction des questions depuis KHELIFA1_VOLUME_01.ocr_ar_fra_full.txt
Je vais :

lire le nouvel OCR,
découper les exercices/questions,
corriger les passages arabes importants,
préparer les objets JSON,
puis seulement après faire le tagging/validation.
Tu peux me dire : continue extraction volume 01.


UTILISE CE OCR PIPELINE #!/usr/bin/env python3

"""

Pipeline OCR orienté production — arabe (principal) + français.

Ce script traite :

PDF

image unique (PNG, JPG, JPEG, TIFF, TIF, BMP, WEBP)

dossier d'images

Objectifs :

reprise robuste par page via état JSON + artefacts intermédiaires

pas de chargement complet du document en RAM

parallélisation par page sans sérialiser les images entre processus

extraction native du texte PDF avant OCR si demandé

métriques d'exécution par page et globales

sortie texte finale réassemblée dans l'ordre

CSV et JSON de reporting

PDF searchable optionnel

journal d'exécution sur disque

Dépendances Python :

  pip install numpy opencv-python pymupdf

Dépendances système :

  tesseract-ocr + langues (par défaut ara et fra)

Exemples :

  python ocr_pipeline_production.py input/doc.pdf --workers 4 --resume

  python ocr_pipeline_production.py input/doc.pdf --searchable-pdf --workers 4

  python ocr_pipeline_production.py input/scans/ --workers 6 --retries 3

"""

from future import annotations

import argparse

import csv

import json

import logging

import os

import re

import shutil

import subprocess

import sys

import tempfile

import time

from concurrent.futures import ProcessPoolExecutor, as_completed

from dataclasses import dataclass

from datetime import datetime

from pathlib import Path

from typing import Dict, List, Optional, Sequence, Set, Tuple

import cv2

import numpy as np

IMAGE_EXTENSIONS = {".png", ".jpg", ".jpeg", ".tiff", ".tif", ".bmp", ".webp"}

PDF_EXTENSIONS = {".pdf"}

SEPARATOR = "=" * 70

STATE_VERSION = 2

LOG = logging.getLogger("ocr_pipeline_production")

@dataclass(frozen=True)

class PageRef:

    page_no: int

    kind: str  # 'pdf' | 'image'

    source_path: str

    pdf_index: Optional[int] = None

@dataclass(frozen=True)

class AttemptSpec:

    variant: str

    psm: int

---------------------------------------------------------------------------
Logging / utilitaires de base
---------------------------------------------------------------------------
def configure_logging(log_file: Optional[Path] = None, verbose: bool = False) -> None:

    LOG.setLevel(logging.DEBUG if verbose else logging.INFO)

    LOG.handlers.clear()

    LOG.propagate = False

    formatter = logging.Formatter(

        fmt="%(asctime)s [%(levelname)s] %(message)s",

        datefmt="%H:%M:%S",

    )

    console = logging.StreamHandler(sys.stdout)

    console.setLevel(logging.DEBUG if verbose else logging.INFO)

    console.setFormatter(formatter)

    LOG.addHandler(console)

    if log_file is not None:

        log_file.parent.mkdir(parents=True, exist_ok=True)

        file_handler = logging.FileHandler(log_file, encoding="utf-8")

        file_handler.setLevel(logging.DEBUG)

        file_handler.setFormatter(formatter)

        LOG.addHandler(file_handler)

def die(message: str, code: int = 1) -> None:

    LOG.error(message)

    raise SystemExit(code)

def utc_now_iso() -> str:

    return datetime.utcnow().replace(microsecond=0).isoformat() + "Z"

def natural_key(value: str):

    return [int(part) if part.isdigit() else part.lower() for part in re.split(r"(\d+)", value)]

def atomic_write_text(path: Path, content: str, encoding: str = "utf-8") -> None:

    path.parent.mkdir(parents=True, exist_ok=True)

    with tempfile.NamedTemporaryFile("w", delete=False, dir=str(path.parent), encoding=encoding) as tmp:

        tmp.write(content)

        tmp_path = Path(tmp.name)

    os.replace(tmp_path, path)

def atomic_write_json(path: Path, data: Dict) -> None:

    atomic_write_text(path, json.dumps(data, ensure_ascii=False, indent=2) + "\n")

def atomic_write_bytes(path: Path, payload: bytes) -> None:

    path.parent.mkdir(parents=True, exist_ok=True)

    with tempfile.NamedTemporaryFile("wb", delete=False, dir=str(path.parent)) as tmp:

        tmp.write(payload)

        tmp_path = Path(tmp.name)

    os.replace(tmp_path, path)

def compact_len(text: str) -> int:

    return len(re.sub(r"\s+", "", text or ""))

def safe_error_text(text: str, limit: int = 500) -> str:

    return (text or "").strip().replace("\n", " ")[:limit] or "Erreur sans détail"

---------------------------------------------------------------------------
Chemins d'artefacts
---------------------------------------------------------------------------
def compute_paths(source: Path, out_dir: Optional[str], suffix: str) -> Dict[str, Path]:

    if out_dir:

        out_root = Path(out_dir)

        out_root.mkdir(parents=True, exist_ok=True)

        txt_out = out_root / f"{source.stem}{suffix}"

        searchable_pdf = out_root / f"{source.stem}.searchable.pdf"

    elif source.is_dir():

        txt_out = source.parent / f"{source.name}{suffix}"

        searchable_pdf = source.parent / f"{source.name}.searchable.pdf"

    else:

        txt_out = source.with_suffix(suffix)

        searchable_pdf = txt_out.parent / f"{source.stem}.searchable.pdf"

    bundle = txt_out.with_name(txt_out.name + ".bundle")

    return {

        "txt_out": txt_out,

        "searchable_pdf": searchable_pdf,

        "bundle": bundle,

        "pages_txt": bundle / "pages_txt",

        "pages_meta": bundle / "pages_meta",

        "pages_pdf": bundle / "pages_pdf",

        "state": bundle / "run.state.json",

        "summary": bundle / "summary.json",

        "pages_csv": bundle / "pages.csv",

        "run_log": bundle / "run.log",

    }

def page_text_path(paths: Dict[str, Path], page_no: int) -> Path:

    return paths["pages_txt"] / f"page_{page_no:06d}.txt"

def page_meta_path(paths: Dict[str, Path], page_no: int) -> Path:

    return paths["pages_meta"] / f"page_{page_no:06d}.json"

def page_pdf_path(paths: Dict[str, Path], page_no: int) -> Path:

    return paths["pages_pdf"] / f"page_{page_no:06d}.pdf"

def reset_outputs(paths: Dict[str, Path], resume: bool) -> None:

    if resume:

        for key in ("bundle",):

            paths[key].mkdir(parents=True, exist_ok=True)

        return

    if paths["bundle"].exists():

        shutil.rmtree(paths["bundle"])

    if paths["txt_out"].exists():

        paths["txt_out"].unlink()

    if paths["searchable_pdf"].exists():

        paths["searchable_pdf"].unlink()

    for key in ("bundle", "pages_txt", "pages_meta", "pages_pdf"):

        paths[key].mkdir(parents=True, exist_ok=True)

---------------------------------------------------------------------------
Validation / prérequis
---------------------------------------------------------------------------
def parse_langs(lang_spec: str) -> List[str]:

    langs = [chunk.strip() for chunk in lang_spec.split("+") if chunk.strip()]

    if not langs:

        raise ValueError("Aucune langue Tesseract valide dans --lang")

    return langs

def check_tesseract_languages(lang_spec: str) -> None:

    if not shutil.which("tesseract"):

        die("Tesseract introuvable dans le PATH.")

    result = subprocess.run(

        ["tesseract", "--list-langs"],

        capture_output=True,

        text=True,

        check=False,

    )

    if result.returncode != 0:

        die(f"Échec de tesseract --list-langs : {safe_error_text(result.stderr)}")

    installed = {

        line.strip()

        for line in (result.stdout + "\n" + result.stderr).splitlines()

        if line.strip() and not line.lower().startswith("list of available languages")

    }

    missing = [lang for lang in parse_langs(lang_spec) if lang not in installed]

    if missing:

        die(

            "Langues Tesseract manquantes : "

            f"{', '.join(missing)}\n"

            f"Ubuntu/Debian : apt install {' '.join(f'tesseract-ocr-{x}' for x in missing)}"

        )

def check_pdf_support_if_needed(source: Path) -> None:

    need_pdf = source.is_file() and source.suffix.lower() in PDF_EXTENSIONS

    if not need_pdf:

        return

    try:

        import fitz  # noqa: F401

    except Exception as exc:

        die(f"Support PDF indisponible. Installez PyMuPDF (pymupdf). Détail : {exc}")

def validate_args(args: argparse.Namespace) -> None:

    source = Path(args.source)

    if not source.exists():

        die(f"Source introuvable : {source}")

    if args.dpi <= 0:

        die("--dpi doit être > 0")

    if args.workers <= 0:

        die("--workers doit être >= 1")

    if args.timeout <= 0:

        die("--timeout doit être > 0")

    if args.start <= 0:

        die("--start doit être >= 1")

    if args.end is not None and args.end < args.start:

        die("--end doit être >= --start")

    if args.psm < 0 or args.psm > 13:

        die("--psm doit être entre 0 et 13")

    if args.oem not in (0, 1, 2, 3):

        die("--oem doit être dans {0,1,2,3}")

    if args.pdf_mode not in {"auto", "ocr", "text"}:

        die("--pdf-mode doit être dans {auto, ocr, text}")

    if args.text_threshold < 0:

        die("--text-threshold doit être >= 0")

    if args.min_chars < 0:

        die("--min-chars doit être >= 0")

    if args.retries < 0:

        die("--retries doit être >= 0")

    if source.is_file() and source.suffix.lower() not in IMAGE_EXTENSIONS | PDF_EXTENSIONS:

        die(f"Extension non supportée : {source.suffix}")

    if source.is_dir():

        files = [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS]

        if not files:

            die(f"Aucune image supportée trouvée dans : {source}")

---------------------------------------------------------------------------
État de reprise
---------------------------------------------------------------------------
def resume_identity(args: argparse.Namespace) -> Dict[str, object]:

    return {

        "source": str(Path(args.source).resolve()),

        "dpi": args.dpi,

        "lang": args.lang,

        "psm": args.psm,

        "oem": args.oem,

        "pdf_mode": args.pdf_mode,

        "text_threshold": args.text_threshold,

        "no_preprocess": args.no_preprocess,

        "min_chars": args.min_chars,

        "retries": args.retries,

        "tsv_metrics": not args.no_tsv_metrics,

        "searchable_pdf": args.searchable_pdf,

        "start": args.start,

        "end": args.end,

    }

def load_state(path: Path) -> Dict:

    if not path.exists():

        return {}

    try:

        return json.loads(path.read_text(encoding="utf-8"))

    except Exception as exc:

        die(f"Fichier d'état illisible : {path} ({exc})")

def save_state(path: Path, state: Dict) -> None:

    state["updated_at"] = utc_now_iso()

    atomic_write_json(path, state)

def ensure_state_compatible(state: Dict, args: argparse.Namespace, paths: Dict[str, Path]) -> None:

    if not state:

        return

    if state.get("version") != STATE_VERSION:

        die("Version d'état incompatible. Relancez sans --resume ou supprimez le bundle.")

    if state.get("identity") != resume_identity(args):

        die("Le bundle existant ne correspond pas à la même configuration fonctionnelle.")

    if state.get("output_txt") != str(paths["txt_out"].resolve()):

        die("Le bundle existant ne correspond pas à cette destination texte.")

def init_state(args: argparse.Namespace, paths: Dict[str, Path], pages: Sequence[PageRef]) -> Dict:

    return {

        "version": STATE_VERSION,

        "created_at": utc_now_iso(),

        "updated_at": utc_now_iso(),

        "identity": resume_identity(args),

        "output_txt": str(paths["txt_out"].resolve()),

        "output_searchable_pdf": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,

        "page_count": len(pages),

        "pages": {},

        "run_config": {

            "workers": args.workers,

            "timeout": args.timeout,

            "verbose": args.verbose,

        },

    }

def completed_pages_from_state(state: Dict, paths: Dict[str, Path], valid_page_nos: Set[int]) -> Set[int]:

    done: Set[int] = set()

    for key, meta in state.get("pages", {}).items():

        try:

            pno = int(key)

        except Exception:

            continue

        if pno not in valid_page_nos:

            continue

        if meta.get("status") == "done" and page_text_path(paths, pno).exists() and page_meta_path(paths, pno).exists():

            done.add(pno)

    return done

---------------------------------------------------------------------------
Pages source
---------------------------------------------------------------------------
def list_pages(source: Path, start: int, end: Optional[int]) -> List[PageRef]:

    if source.is_dir():

        files = sorted(

            [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS],

            key=lambda p: natural_key(p.name),

        )

        return [PageRef(page_no=i, kind="image", source_path=str(path.resolve())) for i, path in enumerate(files, start=1)]

    if source.suffix.lower() in IMAGE_EXTENSIONS:

        return [PageRef(page_no=1, kind="image", source_path=str(source.resolve()))]

    if source.suffix.lower() in PDF_EXTENSIONS:

        import fitz

        with fitz.open(str(source)) as doc:

            total = doc.page_count

        first = max(1, start)

        last = total if end is None else min(total, end)

        if first > last:

            die(f"Intervalle vide : start={first}, end={last}, total={total}")

        return [

            PageRef(page_no=pno, kind="pdf", source_path=str(source.resolve()), pdf_index=pno - 1)

            for pno in range(first, last + 1)

        ]

    die(f"Type de source non supporté : {source}")

    return []

---------------------------------------------------------------------------
Lecture / rendu source
---------------------------------------------------------------------------
def read_image(path: str) -> np.ndarray:

    img = cv2.imread(path, cv2.IMREAD_COLOR)

    if img is None:

        raise RuntimeError(f"Impossible de lire l'image : {path}")

    return img

def render_pdf_page(pdf_path: str, pdf_index: int, dpi: int) -> np.ndarray:

    import fitz

    with fitz.open(pdf_path) as doc:

        page = doc[pdf_index]

        matrix = fitz.Matrix(dpi / 72.0, dpi / 72.0)

        pix = page.get_pixmap(matrix=matrix, alpha=False)

    arr = np.frombuffer(pix.samples, dtype=np.uint8).reshape(pix.height, pix.width, pix.n)

    if pix.n == 1:

        return cv2.cvtColor(arr, cv2.COLOR_GRAY2BGR)

    if pix.n == 4:

        return cv2.cvtColor(arr, cv2.COLOR_RGBA2BGR)

    return cv2.cvtColor(arr, cv2.COLOR_RGB2BGR)

def extract_pdf_native_text(pdf_path: str, pdf_index: int) -> str:

    import fitz

    with fitz.open(pdf_path) as doc:

        return doc[pdf_index].get_text("text").strip()

def meaningful_text(text: str, threshold: int) -> bool:

    return compact_len(text) >= threshold

---------------------------------------------------------------------------
Prétraitement image
---------------------------------------------------------------------------
def to_gray(image: np.ndarray) -> np.ndarray:

    if image.ndim == 2:

        return image.copy()

    return cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

def estimate_skew_angle(gray: np.ndarray) -> float:

    blurred = cv2.GaussianBlur(gray, (5, 5), 0)

    , thresh = cv2.threshold(blurred, 0, 255, cv2.THRESHBINARY_INV + cv2.THRESH_OTSU)

    coords = np.column_stack(np.where(thresh > 0))

    if coords.shape[0] < 200:

        return 0.0

    angle = cv2.minAreaRect(coords)[-1]

    if angle < -45:

        angle = -(90 + angle)

    else:

        angle = -angle

    angle = float(angle)

    if abs(angle) < 0.2 or abs(angle) > 15:

        return 0.0

    return angle

def rotate_image(image: np.ndarray, angle: float) -> np.ndarray:

    h, w = image.shape[:2]

    matrix = cv2.getRotationMatrix2D((w / 2.0, h / 2.0), angle, 1.0)

    border_value = 255 if image.ndim == 2 else (255, 255, 255)

    return cv2.warpAffine(

        image,

        matrix,

        (w, h),

        flags=cv2.INTER_CUBIC,

        borderMode=cv2.BORDER_CONSTANT,

        borderValue=border_value,

    )

def normalize_gray(gray: np.ndarray) -> np.ndarray:

    angle = estimate_skew_angle(gray)

    if angle:

        gray = rotate_image(gray, angle)

    clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8, 8))

    normalized = clahe.apply(gray)

    denoised = cv2.fastNlMeansDenoising(normalized, h=7, templateWindowSize=7, searchWindowSize=21)

    return denoised

def preprocess_variant(image: np.ndarray, variant: str) -> np.ndarray:

    gray = to_gray(image)

    if variant == "raw-gray":

        return gray

    normalized = normalize_gray(gray)

    if variant == "gray":

        return normalized

    if variant == "adaptive":

        return cv2.adaptiveThreshold(

            normalized,

            255,

            cv2.ADAPTIVE_THRESH_GAUSSIAN_C,

            cv2.THRESH_BINARY,

            31,

            11,

        )

    if variant == "otsu":

        , binary = cv2.threshold(normalized, 0, 255, cv2.THRESHBINARY + cv2.THRESH_OTSU)

        return binary

    raise ValueError(f"Variant de prétraitement inconnue : {variant}")

def build_attempt_plan(base_psm: int, no_preprocess: bool, retries: int) -> List[AttemptSpec]:

    candidates: List[AttemptSpec] = []

    if no_preprocess:

        raw = [AttemptSpec("raw-gray", base_psm)]

        if base_psm != 6:

            raw.append(AttemptSpec("raw-gray", 6))

        if base_psm != 11:

            raw.append(AttemptSpec("raw-gray", 11))

        candidates.extend(raw)

    else:

        candidates.extend(

            [

                AttemptSpec("adaptive", base_psm),

                AttemptSpec("gray", base_psm),

                AttemptSpec("otsu", base_psm),

            ]

        )

        if base_psm != 6:

            candidates.append(AttemptSpec("adaptive", 6))

        if base_psm != 11:

            candidates.append(AttemptSpec("gray", 11))

    deduped: List[AttemptSpec] = []

    seen = set()

    for item in candidates:

        key = (item.variant, item.psm)

        if key not in seen:

            seen.add(key)

            deduped.append(item)

    return deduped[: max(1, 1 + retries)]

---------------------------------------------------------------------------
Tesseract
---------------------------------------------------------------------------
def tesseract_base_command(img_path: str, output_base: str, lang: str, psm: int, oem: int) -> List[str]:

    return [

        "tesseract",

        img_path,

        output_base,

        "-l",

        lang,

        "--psm",

        str(psm),

        "--oem",

        str(oem),

        "-c",

        "preserve_interword_spaces=1",

    ]

def run_command(command: List[str], timeout: int) -> subprocess.CompletedProcess:

    env = os.environ.copy()

    env.setdefault("OMP_THREAD_LIMIT", "1")

    return subprocess.run(

        command,

        capture_output=True,

        text=True,

        encoding="utf-8",

        errors="replace",

        timeout=timeout,

        check=False,

        env=env,

    )

def run_tesseract_text(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int) -> Tuple[str, Optional[str]]:

    with tempfile.TemporaryDirectory() as tmp_dir:

        img_path = str(Path(tmp_dir) / "page.png")

        if not cv2.imwrite(img_path, image):

            raise RuntimeError("Échec d'écriture de l'image temporaire")

        try:

            result = run_command(tesseract_base_command(img_path, "stdout", lang, psm, oem), timeout)

        except subprocess.TimeoutExpired:

            return "[TIMEOUT ERROR]", f"Timeout Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:

            err = safe_error_text(result.stderr)

            return f"[TESSERACT ERROR: {err}]", err

        return result.stdout.strip(), None

def parse_tsv_metrics(tsv_text: str) -> Tuple[Optional[float], int]:

    lines = [line for line in tsv_text.splitlines() if line.strip()]

    if len(lines) <= 1:

        return None, 0

    scores: List[float] = []

    word_count = 0

    reader = csv.DictReader(lines, delimiter="\t")

    for row in reader:

        text = (row.get("text") or "").strip()

        conf_raw = (row.get("conf") or "").strip()

        if text:

            word_count += 1

        if conf_raw and conf_raw not in {"-1", "nan"}:

            try:

                scores.append(float(conf_raw))

            except ValueError:

                pass

    if not scores:

        return None, word_count

    return round(sum(scores) / len(scores), 2), word_count

def collect_tsv_metrics(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int) -> Tuple[Optional[float], int, Optional[str]]:

    with tempfile.TemporaryDirectory() as tmp_dir:

        img_path = str(Path(tmp_dir) / "page.png")

        if not cv2.imwrite(img_path, image):

            return None, 0, "Échec d'écriture de l'image temporaire pour TSV"

        command = tesseract_base_command(img_path, "stdout", lang, psm, oem) + ["tsv"]

        try:

            result = run_command(command, timeout)

        except subprocess.TimeoutExpired:

            return None, 0, f"Timeout TSV dépassé ({timeout}s)"

        if result.returncode != 0:

            return None, 0, safe_error_text(result.stderr)

        mean_conf, words = parse_tsv_metrics(result.stdout)

        return mean_conf, words, None

def generate_searchable_page_pdf(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int, dest_path: Path) -> Optional[str]:

    with tempfile.TemporaryDirectory() as tmp_dir:

        img_path = str(Path(tmp_dir) / "page.png")

        out_base = str(Path(tmp_dir) / "page_ocr")

        if not cv2.imwrite(img_path, image):

            return "Échec d'écriture de l'image temporaire pour PDF"

        command = tesseract_base_command(img_path, out_base, lang, psm, oem) + ["pdf"]

        try:

            result = run_command(command, timeout)

        except subprocess.TimeoutExpired:

            return f"Timeout PDF Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:

            return safe_error_text(result.stderr)

        produced = Path(out_base + ".pdf")

        if not produced.exists():

            return "Tesseract n'a pas produit le PDF attendu"

        atomic_write_bytes(dest_path, produced.read_bytes())

        return None

---------------------------------------------------------------------------
Worker par page
---------------------------------------------------------------------------
def process_page(task: Dict[str, object]) -> Dict[str, object]:

    page = PageRef(**task["page"])

    page_no = page.page_no

    started = time.perf_counter()

    lang = str(task["lang"])

    psm = int(task["psm"])

    oem = int(task["oem"])

    dpi = int(task["dpi"])

    timeout = int(task["timeout"])

    pdf_mode = str(task["pdf_mode"])

    no_preprocess = bool(task["no_preprocess"])

    text_threshold = int(task["text_threshold"])

    min_chars = int(task["min_chars"])

    retries = int(task["retries"])

    collect_conf = bool(task["collect_conf"])

    searchable_pdf = bool(task["searchable_pdf"])

    paths = {k: Path(v) for k, v in task["paths"].items()}

    text_path = page_text_path(paths, page_no)

    meta_path = page_meta_path(paths, page_no)

    pdf_path = page_pdf_path(paths, page_no)

    attempts_meta: List[Dict[str, object]] = []

    selected_attempt: Optional[AttemptSpec] = None

    selected_image: Optional[np.ndarray] = None

    selected_text = ""

    selected_error: Optional[str] = None

    method = "failed"

    native_text = ""

    confidence_mean: Optional[float] = None

    confidence_words = 0

    pdf_error: Optional[str] = None

    try:

        if page.kind == "pdf" and pdf_mode in {"auto", "text"}:

            native_text = extract_pdf_native_text(page.source_path, int(page.pdf_index))

            if meaningful_text(native_text, text_threshold):

                method = "native-text"

                selected_text = native_text

                selected_error = None

                atomic_write_text(text_path, selected_text + "\n")

                duration_ms = round((time.perf_counter() - started) * 1000, 2)

                meta = {

                    "page_no": page_no,

                    "status": "done",

                    "source_kind": page.kind,

                    "source_path": page.source_path,

                    "pdf_index": page.pdf_index,

                    "method": method,

                    "chars": len(selected_text),

                    "non_ws_chars": compact_len(selected_text),

                    "attempt_count": 0,

                    "attempts": [],

                    "selected_attempt": None,

                    "confidence_mean": None,

                    "confidence_words": 0,

                    "error": None,

                    "pdf_page_error": None,

                    "searchable_pdf_page": None,

                    "duration_ms": duration_ms,

                    "processed_at": utc_now_iso(),

                }

                atomic_write_json(meta_path, meta)

                return meta

            if pdf_mode == "text":

                selected_error = f"Aucun texte natif exploitable détecté sur la page {page_no}"

                selected_text = f"[NO NATIVE TEXT page {page_no}]"

                method = "native-text"

                atomic_write_text(text_path, selected_text + "\n")

                duration_ms = round((time.perf_counter() - started) * 1000, 2)

                meta = {

                    "page_no": page_no,

                    "status": "done",

                    "source_kind": page.kind,

                    "source_path": page.source_path,

                    "pdf_index": page.pdf_index,

                    "method": method,

                    "chars": len(selected_text),

                    "non_ws_chars": compact_len(selected_text),

                    "attempt_count": 0,

                    "attempts": [],

                    "selected_attempt": None,

                    "confidence_mean": None,

                    "confidence_words": 0,

                    "error": selected_error,

                    "pdf_page_error": None,

                    "searchable_pdf_page": None,

                    "duration_ms": duration_ms,

                    "processed_at": utc_now_iso(),

                }

                atomic_write_json(meta_path, meta)

                return meta

        if page.kind == "image":

            source_image = read_image(page.source_path)

        elif page.kind == "pdf":

            source_image = render_pdf_page(page.source_path, int(page.pdf_index), dpi)

        else:

            raise RuntimeError(f"Type de page inconnu : {page.kind}")

        best_score = -1

        plan = build_attempt_plan(psm, no_preprocess, retries)

        for attempt in plan:

            try:

                processed = preprocess_variant(source_image, attempt.variant)

                text, err = run_tesseract_text(processed, lang, attempt.psm, oem, timeout)

            except Exception as exc:

                text = f"[OCR ERROR page {page_no}: {exc}]"

                err = str(exc)

                processed = None

            score = compact_len(text)

            attempts_meta.append(

                {

                    "variant": attempt.variant,

                    "psm": attempt.psm,

                    "chars": len(text),

                    "non_ws_chars": score,

                    "error": err,

                }

            )

            if score > best_score:

                best_score = score

                selected_attempt = attempt

                selected_image = processed

                selected_text = text

                selected_error = err

                method = "ocr"

            if err is None and score >= min_chars:

                selected_attempt = attempt

                selected_image = processed

                selected_text = text

                selected_error = None

                method = "ocr"

                break

        if selected_attempt is None or selected_image is None:

            raise RuntimeError("Aucune tentative OCR exploitable")

        if collect_conf and method == "ocr" and selected_error is None:

            confidence_mean, confidence_words, conf_err = collect_tsv_metrics(

                selected_image, lang, selected_attempt.psm, oem, timeout

            )

            if conf_err:

                attempts_meta.append(

                    {

                        "variant": selected_attempt.variant,

                        "psm": selected_attempt.psm,

                        "tsv_metrics_error": conf_err,

                    }

                )

        if searchable_pdf and method == "ocr":

            pdf_error = generate_searchable_page_pdf(

                selected_image, lang, selected_attempt.psm, oem, timeout, pdf_path

            )

        atomic_write_text(text_path, selected_text + "\n")

        duration_ms = round((time.perf_counter() - started) * 1000, 2)

        meta = {

            "page_no": page_no,

            "status": "done",

            "source_kind": page.kind,

            "source_path": page.source_path,

            "pdf_index": page.pdf_index,

            "method": method,

            "chars": len(selected_text),

            "non_ws_chars": compact_len(selected_text),

            "attempt_count": len(attempts_meta),

            "attempts": attempts_meta,

            "selected_attempt": None

            if selected_attempt is None

            else {"variant": selected_attempt.variant, "psm": selected_attempt.psm},

            "confidence_mean": confidence_mean,

            "confidence_words": confidence_words,

            "error": selected_error,

            "pdf_page_error": pdf_error,

            "searchable_pdf_page": str(pdf_path.resolve()) if pdf_path.exists() else None,

            "duration_ms": duration_ms,

            "processed_at": utc_now_iso(),

        }

        atomic_write_json(meta_path, meta)

        return meta

    except Exception as exc:

        duration_ms = round((time.perf_counter() - started) * 1000, 2)

        error = str(exc)

        fallback_text = f"[OCR ERROR page {page_no}: {error}]"

        atomic_write_text(text_path, fallback_text + "\n")

        meta = {

            "page_no": page_no,

            "status": "done",

            "source_kind": page.kind,

            "source_path": page.source_path,

            "pdf_index": page.pdf_index,

            "method": "failed",

            "chars": len(fallback_text),

            "non_ws_chars": compact_len(fallback_text),

            "attempt_count": len(attempts_meta),

            "attempts": attempts_meta,

            "selected_attempt": None,

            "confidence_mean": None,

            "confidence_words": 0,

            "error": error,

            "pdf_page_error": None,

            "searchable_pdf_page": None,

            "duration_ms": duration_ms,

            "processed_at": utc_now_iso(),

        }

        atomic_write_json(meta_path, meta)

        return meta

---------------------------------------------------------------------------
Assemblage texte / PDF / rapports
---------------------------------------------------------------------------
def assemble_text_output(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:

    blocks: List[str] = []

    for page in pages:

        part = page_text_path(paths, page.page_no)

        if part.exists():

            text = part.read_text(encoding="utf-8", errors="replace").rstrip()

        else:

            text = f"[MISSING PAGE {page.page_no}]"

        blocks.append(f"{SEPARATOR}\nPAGE {page.page_no}\n{SEPARATOR}\n{text}")

    atomic_write_text(paths["txt_out"], "\n\n".join(blocks) + "\n")

def append_image_as_pdf_page(doc, image_path: str) -> None:

    import fitz

    rect = fitz.Rect(0, 0, 595, 842)

    try:

        pix = fitz.Pixmap(image_path)

        rect = fitz.Rect(0, 0, pix.width, pix.height)

    except Exception:

        pass

    page = doc.new_page(width=rect.width, height=rect.height)

    page.insert_image(rect, filename=image_path)

def assemble_searchable_pdf(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:

    import fitz

    out_doc = fitz.open()

    source_pdf_doc = None

    try:

        if pages and pages[0].kind == "pdf":

            source_pdf_doc = fitz.open(pages[0].source_path)

        for page in pages:

            meta_file = page_meta_path(paths, page.page_no)

            meta = {}

            if meta_file.exists():

                meta = json.loads(meta_file.read_text(encoding="utf-8"))

            method = meta.get("method")

            page_pdf = page_pdf_path(paths, page.page_no)

            if method == "native-text" and source_pdf_doc is not None and page.pdf_index is not None:

                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)

                continue

            if page_pdf.exists():

                with fitz.open(str(page_pdf)) as tmp_doc:

                    out_doc.insert_pdf(tmp_doc)

                continue

            if page.kind == "pdf" and source_pdf_doc is not None and page.pdf_index is not None:

                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)

            elif page.kind == "image":

                append_image_as_pdf_page(out_doc, page.source_path)

        out_doc.save(str(paths["searchable_pdf"]), garbage=3, deflate=True)

    finally:

        if source_pdf_doc is not None:

            source_pdf_doc.close()

        out_doc.close()

def generate_reports(paths: Dict[str, Path], pages: Sequence[PageRef], state: Dict, args: argparse.Namespace) -> None:

    rows: List[Dict[str, object]] = []

    method_counts: Dict[str, int] = {}

    total_chars = 0

    error_count = 0

    total_duration_ms = 0.0

    confidence_values: List[float] = []

    for page in pages:

        meta_path = page_meta_path(paths, page.page_no)

        if not meta_path.exists():

            rows.append(

                {

                    "page_no": page.page_no,

                    "status": "missing",

                    "method": None,

                    "chars": 0,

                    "non_ws_chars": 0,

                    "duration_ms": 0,

                    "confidence_mean": None,

                    "confidence_words": 0,

                    "attempt_count": 0,

                    "error": "missing meta",

                    "searchable_pdf_page": None,

                    "source_kind": page.kind,

                    "source_path": page.source_path,

                    "pdf_index": page.pdf_index,

                }

            )

            error_count += 1

            continue

        meta = json.loads(meta_path.read_text(encoding="utf-8"))

        rows.append(

            {

                "page_no": meta.get("page_no"),

                "status": meta.get("status"),

                "method": meta.get("method"),

                "chars": meta.get("chars"),

                "non_ws_chars": meta.get("non_ws_chars"),

                "duration_ms": meta.get("duration_ms"),

                "confidence_mean": meta.get("confidence_mean"),

                "confidence_words": meta.get("confidence_words"),

                "attempt_count": meta.get("attempt_count"),

                "error": meta.get("error"),

                "searchable_pdf_page": meta.get("searchable_pdf_page"),

                "source_kind": meta.get("source_kind"),

                "source_path": meta.get("source_path"),

                "pdf_index": meta.get("pdf_index"),

            }

        )

        method = str(meta.get("method"))

        method_counts[method] = method_counts.get(method, 0) + 1

        total_chars += int(meta.get("chars") or 0)

        total_duration_ms += float(meta.get("duration_ms") or 0.0)

        if meta.get("error"):

            error_count += 1

        if meta.get("confidence_mean") is not None:

            try:

                confidence_values.append(float(meta["confidence_mean"]))

            except Exception:

                pass

    fieldnames = [

        "page_no",

        "status",

        "method",

        "chars",

        "non_ws_chars",

        "duration_ms",

        "confidence_mean",

        "confidence_words",

        "attempt_count",

        "error",

        "searchable_pdf_page",

        "source_kind",

        "source_path",

        "pdf_index",

    ]

    paths["pages_csv"].parent.mkdir(parents=True, exist_ok=True)

    with paths["pages_csv"].open("w", encoding="utf-8", newline="") as f:

        writer = csv.DictWriter(f, fieldnames=fieldnames)

        writer.writeheader()

        writer.writerows(rows)

    summary = {

        "generated_at": utc_now_iso(),

        "source": str(Path(args.source).resolve()),

        "text_output": str(paths["txt_out"].resolve()),

        "searchable_pdf_output": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,

        "page_count": len(pages),

        "method_counts": method_counts,

        "error_pages": error_count,

        "total_chars": total_chars,

        "avg_confidence_mean": None if not confidence_values else round(sum(confidence_values) / len(confidence_values), 2),

        "total_duration_ms": round(total_duration_ms, 2),

        "bundle": str(paths["bundle"].resolve()),

        "csv_report": str(paths["pages_csv"].resolve()),

        "state_file": str(paths["state"].resolve()),

        "config": {

            "dpi": args.dpi,

            "lang": args.lang,

            "psm": args.psm,

            "oem": args.oem,

            "workers": args.workers,

            "timeout": args.timeout,

            "pdf_mode": args.pdf_mode,

            "text_threshold": args.text_threshold,

            "no_preprocess": args.no_preprocess,

            "min_chars": args.min_chars,

            "retries": args.retries,

            "searchable_pdf": args.searchable_pdf,

            "tsv_metrics": not args.no_tsv_metrics,

        },

        "state_snapshot": state,

    }

    atomic_write_json(paths["summary"], summary)

---------------------------------------------------------------------------
Pipeline principal
---------------------------------------------------------------------------
def ocr_pipeline(args: argparse.Namespace, paths: Dict[str, Path]) -> str:

    source = Path(args.source)

    pages = list_pages(source, args.start, args.end)

    valid_page_nos = {p.page_no for p in pages}

    state = load_state(paths["state"]) if args.resume else {}

    if state:

        ensure_state_compatible(state, args, paths)

    else:

        state = init_state(args, paths, pages)

        save_state(paths["state"], state)

    done_pages = completed_pages_from_state(state, paths, valid_page_nos)

    pages_to_do = [p for p in pages if p.page_no not in done_pages]

    if done_pages:

        LOG.info("Mode reprise — %d page(s) déjà finalisées", len(done_pages))

    if not pages_to_do:

        LOG.info("Toutes les pages demandées sont déjà traitées.")

        assemble_text_output(paths, pages)

        if args.searchable_pdf:

            assemble_searchable_pdf(paths, pages)

        generate_reports(paths, pages, state, args)

        return str(paths["txt_out"])

    worker_count = min(args.workers, len(pages_to_do))

    LOG.info(

        "%d page(s) à traiter (workers=%d, dpi=%d, psm=%d, oem=%d, pdf_mode=%s, preprocess=%s, retries=%d)",

        len(pages_to_do),

        worker_count,

        args.dpi,

        args.psm,

        args.oem,

        args.pdf_mode,

        not args.no_preprocess,

        args.retries,

    )

    base_task = {

        "lang": args.lang,

        "psm": args.psm,

        "oem": args.oem,

        "dpi": args.dpi,

        "timeout": args.timeout,

        "pdf_mode": args.pdf_mode,

        "no_preprocess": args.no_preprocess,

        "text_threshold": args.text_threshold,

        "min_chars": args.min_chars,

        "retries": args.retries,

        "collect_conf": not args.no_tsv_metrics,

        "searchable_pdf": args.searchable_pdf,

        "paths": {k: str(v) for k, v in paths.items()},

    }

    tasks: List[Dict[str, object]] = []

    for page in pages_to_do:

        task = dict(base_task)

        task["page"] = {

            "page_no": page.page_no,

            "kind": page.kind,

            "source_path": page.source_path,

            "pdf_index": page.pdf_index,

        }

        tasks.append(task)

    total = len(tasks)

    completed = 0

    with ProcessPoolExecutor(max_workers=worker_count) as executor:

        future_map = {executor.submit(process_page, task): task["page"]["page_no"] for task in tasks}

        for future in as_completed(future_map):

            page_no = future_map[future]

            try:

                meta = future.result()

            except Exception as exc:

                error = f"Crash worker: {exc}"

                fallback = {

                    "page_no": page_no,

                    "status": "done",

                    "source_kind": None,

                    "source_path": None,

                    "pdf_index": None,

                    "method": "failed",

                    "chars": len(error),

                    "non_ws_chars": compact_len(error),

                    "attempt_count": 0,

                    "attempts": [],

                    "selected_attempt": None,

                    "confidence_mean": None,

                    "confidence_words": 0,

                    "error": error,

                    "pdf_page_error": None,

                    "searchable_pdf_page": None,

                    "duration_ms": 0.0,

                    "processed_at": utc_now_iso(),

                }

                atomic_write_text(page_text_path(paths, page_no), f"[OCR ERROR page {page_no}: {error}]\n")

                atomic_write_json(page_meta_path(paths, page_no), fallback)

                meta = fallback

            state["pages"][str(page_no)] = {

                "status": meta.get("status"),

                "method": meta.get("method"),

                "chars": meta.get("chars"),

                "error": meta.get("error"),

                "duration_ms": meta.get("duration_ms"),

                "processed_at": meta.get("processed_at"),

            }

            save_state(paths["state"], state)

            completed += 1

            error_mark = " ⚠️ ERREUR" if meta.get("error") or meta.get("pdf_page_error") else ""

            LOG.info(

                "Page %d traitée [%d/%d] — %s — %d chars%s",

                page_no,

                completed,

                total,

                meta.get("method"),

                int(meta.get("chars") or 0),

                error_mark,

            )

    assemble_text_output(paths, pages)

    if args.searchable_pdf:

        LOG.info("Assemblage du PDF searchable…")

        assemble_searchable_pdf(paths, pages)

    state["assembled_at"] = utc_now_iso()

    save_state(paths["state"], state)

    generate_reports(paths, pages, state, args)

    total_chars = sum(

        len(page_text_path(paths, p.page_no).read_text(encoding="utf-8", errors="replace"))

        for p in pages

        if page_text_path(paths, p.page_no).exists()

    )

    LOG.info("OCR terminé → %s (%d chars)", paths["txt_out"], total_chars)

    if args.searchable_pdf:

        LOG.info("PDF searchable → %s", paths["searchable_pdf"])

    LOG.info("Bundle de run → %s", paths["bundle"])

    return str(paths["txt_out"])

---------------------------------------------------------------------------
CLI
---------------------------------------------------------------------------
def build_arg_parser() -> argparse.ArgumentParser:

    ap = argparse.ArgumentParser(description="OCR orienté production — PDF, images, dossiers")

    ap.add_argument("source", help="PDF, image ou dossier d'images")

    ap.add_argument("--dpi", type=int, default=300, help="Résolution de rendu PDF (défaut: 300)")

    ap.add_argument("--start", type=int, default=1, help="Première page PDF (défaut: 1)")

    ap.add_argument("--end", type=int, default=None, help="Dernière page PDF (défaut: fin document)")

    ap.add_argument("--timeout", type=int, default=120, help="Timeout Tesseract par page")

    ap.add_argument("--psm", type=int, default=3, help="Page Segmentation Mode Tesseract")

    ap.add_argument("--oem", type=int, default=1, help="OCR Engine Mode Tesseract")

    ap.add_argument(

        "--workers",

        type=int,

        default=max(1, (os.cpu_count() or 2) // 2),

        help="Nombre de workers parallèles",

    )

    ap.add_argument("--resume", action="store_true", help="Reprendre une run interrompue")

    ap.add_argument("--out-dir", default=None, help="Dossier de sortie")

    ap.add_argument("--suffix", default=".ocr.txt", help="Suffixe de la sortie texte")

    ap.add_argument("--lang", default="ara+fra", help="Langues Tesseract (défaut: ara+fra)")

    ap.add_argument(

        "--pdf-mode",

        choices=["auto", "ocr", "text"],

        default="auto",

        help="PDF: auto=texte natif si exploitable sinon OCR ; ocr=OCR forcé ; text=texte natif seul",

    )

    ap.add_argument(

        "--text-threshold",

        type=int,

        default=25,

        help="Seuil minimal de caractères non blancs pour accepter le texte natif PDF",

    )

    ap.add_argument("--no-preprocess", action="store_true", help="Désactive le prétraitement image")

    ap.add_argument(

        "--min-chars",

        type=int,

        default=10,

        help="Seuil minimal de caractères non blancs pour accepter une tentative OCR",

    )

    ap.add_argument(

        "--retries",

        type=int,

        default=2,

        help="Nombre de tentatives supplémentaires avec variantes de prétraitement / PSM",

    )

    ap.add_argument(

        "--no-tsv-metrics",

        action="store_true",

        help="Désactive la collecte de métriques TSV (confiance moyenne, nombre de mots)",

    )

    ap.add_argument(

        "--searchable-pdf",

        action="store_true",

        help="Produit un PDF searchable final quand possible",

    )

    ap.add_argument("--verbose", action="store_true", help="Logs verbeux")

    return ap

def main() -> None:

    parser = build_arg_parser()

    args = parser.parse_args()

    validate_args(args)

    paths = compute_paths(Path(args.source), args.out_dir, args.suffix)

    reset_outputs(paths, args.resume)

    configure_logging(paths["run_log"], verbose=args.verbose)

    check_tesseract_languages(args.lang)

    check_pdf_support_if_needed(Path(args.source))

    try:

        ocr_pipeline(args, paths)

    except KeyboardInterrupt:

        die("Interruption utilisateur.", code=130)

if name == "main":

    main()


Write
downloads/annales_svt/scripts/ocr_pipeline_production.py
1298 lines
open
ocr_pipeline_production.py
PYTHON
#!/usr/bin/env python3
"""
Pipeline OCR orienté production — arabe (principal) + français.

Ce script traite :
- PDF
- image unique (PNG, JPG, JPEG, TIFF, TIF, BMP, WEBP)
- dossier d'images

Objectifs :
- reprise robuste par page via état JSON + artefacts intermédiaires
- pas de chargement complet du document en RAM
- parallélisation par page sans sérialiser les images entre processus
- extraction native du texte PDF avant OCR si demandé
- métriques d'exécution par page et globales
- sortie texte finale réassemblée dans l'ordre
- CSV et JSON de reporting
- PDF searchable optionnel
- journal d'exécution sur disque

Dépendances Python :
  pip install numpy opencv-python pymupdf

Dépendances système :
  tesseract-ocr + langues (par défaut ara et fra)

Exemples :
  python ocr_pipeline_production.py input/doc.pdf --workers 4 --resume
  python ocr_pipeline_production.py input/doc.pdf --searchable-pdf --workers 4
  python ocr_pipeline_production.py input/scans/ --workers 6 --retries 3
"""

from __future__ import annotations

import argparse
import csv
import json
import logging
import os
import re
import shutil
import subprocess
import sys
import tempfile
import time
from concurrent.futures import ProcessPoolExecutor, as_completed
from dataclasses import dataclass
from datetime import datetime, timezone
from pathlib import Path
from typing import Dict, List, Optional, Sequence, Set, Tuple

import cv2
import numpy as np


IMAGE_EXTENSIONS = {".png", ".jpg", ".jpeg", ".tiff", ".tif", ".bmp", ".webp"}
PDF_EXTENSIONS = {".pdf"}
SEPARATOR = "=" * 70
STATE_VERSION = 2
LOG = logging.getLogger("ocr_pipeline_production")


@dataclass(frozen=True)
class PageRef:
    page_no: int
    kind: str  # 'pdf' | 'image'
    source_path: str
    pdf_index: Optional[int] = None


@dataclass(frozen=True)
class AttemptSpec:
    variant: str
    psm: int


# ---------------------------------------------------------------------------
# Logging / utilitaires de base
# ---------------------------------------------------------------------------

def configure_logging(log_file: Optional[Path] = None, verbose: bool = False) -> None:
    LOG.setLevel(logging.DEBUG if verbose else logging.INFO)
    LOG.handlers.clear()
    LOG.propagate = False

    formatter = logging.Formatter(
        fmt="%(asctime)s [%(levelname)s] %(message)s",
        datefmt="%H:%M:%S",
    )

    console = logging.StreamHandler(sys.stdout)
    console.setLevel(logging.DEBUG if verbose else logging.INFO)
    console.setFormatter(formatter)
    LOG.addHandler(console)

    if log_file is not None:
        log_file.parent.mkdir(parents=True, exist_ok=True)
        file_handler = logging.FileHandler(log_file, encoding="utf-8")
        file_handler.setLevel(logging.DEBUG)
        file_handler.setFormatter(formatter)
        LOG.addHandler(file_handler)


def die(message: str, code: int = 1) -> None:
    LOG.error(message)
    raise SystemExit(code)


def utc_now_iso() -> str:
    return datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z")


def natural_key(value: str):
    return [int(part) if part.isdigit() else part.lower() for part in re.split(r"(\d+)", value)]


def atomic_write_text(path: Path, content: str, encoding: str = "utf-8") -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with tempfile.NamedTemporaryFile("w", delete=False, dir=str(path.parent), encoding=encoding) as tmp:
        tmp.write(content)
        tmp_path = Path(tmp.name)
    os.replace(tmp_path, path)


def atomic_write_json(path: Path, data: Dict) -> None:
    atomic_write_text(path, json.dumps(data, ensure_ascii=False, indent=2) + "\n")


def atomic_write_bytes(path: Path, payload: bytes) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with tempfile.NamedTemporaryFile("wb", delete=False, dir=str(path.parent)) as tmp:
        tmp.write(payload)
        tmp_path = Path(tmp.name)
    os.replace(tmp_path, path)


def compact_len(text: str) -> int:
    return len(re.sub(r"\s+", "", text or ""))


def safe_error_text(text: str, limit: int = 500) -> str:
    return (text or "").strip().replace("\n", " ")[:limit] or "Erreur sans détail"


# ---------------------------------------------------------------------------
# Chemins d'artefacts
# ---------------------------------------------------------------------------

def compute_paths(source: Path, out_dir: Optional[str], suffix: str) -> Dict[str, Path]:
    if out_dir:
        out_root = Path(out_dir)
        out_root.mkdir(parents=True, exist_ok=True)
        txt_out = out_root / f"{source.stem}{suffix}"
        searchable_pdf = out_root / f"{source.stem}.searchable.pdf"
    elif source.is_dir():
        txt_out = source.parent / f"{source.name}{suffix}"
        searchable_pdf = source.parent / f"{source.name}.searchable.pdf"
    else:
        txt_out = source.with_suffix(suffix)
        searchable_pdf = txt_out.parent / f"{source.stem}.searchable.pdf"

    bundle = txt_out.with_name(txt_out.name + ".bundle")
    return {
        "txt_out": txt_out,
        "searchable_pdf": searchable_pdf,
        "bundle": bundle,
        "pages_txt": bundle / "pages_txt",
        "pages_meta": bundle / "pages_meta",
        "pages_pdf": bundle / "pages_pdf",
        "state": bundle / "run.state.json",
        "summary": bundle / "summary.json",
        "pages_csv": bundle / "pages.csv",
        "run_log": bundle / "run.log",
    }


def page_text_path(paths: Dict[str, Path], page_no: int) -> Path:
    return paths["pages_txt"] / f"page_{page_no:06d}.txt"


def page_meta_path(paths: Dict[str, Path], page_no: int) -> Path:
    return paths["pages_meta"] / f"page_{page_no:06d}.json"


def page_pdf_path(paths: Dict[str, Path], page_no: int) -> Path:
    return paths["pages_pdf"] / f"page_{page_no:06d}.pdf"


def reset_outputs(paths: Dict[str, Path], resume: bool) -> None:
    if resume:
        paths["bundle"].mkdir(parents=True, exist_ok=True)
        paths["pages_txt"].mkdir(parents=True, exist_ok=True)
        paths["pages_meta"].mkdir(parents=True, exist_ok=True)
        paths["pages_pdf"].mkdir(parents=True, exist_ok=True)
        return

    if paths["bundle"].exists():
        shutil.rmtree(paths["bundle"])
    if paths["txt_out"].exists():
        paths["txt_out"].unlink()
    if paths["searchable_pdf"].exists():
        paths["searchable_pdf"].unlink()

    for key in ("bundle", "pages_txt", "pages_meta", "pages_pdf"):
        paths[key].mkdir(parents=True, exist_ok=True)


# ---------------------------------------------------------------------------
# Validation / prérequis
# ---------------------------------------------------------------------------

def parse_langs(lang_spec: str) -> List[str]:
    langs = [chunk.strip() for chunk in lang_spec.split("+") if chunk.strip()]
    if not langs:
        raise ValueError("Aucune langue Tesseract valide dans --lang")
    return langs


def check_tesseract_languages(lang_spec: str) -> None:
    if not shutil.which("tesseract"):
        die("Tesseract introuvable dans le PATH.")

    result = subprocess.run(
        ["tesseract", "--list-langs"],
        capture_output=True,
        text=True,
        check=False,
    )
    if result.returncode != 0:
        die(f"Échec de tesseract --list-langs : {safe_error_text(result.stderr)}")

    installed = {
        line.strip()
        for line in (result.stdout + "\n" + result.stderr).splitlines()
        if line.strip() and not line.lower().startswith("list of available languages")
    }
    missing = [lang for lang in parse_langs(lang_spec) if lang not in installed]
    if missing:
        die(
            "Langues Tesseract manquantes : "
            f"{', '.join(missing)}\n"
            f"Ubuntu/Debian : apt install {' '.join(f'tesseract-ocr-{x}' for x in missing)}"
        )


def check_pdf_support_if_needed(source: Path) -> None:
    need_pdf = source.is_file() and source.suffix.lower() in PDF_EXTENSIONS
    if not need_pdf:
        return
    try:
        import fitz  # noqa: F401
    except Exception as exc:
        die(f"Support PDF indisponible. Installez PyMuPDF (pymupdf). Détail : {exc}")


def validate_args(args: argparse.Namespace) -> None:
    source = Path(args.source)
    if not source.exists():
        die(f"Source introuvable : {source}")

    if args.dpi <= 0:
        die("--dpi doit être > 0")
    if args.workers <= 0:
        die("--workers doit être >= 1")
    if args.timeout <= 0:
        die("--timeout doit être > 0")
    if args.start <= 0:
        die("--start doit être >= 1")
    if args.end is not None and args.end < args.start:
        die("--end doit être >= --start")
    if args.psm < 0 or args.psm > 13:
        die("--psm doit être entre 0 et 13")
    if args.oem not in (0, 1, 2, 3):
        die("--oem doit être dans {0,1,2,3}")
    if args.pdf_mode not in {"auto", "ocr", "text"}:
        die("--pdf-mode doit être dans {auto, ocr, text}")
    if args.text_threshold < 0:
        die("--text-threshold doit être >= 0")
    if args.min_chars < 0:
        die("--min-chars doit être >= 0")
    if args.retries < 0:
        die("--retries doit être >= 0")

    if source.is_file() and source.suffix.lower() not in (IMAGE_EXTENSIONS | PDF_EXTENSIONS):
        die(f"Extension non supportée : {source.suffix}")

    if source.is_dir():
        files = [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS]
        if not files:
            die(f"Aucune image supportée trouvée dans : {source}")


# ---------------------------------------------------------------------------
# État de reprise
# ---------------------------------------------------------------------------

def resume_identity(args: argparse.Namespace) -> Dict[str, object]:
    return {
        "source": str(Path(args.source).resolve()),
        "dpi": args.dpi,
        "lang": args.lang,
        "psm": args.psm,
        "oem": args.oem,
        "pdf_mode": args.pdf_mode,
        "text_threshold": args.text_threshold,
        "no_preprocess": args.no_preprocess,
        "min_chars": args.min_chars,
        "retries": args.retries,
        "tsv_metrics": not args.no_tsv_metrics,
        "searchable_pdf": args.searchable_pdf,
        "start": args.start,
        "end": args.end,
    }


def load_state(path: Path) -> Dict:
    if not path.exists():
        return {}
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except Exception as exc:
        die(f"Fichier d'état illisible : {path} ({exc})")


def save_state(path: Path, state: Dict) -> None:
    state["updated_at"] = utc_now_iso()
    atomic_write_json(path, state)


def ensure_state_compatible(state: Dict, args: argparse.Namespace, paths: Dict[str, Path]) -> None:
    if not state:
        return
    if state.get("version") != STATE_VERSION:
        die("Version d'état incompatible. Relancez sans --resume ou supprimez le bundle.")
    if state.get("identity") != resume_identity(args):
        die("Le bundle existant ne correspond pas à la même configuration fonctionnelle.")
    if state.get("output_txt") != str(paths["txt_out"].resolve()):
        die("Le bundle existant ne correspond pas à cette destination texte.")


def init_state(args: argparse.Namespace, paths: Dict[str, Path], pages: Sequence[PageRef]) -> Dict:
    return {
        "version": STATE_VERSION,
        "created_at": utc_now_iso(),
        "updated_at": utc_now_iso(),
        "identity": resume_identity(args),
        "output_txt": str(paths["txt_out"].resolve()),
        "output_searchable_pdf": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,
        "page_count": len(pages),
        "pages": {},
        "run_config": {
            "workers": args.workers,
            "timeout": args.timeout,
            "verbose": args.verbose,
        },
    }


def completed_pages_from_state(state: Dict, paths: Dict[str, Path], valid_page_nos: Set[int]) -> Set[int]:
    done: Set[int] = set()
    for key, meta in state.get("pages", {}).items():
        try:
            pno = int(key)
        except Exception:
            continue
        if pno not in valid_page_nos:
            continue
        if meta.get("status") == "done" and page_text_path(paths, pno).exists() and page_meta_path(paths, pno).exists():
            done.add(pno)
    return done


# ---------------------------------------------------------------------------
# Pages source
# ---------------------------------------------------------------------------

def list_pages(source: Path, start: int, end: Optional[int]) -> List[PageRef]:
    if source.is_dir():
        files = sorted(
            [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS],
            key=lambda p: natural_key(p.name),
        )
        first = max(1, start)
        last = len(files) if end is None else min(len(files), end)
        if first > last:
            die(f"Intervalle vide : start={first}, end={last}, total={len(files)}")
        return [
            PageRef(page_no=i, kind="image", source_path=str(path.resolve()))
            for i, path in enumerate(files[first - 1:last], start=first)
        ]

    if source.suffix.lower() in IMAGE_EXTENSIONS:
        return [PageRef(page_no=1, kind="image", source_path=str(source.resolve()))]

    if source.suffix.lower() in PDF_EXTENSIONS:
        import fitz
        with fitz.open(str(source)) as doc:
            total = doc.page_count

        first = max(1, start)
        last = total if end is None else min(total, end)
        if first > last:
            die(f"Intervalle vide : start={first}, end={last}, total={total}")

        return [
            PageRef(page_no=pno, kind="pdf", source_path=str(source.resolve()), pdf_index=pno - 1)
            for pno in range(first, last + 1)
        ]

    die(f"Type de source non supporté : {source}")
    return []


# ---------------------------------------------------------------------------
# Lecture / rendu source
# ---------------------------------------------------------------------------

def read_image(path: str) -> np.ndarray:
    img = cv2.imread(path, cv2.IMREAD_COLOR)
    if img is None:
        raise RuntimeError(f"Impossible de lire l'image : {path}")
    return img


def render_pdf_page(pdf_path: str, pdf_index: int, dpi: int) -> np.ndarray:
    import fitz

    with fitz.open(pdf_path) as doc:
        page = doc[pdf_index]
        matrix = fitz.Matrix(dpi / 72.0, dpi / 72.0)
        pix = page.get_pixmap(matrix=matrix, alpha=False)
        arr = np.frombuffer(pix.samples, dtype=np.uint8).reshape(pix.height, pix.width, pix.n).copy()

    if pix.n == 1:
        return cv2.cvtColor(arr, cv2.COLOR_GRAY2BGR)
    if pix.n == 4:
        return cv2.cvtColor(arr, cv2.COLOR_RGBA2BGR)
    return cv2.cvtColor(arr, cv2.COLOR_RGB2BGR)


def extract_pdf_native_text(pdf_path: str, pdf_index: int) -> str:
    import fitz
    with fitz.open(pdf_path) as doc:
        return doc[pdf_index].get_text("text").strip()


def meaningful_text(text: str, threshold: int) -> bool:
    return compact_len(text) >= threshold


# ---------------------------------------------------------------------------
# Prétraitement image
# ---------------------------------------------------------------------------

def to_gray(image: np.ndarray) -> np.ndarray:
    if image.ndim == 2:
        return image.copy()
    return cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)


def estimate_skew_angle(gray: np.ndarray) -> float:
    blurred = cv2.GaussianBlur(gray, (5, 5), 0)
    _ret, thresh = cv2.threshold(blurred, 0, 255, cv2.THRESH_BINARY_INV + cv2.THRESH_OTSU)
    coords = np.column_stack(np.where(thresh > 0))
    if coords.shape[0] < 200:
        return 0.0

    angle = cv2.minAreaRect(coords)[-1]
    if angle < -45:
        angle = -(90 + angle)
    else:
        angle = -angle

    angle = float(angle)
    if abs(angle) < 0.2 or abs(angle) > 15:
        return 0.0
    return angle


def rotate_image(image: np.ndarray, angle: float) -> np.ndarray:
    h, w = image.shape[:2]
    matrix = cv2.getRotationMatrix2D((w / 2.0, h / 2.0), angle, 1.0)
    border_value = 255 if image.ndim == 2 else (255, 255, 255)
    return cv2.warpAffine(
        image,
        matrix,
        (w, h),
        flags=cv2.INTER_CUBIC,
        borderMode=cv2.BORDER_CONSTANT,
        borderValue=border_value,
    )


def normalize_gray(gray: np.ndarray) -> np.ndarray:
    angle = estimate_skew_angle(gray)
    if angle:
        gray = rotate_image(gray, angle)
    clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8, 8))
    normalized = clahe.apply(gray)
    denoised = cv2.fastNlMeansDenoising(normalized, h=7, templateWindowSize=7, searchWindowSize=21)
    return denoised


def preprocess_variant(image: np.ndarray, variant: str) -> np.ndarray:
    gray = to_gray(image)

    if variant == "raw-gray":
        return gray

    normalized = normalize_gray(gray)

    if variant == "gray":
        return normalized

    if variant == "adaptive":
        return cv2.adaptiveThreshold(
            normalized,
            255,
            cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
            cv2.THRESH_BINARY,
            31,
            11,
        )

    if variant == "otsu":
        _ret, binary = cv2.threshold(normalized, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
        return binary

    raise ValueError(f"Variant de prétraitement inconnue : {variant}")


def build_attempt_plan(base_psm: int, no_preprocess: bool, retries: int) -> List[AttemptSpec]:
    candidates: List[AttemptSpec] = []

    if no_preprocess:
        raw = [AttemptSpec("raw-gray", base_psm)]
        if base_psm != 6:
            raw.append(AttemptSpec("raw-gray", 6))
        if base_psm != 11:
            raw.append(AttemptSpec("raw-gray", 11))
        candidates.extend(raw)
    else:
        candidates.extend([
            AttemptSpec("adaptive", base_psm),
            AttemptSpec("gray", base_psm),
            AttemptSpec("otsu", base_psm),
        ])
        if base_psm != 6:
            candidates.append(AttemptSpec("adaptive", 6))
        if base_psm != 11:
            candidates.append(AttemptSpec("gray", 11))

    deduped: List[AttemptSpec] = []
    seen = set()
    for item in candidates:
        key = (item.variant, item.psm)
        if key not in seen:
            seen.add(key)
            deduped.append(item)

    return deduped[: max(1, 1 + retries)]


# ---------------------------------------------------------------------------
# Tesseract
# ---------------------------------------------------------------------------

def tesseract_base_command(img_path: str, output_base: str, lang: str, psm: int, oem: int) -> List[str]:
    return [
        "tesseract",
        img_path,
        output_base,
        "-l",
        lang,
        "--psm",
        str(psm),
        "--oem",
        str(oem),
        "-c",
        "preserve_interword_spaces=1",
    ]


def run_command(command: List[str], timeout: int) -> subprocess.CompletedProcess:
    env = os.environ.copy()
    env.setdefault("OMP_THREAD_LIMIT", "1")
    return subprocess.run(
        command,
        capture_output=True,
        text=True,
        encoding="utf-8",
        errors="replace",
        timeout=timeout,
        check=False,
        env=env,
    )


def run_tesseract_text(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int) -> Tuple[str, Optional[str]]:
    with tempfile.TemporaryDirectory() as tmp_dir:
        img_path = str(Path(tmp_dir) / "page.png")
        if not cv2.imwrite(img_path, image):
            raise RuntimeError("Échec d'écriture de l'image temporaire")

        try:
            result = run_command(tesseract_base_command(img_path, "stdout", lang, psm, oem), timeout)
        except subprocess.TimeoutExpired:
            return "[TIMEOUT ERROR]", f"Timeout Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:
            err = safe_error_text(result.stderr)
            return f"[TESSERACT ERROR: {err}]", err

        return result.stdout.strip(), None


def parse_tsv_metrics(tsv_text: str) -> Tuple[Optional[float], int]:
    lines = [line for line in tsv_text.splitlines() if line.strip()]
    if len(lines) <= 1:
        return None, 0

    scores: List[float] = []
    word_count = 0
    reader = csv.DictReader(lines, delimiter="\t")
    for row in reader:
        text = (row.get("text") or "").strip()
        conf_raw = (row.get("conf") or "").strip()
        if text:
            word_count += 1
        if conf_raw and conf_raw not in {"-1", "nan"}:
            try:
                scores.append(float(conf_raw))
            except ValueError:
                pass

    if not scores:
        return None, word_count
    return round(sum(scores) / len(scores), 2), word_count


def collect_tsv_metrics(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int) -> Tuple[Optional[float], int, Optional[str]]:
    with tempfile.TemporaryDirectory() as tmp_dir:
        img_path = str(Path(tmp_dir) / "page.png")
        if not cv2.imwrite(img_path, image):
            return None, 0, "Échec d'écriture de l'image temporaire pour TSV"

        command = tesseract_base_command(img_path, "stdout", lang, psm, oem) + ["tsv"]
        try:
            result = run_command(command, timeout)
        except subprocess.TimeoutExpired:
            return None, 0, f"Timeout TSV dépassé ({timeout}s)"

        if result.returncode != 0:
            return None, 0, safe_error_text(result.stderr)

        mean_conf, words = parse_tsv_metrics(result.stdout)
        return mean_conf, words, None


def generate_searchable_page_pdf(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int, dest_path: Path) -> Optional[str]:
    with tempfile.TemporaryDirectory() as tmp_dir:
        img_path = str(Path(tmp_dir) / "page.png")
        out_base = str(Path(tmp_dir) / "page_ocr")

        if not cv2.imwrite(img_path, image):
            return "Échec d'écriture de l'image temporaire pour PDF"

        command = tesseract_base_command(img_path, out_base, lang, psm, oem) + ["pdf"]
        try:
            result = run_command(command, timeout)
        except subprocess.TimeoutExpired:
            return f"Timeout PDF Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:
            return safe_error_text(result.stderr)

        produced = Path(out_base + ".pdf")
        if not produced.exists():
            return "Tesseract n'a pas produit le PDF attendu"

        atomic_write_bytes(dest_path, produced.read_bytes())
        return None


# ---------------------------------------------------------------------------
# Worker par page
# ---------------------------------------------------------------------------

def process_page(task: Dict[str, object]) -> Dict[str, object]:
    page = PageRef(**task["page"])
    page_no = page.page_no
    started = time.perf_counter()

    lang = str(task["lang"])
    psm = int(task["psm"])
    oem = int(task["oem"])
    dpi = int(task["dpi"])
    timeout = int(task["timeout"])
    pdf_mode = str(task["pdf_mode"])
    no_preprocess = bool(task["no_preprocess"])
    text_threshold = int(task["text_threshold"])
    min_chars = int(task["min_chars"])
    retries = int(task["retries"])
    collect_conf = bool(task["collect_conf"])
    searchable_pdf = bool(task["searchable_pdf"])
    paths = {k: Path(v) for k, v in task["paths"].items()}

    text_path = page_text_path(paths, page_no)
    meta_path = page_meta_path(paths, page_no)
    pdf_path = page_pdf_path(paths, page_no)

    attempts_meta: List[Dict[str, object]] = []
    selected_attempt: Optional[AttemptSpec] = None
    selected_image: Optional[np.ndarray] = None
    selected_text = ""
    selected_error: Optional[str] = None
    method = "failed"
    confidence_mean: Optional[float] = None
    confidence_words = 0
    pdf_error: Optional[str] = None

    try:
        if page.kind == "pdf" and pdf_mode in {"auto", "text"}:
            native_text = extract_pdf_native_text(page.source_path, int(page.pdf_index))
            if meaningful_text(native_text, text_threshold):
                method = "native-text"
                selected_text = native_text
                atomic_write_text(text_path, selected_text + "\n")

                duration_ms = round((time.perf_counter() - started) * 1000, 2)
                meta = {
                    "page_no": page_no,
                    "status": "done",
                    "source_kind": page.kind,
                    "source_path": page.source_path,
                    "pdf_index": page.pdf_index,
                    "method": method,
                    "chars": len(selected_text),
                    "non_ws_chars": compact_len(selected_text),
                    "attempt_count": 0,
                    "attempts": [],
                    "selected_attempt": None,
                    "confidence_mean": None,
                    "confidence_words": 0,
                    "error": None,
                    "pdf_page_error": None,
                    "searchable_pdf_page": None,
                    "duration_ms": duration_ms,
                    "processed_at": utc_now_iso(),
                }
                atomic_write_json(meta_path, meta)
                return meta

            if pdf_mode == "text":
                selected_error = f"Aucun texte natif exploitable détecté sur la page {page_no}"
                selected_text = f"[NO NATIVE TEXT page {page_no}]"
                method = "native-text"
                atomic_write_text(text_path, selected_text + "\n")

                duration_ms = round((time.perf_counter() - started) * 1000, 2)
                meta = {
                    "page_no": page_no,
                    "status": "done",
                    "source_kind": page.kind,
                    "source_path": page.source_path,
                    "pdf_index": page.pdf_index,
                    "method": method,
                    "chars": len(selected_text),
                    "non_ws_chars": compact_len(selected_text),
                    "attempt_count": 0,
                    "attempts": [],
                    "selected_attempt": None,
                    "confidence_mean": None,
                    "confidence_words": 0,
                    "error": selected_error,
                    "pdf_page_error": None,
                    "searchable_pdf_page": None,
                    "duration_ms": duration_ms,
                    "processed_at": utc_now_iso(),
                }
                atomic_write_json(meta_path, meta)
                return meta

        if page.kind == "image":
            source_image = read_image(page.source_path)
        elif page.kind == "pdf":
            source_image = render_pdf_page(page.source_path, int(page.pdf_index), dpi)
        else:
            raise RuntimeError(f"Type de page inconnu : {page.kind}")

        best_score = -1
        plan = build_attempt_plan(psm, no_preprocess, retries)

        for attempt in plan:
            try:
                processed = preprocess_variant(source_image, attempt.variant)
                text, err = run_tesseract_text(processed, lang, attempt.psm, oem, timeout)
            except Exception as exc:
                text = f"[OCR ERROR page {page_no}: {exc}]"
                err = str(exc)
                processed = None

            score = compact_len(text)
            attempts_meta.append({
                "variant": attempt.variant,
                "psm": attempt.psm,
                "chars": len(text),
                "non_ws_chars": score,
                "error": err,
            })

            if score > best_score:
                best_score = score
                selected_attempt = attempt
                selected_image = processed
                selected_text = text
                selected_error = err
                method = "ocr"

            if err is None and score >= min_chars:
                selected_attempt = attempt
                selected_image = processed
                selected_text = text
                selected_error = None
                method = "ocr"
                break

        if selected_attempt is None or selected_image is None:
            raise RuntimeError("Aucune tentative OCR exploitable")

        if collect_conf and method == "ocr" and selected_error is None:
            confidence_mean, confidence_words, conf_err = collect_tsv_metrics(
                selected_image, lang, selected_attempt.psm, oem, timeout
            )
            if conf_err:
                attempts_meta.append({
                    "variant": selected_attempt.variant,
                    "psm": selected_attempt.psm,
                    "tsv_metrics_error": conf_err,
                })

        if searchable_pdf and method == "ocr":
            pdf_error = generate_searchable_page_pdf(
                selected_image, lang, selected_attempt.psm, oem, timeout, pdf_path
            )

        atomic_write_text(text_path, selected_text + "\n")

        duration_ms = round((time.perf_counter() - started) * 1000, 2)
        meta = {
            "page_no": page_no,
            "status": "done",
            "source_kind": page.kind,
            "source_path": page.source_path,
            "pdf_index": page.pdf_index,
            "method": method,
            "chars": len(selected_text),
            "non_ws_chars": compact_len(selected_text),
            "attempt_count": len(attempts_meta),
            "attempts": attempts_meta,
            "selected_attempt": None if selected_attempt is None else {"variant": selected_attempt.variant, "psm": selected_attempt.psm},
            "confidence_mean": confidence_mean,
            "confidence_words": confidence_words,
            "error": selected_error,
            "pdf_page_error": pdf_error,
            "searchable_pdf_page": str(pdf_path.resolve()) if pdf_path.exists() else None,
            "duration_ms": duration_ms,
            "processed_at": utc_now_iso(),
        }
        atomic_write_json(meta_path, meta)
        return meta

    except Exception as exc:
        duration_ms = round((time.perf_counter() - started) * 1000, 2)
        error = str(exc)
        fallback_text = f"[OCR ERROR page {page_no}: {error}]"
        atomic_write_text(text_path, fallback_text + "\n")
        meta = {
            "page_no": page_no,
            "status": "done",
            "source_kind": page.kind,
            "source_path": page.source_path,
            "pdf_index": page.pdf_index,
            "method": "failed",
            "chars": len(fallback_text),
            "non_ws_chars": compact_len(fallback_text),
            "attempt_count": len(attempts_meta),
            "attempts": attempts_meta,
            "selected_attempt": None,
            "confidence_mean": None,
            "confidence_words": 0,
            "error": error,
            "pdf_page_error": None,
            "searchable_pdf_page": None,
            "duration_ms": duration_ms,
            "processed_at": utc_now_iso(),
        }
        atomic_write_json(meta_path, meta)
        return meta


# ---------------------------------------------------------------------------
# Assemblage texte / PDF / rapports
# ---------------------------------------------------------------------------

def assemble_text_output(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:
    blocks: List[str] = []
    for page in pages:
        part = page_text_path(paths, page.page_no)
        if part.exists():
            text = part.read_text(encoding="utf-8", errors="replace").rstrip()
        else:
            text = f"[MISSING PAGE {page.page_no}]"
        blocks.append(f"{SEPARATOR}\nPAGE {page.page_no}\n{SEPARATOR}\n{text}")
    atomic_write_text(paths["txt_out"], "\n\n".join(blocks) + "\n")


def append_image_as_pdf_page(doc, image_path: str) -> None:
    import fitz
    rect = fitz.Rect(0, 0, 595, 842)
    try:
        pix = fitz.Pixmap(image_path)
        rect = fitz.Rect(0, 0, pix.width, pix.height)
    except Exception:
        pass

    page = doc.new_page(width=rect.width, height=rect.height)
    page.insert_image(rect, filename=image_path)


def assemble_searchable_pdf(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:
    import fitz

    out_doc = fitz.open()
    source_pdf_doc = None

    try:
        if pages and pages[0].kind == "pdf":
            source_pdf_doc = fitz.open(pages[0].source_path)

        for page in pages:
            meta_file = page_meta_path(paths, page.page_no)
            meta = {}
            if meta_file.exists():
                meta = json.loads(meta_file.read_text(encoding="utf-8"))

            method = meta.get("method")
            page_pdf = page_pdf_path(paths, page.page_no)

            if method == "native-text" and source_pdf_doc is not None and page.pdf_index is not None:
                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)
                continue

            if page_pdf.exists():
                with fitz.open(str(page_pdf)) as tmp_doc:
                    out_doc.insert_pdf(tmp_doc)
                continue

            if page.kind == "pdf" and source_pdf_doc is not None and page.pdf_index is not None:
                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)
            elif page.kind == "image":
                append_image_as_pdf_page(out_doc, page.source_path)

        out_doc.save(str(paths["searchable_pdf"]), garbage=3, deflate=True)
    finally:
        if source_pdf_doc is not None:
            source_pdf_doc.close()
        out_doc.close()


def generate_reports(paths: Dict[str, Path], pages: Sequence[PageRef], state: Dict, args: argparse.Namespace) -> None:
    rows: List[Dict[str, object]] = []
    method_counts: Dict[str, int] = {}
    total_chars = 0
    error_count = 0
    total_duration_ms = 0.0
    confidence_values: List[float] = []

    for page in pages:
        meta_path = page_meta_path(paths, page.page_no)
        if not meta_path.exists():
            rows.append({
                "page_no": page.page_no,
                "status": "missing",
                "method": None,
                "chars": 0,
                "non_ws_chars": 0,
                "duration_ms": 0,
                "confidence_mean": None,
                "confidence_words": 0,
                "attempt_count": 0,
                "error": "missing meta",
                "searchable_pdf_page": None,
                "source_kind": page.kind,
                "source_path": page.source_path,
                "pdf_index": page.pdf_index,
            })
            error_count += 1
            continue

        meta = json.loads(meta_path.read_text(encoding="utf-8"))
        rows.append({
            "page_no": meta.get("page_no"),
            "status": meta.get("status"),
            "method": meta.get("method"),
            "chars": meta.get("chars"),
            "non_ws_chars": meta.get("non_ws_chars"),
            "duration_ms": meta.get("duration_ms"),
            "confidence_mean": meta.get("confidence_mean"),
            "confidence_words": meta.get("confidence_words"),
            "attempt_count": meta.get("attempt_count"),
            "error": meta.get("error"),
            "searchable_pdf_page": meta.get("searchable_pdf_page"),
            "source_kind": meta.get("source_kind"),
            "source_path": meta.get("source_path"),
            "pdf_index": meta.get("pdf_index"),
        })

        method = str(meta.get("method"))
        method_counts[method] = method_counts.get(method, 0) + 1
        total_chars += int(meta.get("chars") or 0)
        total_duration_ms += float(meta.get("duration_ms") or 0.0)
        if meta.get("error"):
            error_count += 1
        if meta.get("confidence_mean") is not None:
            try:
                confidence_values.append(float(meta["confidence_mean"]))
            except Exception:
                pass

    fieldnames = [
        "page_no", "status", "method", "chars", "non_ws_chars", "duration_ms",
        "confidence_mean", "confidence_words", "attempt_count", "error",
        "searchable_pdf_page", "source_kind", "source_path", "pdf_index",
    ]
    paths["pages_csv"].parent.mkdir(parents=True, exist_ok=True)
    with paths["pages_csv"].open("w", encoding="utf-8", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=fieldnames)
        writer.writeheader()
        writer.writerows(rows)

    summary = {
        "generated_at": utc_now_iso(),
        "source": str(Path(args.source).resolve()),
        "text_output": str(paths["txt_out"].resolve()),
        "searchable_pdf_output": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,
        "page_count": len(pages),
        "method_counts": method_counts,
        "error_pages": error_count,
        "total_chars": total_chars,
        "avg_confidence_mean": None if not confidence_values else round(sum(confidence_values) / len(confidence_values), 2),
        "total_duration_ms": round(total_duration_ms, 2),
        "bundle": str(paths["bundle"].resolve()),
        "csv_report": str(paths["pages_csv"].resolve()),
        "state_file": str(paths["state"].resolve()),
        "config": {
            "dpi": args.dpi,
            "lang": args.lang,
            "psm": args.psm,
            "oem": args.oem,
            "workers": args.workers,
            "timeout": args.timeout,
            "pdf_mode": args.pdf_mode,
            "text_threshold": args.text_threshold,
            "no_preprocess": args.no_preprocess,
            "min_chars": args.min_chars,
            "retries": args.retries,
            "searchable_pdf": args.searchable_pdf,
            "tsv_metrics": not args.no_tsv_metrics,
        },
        "state_snapshot": state,
    }
    atomic_write_json(paths["summary"], summary)


# ---------------------------------------------------------------------------
# Pipeline principal
# ---------------------------------------------------------------------------

def ocr_pipeline(args: argparse.Namespace, paths: Dict[str, Path]) -> str:
    source = Path(args.source)
    pages = list_pages(source, args.start, args.end)
    valid_page_nos = {p.page_no for p in pages}

    state = load_state(paths["state"]) if args.resume else {}
    if state:
        ensure_state_compatible(state, args, paths)
    else:
        state = init_state(args, paths, pages)
        save_state(paths["state"], state)

    done_pages = completed_pages_from_state(state, paths, valid_page_nos)
    pages_to_do = [p for p in pages if p.page_no not in done_pages]

    if done_pages:
        LOG.info("Mode reprise — %d page(s) déjà finalisées", len(done_pages))

    if not pages_to_do:
        LOG.info("Toutes les pages demandées sont déjà traitées.")
        assemble_text_output(paths, pages)
        if args.searchable_pdf:
            assemble_searchable_pdf(paths, pages)
        generate_reports(paths, pages, state, args)
        return str(paths["txt_out"])

    worker_count = min(args.workers, len(pages_to_do))
    LOG.info(
        "%d page(s) à traiter (workers=%d, dpi=%d, psm=%d, oem=%d, pdf_mode=%s, preprocess=%s, retries=%d)",
        len(pages_to_do), worker_count, args.dpi, args.psm, args.oem,
        args.pdf_mode, not args.no_preprocess, args.retries,
    )

    base_task = {
        "lang": args.lang,
        "psm": args.psm,
        "oem": args.oem,
        "dpi": args.dpi,
        "timeout": args.timeout,
        "pdf_mode": args.pdf_mode,
        "no_preprocess": args.no_preprocess,
        "text_threshold": args.text_threshold,
        "min_chars": args.min_chars,
        "retries": args.retries,
        "collect_conf": not args.no_tsv_metrics,
        "searchable_pdf": args.searchable_pdf,
        "paths": {k: str(v) for k, v in paths.items()},
    }

    tasks: List[Dict[str, object]] = []
    for page in pages_to_do:
        task = dict(base_task)
        task["page"] = {
            "page_no": page.page_no,
            "kind": page.kind,
            "source_path": page.source_path,
            "pdf_index": page.pdf_index,
        }
        tasks.append(task)

    total = len(tasks)
    completed = 0

    with ProcessPoolExecutor(max_workers=worker_count) as executor:
        future_map = {executor.submit(process_page, task): task["page"]["page_no"] for task in tasks}

        for future in as_completed(future_map):
            page_no = future_map[future]
            try:
                meta = future.result()
            except Exception as exc:
                error = f"Crash worker: {exc}"
                fallback = {
                    "page_no": page_no,
                    "status": "done",
                    "source_kind": None,
                    "source_path": None,
                    "pdf_index": None,
                    "method": "failed",
                    "chars": len(error),
                    "non_ws_chars": compact_len(error),
                    "attempt_count": 0,
                    "attempts": [],
                    "selected_attempt": None,
                    "confidence_mean": None,
                    "confidence_words": 0,
                    "error": error,
                    "pdf_page_error": None,
                    "searchable_pdf_page": None,
                    "duration_ms": 0.0,
                    "processed_at": utc_now_iso(),
                }
                atomic_write_text(page_text_path(paths, page_no), f"[OCR ERROR page {page_no}: {error}]\n")
                atomic_write_json(page_meta_path(paths, page_no), fallback)
                meta = fallback

            state["pages"][str(page_no)] = {
                "status": meta.get("status"),
                "method": meta.get("method"),
                "chars": meta.get("chars"),
                "error": meta.get("error"),
                "duration_ms": meta.get("duration_ms"),
                "processed_at": meta.get("processed_at"),
            }
            save_state(paths["state"], state)

            completed += 1
            error_mark = " ⚠️ ERREUR" if meta.get("error") or meta.get("pdf_page_error") else ""
            LOG.info(
                "Page %d traitée [%d/%d] — %s — %d chars%s",
                page_no, completed, total, meta.get("method"), int(meta.get("chars") or 0), error_mark,
            )

    assemble_text_output(paths, pages)
    if args.searchable_pdf:
        LOG.info("Assemblage du PDF searchable…")
        assemble_searchable_pdf(paths, pages)

    state["assembled_at"] = utc_now_iso()
    save_state(paths["state"], state)
    generate_reports(paths, pages, state, args)

    total_chars = sum(
        len(page_text_path(paths, p.page_no).read_text(encoding="utf-8", errors="replace"))
        for p in pages
        if page_text_path(paths, p.page_no).exists()
    )
    LOG.info("OCR terminé → %s (%d chars)", paths["txt_out"], total_chars)
    if args.searchable_pdf:
        LOG.info("PDF searchable → %s", paths["searchable_pdf"])
    LOG.info("Bundle de run → %s", paths["bundle"])
    return str(paths["txt_out"])


# ---------------------------------------------------------------------------
# CLI
# ---------------------------------------------------------------------------

def build_arg_parser() -> argparse.ArgumentParser:
    ap = argparse.ArgumentParser(description="OCR orienté production — PDF, images, dossiers")
    ap.add_argument("source", help="PDF, image ou dossier d'images")
    ap.add_argument("--dpi", type=int, default=300, help="Résolution de rendu PDF (défaut: 300)")
    ap.add_argument("--start", type=int, default=1, help="Première page PDF (défaut: 1)")
    ap.add_argument("--end", type=int, default=None, help="Dernière page PDF (défaut: fin document)")
    ap.add_argument("--timeout", type=int, default=120, help="Timeout Tesseract par page")
    ap.add_argument("--psm", type=int, default=3, help="Page Segmentation Mode Tesseract")
    ap.add_argument("--oem", type=int, default=1, help="OCR Engine Mode Tesseract")
    ap.add_argument(
        "--workers",
        type=int,
        default=max(1, (os.cpu_count() or 2) // 2),
        help="Nombre de workers parallèles",
    )
    ap.add_argument("--resume", action="store_true", help="Reprendre une run interrompue")
    ap.add_argument("--out-dir", default=None, help="Dossier de sortie")
    ap.add_argument("--suffix", default=".ocr.txt", help="Suffixe de la sortie texte")
    ap.add_argument("--lang", default="ara+fra", help="Langues Tesseract (défaut: ara+fra)")
    ap.add_argument(
        "--pdf-mode",
        choices=["auto", "ocr", "text"],
        default="auto",
        help="PDF: auto=texte natif si exploitable sinon OCR ; ocr=OCR forcé ; text=texte natif seul",
    )
    ap.add_argument(
        "--text-threshold",
        type=int,
        default=25,
        help="Seuil minimal de caractères non blancs pour accepter le texte natif PDF",
    )
    ap.add_argument("--no-preprocess", action="store_true", help="Désactive le prétraitement image")
    ap.add_argument(
        "--min-chars",
        type=int,
        default=10,
        help="Seuil minimal de caractères non blancs pour accepter une tentative OCR",
    )
    ap.add_argument(
        "--retries",
        type=int,
        default=2,
        help="Nombre de tentatives supplémentaires avec variantes de prétraitement / PSM",
    )
    ap.add_argument(
        "--no-tsv-metrics",
        action="store_true",
        help="Désactive la collecte de métriques TSV (confiance moyenne, nombre de mots)",
    )
    ap.add_argument(
        "--searchable-pdf",
        action="store_true",
        help="Produit un PDF searchable final quand possible",
    )
    ap.add_argument("--verbose", action="store_true", help="Logs verbeux")
    return ap


def main() -> None:
    parser = build_arg_parser()
    args = parser.parse_args()

    # Configure console early so validation errors are visible.
    configure_logging(None, verbose=args.verbose)
    validate_args(args)
    paths = compute_paths(Path(args.source), args.out_dir, args.suffix)
    reset_outputs(paths, args.resume)
    configure_logging(paths["run_log"], verbose=args.verbose)

    check_tesseract_languages(args.lang)
    check_pdf_support_if_needed(Path(args.source))

    try:
        ocr_pipeline(args, paths)
    except KeyboardInterrupt:
        die("Interruption utilisateur.", code=130)


if __name__ == "__main__":
    main()
Open

Edit
downloads/annales_svt/MANIFESTE.md
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR complet ara+fra (KHELIFA1_VOLUME_01.ocr_ar_fra_full.txt)	À extraire
KHELIFA 1	Vol 02-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 80 questions / 30 micro-concepts couverts sur 42
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
D’accord ✅ J’ai remplacé l’approche précédente par ton OCR pipeline production.

Le code copié contenait encore des artefacts Markdown, donc je l’ai nettoyé/corrigé puis sauvegardé ici :

text

downloads/annales_svt/scripts/ocr_pipeline_production.py
✅ Vérifications faites
1. Compilation Python
Bash

python -m py_compile scripts/ocr_pipeline_production.py
Résultat : ✅ aucune erreur.

2. Test réel sur PDF
J’ai testé sur KHELIFA1_VOLUME_01.pdf, pages 9 à 10 :

Bash

python scripts/ocr_pipeline_production.py input/KHELIFA1_VOLUME_01.pdf \
  --start 9 \
  --end 10 \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_test.txt
Résultat :

text

2 pages traitées
method_counts: {'ocr': 2}
errors: 0
avg_confidence_mean: 63.64
Fichiers générés
Sortie texte :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_test.txt
Bundle de production :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_test.txt.bundle/
Dedans il y a :

text

pages_txt/
pages_meta/
pages_pdf/
run.state.json
summary.json
pages.csv
run.log
Donc le système de :

reprise par page,
état JSON,
rapport CSV,
résumé JSON,
logs disque,
fonctionne bien ✅

✅ Test du mode reprise
J’ai relancé la même commande avec --resume.

Résultat :

text

Mode reprise — 2 page(s) déjà finalisées
Toutes les pages demandées sont déjà traitées.
Donc la reprise fonctionne ✅

Manifeste mis à jour
J’ai mis à jour :

text

downloads/annales_svt/MANIFESTE.md
avec le pipeline actif :

text

scripts/ocr_pipeline_production.py
Tesseract ara+fra
reprise JSON
bundle
CSV
summary
logs
Important pour la suite
Pour les PDF KHELIFA, je recommande d’utiliser ce pipeline avec :

Bash

--dpi 140
--workers 1
--timeout 180
--pdf-mode ocr
--retries 2
Pas --dpi 300 pour le moment, car les pages KHELIFA sont très grandes et cela peut provoquer RAM excessive / timeout.

À partir de maintenant, j’utiliserai donc :

text

ocr_pipeline_production.py
pour les prochains OCR.


OUI



Edit
downloads/annales_svt/MANIFESTE.md
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet (KHELIFA1_VOLUME_01.ocr_prod_full.txt + bundle/reporting)	À extraire
KHELIFA 1	Vol 02-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 80 questions / 30 micro-concepts couverts sur 42
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
Oui ✅ J’ai lancé le pipeline OCR production complet sur :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.pdf
Résultat
Sortie OCR finale :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_full.txt
Bundle de production :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_full.txt.bundle/
Le bundle contient :

text

pages_txt/
pages_meta/
pages_pdf/
run.state.json
summary.json
pages.csv
run.log
Paramètres utilisés
Bash

python scripts/ocr_pipeline_production.py input/KHELIFA1_VOLUME_01.pdf \
  --start 1 \
  --end 28 \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt
Rapport final
text

Pages traitées : 28 / 28
Méthode : OCR sur 28 pages
Erreurs : 0
Total caractères OCR : 133,913
Taille fichier final : 172,809 bytes
Confiance moyenne OCR : 57.59
Durée totale : ~14 min 44 s
Fichiers de rapport
CSV :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_full.txt.bundle/pages.csv
Résumé JSON :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_full.txt.bundle/summary.json
État de reprise :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_full.txt.bundle/run.state.json
Log :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_01.ocr_prod_full.txt.bundle/run.log
Manifeste mis à jour
J’ai aussi mis à jour :

text

downloads/annales_svt/MANIFESTE.md
avec :

text

KHELIFA 1 Vol 01 — OCR production complet — À extraire
Prochaine phase
Maintenant on peut passer à :

Phase extraction du contenu utile depuis :

text

KHELIFA1_VOLUME_01.ocr_prod_full.txt
Je vais ignorer les pages bruitées/couvertures et extraire uniquement les exercices/questions exploitables.


OK



Ran commands
3

Edited files
3
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 80 questions / 30 micro-concepts couverts sur 42
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
Phase 2 — Extraction des questions candidates
MD
Phase 2 — Extraction des questions candidates
Source : KHELIFA1_VOLUME_01.pdf
OCR de référence demandé : KHELIFA1_VOLUME_01.ocr_prod_full.txt + bundle production
Fichier JSON détaillé : output/khelifa1_volume01_extraction_phase2.json

Résultat
32 blocs de questions/exercices candidats extraits.
Domaine dominant : synthèse des protéines / expression de l'information génétique.
Pages utiles principales : 9 à 28.
Pages 1 à 8 : couverture, préface, pages bruitées ou peu exploitables, sauf fragments.
Micro-concepts probables dominants
mc_prot_01 — Transcription de l'ADN
mc_prot_02 — Traduction de l'ARNm
mc_prot_03 — Code génétique
mc_prot_04 — ARNm
mc_prot_05 — ARNt
mc_prot_06 — Ribosome
mc_prot_07 — Initiation de la traduction
mc_prot_08 — Élongation et terminaison
mc_struc_01 — Structure primaire
mc_struc_05 — Relation structure-fonction
Liste courte des blocs extraits
ID	Pages	Thème	Qualité OCR
khelifa1_vol01_c001	9	HbA/HbS et drépanocytose	moyenne
khelifa1_vol01_c002	9-10	Locus de synthèse des protéines pancréatiques	moyenne
khelifa1_vol01_c003	10	ARNm et formation des polysomes	moyenne
khelifa1_vol01_c004	10	ADN viral et protéines virales	faible
khelifa1_vol01_c005	11	Exons/introns par hybridation ARNm-ADN	bonne
khelifa1_vol01_c006	11	Cinétique ADN/ARN/protéines chez la souris	moyenne
khelifa1_vol01_c007	12	ADN viral + uridine radioactive	moyenne
khelifa1_vol01_c008	13	Calcul du nombre de gènes chez E. coli	bonne
khelifa1_vol01_c009	13	ARNm ajouté à un extrait cellulaire	bonne
khelifa1_vol01_c010	13	Procaryotes vs eucaryotes	bonne
khelifa1_vol01_c011	14	Traduction d'un ARNm donné	bonne
khelifa1_vol01_c012	14	ARNm UGUG... et polysome	moyenne
khelifa1_vol01_c013	15	Insuline humain/rat	bonne
khelifa1_vol01_c014	15	ADN → ARNm → protéine + mutation	bonne
khelifa1_vol01_c015	15-16	ARNm de la mélanine injecté dans une cellule œuf	moyenne
khelifa1_vol01_c016	16	Mucoviscidose et CFTR	bonne
khelifa1_vol01_c017	18-19	ARNt et étapes de la traduction	moyenne
khelifa1_vol01_c018	19-20	Système ABO et allèles	bonne
khelifa1_vol01_c019	20	Codon stop / Hb anormal	bonne
khelifa1_vol01_c020	21	ADN, ARNm, protéine et code génétique	bonne
khelifa1_vol01_c021	21-22	ARNm des globules rouges injecté dans ovocytes	bonne
khelifa1_vol01_c022	22-23	Chromatine active et transcription	moyenne
khelifa1_vol01_c023	23	Trajet des protéines sécrétées au leucine marqué	bonne
khelifa1_vol01_c024	24	Protéine LamB et résistance aux virus	bonne
khelifa1_vol01_c025	25	Expériences noyau/ADN/ARNm	bonne
khelifa1_vol01_c026	25	Caséine : traduction d'ARNm	bonne
khelifa1_vol01_c027	26-27	Cellule sécrétrice et liaison peptidique	moyenne
khelifa1_vol01_c028	27	Amibe : noyau, RNAase, uridine	bonne
khelifa1_vol01_c029	27	Myopathie et dystrophine	bonne
khelifa1_vol01_c030	28	Acétabulaire : rôle du noyau	moyenne
khelifa1_vol01_c031	28	Synthèse in vitro avec ARNm	bonne
khelifa1_vol01_c032	28	Caséine et glandes mammaires	moyenne
Remarques qualité
L'OCR production est complet et traçable, mais certaines pages contiennent beaucoup de bruit typographique.
Pour l'extraction sémantique, j'ai recoupé les sorties OCR disponibles, surtout quand le fichier production était trop bruité.
Les séquences nucléotidiques doivent être vérifiées visuellement avant insertion finale, car une seule base fausse change le tag ou la correction attendue.
Ces 32 blocs ne sont pas encore injectés dans questions_taggees.json : ils constituent la phase extraction.
Prochaine phase proposée
Phase 3 : transformer ces blocs en entrées normalisées finales :

id
texte_corrige
micro_concept_id
secondary_concepts
source
type
difficulte
bac_frequent
notes
Puis validation avec :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Open
khelifa1_volume01_extraction_phase2.json
JSON
[
  {
    "candidate_id": "khelifa1_vol01_c001",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [9],
    "theme": "فقر الدم المنجلي: HbA / HbS وعلاقة المورثة بالبروتين",
    "texte_corrige": "تبين الوثائق اختلافا بين الخضاب الدموي العادي HbA والخضاب HbS المسؤول عن فقر الدم المنجلي. تمثل الوثيقة جزءا من الشريط المستنسخ لجزيئة ADN عند شخص سليم وآخر مريض. باستعمال جدول الشفرات الوراثية، حدد قطعة ARNm ثم متتالية الأحماض الأمينية المقابلة لدى الشخصين، وحدد الاختلاف بين HbA وHbS، ثم فسر عمل الأليلين HbA وHbS انطلاقا من نتيجة الهجرة الكهربائية.",
    "sous_questions": [
      "تحديد ARNm انطلاقا من الشريط المستنسخ للـ ADN.",
      "ترجمة ARNm إلى أحماض أمينية باستعمال جدول الشفرة الوراثية.",
      "تحديد الفرق بين HbA وHbS.",
      "تفسير أثر الاختلاف الوراثي على الصفة المرضية والهجرة الكهربائية."
    ],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "النص قابل للاستغلال لكن تسلسلات القواعد تحتاج مراجعة من الصورة الأصلية."
  },
  {
    "candidate_id": "khelifa1_vol01_c002",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [9, 10],
    "theme": "تتبع مسار البروتينات الإفرازية باللوسين المشع في الخلايا البنكرياسية",
    "texte_corrige": "لتحديد موقع صنع البروتينات في الخلايا البنكرياسية ومصيرها بعد الإفراز، نحقن فأرا باللوسين المشع ثم نتتبع الإشعاع في الخلايا بطريقة التصوير الإشعاعي الذاتي في أزمنة مختلفة. سَمِّ العضيات المشار إليها في الوثيقة، حلل انتقال الإشعاع بينها، واستخرج المعلومات المتعلقة بمقر تركيب البروتين ومساره داخل الخلية.",
    "sous_questions": [
      "تسمية العضيات الخلوية المعنية.",
      "تحليل تغير موضع الإشعاع مع الزمن.",
      "استنتاج مقر تركيب البروتين ومسار إفرازه."
    ],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "moyenne",
    "notes": "الوثيقة تحتوي رسوما بيانية/عضيات لم تُستخرج بدقة من OCR."
  },
  {
    "candidate_id": "khelifa1_vol01_c003",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [10],
    "theme": "دور ARNm في تشكيل البوليزومات",
    "texte_corrige": "بعد وسم ARNm نقوم بعملية الطرد المركزي لعناصر الشبكة الهيولية المحببة ونلاحظ توزع الإشعاع بين الريبوزومات الحرة والبوليزومات. قبل الطرد المركزي نضيف إنزيم ARNase الذي يخرب ARNm فتتغير نتيجة التجربة. حلل الوثائق واستنتج دور ARNm في تركيب البوليزوم وفي تركيب البروتين.",
    "sous_questions": [
      "تحليل نتائج الطرد المركزي قبل وبعد إضافة ARNase.",
      "استنتاج تركيب البوليزوم.",
      "بيان علاقة ARNm بالريبوزومات أثناء الترجمة."
    ],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_06", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "المخططات غير واضحة في OCR لكن مضمون السؤال مفهوم."
  },
  {
    "candidate_id": "khelifa1_vol01_c004",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [10],
    "theme": "تكاثر الفيروس داخل بكتيريا وعلاقة ADN بالبروتينات الفيروسية",
    "texte_corrige": "يتطفل فيروس على بكتيريا. تمثل الوثيقة أهم الظواهر التي تسمح بتكاثر الفيروس داخل الخلية البكتيرية: دخول ADN الفيروسي، تضاعفه، تركيب بروتينات الفيروس، ثم تحرر فيروسات جديدة. حلل الوثيقة لتحديد العلاقة بين ADN الفيروسي وتركيب البروتينات الفيروسية.",
    "sous_questions": [
      "تحديد مراحل تكاثر الفيروس داخل البكتيريا.",
      "ربط ADN الفيروسي بتركيب البروتينات الفيروسية.",
      "استنتاج دور المعلومة الوراثية في التعبير البروتيني."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_struc_05"],
    "qualite_ocr": "faible",
    "notes": "النص المستخرج من الصفحة 10 جزئي، يحتاج مراجعة الصورة الأصلية."
  },
  {
    "candidate_id": "khelifa1_vol01_c005",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [11],
    "theme": "تهجين ARNm مع مورثة بياض البيض: الإكسونات والإنترونات",
    "texte_corrige": "تمثل الوثيقة تهجينا جزئيا بين ARNm بياض البيض وسلسلة ADN التي تحتوي المورثة الموافقة. استعمل الوثيقة لإثبات أن المورثة ذات بنية فسيفسائية مجزأة، ثم احسب عدد القطع الدالة (الإكسونات) وغير الدالة (الإنترونات). إذا قمنا بتهجين ARNm مع المورثة المسؤولة عنه في بدائيات النواة فنجد أن لهما نفس الطول، ماذا تستنتج؟",
    "sous_questions": [
      "إثبات البنية المجزأة للمورثة عند حقيقيات النواة.",
      "حساب عدد الإكسونات والإنترونات.",
      "مقارنة مورثات حقيقيات وبدائيات النواة."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "مضمون السؤال واضح من OCR ara+fra."
  },
  {
    "candidate_id": "khelifa1_vol01_c006",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [11],
    "theme": "سرعة صنع ADN وARN والبروتينات في الفئران",
    "texte_corrige": "أجريت دراسة مقارنة لسرعة صنع الجزيئات العملاقة ADN وARN والبروتينات عند مجموعتين من الفئران: فئران طبيعية شاهدة وفئران نزع جزء من كبدها. حقنت المجموعات بمواد مشعة مختلفة ثم قيست نسبة الإشعاع مع الزمن. حلل النتائج وقارن سرعة تركيب الجزيئات المختلفة، واستنتج العلاقة بين النشاط الخلوي وتركيب ADN وARN والبروتينات.",
    "sous_questions": [
      "تحليل منحنيات الإشعاع حسب الزمن.",
      "مقارنة تركيب ADN وARN والبروتينات.",
      "استخراج العلاقة بين التجدد الخلوي والتعبير الوراثي."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "المنحنيات غير مستخرجة نصيا؛ يلزم الرجوع للوثيقة عند التصحيح النهائي."
  },
  {
    "candidate_id": "khelifa1_vol01_c007",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [12],
    "theme": "حقن ADN فيروس في نواة بويضة ضفدع وتتبع اليوريدين المشع",
    "texte_corrige": "تحقن ADN فيروس في نواة بويضة ضفدع مع حقن اليوريدين المشع. يظهر الإشعاع أولا في الهيولى ثم في النواة وأخيرا في الهيولى، وبعد ذلك تصنع الأغلفة الخارجية للفيروس. استخرج المعلومات التي تقدمها التجربة حول انتقال المعلومة الوراثية ودور ARN في تركيب البروتين.",
    "sous_questions": [
      "تفسير ظهور الإشعاع في النواة ثم الهيولى.",
      "تحديد دور اليوريدين المشع في تتبع ARN.",
      "استنتاج مسار المعلومة الوراثية من ADN إلى بروتين."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "السؤال يظهر في OCR جزئيا لكنه متكرر في صفحات أخرى أيضا."
  },
  {
    "candidate_id": "khelifa1_vol01_c008",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [13],
    "theme": "حساب عدد مورثات E. coli من حجم ADN ومتوسط طول المورثة",
    "texte_corrige": "إذا كان ADN بكتيريا E. coli يحتوي على 4.2 × 10^6 زوج من النيوكليوتيدات، وكان متوسط طول المورثة الواحدة 1500 نيوكليوتيدة، احسب عدد المورثات التي تشكل الذخيرة الوراثية لهذه البكتيريا.",
    "sous_questions": [
      "توظيف مفهوم زوج القواعد النيتروجينية.",
      "حساب عدد المورثات انطلاقا من طول ADN ومتوسط طول المورثة."
    ],
    "micro_concepts_probables": ["mc_prot_01"],
    "qualite_ocr": "bonne",
    "notes": "قيمة 4.2 × 10^6 مستنتجة من OCR وقد تحتاج تدقيقا بصريا."
  },
  {
    "candidate_id": "khelifa1_vol01_c009",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [13],
    "theme": "إضافة ARNm إلى مستخلص خلوي لعصيات القولون وتركيب البروتين",
    "texte_corrige": "أضيفت أحماض أمينية إلى مستخلص خلوي لعصيات القولون، ثم أضيف ARNm بعد مدة زمنية، فقيست كمية ARNm والبروتين المصنع في الوسط. ادرس تطور كميتي ARNm والبروتين، وضع علاقة بينهما، ثم استخرج خصائص ARNm ودوره في تركيب البروتين.",
    "sous_questions": [
      "تحليل تغير كمية ARNm والبروتين مع الزمن.",
      "استنتاج العلاقة بين وجود ARNm وتركيب البروتين.",
      "تحديد خصائص ARNm."
    ],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "المنحنى غير متاح نصيا؛ السؤال قابل للاستخراج."
  },
  {
    "candidate_id": "khelifa1_vol01_c010",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [13],
    "theme": "اختلاف صنع البروتين في بدائيات وحقيقيات النواة",
    "texte_corrige": "توضح وثيقتان بعض أوجه الاختلاف في صنع البروتين بين بدائيات النواة وحقيقيات النواة. ضع عنوانا مناسبا لكل شكل، ضع البيانات حسب الترقيم، ثم استخرج أوجه الاختلاف من حيث الزمان والمكان ومراحل التعبير المورثي.",
    "sous_questions": [
      "تسمية الوثائق والبيانات.",
      "المقارنة بين بدائيات وحقيقيات النواة.",
      "توضيح العلاقة بين النسخ والترجمة."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "ينقص وصف الرسوم من الوثيقة الأصلية."
  },
  {
    "candidate_id": "khelifa1_vol01_c011",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [14],
    "theme": "ترجمة جزء من ARNm إلى سلسلة ببتيدية",
    "texte_corrige": "يمثل الشكل جزءا من جزيئة ARNm تشرف على تركيب إنزيم عند بعض أنواع الفيروسات. المتتالية المعطاة هي: AAA AGU CCA UCA CUU AAU. ما المكونات الكيميائية لنيوكليوتيدات ARNm؟ ثم ترجم هذه الجزيئة إلى سلسلة ببتيدية باستعمال جدول الشفرات الوراثية.",
    "sous_questions": [
      "تحديد مكونات نيوكليوتيدة ARNm.",
      "قراءة الرامزات الثلاثية.",
      "ترجمة ARNm إلى أحماض أمينية."
    ],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "المتتالية واضحة نسبيا في OCR."
  },
  {
    "candidate_id": "khelifa1_vol01_c012",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [14],
    "theme": "ARNm متناوب UGUG... وتعدد الرامزات",
    "texte_corrige": "لدينا بروتين يتميز بتناوب الحمضين الأمينيين السيستئين Cys والفالين Val، وARNm الموافق له عبارة عن تتابع متكرر UGUGUGUG... ماذا تستخلص حول الشفرة الوراثية ونوع الأحماض الأمينية؟ وما المقصود بالبوليزوم؟ وهل يصنع نوعا واحدا أو عدة أنواع من البروتينات؟",
    "sous_questions": [
      "استنتاج معنى الرامزات المتعاقبة في ARNm.",
      "توضيح مفهوم الشفرة الوراثية.",
      "تعريف البوليزوم ودوره."
    ],
    "micro_concepts_probables": ["mc_prot_03", "mc_prot_06", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "بعض الكلمات ناقصة لكن بنية السؤال واضحة."
  },
  {
    "candidate_id": "khelifa1_vol01_c013",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [15],
    "theme": "مقارنة سلسلتي الأنسولين عند الإنسان والجرذ",
    "texte_corrige": "اعتمادا على الشفرة الوراثية المقترحة، مثل قطعتي ARNm اللتين ساهمتا في بناء السلسلتين الببتيديتين المذكورتين، ثم حدد أي الشكلين يمثل السلسلة B لأنسولين الإنسان وأيهما يمثل السلسلة B لأنسولين الجرذ مع التعليل، وبين مصدر الاختلاف في تتابع الأحماض الأمينية.",
    "sous_questions": [
      "تمثيل ARNm الموافق لكل سلسلة.",
      "تمييز سلسلة الإنسان عن سلسلة الجرذ.",
      "تفسير الفرق بمصدر مورثي."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "يتطلب الرجوع إلى الشكلين الأصليين لتأكيد المتتاليات."
  },
  {
    "candidate_id": "khelifa1_vol01_c014",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [15],
    "theme": "من الشريط الناسخ ADN إلى ARNm ثم البروتين وتأثير الاستبدال",
    "texte_corrige": "لدينا ترتيب القواعد التالية للسلسلة الناسخة من ADN: TAC ACG CGA TTT TAT GTA. مثل ترتيب القواعد في الشريط المكمل غير الناسخ، ثم مثل ترتيب القواعد الآزوتية في ARNm، ثم مثل ترتيب الأحماض الأمينية للبروتين المترجم. بعد ذلك ادرس أثر استبدال قاعدة في الثلاثية الثانية على ترتيب ونوع الأحماض الأمينية وعلى البنية الأولية للبروتين.",
    "sous_questions": [
      "تمثيل الشريط المكمل غير الناسخ.",
      "استنتاج ARNm.",
      "ترجمة ARNm إلى بروتين.",
      "دراسة أثر طفرة استبدال على البنية الأولية."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "السلسلة واضحة في OCR ara+fra."
  },
  {
    "candidate_id": "khelifa1_vol01_c015",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [15, 16],
    "theme": "حقن ARNm الميلانين في بيضة منزوعة النواة",
    "texte_corrige": "تعزل ARNm من هيولى خلية تعمل على إظهار صبغة الميلانين، ثم يحقن هذا ARNm في بيضة منزوعة النواة، فنلاحظ تركيب الميلانين في خلية البيضة. فسر النتيجة، وحدد ترتيب الأحماض الأمينية الداخلة في تركيب جزء البروتين المتشكل باستعمال جدول الشفرة الوراثية، ثم حدد الظاهرة الوراثية التي تسمح بظهور هذه النتيجة.",
    "sous_questions": [
      "تفسير دور ARNm في ظهور الميلانين.",
      "استعمال جدول الشفرة الوراثية لتحديد الأحماض الأمينية.",
      "تحديد ظاهرة التعبير المورثي."
    ],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02", "mc_prot_03"],
    "qualite_ocr": "moyenne",
    "notes": "جزء من معطيات الأوساط التجريبية ظاهر في OCR، لكن المتتالية قد تكون ناقصة."
  },
  {
    "candidate_id": "khelifa1_vol01_c016",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [16],
    "theme": "داء mucoviscidose وطفرة مورثة CFTR",
    "texte_corrige": "مرض mucoviscidose وراثي يتمثل في لزوجة مفرطة في إفرازات البنكرياس والقصبات الهوائية. تتحكم فيه مورثة على الذراع الطويل للصبغي 7 تدعى CFTR وترمز إلى بروتين غشائي. تقارن الوثيقة ترتيب نيوكليوتيدات قطعة من الشريط المستنسخ عند شخص عادي وآخر مصاب. قارن الترتيبين، وحدد نوع الطفرة، ثم فسر أثرها على البروتين والصفة المرضية.",
    "sous_questions": [
      "مقارنة ترتيب النيوكليوتيدات عند السليم والمريض.",
      "تحديد نوع الطفرة.",
      "ربط الطفرة بتغير البروتين والصفة."
    ],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "المتتاليات تحتاج مراجعة بصرية قبل الإدخال النهائي."
  },
  {
    "candidate_id": "khelifa1_vol01_c017",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [18, 19],
    "theme": "أنواع ARN المشاركة في تركيب البروتين: ARNm وARNt والريبوزومات",
    "texte_corrige": "من أجل تتبع المراحل الأساسية لتركيب البروتين والعناصر المتدخلة، استعملت تجارب باليوريدين المشع والأحماض الأمينية المشعة، كما تعرض الوثيقة بنية نوع من ARN يتدخل أثناء تركيب البروتين. تعرف على هذا ARN، أنجز رسما تخطيطيا تفسيريا يحمل البيانات ويبرز خصوصياته، ثم بين دوره في تركيب البروتين واستخرج المراحل الأساسية لتركيب البروتين مع مقرها والعناصر المتدخلة.",
    "sous_questions": [
      "تعليل استعمال اليوريدين المشع والأحماض الأمينية المشعة.",
      "تعرف ARNt وبنيته ودوره.",
      "استخراج مراحل تركيب البروتين ومقر كل مرحلة."
    ],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_02", "mc_prot_06", "mc_prot_07", "mc_prot_08"],
    "qualite_ocr": "moyenne",
    "notes": "نص الصفحة 18-19 مفهوم جزئيا ويحتاج تثبيت الرسم الأصلي."
  },
  {
    "candidate_id": "khelifa1_vol01_c018",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [19, 20],
    "theme": "نظام الزمر الدموية ABO: تعدد الأليلات ومولدات الضد",
    "texte_corrige": "يحدد نظام ABO بوجود أو غياب مولدي الضد A وB على سطح الكرية الدموية الحمراء. تنشط الأليلات تركيب إنزيمات مسؤولة عن هذه المولدات، بينما يدل غيابها على إنزيم غير فعال. اعتمادا على الوثائق، حدد الاختلاف بين الأليلات على مستوى ADN، وبين كيف يؤدي تعدد الأليلات إلى اختلاف البروتينات والصفة الظاهرية.",
    "sous_questions": [
      "تحديد الاختلاف بين الأليلات A وB وO على مستوى ADN.",
      "تفسير تعدد الأليلات.",
      "ربط المورثة بالإنزيم ثم بمولد الضد والصفة."
    ],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "تسلسلات الأليلات مذكورة لكنها تحتاج مراجعة دقيقة."
  },
  {
    "candidate_id": "khelifa1_vol01_c019",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [20],
    "theme": "توقف تركيب HbA وظهور خضاب غير عادي بسبب تغير رامزة",
    "texte_corrige": "تقدم الوثيقة الأحماض الأمينية المكونة للجزء الأخير من خضابين، أحدهما HbA والآخر غير عادي. علل سبب توقف تركيب HbA عند حمض الأرجنين، وفسر إدماج الحمض الأميني Glu بعد الأرجنين في الخضاب غير العادي، ثم مثل جزء ARNm المسؤول عن التوقف أو الإدماج وبين كيف يؤدي استبدال نيوكليوتيدة واحدة في ADN إلى ظهور الخضاب غير العادي.",
    "sous_questions": [
      "تفسير رامزة التوقف.",
      "تمثيل ARNm المسؤول عن التوقف أو الإدماج.",
      "ربط استبدال نيوكليوتيدة بتغير البروتين."
    ],
    "micro_concepts_probables": ["mc_prot_03", "mc_prot_02", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "يحتاج تأكيد اسم الخضاب غير العادي من الوثيقة الأصلية."
  },
  {
    "candidate_id": "khelifa1_vol01_c020",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [21],
    "theme": "مراحل التعبير المورثي: ADN وARNm والبروتين",
    "texte_corrige": "تعرض الوثيقة بنيات تمثل ADN وARNm وبروتينا، وصورة بالمجهر الإلكتروني لمرحلة أساسية من تركيب البروتين. تعرف على البنيات، واشرح لماذا ترتب القواعد في ثلاثيات، وتعرف على المرحلة الممثلة في الصورة، ثم بين كيف تتحكم البنية الوراثية في تصنيع البروتين من حيث عدد وترتيب ونوعية الأحماض الأمينية.",
    "sous_questions": [
      "تعرف ADN وARNm والبروتين.",
      "تفسير الرامزات الثلاثية رياضيا.",
      "تعرف مرحلة النسخ أو الترجمة حسب الوثيقة.",
      "بيان العلاقة بين المعلومة الوراثية وخصوصية البروتين."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "المتتاليات ظاهرة في OCR ولكن الرسم مطلوب للتصحيح النهائي."
  },
  {
    "candidate_id": "khelifa1_vol01_c021",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [21, 22],
    "theme": "حقن ARNm الكريات الحمراء في خلايا بيضية برمائية",
    "texte_corrige": "توضع ثلاث مجموعات من الخلايا في وسط يحوي أحماضا أمينية موسومة: خلايا أصلية للكريات الحمراء قادرة على تركيب الهيموغلوبين، خلايا بيضية لحيوان برمائي، وخلايا بيضية محقونة بـ ARNm معزول من الخلايا الأصلية للكريات الحمراء. بعد فصل البروتينات المشعة، قارن نتائج التجارب الثلاث واستنتج دور ARNm في تحديد نوع البروتين المصطنع.",
    "sous_questions": [
      "مقارنة البروتينات المصطنعة في المجموعات الثلاث.",
      "استنتاج أن ARNm يحمل المعلومة اللازمة لتركيب بروتين نوعي.",
      "ربط نوع البروتين بنوع ARNm."
    ],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "السؤال واضح رغم غياب منحنيات الوثيقة."
  },
  {
    "candidate_id": "khelifa1_vol01_c022",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [22, 23],
    "theme": "الكروماتين في حالة نشاط والنسخ في خلية بنكرياسية",
    "texte_corrige": "تمثل الوثيقة كروماتين في حالة نشاط عند خلية بنكرياسية. سم الظاهرة المعنية، وحدد ماذا تمثل الأسهم، ثم وضح النشاط على المستوى الجزيئي برسم تخطيطي. مثل تتابع نيوكليوتيدات المورثة التي تشرف على الأحماض الأمينية الأخيرة لسلسلة الأنسولين، واستخرج من تجربة التهجين الجزيئي المعلومة المكملة حول الآلية المدروسة.",
    "sous_questions": [
      "تعرف ظاهرة النسخ.",
      "تفسير كروماتين نشط وسلاسل ARN في طور التشكل.",
      "تمثيل جزء مورثة/ARNm انطلاقا من معطيات الأنسولين.",
      "استنتاج معلومة من التهجين الجزيئي."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "moyenne",
    "notes": "تحتاج الوثائق الأصلية لتحديد الأسهم والمتتاليات."
  },
  {
    "candidate_id": "khelifa1_vol01_c023",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [23],
    "theme": "تتبع مسار بروتينات إفرازية في خلايا بنكرياسية باللوسين المشع",
    "texte_corrige": "أخذت خلايا بنكرياسية من فأر ووضعت لمدة قصيرة في وسط يحوي اللوسين المشع، ثم نقلت إلى وسط يحوي لوسينا غير مشع. بعد أزمنة مختلفة سحقت الخلايا وعوملت بالطرد المركزي، وقيس الإشعاع في العضيات المعزولة. استخرج المراحل الأساسية لمسار البروتينات الإفرازية وفسرها.",
    "sous_questions": [
      "تحليل انتقال الإشعاع بين الشبكة الهيولية وجهاز غولجي والحويصلات الإفرازية.",
      "استنتاج مسار تصنيع وإفراز البروتين.",
      "ربط اللوسين المشع بتتبع البروتينات."
    ],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "منحنى الإشعاع غير ممثل نصيا."
  },
  {
    "candidate_id": "khelifa1_vol01_c024",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [24],
    "theme": "بروتين LamB في E. coli: من ARNm إلى مقاومة الفيروسات",
    "texte_corrige": "الغلاف الخارجي لبكتيريا E. coli يحتوي على بروتين LamB يسمح بتثبيت بعض الفيروسات عليها، مما يعرضها للتخريب. تقدم الوثائق تتابع أحماض أمينية من هذا البروتين ورامزات ARNm الموافقة. مثل جزء المورثة المسؤولة عن تركيب هذا الجزء، وحدد مرحلة إدماج حمض أميني أثناء تركيب السلسلة، ثم فسر كيف يؤدي تغير في المورثة إلى ظهور حمض أميني مختلف ومقاومة البكتيريا للفيروسات.",
    "sous_questions": [
      "تمثيل جزء المورثة انطلاقا من ARNm.",
      "تحديد مرحلة الترجمة ومقرها.",
      "تفسير طفرة تغير حمضا أمينيا في LamB.",
      "ربط المورثة بالبروتين والصفة."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "تتابع الرامزات في الوثيقة يحتاج مراجعة دقيقة."
  },
  {
    "candidate_id": "khelifa1_vol01_c025",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [25],
    "theme": "تجارب الاستنساخ النووي وADN وARNm لإثبات مصدر المعلومة الوراثية",
    "texte_corrige": "تعرض مجموعة تجارب: زرع نوى خلايا شرغوف أمهق في بيوض منزوعة النواة، زرع ADN بكتيريا هوائية في بكتيريا لاهوائية، تتبع اليوريدين المشع في الأميبا، وحقن ARNm مسؤول عن تركيب بروتين نوعي في بيوض ضفدعة. استخرج المعلومات التي تقدمها هذه التجارب حول دور النواة وADN وARNm في تركيب البروتين، ثم استنتج مراحل التعبير المورثي.",
    "sous_questions": [
      "استخراج معلومة كل تجربة.",
      "بيان دور النواة وADN في حمل المعلومة الوراثية.",
      "بيان دور ARNm في نقل المعلومة إلى الهيولى.",
      "استنتاج مراحل النسخ والترجمة."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "نص التجارب واضح نسبيا."
  },
  {
    "candidate_id": "khelifa1_vol01_c026",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [25],
    "theme": "كازيين حيوانين ثديين: ترجمة ARNm ومقارنة السلاسل",
    "texte_corrige": "تقدم الوثيقة جزءا من ARNm المستنسخ من مورثة الكازيين عند حيوانين ثديين مختلفين، وجدول الشفرة الوراثية. حدد تتابع الأحماض الأمينية الموافقة لكل ARNm، ثم بين الفرق بين السلسلتين المحصل عليهما.",
    "sous_questions": [
      "ترجمة ARNm باستعمال جدول الشفرة الوراثية.",
      "مقارنة تتابع الأحماض الأمينية.",
      "استنتاج أثر اختلاف ARNm على البروتين."
    ],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "بعض الرامزات ظاهرة؛ يلزم تدقيق المتتاليات قبل الحل النهائي."
  },
  {
    "candidate_id": "khelifa1_vol01_c027",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [26, 27],
    "theme": "صنع وإفراز البروتين في خلية حيوانية: العضيات والرابطة الببتيدية",
    "texte_corrige": "نريد دراسة آلية صنع وإفراز البروتينات ومصدر المعلومات الضرورية لصنعها. تعرض الوثيقة فوق بنية خلية حيوانية، وتجربة وسم بالأحماض الأمينية المشعة. ضع البيانات من 1 إلى 8، حدد الوظيفة التي يؤهلها شكل الخلية، استخلص مقر اصطناع البروتين، ثم اكتب معادلة اتحاد حمضين أمينيين وحدد نوع الرابطة المتشكلة واسم المركب الناتج وسلوكه في تفاعل بيوري.",
    "sous_questions": [
      "تسمية العضيات الخلوية.",
      "تحديد وظيفة الخلية الإفرازية.",
      "استنتاج مقر تركيب البروتين.",
      "تحديد الرابطة الببتيدية وتفاعل بيوري."
    ],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_06", "mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "الوثيقة الخلوية تحتاج الرجوع للصورة الأصلية."
  },
  {
    "candidate_id": "khelifa1_vol01_c028",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [27],
    "theme": "تجارب الأميبا: النواة وRNAase واليوردين المشع",
    "texte_corrige": "لدراسة الآليات المؤدية إلى تشكل البروتين ومصدر المعلومات، تعرض الوثيقة نتائج تجارب على الأميبا: نزع النواة، وضع الخلايا في وسط يحوي يوريدين مشع، إضافة RNAase، ونقل النواة إلى أميبا منزوعة النواة. فسر النتائج المسجلة واستنتج دور النواة وARN في تشكل البروتين.",
    "sous_questions": [
      "تفسير تأثير نزع النواة.",
      "تفسير ظهور الإشعاع عند استعمال اليوريدين المشع.",
      "تفسير أثر RNAase.",
      "استخلاص العلاقة بين النواة وARN والبروتين."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "جدول النتائج ظاهر جزئيا."
  },
  {
    "candidate_id": "khelifa1_vol01_c029",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [27],
    "theme": "مرض Myopathie وطفرة مورثة الديستروفين",
    "texte_corrige": "مرض Myopathie وراثي يصيب العضلات. يوجد نوع يرجع إلى توقف تركيب بروتين الديستروفين، ونوع آخر يعود إلى تركيب ديستروفين غير عادي. تقارن الوثيقة ترتيب نيوكليوتيدات جزء من المورثة المسؤولة عن الديستروفين عند شخص سليم وشخصين مصابين. قارن المورثات، سم الظاهرة المسؤولة عن الاختلاف، ثم حدد متتالية الأحماض الأمينية المشكلة للبروتين لدى الأشخاص الثلاثة باستعمال جدول الشفرة الوراثية.",
    "sous_questions": [
      "مقارنة متتاليات ADN بين السليم والمصابين.",
      "تسمية الطفرة المسؤولة.",
      "ترجمة المتتاليات إلى أحماض أمينية.",
      "تفسير توقف التركيب أو تركيب بروتين غير عادي."
    ],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "المتتاليات واضحة نسبيا: CCA AAC TAA... لكنها تحتاج مراجعة بصرية."
  },
  {
    "candidate_id": "khelifa1_vol01_c030",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [28],
    "theme": "الأستبلاريا: دور النواة في نشاط بناء البروتين",
    "texte_corrige": "نقوم بقطع أشنة الأستبلاريا إلى جزأين: جزء عديم النواة وجزء يحوي النواة. نقيس نشاط بناء البروتين في كل جزء قبل وبعد القطع. لماذا يزداد نشاط بناء البروتين في الجزء الحاوي للنواة؟ حلل وفسر النتائج، واستنتج دور النواة في تركيب البروتين.",
    "sous_questions": [
      "تحليل نتائج نشاط بناء البروتين في الجزأين.",
      "تفسير أثر وجود النواة.",
      "استنتاج علاقة النواة بالمعلومة الضرورية لتركيب البروتين."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "جدول النتائج غير مستخرج نصيا."
  },
  {
    "candidate_id": "khelifa1_vol01_c031",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [28],
    "theme": "تركيب بروتين في أنبوب اختبار باستعمال ARNm ومستخلصات خلوية",
    "texte_corrige": "نضع في أنبوب اختبار سائلا فيزيولوجيا مضافا إليه مستخلصات خلوية وARNm وأحماضا أمينية، فيتشكل بروتين معين. حلل التجربة واستنتج دور ARNm والمستخلصات الخلوية، ثم حدد إن كان بالإمكان معرفة ADN الموافق، وما الاسم الذي يطلق عليه، وكون علاقة وظيفية بين ADN وARN والبروتين.",
    "sous_questions": [
      "تحليل تجربة التركيب خارج الخلية.",
      "تحديد دور ARNm والمستخلصات الخلوية.",
      "استنتاج ADN الموافق وتعريف المورثة.",
      "صياغة العلاقة ADN → ARN → بروتين."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "تتابع الأسس في ARNm مشوش ويحتاج مراجعة الصورة."
  },
  {
    "candidate_id": "khelifa1_vol01_c032",
    "source": "KHELIFA1_VOLUME_01.pdf",
    "pages": [28],
    "theme": "تركيب وإفراز الكازيين في الغدد اللبنية باللوسين المشع",
    "texte_corrige": "لدراسة آلية تركيب وإفراز الكازيين في الغدد الثديية، يوضع نسيج من الغدة الثديية في وسط يحوي لوسينا مشعا ثم ينقل إلى وسط يحوي أحماضا أمينية غير مشعة. تقاس شدة الإشعاع داخل الخلية في أزمنة مختلفة وتقدم الوثيقة تتابع نيوكليوتيدات جزء من مورثة الكازيين في السلسلة غير المستنسخة عند النعجة والبقرة. حلل المنحنى والجدول لتحديد مسار تركيب وإفراز الكازيين وعلاقة المورثة بالبروتين.",
    "sous_questions": [
      "تحليل تغير شدة الإشعاع مع الزمن.",
      "استنتاج مسار تركيب وإفراز الكازيين.",
      "استعمال تتابع النيوكليوتيدات لربط المورثة بالبروتين.",
      "مقارنة مورثة الكازيين عند النعجة والبقرة."
    ],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "السؤال يبدأ في الصفحة 28 وقد يكمل في الصفحة التالية/volume suivant حسب PDF."
  }
]
Open
OK ✅ Phase 2 faite pour KHELIFA1_VOLUME_01.

Fichiers créés
1. Extraction JSON détaillée
text

downloads/annales_svt/output/khelifa1_volume01_extraction_phase2.json
Contient :

text

32 blocs de questions/exercices candidats
Chaque bloc contient :

candidate_id
source
pages
theme
texte_corrige
sous_questions
micro_concepts_probables
qualite_ocr
notes
2. Résumé Markdown
text

downloads/annales_svt/output/khelifa1_volume01_extraction_phase2.md
Avec la liste courte des 32 blocs extraits.

Résultat de la phase
J’ai extrait les questions utiles depuis les pages principales :

text

pages 9 à 28
Les pages 1 à 8 sont surtout :

couverture,
préface,
pages très bruitées,
fragments peu exploitables.
Domaine dominant
Ce volume porte presque entièrement sur :

text

Synthèse des protéines / expression de l'information génétique
Micro-concepts probables :

text

mc_prot_01  Transcription de l'ADN
mc_prot_02  Traduction de l'ARNm
mc_prot_03  Code génétique
mc_prot_04  ARNm
mc_prot_05  ARNt
mc_prot_06  Ribosome
mc_prot_07  Initiation de la traduction
mc_prot_08  Élongation et terminaison
mc_struc_01 Structure primaire
mc_struc_05 Relation structure-fonction
Exemples de blocs extraits
HbA / HbS et drépanocytose
ARNm et polysomes
exons / introns
traduction d’un ARNm donné
mutations et effet sur la protéine
mucoviscidose / CFTR
système ABO
ARNt
LamB chez E. coli
caséine
dystrophine / myopathie
Manifeste mis à jour
J’ai mis à jour :

text

downloads/annales_svt/MANIFESTE.md
État actuel :

text

KHELIFA 1 Vol 01 — OCR production complet + extraction phase 2 — 32 blocs candidats
Prochaine phase
Phase 3 :

Transformer ces 32 blocs en entrées finales compatibles avec :

text

downloads/annales_svt/output/questions_taggees.json
Puis validation automatique avec :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Je peux continuer avec la phase 3 tagging + intégration JSON.


oui



Explored
1 read
Read
validate_tags.py

Ran commands
3

Edited files
2
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 112 questions / 32 micro-concepts couverts sur 42
Dernier ajout
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
Phase 3 — Tagging + intégration JSON
MD
Phase 3 — Tagging + intégration JSON
Source : KHELIFA1_VOLUME_01.pdf
Extraction source : output/khelifa1_volume01_extraction_phase2.json
Sortie consolidée : output/questions_taggees.json

Actions réalisées
Sauvegarde du JSON précédent :
text

output/questions_taggees.before_khelifa1_vol01.json
Transformation des 32 blocs candidats en entrées normalisées.

Ajout dans :

text

output/questions_taggees.json
Validation automatique avec :
Bash

python scripts/validate_tags.py output/questions_taggees.json
Résultat validation
text

Total questions : 112
Concepts distincts utilisés : 32
Erreurs : 0
Avertissements : 0
Ajout KHELIFA1 Vol 01
Questions ajoutées : 32
IDs : q_khelifa1_vol01_001 à q_khelifa1_vol01_032
Distribution des concepts principaux ajoutés
Micro-concept	Questions
mc_prot_01 — Transcription de l'ADN	16
mc_prot_02 — Traduction de l'ARNm	7
mc_prot_03 — Code génétique	4
mc_prot_04 — ARNm	4
mc_prot_05 — ARNt	1
Distribution des types
Type	Questions
analyse_document	15
raisonnement	7
application	5
schema_a_completer	3
comparaison	2
Distribution des difficultés
Difficulté	Questions
facile	2
moyenne	20
difficile	10
Remarques
Les entrées sont validées structurellement et utilisent uniquement les 42 micro-concepts autorisés.
Plusieurs notes indiquent que les séquences nucléotidiques exactes doivent être vérifiées visuellement si une correction scientifique ultra-fidèle est requise.
Les tags sont centrés sur le domaine prot, ce qui correspond au contenu du volume.
Open
Oui ✅ Phase 3 terminée pour KHELIFA1_VOLUME_01.

Ce qui a été fait
J’ai transformé les 32 blocs candidats de la phase 2 en entrées finales normalisées, puis je les ai ajoutées dans :

text

downloads/annales_svt/output/questions_taggees.json
Sauvegarde créée
Avant modification, j’ai sauvegardé l’ancien JSON ici :

text

downloads/annales_svt/output/questions_taggees.before_khelifa1_vol01.json
IDs ajoutés
Les nouvelles questions ajoutées vont de :

text

q_khelifa1_vol01_001
à :

text

q_khelifa1_vol01_032
Validation automatique
J’ai lancé :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Résultat :

text

Total questions : 112
Concepts distincts utilisés : 32
Erreurs : 0
Avertissements : 0
Donc le JSON consolidé est valide ✅

Distribution des 32 questions ajoutées
Concepts principaux
text

mc_prot_01  Transcription de l'ADN       16
mc_prot_02  Traduction de l'ARNm          7
mc_prot_03  Code génétique                4
mc_prot_04  ARNm                          4
mc_prot_05  ARNt                          1
Types
text

analyse_document      15
raisonnement           7
application            5
schema_a_completer     3
comparaison            2
Difficulté
text

facile       2
moyenne     20
difficile   10
Rapport de phase créé
J’ai créé ce fichier de suivi :

text

downloads/annales_svt/output/khelifa1_volume01_phase3_integration.md
Manifeste mis à jour
J’ai mis à jour :

text

downloads/annales_svt/MANIFESTE.md
Nouvel état global :

text

112 questions traitées
32 micro-concepts couverts
0 erreur
0 avertissement
Prochaine étape
On peut maintenant passer au volume suivant :

text

KHELIFA1_VOLUME_02.pdf
Même méthode :

OCR avec 
ocr_pipeline_production.py
extraction phase 2
tagging/intégration phase 3
validation globale.

oui



Ran commands
7
Write
downloads/annales_svt/output/khelifa1_volume02_extraction_phase2.json
322 lines
open
khelifa1_volume02_extraction_phase2.json
JSON
[
  {
    "candidate_id": "khelifa1_vol02_c001",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [2],
    "theme": "تأثير وجود الغلوكوز واللاكتوز على تركيب إنزيم اللاكتاز عند البكتيريا",
    "texte_corrige": "تمت تنمية سلالتين من البكتيريا في وسطين يحتويان على نفس المغذيات وكمية محدودة من الغلوكوز واللاكتوز، ثم قيست أعداد البكتيريا وكمية إنزيم اللاكتاز. حلل المنحنيات، وفسر تطور عدد البكتيريا في الوسطين، واقترح فرضية تربط النتائج بالتعبير المورثي ومرحلة الاستنساخ. توقع شكل المنحنيات في حالة وجود الغلوكوز فقط بكمية غير محدودة.",
    "sous_questions": ["تحليل منحنيات النمو وكمية الإنزيم", "تفسير ظهور أو غياب إنزيم اللاكتاز", "ربط النتائج بالتعبير المورثي والنسخ"],
    "micro_concepts_probables": ["mc_prot_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "السؤال يتعلق بتنظيم التعبير المورثي، وأقرب مفهوم متاح في المرجع هو النسخ/التعبير المورثي."
  },
  {
    "candidate_id": "khelifa1_vol02_c002",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [2],
    "theme": "تحديد ARNm والأحماض الأمينية وأثر الطفرة على بروتين",
    "texte_corrige": "مثل تتابع النيوكليوتيدات في ARNm الحامل للمعلومات الخاصة بجزء من بروتين مصنع، ثم باستعمال جدول الشفرات الوراثية حدد تتابع الأحماض الأمينية في هذا الجزء. إذا عزل بروتين ناتج من طفرة استبدلت حمضا أمينيا بآخر، حدد موقع الطفرة في المورثة، وفسر حالة مرض وراثي لا يتم فيه تصنيع البروتين الكامل بل جزء صغير منه فقط.",
    "sous_questions": ["تمثيل ARNm", "ترجمة ARNm باستعمال جدول الشفرة", "تحديد أثر طفرة على تتابع الأحماض الأمينية", "تفسير توقف تركيب البروتين"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "بعض أسماء الأحماض الأمينية والمتتاليات تحتاج مراجعة من الصورة الأصلية."
  },
  {
    "candidate_id": "khelifa1_vol02_c003",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [2],
    "theme": "تعبير مورثات البراميسيوم داخل خلايا الأرنب",
    "texte_corrige": "تم عزل مورثات ترمز لبروتينات الغشاء الهيولي للبراميسيوم وإدخالها داخل خلايا أرنب، فركبت خلايا الأرنب بروتينات البراميسيوم بصورة جزئية. حلل هذه النتيجة واستنتج ما تكشفه عن العلاقة بين المورثة والبروتين وعن اختلاف التعبير المورثي بين الكائنات.",
    "sous_questions": ["تفسير تركيب بروتين غريب داخل خلية أخرى", "ربط المورثة بالبروتين", "تفسير التركيب الجزئي للبروتين"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "قد يتعلق السؤال بإشكالية الشفرة/تدخل الإنترونات؛ النص الأصلي يحتاج تدقيقا."
  },
  {
    "candidate_id": "khelifa1_vol02_c004",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [3],
    "theme": "إضافة ARN إلى وسط تركيب بروتين وقياس إدماج الأحماض الأمينية",
    "texte_corrige": "في أنبوب اختبار يحتوي على العناصر الضرورية لتركيب البروتين والمستخلصة من بكتيريا، أضيفت أحماض أمينية موسومة وكمية قليلة من ARN عند زمن محدد، ثم قيست كمية ARN وكمية الإشعاع في البروتينات. حلل المنحنى واستنتج دور ARN في تركيب البروتين، وفسر لماذا لا تستطيع خلية منزوعة النواة تركيب البروتين إلا لمدة قصيرة.",
    "sous_questions": ["تحليل تغير كمية ARN والبروتين", "استنتاج دور ARN", "تفسير استمرار قصير لتركيب البروتين في خلية منزوعة النواة"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "السؤال قريب من تجارب ARNm في تركيب البروتين."
  },
  {
    "candidate_id": "khelifa1_vol02_c005",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [3],
    "theme": "أثر الأشعة السينية على مورثة وترتيب الأحماض الأمينية",
    "texte_corrige": "يعطى ترتيب قواعد آزوتية لقطعة من مورثة تشفر لبروتين، ثم تعرضت البيضة الأصلية لأشعة سينية فأصبحت القطعة نفسها تشفر لترتيب مختلف من الأحماض الأمينية. فسر ظهور تأثير الأشعة السينية في خلية الشخص البالغ، واستنتج نوع التغير الذي أصاب المعلومة الوراثية وأثره على البروتين.",
    "sous_questions": ["مقارنة المتتاليات قبل وبعد التعرض للأشعة", "تسمية التغير الوراثي", "تفسير تغير تتابع الأحماض الأمينية"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "المتتاليات يجب تدقيقها بصريا قبل الحل النهائي."
  },
  {
    "candidate_id": "khelifa1_vol02_c006",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [5],
    "theme": "التعرف على طبيعة ADN وARN من المكونات الكيميائية والنسب القاعدية",
    "texte_corrige": "تعرض الوثائق المكونات الأساسية للخلية، ولغرض التعرف على الطبيعة الكيميائية لعنصر معين يقدم تحليل كيميائي يبين وجود سكر خماسي وقواعد آزوتية ومركبات AMP وGMP وUMP وCMP. حدد طبيعة المركب، واستعمل النسب القاعدية والعلاقات بين القواعد لرسم مخطط للجزيئة وتحديد طبيعتها الكيميائية وكيفية الكشف عنها.",
    "sous_questions": ["تحديد طبيعة المركب من مكوناته", "استعمال نسب القواعد الآزوتية", "رسم مخطط للجزيئة", "التمييز بين ADN وARN"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_01"],
    "qualite_ocr": "moyenne",
    "notes": "يتضمن مفاهيم بنية الأحماض النووية؛ لا يوجد مفهوم مستقل للـ ADN في المرجع."
  },
  {
    "candidate_id": "khelifa1_vol02_c007",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [6],
    "theme": "أنواع ARN المتدخلة في تصنيع البروتين والكروماتين النشط",
    "texte_corrige": "إذا اعتبرنا أن جزيئات ARN المساهمة في عملية تصنيع البروتين تكون حسب تسلسل موضح في وثيقة، حدد نوع ARN ومرحلة تدخله ووسيلة تدخله. كما تمثل وثيقة أخرى الكروماتين في حالة نشاط عند خلية بنكرياسية؛ سم الظاهرة، ومثل تتابع نيوكليوتيدات المورثة المسؤولة عن جزء من الأنسولين، واستخرج معلومة من تجربة التهجين الجزيئي.",
    "sous_questions": ["تعرف أنواع ARN", "تحديد مرحلة تدخل كل نوع", "تعرف النسخ على كروماتين نشط", "استنتاج معلومة من التهجين الجزيئي"],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "moyenne",
    "notes": "مستخرج من OCR القديم والحديث معا."
  },
  {
    "candidate_id": "khelifa1_vol02_c008",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [7],
    "theme": "تعطيل ARNt وأثره على تركيب البروتين",
    "texte_corrige": "حضرت خليتان أو وسطا زرع بنفس المكونات، ثم أضيف إلى الوسط الثاني مادة تعطل عمل ARNt. سمحت نتائج قياس كمية الأحماض الأمينية والبروتينات بالحصول على منحنيات. فسر النتائج في الوسط الأول، واستنتج من نتائج الوسط الثاني دور ARNt في تركيب البروتين، ثم وضح المرحلة الممثلة في صورة المجهر الإلكتروني والمرحلة الموالية المؤدية إلى إنتاج البروتين.",
    "sous_questions": ["تحليل أثر تعطيل ARNt", "استنتاج دور ARNt", "تعرف مرحلة من الترجمة", "رسم المرحلة الموالية"],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "السؤال واضح في OCR القديم."
  },
  {
    "candidate_id": "khelifa1_vol02_c009",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [8],
    "theme": "تتبع اليوراسيل المشع والنسخ ثم ترجمة البروتين",
    "texte_corrige": "توضع خلايا في وسط يحوي اليوراسيل المشع لمدة معينة ثم تحول إلى وسط يحوي اليوراسيل العادي. علل استعمال اليوراسيل المشع، واستخرج المعلومات التي تقدمها التجربة حول التعبير المورثي. ثم تبين وثيقة أخرى رسما تخطيطيا لتصنيع البروتين؛ اكتب بيانات العناصر، حدد الظاهرة، واستخرج مراحلها، ثم بين التغيرات التي تطرأ على البنية المتشكلة لتصبح وظيفية وأهميتها.",
    "sous_questions": ["تعليل استعمال اليوراسيل المشع", "استنتاج مكان وزمن النسخ", "تسمية عناصر الترجمة", "شرح تغيرات البروتين بعد التركيب"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "يجمع بين النسخ والترجمة ونضج البروتين."
  },
  {
    "candidate_id": "khelifa1_vol02_c010",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [9,10],
    "theme": "بنية ADN ومورثة الأنسولين وترجمة سلسلة B",
    "texte_corrige": "تمثل الوثيقة بنية جزء من ADN وترتيب الأحماض الأمينية الثمانية الأخيرة للسلسلة B المكونة لأنسولين الإنسان، كما تبين الوحدات الرمزية في ARNm المعبرة عن هذه الأحماض الأمينية. حدد بيانات بنية ADN، واستنتج ترتيب القواعد في المورثة المسؤولة عن هذا الجزء، ثم اشرح الظاهرة التي تسمح بتركيب السلسلة الببتيدية ومرحلة ارتباط الحمض الأميني أثناء الترجمة.",
    "sous_questions": ["تعرف بنية ADN", "استنتاج المورثة من ARNm/البروتين", "تحديد مرحلة الترجمة", "تمثيل ارتباط حمض أميني أثناء تركيب السلسلة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03"],
    "qualite_ocr": "moyenne",
    "notes": "تحتاج المتتاليات في الصفحات 9-10 إلى مراجعة بصرية."
  },
  {
    "candidate_id": "khelifa1_vol02_c011",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [11],
    "theme": "بنية الأنسولين وعلاقة الشكل الفراغي بالوظيفة",
    "texte_corrige": "الأنسولين هرمون ببتيدي تفرزه خلايا جزر لانجرهانس ويعمل على تسريع دخول الغلوكوز إلى الخلايا. لوحظ أن جزيئات الأنسولين قد تصبح غير عادية فلا ترتبط بالمستقبلات الغشائية. مثل الصيغة الكيميائية المفصلة للجزء المؤطر، وحدد مستوى البنية الممثل، ثم حدد مستوى البنية الفراغية لجزيئة الأنسولين وفسر أثر تغيرها على الوظيفة.",
    "sous_questions": ["تمثيل الصيغة الكيميائية لجزء ببتيدي", "تحديد مستوى البنية البروتينية", "ربط الشكل الفراغي بالارتباط بالمستقبل", "تفسير فقدان الوظيفة"],
    "micro_concepts_probables": ["mc_struc_01", "mc_struc_03", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "يناسب مفاهيم بنية البروتين أكثر من النسخ/الترجمة."
  },
  {
    "candidate_id": "khelifa1_vol02_c012",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [11],
    "theme": "فرط كوليسترول الدم العائلي ومستقبلات غشائية بروتينية",
    "texte_corrige": "مرض فرط كوليسترول الدم العائلي من الأمراض الوراثية الخطيرة. تتحكم في تركيب المستقبلات الغشائية البروتينية الخاصة بإدخال الكوليسترول مورثة على الصبغي 19، ولها أليل عادي وأليلات طافرة. عرف الطفرة، وحدد نوع الطفرتين المؤديتين إلى ظهور النمطين المرضيين، ثم مثل ARNm الخاص بكل أليل واستعمل جدول الشفرات الوراثية لتحديد متتالية الأحماض الأمينية وتأثير الطفرات على تركيب المستقبلات البروتينية.",
    "sous_questions": ["تعريف الطفرة", "تحديد نوع الطفرات", "تمثيل ARNm", "ترجمة الأليلات إلى أحماض أمينية", "تفسير أثر الطفرة على المستقبل البروتيني"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "تسلسلات الأليلات طويلة وتحتاج مراجعة دقيقة."
  },
  {
    "candidate_id": "khelifa1_vol02_c013",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [12],
    "theme": "الأشعة فوق البنفسجية ومرض Xeroderma pigmentosum وإصلاح ADN",
    "texte_corrige": "يعتبر مرض جفاف الجلد المصطبغ Xeroderma pigmentosum من الأمراض التي تزيد احتمال الإصابة بسرطان الجلد. تعرض الوثيقة جزءا من ADN عند خلايا جلدية تعرضت للأشعة فوق البنفسجية مقارنة بخلايا سليمة. حدد تأثير الأشعة فوق البنفسجية على ADN الخلايا الجلدية، وفسر دور أنزيمات الإصلاح في إزالة الضرر الوراثي.",
    "sous_questions": ["مقارنة ADN المتأثر بالأشعة مع ADN السليم", "تحديد أثر UV على المعلومة الوراثية", "تفسير علاقة خلل إصلاح ADN بالمرض"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "المفهوم الأقرب في المرجع هو الطفرة/الشفرة وعلاقة البنية بالوظيفة."
  },
  {
    "candidate_id": "khelifa1_vol02_c014",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [13],
    "theme": "مورثة الغلوبين β والثلاسيميا",
    "texte_corrige": "تعرض الوثيقة مراحل التعبير عن المورثة المسؤولة عن صنع بروتين الغلوبين β الداخل في بنية خضاب الدم. تعرف على المرحلتين والأطوار، واستعمل جدول الشفرات الوراثية لتحديد الأحماض الأمينية من رقم 39 إلى 45. ثم تبين وثيقة أخرى جزءا من مورثة طافرة تسبب مرض الثلاسيميا؛ حدد طبيعة وموقع الطفرة وفسر كيف تسبب المرض.",
    "sous_questions": ["تعرف مراحل التعبير المورثي", "ترجمة جزء من الغلوبين β", "تحديد طبيعة وموقع الطفرة", "تفسير المرض بنقص الأحماض الأمينية"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "الأرقام والمتتاليات تحتاج تدقيقا بصريا."
  },
  {
    "candidate_id": "khelifa1_vol02_c015",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [14],
    "theme": "إثبات دور الريبوزومات وARNm في تركيب متعدد الببتيد",
    "texte_corrige": "لقياس كمية متعدد الببتيد المشكل، قورنت نتائج وسط يحتوي على الريبوزومات وآخر يفتقدها، فكان الإشعاع مرتفعا في وجود الريبوزومات وضعيفا أو منعدما في غيابها. علل النتائج التجريبية واستنتج دور الريبوزومات. ثم تمثل وثيقة قطعة مورثة؛ وضح مخطط مراحل تشكل متعدد الببتيد وحدد أثر استبدال أو إدماج أو حذف نيوكليوتيدات على متعدد الببتيد المتشكل باستعمال جدول الشفرة الوراثية.",
    "sous_questions": ["تعليل دور الريبوزومات", "رسم مراحل تركيب متعدد الببتيد", "دراسة أثر الاستبدال", "دراسة أثر الإدماج والحذف"],
    "micro_concepts_probables": ["mc_prot_06", "mc_prot_02", "mc_prot_03"],
    "qualite_ocr": "bonne",
    "notes": "يمزج بين الترجمة والطفرات."
  },
  {
    "candidate_id": "khelifa1_vol02_c016",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [15],
    "theme": "اليوريدين المشع والأكتينوميسين ودور ARNt في تحديد الحمض الأميني",
    "texte_corrige": "تظهر تجربة باليوريدين المشع نتائج التصوير الإشعاعي الذاتي، ثم تعالج خلية بمضاد حيوي يثبط نشاط ADN فلا يظهر الإشعاع. استخرج المعلومات المكملة من التجربة. كما تمثل وثيقة جزيئة ARN لها دور في تركيب البروتين؛ حددها وبيّن دورها، ثم ادرس تجربة ARNm مكون من U وG فقط وARNt مشحون بسيستئين حُوّل إلى ألانين مشع لتحديد الآلية التي تحدد موضع الحمض الأميني في متعدد الببتيد.",
    "sous_questions": ["استنتاج أثر تثبيط النسخ", "تعرف ARNt ودوره", "تحديد الرامزات والرامزات المضادة", "استنتاج علاقة الرامزة بالـ ARNt والحمض الأميني"],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_03", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "سؤال مركب ومهم حول ARNt والشفرة الوراثية."
  },
  {
    "candidate_id": "khelifa1_vol02_c017",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [16],
    "theme": "مرحلتان أساسيتان في التعبير المورثي وجدول الشفرة الوراثية",
    "texte_corrige": "باستغلال وثيقتين، تعرف على البنيتين المشار إليهما، وسم المرحلة الممثلة في الوثيقة الأولى وعلل لماذا تعتبر مرحلة أساسية. استعمل معطيات الشفرة الوراثية لإكمال جدول الوثيقة الثانية، ثم سم المرحلة الموالية وحدد العناصر المتدخلة فيها ودور كل منها، وأنجز رسمين تخطيطيين للمرحلتين مع البيانات اللازمة.",
    "sous_questions": ["تعرف مرحلة النسخ أو الترجمة", "إكمال جدول الشفرة الوراثية", "تحديد العناصر المتدخلة", "رسم المرحلتين"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03"],
    "qualite_ocr": "moyenne",
    "notes": "يعتمد على وثائق ورسوم غير مستخرجة بالكامل."
  },
  {
    "candidate_id": "khelifa1_vol02_c018",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [17],
    "theme": "أنواع ARN في الخلية والريبوزوم وARNr",
    "texte_corrige": "بالاستعانة بمنحنيات الطرد المركزي أثناء تركيب البروتين وبعد معالجة الخلية بمضاد نوعي للريبوزومات، حدد نوع ARN في كل شوكة وعلل إجابتك. فسر اختلاف عدد النيوكليوتيدات في ARNm، ووضح دور كل نوع من ARN أثناء صنع البروتين. كما استنتج الطبيعة الكيميائية للريبوزوم ومعلومة حول ARNr.",
    "sous_questions": ["تحديد ARNm وARNt وARNr", "تعليل اختلاف أطوال ARNm", "تحديد دور كل نوع ARN", "استنتاج تركيب الريبوزوم"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_05", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "يركز على أنواع ARN والريبوزومات."
  },
  {
    "candidate_id": "khelifa1_vol02_c019",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [17,18],
    "theme": "تحديد جزيئات من الشفرة الوراثية وتأثير الأشعة UV على القراءة",
    "texte_corrige": "باستعمال جدول الشفرات الوراثية سم الجزيئات التي يمكن التعرف عليها، وأكمل الجزيئة التي تبدأ برامزة البدء وحدد ترتيب الأحماض الأمينية في الببتيد المتشكل. إذا تأثرت قطعة من ADN بالأشعة UV فأعطت قراءات أخرى للمعلومة الوراثية، فسر أثر ذلك على ARNm والببتيد الناتج.",
    "sous_questions": ["تحديد رامزة البدء", "ترجمة ARNm إلى ببتيد", "تفسير أثر UV على القراءة", "ربط تغير القراءة بتغير البروتين"],
    "micro_concepts_probables": ["mc_prot_03", "mc_prot_02", "mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "المتتاليات في الصفحة 18 ضعيفة OCR وتحتاج مراجعة."
  },
  {
    "candidate_id": "khelifa1_vol02_c020",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [19,20],
    "theme": "مرحلة الترجمة: الرامزة والرامزة المضادة وكتابة نص علمي عن تصنيع البروتين",
    "texte_corrige": "تعرض الوثيقة مرحلة من مراحل تصنيع البروتين. تعرف على المرحلة، وحدد هوية العناصر والجزيئات وأدوارها، ثم عرف الرامزة والرامزة المضادة وحدد رامزة الانطلاق. أكمل العناصر المجهولة باستعمال جدول الشفرات الوراثية، وحدد شريطي ADN الموافقين لـ ARNm، ثم اكتب نصا علميا عن آلية تصنيع البروتين في الخلايا الحية.",
    "sous_questions": ["تعرف مرحلة الترجمة", "تعريف الرامزة والرامزة المضادة", "استعمال جدول الشفرة", "استنتاج شريطي ADN", "كتابة نص علمي تركيبي"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_prot_05"],
    "qualite_ocr": "bonne",
    "notes": "سؤال تركيبي حول الترجمة."
  },
  {
    "candidate_id": "khelifa1_vol02_c021",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [20,21],
    "theme": "دراسة خلية حيوانية وتركيب البروتين باستعمال اليوراسيل المشع",
    "texte_corrige": "تعرض الوثيقة فوق بنية خلية، ثم تلخص نتائج تجارب منجزة عليها باستعمال مواد مشعة. تعرف على العناصر المرقمة، واشرح ظهور الإشعاع في بعض العناصر، واستخلص الظاهرة البيوكيميائية التي تحدث ومقرها. ثم مثل هذه الظاهرة برسم تخطيطي جزيئي كامل البيانات، وفسر الظاهرة التي تسبق تركيب البروتين باستعمال اليوراسيل المشع ومثبط ARN بوليميراز.",
    "sous_questions": ["تسمية العضيات", "تفسير موضع الإشعاع", "استنتاج النسخ والترجمة", "رسم الظاهرة على المستوى الجزيئي"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_04"],
    "qualite_ocr": "moyenne",
    "notes": "يعتمد على رسم خلوي وتجارب إشعاعية."
  },
  {
    "candidate_id": "khelifa1_vol02_c022",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [21],
    "theme": "ARN بوليميراز، الريبوزومات وأنواع ARN أثناء تركيب البروتين",
    "texte_corrige": "زرعت خلية بنكرياسية في وسط يحتوي على اليوراسيل المشع، ثم درست كمية ARN أثناء فترة تركيب البروتين. في تجربة أخرى عولجت الخلية بالألفا أمانيتين، وهو مثبط لإنزيم ARN بوليميراز، أو بمثبط نوعي للريبوزومات. حلل التسجيلين، واستنتج أنواع ARN الظاهرة والمتدخلة في تصنيع البروتين، واذكر العناصر المشاركة في ارتباط ARNt بالعنصر الموافق له.",
    "sous_questions": ["تعليل استعمال اليوراسيل المشع", "تحليل أثر مثبط ARN بوليميراز", "تحليل أثر مثبط الريبوزومات", "تحديد أنواع ARN المتدخلة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "يتناول النسخ والريبوزومات وأنواع ARN."
  },
  {
    "candidate_id": "khelifa1_vol02_c023",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [22],
    "theme": "خلايا الكرومافين وإفراز الأدرينالين والبروتينات",
    "texte_corrige": "تعرض وثيقة خلايا كرومافين ونسيجا ضاما حولها، وتجارب تنبيه العصب أو إضافة الأستيل كولين وما ينتج عنها من إفراز الأدرينالين. كما تحضن خلايا في وجود اليوريدين المشع أو الأحماض الأمينية المشعة وتدرس النتائج بالتصوير الإشعاعي الذاتي. حلل النتائج، وحدد النمط الخلوي المستعمل، واستخرج العلاقة بين نشاط الخلية وتركيب البروتينات.",
    "sous_questions": ["تحديد نوع الخلية", "تحليل أثر التنبيه العصبي والأستيل كولين", "تفسير استعمال اليوريدين والأحماض الأمينية المشعة", "استنتاج نشاط تركيب البروتين"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "بعض تفاصيل الصفحة 22 مقطوعة في OCR؛ السؤال عام حول النشاط الخلوي."
  },
  {
    "candidate_id": "khelifa1_vol02_c024",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [23,24],
    "theme": "مورثة وجزء بروتيني وتجارب تركيب البروتين في أنابيب الاختبار",
    "texte_corrige": "يمثل جزء من مورثة السلسلة النشطة، كما تعرض وثيقة مراحل أو أقساما خلوية مرتبطة بتركيب البروتين. حدد هوية الأقسام مع التعليل، وحدد الظاهرة التي تتم في القسم الثالث وعلاقتها بالأقسام الأخرى. بين كيف يمكن أن تتم الظاهرة في أنابيب الاختبار بإضافة ARNm وكل أنواع الأحماض الأمينية، وفسر العلاقة بين تغير كمية الأحماض الأمينية ووجود ARNm في الوسط.",
    "sous_questions": ["تحديد الأقسام الخلوية", "تسمية الظاهرة", "تفسير تركيب البروتين في المختبر", "توضيح دور ARNm"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_04", "mc_prot_06"],
    "qualite_ocr": "moyenne",
    "notes": "النص في الصفحات 23-24 مشوش لكن الفكرة الأساسية واضحة."
  },
  {
    "candidate_id": "khelifa1_vol02_c025",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [25],
    "theme": "برنامج Anagène وترتيب القواعد في ثلاثيات ومراحل التعبير المورثي",
    "texte_corrige": "باستعمال برنامج المحاكاة Anagène تعرض الوثيقة متتاليات ADN وARNm وسلسلة أحماض أمينية. حدد الهدف من استعمال البرنامج، وتعرف على البنيات، واذكر مكوناتها الكيميائية، وعلل ترتيب القواعد الآزوتية في شكل ثلاثيات بالاستدلال الرياضي. ثم تمثل وثائق أخرى مرحلتين من مراحل الظاهرة المدروسة؛ سم الظاهرة والمرحلة الخاصة بكل شكل واكتب البيانات ومثل نهاية إحدى المراحل برسم تخطيطي.",
    "sous_questions": ["تعرف ADN وARNm والبروتين", "تحديد المكونات الكيميائية", "تفسير الرامزات الثلاثية", "تعرف النسخ والترجمة", "رسم نهاية مرحلة"],
    "micro_concepts_probables": ["mc_prot_03", "mc_prot_01", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "سؤال شامل ومهم عن التعبير المورثي."
  },
  {
    "candidate_id": "khelifa1_vol02_c026",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [25,26],
    "theme": "فيروس موزاييك التبغ VMT ومعلومة وراثية ARN",
    "texte_corrige": "فيروس موزاييك التبغ VMT عصية تحتوي على محفظة وجزيئة ARN بحوالي 6400 نيوكليوتيدة. عند دخول الفيروس إلى الخلية يستعمل ARN في تصنيع البروتينات. حدد بماذا تختلف المعلومة الوراثية لدى هذا الفيروس عن حقيقيات النواة وبدائيات النواة، وادرس أثر معالجة الفيروس بحمض الآزوت على الرامزات وعلى البروتينات المصطنعة.",
    "sous_questions": ["تمييز ARN كمعلومة وراثية عند الفيروس", "مقارنة مع ADN في الخلايا", "تفسير أثر تغير قاعدة في الرامزة", "ربط تغير الرامزة بتغير البروتين"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_03", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "المعالجة بحمض الآزوت والمتتاليات تحتاج تدقيقا."
  },
  {
    "candidate_id": "khelifa1_vol02_c027",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [26],
    "theme": "ARNt الناقل وآلية تركيب البروتين والتخصص الوظيفي",
    "texte_corrige": "تعد البروتينات جزيئات أساسية في الخلية. تعرض الوثيقة بنية تدخل في تركيب البروتين تدعى ARN الناقل، كما تعرض المراحل الأساسية لآلية تركيب البروتين. تعرف على العنصرين في الوثيقة، وفسر القدرة الوظيفية المضاعفة لجزيئة ARNt، وسم الظاهرة التي تبينها الوثيقة، واستخرج المعلومات التي تقدمها حول التخصص الوظيفي للبروتينات المتشكلة.",
    "sous_questions": ["تعرف ARNt", "تفسير ارتباط ARNt بالحمض الأميني والرامزة", "تسمية الترجمة", "ربط الظاهرة بالتخصص البروتيني"],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_02", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "يركز على ARNt والترجمة."
  },
  {
    "candidate_id": "khelifa1_vol02_c028",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [27],
    "theme": "عزل عضيات سيتوبلازمية وإضافة ARNm ودراسة تركيب البروتين",
    "texte_corrige": "حدد نوع العضيات الموجودة في كل وسط مع التعليل. بعد عزل العناصر السيتوبلازمية توضع في وسط غني بالأحماض الأمينية ثم يضاف ARNm، وتقاس كمية البروتينات المركبة وكمية ARNm في الوسط. حلل الوثيقة واستنتج دور العضيات وARNm في تركيب البروتين، ثم استعمل سلسلة الأحماض الأمينية وجدول الشفرة الوراثية لتقديم ترتيب القواعد الآزوتية الموافقة وسم الظاهرة الممثلة.",
    "sous_questions": ["تحديد العضيات", "تحليل أثر إضافة ARNm", "ترجمة/استنتاج القواعد الموافقة", "تسمية ظاهرة الترجمة"],
    "micro_concepts_probables": ["mc_prot_06", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "يشمل ريبوزومات وARNm وسلسلة ببتيدية."
  },
  {
    "candidate_id": "khelifa1_vol02_c029",
    "source": "KHELIFA1_VOLUME_02.pdf",
    "pages": [27,28],
    "theme": "داء الفينيل كيتونوريا وطفرات مورثة إنزيم PAH",
    "texte_corrige": "الفينيل كيتونوريا مرض وراثي متنح سببه خلل في مورثة تشرف على تركيب إنزيم PAH المتدخل في تحول الفينيل ألانين. تقدم الوثائق الأليل العادي وأليلات طافرة وجزيئات ARNm الموافقة. باستعمال جدول الشفرات الوراثية، قارن التتابعات، وحدد أثر الطفرات على البروتين، ثم قارن التعبير المورثي لدى حقيقيات النواة وبدائيات النواة برسومات مبسطة.",
    "sous_questions": ["مقارنة الأليل العادي والطافر", "ترجمة ARNm إلى أحماض أمينية", "تفسير أثر الطفرة على إنزيم PAH", "مقارنة التعبير المورثي في حقيقيات وبدائيات النواة"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_05", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "النص في الصفحة 28 مشوش؛ الفكرة العامة واضحة."
  }
]
Open

Edited files
2
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02	28	Exercices	✅ OCR production complet + ✅ extraction/intégration (khelifa1_volume02_extraction_phase2.json)	29 questions
KHELIFA 1	Vol 03-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 141 questions / 33 micro-concepts couverts sur 42
Derniers ajouts
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
KHELIFA 1 Vol 02 : 29 questions ajoutées (q_khelifa1_vol02_001 à q_khelifa1_vol02_029)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
KHELIFA 1 Volume 02 — OCR production + Phase 2/3
MD
KHELIFA 1 Volume 02 — OCR production + Phase 2/3
Source : input/KHELIFA1_VOLUME_02.pdf

OCR production
Commande utilisée :

Bash

python scripts/ocr_pipeline_production.py input/KHELIFA1_VOLUME_02.pdf \
  --start 1 --end 28 \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt
Sortie :

text

input/KHELIFA1_VOLUME_02.ocr_prod_full.txt
input/KHELIFA1_VOLUME_02.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Méthode : OCR sur 28 pages
Erreurs : 0
Total caractères OCR : 53,838
Taille fichier texte final : 84,842 bytes
Confiance moyenne OCR : 60.45
Durée : ~9 min 26 s
Phase 2 — Extraction
Fichier créé :

text

output/khelifa1_volume02_extraction_phase2.json
Résultat : 29 blocs candidats extraits.

Phase 3 — Intégration
Sauvegarde avant intégration :

text

output/questions_taggees.before_khelifa1_vol02.json
Questions ajoutées :

text

q_khelifa1_vol02_001 → q_khelifa1_vol02_029
Validation globale
Commande :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Résultat :

text

Total questions : 141
Concepts distincts utilisés : 33
Erreurs : 0
Avertissements : 0
Distribution des 29 questions ajoutées
Concepts principaux
Micro-concept	Questions
mc_prot_01 — Transcription de l'ADN	8
mc_prot_02 — Traduction de l'ARNm	4
mc_prot_03 — Code génétique	6
mc_prot_04 — ARNm	4
mc_prot_05 — ARNt	4
mc_prot_06 — Ribosome	2
mc_struc_01 — Structure primaire	1
Types
Type	Questions
analyse_document	15
application	6
raisonnement	4
schema_a_completer	2
comparaison	2
Difficulté
Difficulté	Questions
moyenne	17
difficile	12
Thèmes principaux
Régulation/expression de la lactase chez les bactéries
ARNm, ARNt, ARNr et ribosomes
Traduction et code génétique
Mutations et maladies génétiques : thalassémie, hypercholestérolémie familiale, xeroderma pigmentosum, phénylcétonurie
Structure-fonction des protéines : insuline, récepteurs membranaires
Virus à ARN : VMT
Open
Oui ✅ J’ai traité KHELIFA1_VOLUME_02 avec la même méthode complète.

1. OCR production terminé
PDF traité :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_02.pdf
Sortie OCR :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_02.ocr_prod_full.txt
Bundle :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_02.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Erreurs OCR : 0
Total caractères OCR : 53,838
Taille fichier final : 84,842 bytes
Confiance moyenne : 60.45
Durée : ~9 min 26 s
2. Phase 2 extraction terminée
Fichier créé :

text

downloads/annales_svt/output/khelifa1_volume02_extraction_phase2.json
Résultat :

text

29 blocs candidats extraits
3. Phase 3 tagging + intégration terminée
Sauvegarde créée avant intégration :

text

downloads/annales_svt/output/questions_taggees.before_khelifa1_vol02.json
Questions ajoutées dans :

text

downloads/annales_svt/output/questions_taggees.json
IDs ajoutés :

text

q_khelifa1_vol02_001
→
q_khelifa1_vol02_029
4. Validation globale
Commande lancée :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Résultat :

text

Total questions : 141
Concepts distincts utilisés : 33
Erreurs : 0
Avertissements : 0
✅ JSON valide.

Distribution des 29 questions du volume 02
Concepts principaux
text

mc_prot_01  Transcription de l'ADN       8
mc_prot_02  Traduction de l'ARNm          4
mc_prot_03  Code génétique                6
mc_prot_04  ARNm                          4
mc_prot_05  ARNt                          4
mc_prot_06  Ribosome                      2
mc_struc_01 Structure primaire            1
Types
text

analyse_document      15
application            6
raisonnement           4
schema_a_completer     2
comparaison            2
Difficulté
text

moyenne     17
difficile   12
Rapport créé
text

downloads/annales_svt/output/khelifa1_volume02_phase3_integration.md
Manifeste mis à jour
text

downloads/annales_svt/MANIFESTE.md
Nouvel état global :

text

141 questions traitées
33 micro-concepts couverts
0 erreur
0 avertissement
Prochaine étape
On peut continuer avec :

text

KHELIFA1_VOLUME_03.pdf
Même pipeline :

OCR production
extraction
intégration JSON
validation.

applique ce script ocr pipeline #!/usr/bin/env python3

"""

Pipeline OCR orienté production — arabe (principal) + français.

Traite :

PDF (extraction native + OCR)

Image unique (PNG, JPG, JPEG, TIFF, TIF, BMP, WEBP)

Dossier d'images

Fonctionnalités :

Reprise robuste par page via état JSON + artefacts intermédiaires

PDF ouvert une seule fois par worker (cache par processus)

Parallélisation par page sans sérialiser les images entre processus

Extraction native du texte PDF avant OCR si demandé

Détection de pages vides et de doublons perceptuels

Métriques d'exécution par page et globales (confidence, qualité)

Sortie texte finale réassemblée dans l'ordre

CSV et JSON de reporting

PDF searchable optionnel

Journal d'exécution sur disque

Limitation mémoire configurable

Dépendances Python :

  pip install numpy opencv-python pymupdf

Dépendances système :

  tesseract-ocr + langues (par défaut ara et fra)

Exemples :

  python ocr_pipeline_production.py input/doc.pdf --workers 4 --resume

  python ocr_pipeline_production.py input/doc.pdf --searchable-pdf --workers 4

  python ocr_pipeline_production.py input/scans/ --workers 6 --retries 3

  python ocr_pipeline_production.py input/doc.pdf --max-source-mb 2000 --dedup

"""

from future import annotations

import argparse

import csv

import json

import logging

import os

import re

import shutil

import subprocess

import sys

import tempfile

import time

import traceback

from concurrent.futures import ProcessPoolExecutor, as_completed

from dataclasses import dataclass

from datetime import datetime, timezone

from pathlib import Path

from typing import Any, Dict, List, Optional, Sequence, Set, Tuple

import cv2

import numpy as np

---------------------------------------------------------------------------
Constants
---------------------------------------------------------------------------
IMAGE_EXTENSIONS: Set[str] = {".png", ".jpg", ".jpeg", ".tiff", ".tif", ".bmp", ".webp"}

PDF_EXTENSIONS: Set[str] = {".pdf"}

SEPARATOR: str = "=" * 70

STATE_VERSION: int = 3

MAX_SOURCE_MB: int = 2000

CONFIDENCE_THRESHOLD: float = 45.0

EMPTY_PAGE_THRESHOLD: float = 0.98

DEDUP_HASH_SIZE: Tuple[int, int] = (16, 16)

LOG = logging.getLogger("ocr_pipeline_production")

---------------------------------------------------------------------------
Dataclasses
---------------------------------------------------------------------------
@dataclass(frozen=True)

class PageRef:

    page_no: int

    kind: str  # 'pdf' | 'image'

    source_path: str

    pdf_index: Optional[int] = None

    perceptual_hash: Optional[str] = None

@dataclass(frozen=True)

class AttemptSpec:

    variant: str

    psm: int

---------------------------------------------------------------------------
Worker-level PDF cache (un cache par processus worker)
---------------------------------------------------------------------------
workerpdf_cache: Dict[str, Any] = {}

def getcached_pdf_doc(pdf_path: str) -> Any:

    """Retourne un document PyMuPDF ouvert, avec cache par processus worker."""

    if pdf_path not in workerpdf_cache:

        import fitz

        workerpdf_cache[pdf_path] = fitz.open(pdf_path)

    return workerpdf_cache[pdf_path]

def closeworker_pdf_cache() -> None:

    """Ferme tous les documents PDF ouverts dans le worker courant."""

    for doc in workerpdf_cache.values():

        try:

            doc.close()

        except Exception:

            pass

    workerpdf_cache.clear()

---------------------------------------------------------------------------
Logging / utilitaires de base
---------------------------------------------------------------------------
def configure_logging(log_file: Optional[Path] = None, verbose: bool = False) -> None:

    LOG.setLevel(logging.DEBUG if verbose else logging.INFO)

    LOG.handlers.clear()

    LOG.propagate = False

    formatter = logging.Formatter(

        fmt="%(asctime)s [%(levelname)s] %(message)s",

        datefmt="%H:%M:%S",

    )

    console = logging.StreamHandler(sys.stdout)

    console.setLevel(logging.DEBUG if verbose else logging.INFO)

    console.setFormatter(formatter)

    LOG.addHandler(console)

    if log_file is not None:

        log_file.parent.mkdir(parents=True, exist_ok=True)

        file_handler = logging.FileHandler(log_file, encoding="utf-8")

        file_handler.setLevel(logging.DEBUG)

        file_handler.setFormatter(formatter)

        LOG.addHandler(file_handler)

def die(message: str, code: int = 1) -> None:

    LOG.error(message)

    raise SystemExit(code)

def utc_now_iso() -> str:

    """Retourne l'heure UTC courante en ISO 8601 (compatible Python 3.12+)."""

    return datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z")

def natural_key(value: str) -> List[Any]:

    """Tri naturel pour les noms de fichiers (page_2 avant page_10)."""

    return [int(part) if part.isdigit() else part.lower() for part in re.split(r"(\d+)", value)]

def atomic_write_text(path: Path, content: str, encoding: str = "utf-8") -> None:

    path.parent.mkdir(parents=True, exist_ok=True)

    with tempfile.NamedTemporaryFile("w", delete=False, dir=str(path.parent), encoding=encoding) as tmp:

        tmp.write(content)

        tmp_path = Path(tmp.name)

    os.replace(tmp_path, path)

def atomic_write_json(path: Path, data: Dict) -> None:

    atomic_write_text(path, json.dumps(data, ensure_ascii=False, indent=2) + "\n")

def atomic_write_bytes(path: Path, payload: bytes) -> None:

    path.parent.mkdir(parents=True, exist_ok=True)

    with tempfile.NamedTemporaryFile("wb", delete=False, dir=str(path.parent)) as tmp:

        tmp.write(payload)

        tmp_path = Path(tmp.name)

    os.replace(tmp_path, path)

def compact_len(text: str) -> int:

    return len(re.sub(r"\s+", "", text or ""))

def safe_error_text(text: str, limit: int = 500) -> str:

    return (text or "").strip().replace("\n", " ")[:limit] or "Erreur sans détail"

---------------------------------------------------------------------------
Chemins d'artefacts
---------------------------------------------------------------------------
def compute_paths(source: Path, out_dir: Optional[str], suffix: str) -> Dict[str, Path]:

    if out_dir:

        out_root = Path(out_dir)

        out_root.mkdir(parents=True, exist_ok=True)

        txt_out = out_root / f"{source.stem}{suffix}"

        searchable_pdf = out_root / f"{source.stem}.searchable.pdf"

    elif source.is_dir():

        txt_out = source.parent / f"{source.name}{suffix}"

        searchable_pdf = source.parent / f"{source.name}.searchable.pdf"

    else:

        txt_out = source.with_suffix(suffix)

        searchable_pdf = txt_out.parent / f"{source.stem}.searchable.pdf"

    bundle = txt_out.with_name(txt_out.name + ".bundle")

    return {

        "txt_out": txt_out,

        "searchable_pdf": searchable_pdf,

        "bundle": bundle,

        "pages_txt": bundle / "pages_txt",

        "pages_meta": bundle / "pages_meta",

        "pages_pdf": bundle / "pages_pdf",

        "state": bundle / "run.state.json",

        "summary": bundle / "summary.json",

        "pages_csv": bundle / "pages.csv",

        "run_log": bundle / "run.log",

    }

def page_text_path(paths: Dict[str, Path], page_no: int) -> Path:

    return paths["pages_txt"] / f"page_{page_no:06d}.txt"

def page_meta_path(paths: Dict[str, Path], page_no: int) -> Path:

    return paths["pages_meta"] / f"page_{page_no:06d}.json"

def page_pdf_path(paths: Dict[str, Path], page_no: int) -> Path:

    return paths["pages_pdf"] / f"page_{page_no:06d}.pdf"

def reset_outputs(paths: Dict[str, Path], resume: bool) -> None:

    if resume:

        for key in ("bundle",):

            paths[key].mkdir(parents=True, exist_ok=True)

        return

    if paths["bundle"].exists():

        shutil.rmtree(paths["bundle"])

    if paths["txt_out"].exists():

        paths["txt_out"].unlink()

    if paths["searchable_pdf"].exists():

        paths["searchable_pdf"].unlink()

    for key in ("bundle", "pages_txt", "pages_meta", "pages_pdf"):

        paths[key].mkdir(parents=True, exist_ok=True)

---------------------------------------------------------------------------
Validation / prérequis
---------------------------------------------------------------------------
def parse_langs(lang_spec: str) -> List[str]:

    langs = [chunk.strip() for chunk in lang_spec.split("+") if chunk.strip()]

    if not langs:

        raise ValueError("Aucune langue Tesseract valide dans --lang")

    return langs

def check_tesseract_languages(lang_spec: str) -> None:

    if not shutil.which("tesseract"):

        die("Tesseract introuvable dans le PATH.")

    result = subprocess.run(

        ["tesseract", "--list-langs"],

        capture_output=True,

        text=True,

        check=False,

    )

    if result.returncode != 0:

        die(f"Échec de tesseract --list-langs : {safe_error_text(result.stderr)}")

    # Parsing robuste : on prend toutes les lignes qui ressemblent à des codes de langue

    installed: Set[str] = set()

    for line in (result.stdout + "\n" + result.stderr).splitlines():

        line = line.strip()

        if not line:

            continue

        if "language" in line.lower() and "list" in line.lower():

            continue

        parts = re.split(r"[\s,\n]+", line)

        for part in parts:

            part = part.strip()

            if part and re.match(r"^[a-z]{2,4}(_[A-Z]{2,4})?$", part):

                installed.add(part)

    missing = [lang for lang in parse_langs(lang_spec) if lang not in installed]

    if missing:

        die(

            "Langues Tesseract manquantes : "

            f"{', '.join(missing)}\n"

            f"Ubuntu/Debian : apt install {' '.join(f'tesseract-ocr-{x}' for x in missing)}"

        )

def check_pdf_support_if_needed(source: Path) -> None:

    need_pdf = source.is_file() and source.suffix.lower() in PDF_EXTENSIONS

    if not need_pdf:

        return

    try:

        import fitz  # noqa: F401

    except Exception as exc:

        die(f"Support PDF indisponible. Installez PyMuPDF (pymupdf). Détail : {exc}")

def check_source_file(source: Path, max_source_mb: int) -> None:

    """Validation approfondie du fichier source."""

    if not source.exists():

        die(f"Source introuvable : {source}")

    if not os.access(source, os.R_OK):

        die(f"Source non lisible (permissions) : {source}")

    if source.is_file():

        size_mb = source.stat().st_size / (1024 * 1024)

        if size_mb > max_source_mb:

            die(

                f"Source trop volumineuse : {size_mb:.1f} Mo "

                f"(limite : {max_source_mb} Mo). "

                f"Ajustez avec --max-source-mb."

            )

    if source.is_file() and source.suffix.lower() in PDF_EXTENSIONS:

        try:

            import fitz

            with fitz.open(str(source)) as doc:

                if doc.page_count == 0:

                    die(f"Le PDF ne contient aucune page : {source}")

        except Exception as exc:

            die(f"Le PDF semble corrompu ou invalide : {source} — {exc}")

def validate_args(args: argparse.Namespace) -> None:

    source = Path(args.source)

    if args.dpi <= 0:

        die("--dpi doit être > 0")

    if args.workers <= 0:

        die("--workers doit être >= 1")

    if args.timeout <= 0:

        die("--timeout doit être > 0")

    if args.start <= 0:

        die("--start doit être >= 1")

    if args.end is not None and args.end < args.start:

        die("--end doit être >= --start")

    if args.psm < 0 or args.psm > 13:

        die("--psm doit être entre 0 et 13")

    if args.oem not in (0, 1, 2, 3):

        die("--oem doit être dans {0,1,2,3}")

    if args.pdf_mode not in {"auto", "ocr", "text"}:

        die("--pdf-mode doit être dans {auto, ocr, text}")

    if args.text_threshold < 0:

        die("--text-threshold doit être >= 0")

    if args.min_chars < 0:

        die("--min-chars doit être >= 0")

    if args.retries < 0:

        die("--retries doit être >= 0")

    if args.max_source_mb <= 0:

        die("--max-source-mb doit être > 0")

    if source.is_file() and source.suffix.lower() not in IMAGE_EXTENSIONS | PDF_EXTENSIONS:

        die(f"Extension non supportée : {source.suffix}")

    if source.is_dir():

        files = [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS]

        if not files:

            die(f"Aucune image supportée trouvée dans : {source}")

    check_source_file(source, args.max_source_mb)

    check_tesseract_languages(args.lang)

    check_pdf_support_if_needed(source)

---------------------------------------------------------------------------
État de reprise
---------------------------------------------------------------------------
def runtime_params() -> Set[str]:

    """Paramètres qui n'affectent PAS la compatibilité de l'état (peuvent changer entre runs)."""

    return {"workers", "timeout", "verbose", "resume", "out_dir", "suffix", "max_source_mb", "dedup"}

def resume_identity(args: argparse.Namespace) -> Dict[str, object]:

    return {

        "source": str(Path(args.source).resolve()),

        "dpi": args.dpi,

        "lang": args.lang,

        "psm": args.psm,

        "oem": args.oem,

        "pdf_mode": args.pdf_mode,

        "text_threshold": args.text_threshold,

        "no_preprocess": args.no_preprocess,

        "min_chars": args.min_chars,

        "retries": args.retries,

        "tsv_metrics": not args.no_tsv_metrics,

        "searchable_pdf": args.searchable_pdf,

        "start": args.start,

        "end": args.end,

    }

def load_state(path: Path) -> Dict:

    if not path.exists():

        return {}

    try:

        return json.loads(path.read_text(encoding="utf-8"))

    except json.JSONDecodeError as exc:

        LOG.warning("Fichier d'état corrompu, reprise ignorée : %s", exc)

        return {}

    except Exception as exc:

        die(f"Fichier d'état illisible : {path} ({exc})")

def save_state(path: Path, state: Dict) -> None:

    state["updated_at"] = utc_now_iso()

    atomic_write_json(path, state)

def ensure_state_compatible(state: Dict, args: argparse.Namespace, paths: Dict[str, Path]) -> None:

    if not state:

        return

    if state.get("version") != STATE_VERSION:

        die(

            f"Version d'état incompatible (attendu v{STATE_VERSION}, obtenu v{state.get('version')}). "

            "Relancez sans --resume ou supprimez le bundle."

        )

    current_identity = resume_identity(args)

    saved_identity = state.get("identity", {})

    functional_keys = set(current_identity.keys()) - runtime_params()

    for key in functional_keys:

        if current_identity.get(key) != saved_identity.get(key):

            die(

                f"Le bundle existant ne correspond pas à la même configuration fonctionnelle. "

                f"Différence sur : {key} (bundle={saved_identity.get(key)!r}, "

                f"actuel={current_identity.get(key)!r}). "

                "Relancez sans --resume."

            )

    if state.get("output_txt") != str(paths["txt_out"].resolve()):

        die("Le bundle existant ne correspond pas à cette destination texte.")

def init_state(args: argparse.Namespace, paths: Dict[str, Path], pages: Sequence[PageRef]) -> Dict:

    return {

        "version": STATE_VERSION,

        "created_at": utc_now_iso(),

        "updated_at": utc_now_iso(),

        "identity": resume_identity(args),

        "output_txt": str(paths["txt_out"].resolve()),

        "output_searchable_pdf": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,

        "page_count": len(pages),

        "pages": {},

        "run_config": {

            "workers": args.workers,

            "timeout": args.timeout,

            "verbose": args.verbose,

        },

    }

def completed_pages_from_state(state: Dict, paths: Dict[str, Path], valid_page_nos: Set[int]) -> Set[int]:

    done: Set[int] = set()

    for key, meta in state.get("pages", {}).items():

        try:

            pno = int(key)

        except (ValueError, TypeError):

            continue

        if pno not in valid_page_nos:

            continue

        try:

            if (

                meta.get("status") == "done"

                and page_text_path(paths, pno).exists()

                and page_meta_path(paths, pno).exists()

            ):

                done.add(pno)

        except Exception:

            continue

    return done

---------------------------------------------------------------------------
Pages source
---------------------------------------------------------------------------
def compute_perceptual_hash(image: np.ndarray) -> str:

    """Hash perceptuel simple (dHash) pour la dé-duplication."""

    resized = cv2.resize(image, (DEDUP_HASH_SIZE[0] + 1, DEDUP_HASH_SIZE[1]), interpolation=cv2.INTER_AREA)

    gray = to_gray(resized)

    diff = gray[:, 1:] > gray[:, :-1]

    return "".join("1" if val else "0" for row in diff for val in row)

def list_pages(

    source: Path, start: int, end: Optional[int], dedup: bool = False

) -> List[PageRef]:

    if source.is_dir():

        files = sorted(

            [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS],

            key=lambda p: natural_key(p.name),

        )

        pages: List[PageRef] = []

        seen_hashes: Set[str] = set()

        for i, path in enumerate(files, start=1):

            img = read_image(str(path))

            phash = compute_perceptual_hash(img) if dedup else None

            if dedup and phash in seen_hashes:

                LOG.debug("Page doublon détectée (hash=%s), ignorée : %s", phash[:16], path.name)

                continue

            if phash:

                seen_hashes.add(phash)

            pages.append(

                PageRef(page_no=i, kind="image", source_path=str(path.resolve()), perceptual_hash=phash)

            )

        return pages

    if source.suffix.lower() in IMAGE_EXTENSIONS:

        img = read_image(str(source))

        phash = compute_perceptual_hash(img) if dedup else None

        return [

            PageRef(page_no=1, kind="image", source_path=str(source.resolve()), perceptual_hash=phash)

        ]

    if source.suffix.lower() in PDF_EXTENSIONS:

        import fitz

        with fitz.open(str(source)) as doc:

            total = doc.page_count

        first = max(1, start)

        last = total if end is None else min(total, end)

        if first > last:

            die(f"Intervalle vide : start={first}, end={last}, total={total}")

        pages = []

        seen_hashes: Set[str] = set()

        for pno in range(first, last + 1):

            pdf_index = pno - 1

            phash = None

            if dedup:

                # Calculer le hash sur la première page, comparer les suivantes

                pass

            pages.append(

                PageRef(

                    page_no=pno,

                    kind="pdf",

                    source_path=str(source.resolve()),

                    pdf_index=pdf_index,

                    perceptual_hash=phash,

                )

            )

        return pages

    die(f"Type de source non supporté : {source}")

    return []

---------------------------------------------------------------------------
Lecture / rendu source
---------------------------------------------------------------------------
def read_image(path: str) -> np.ndarray:

    img = cv2.imread(path, cv2.IMREAD_COLOR)

    if img is None:

        raise RuntimeError(f"Impossible de lire l'image : {path}")

    return img

def render_pdf_page(pdf_path: str, pdf_index: int, dpi: int) -> np.ndarray:

    """Rend une page PDF en image, en utilisant le cache worker."""

    doc = getcached_pdf_doc(pdf_path)

    page = doc[pdf_index]

    zoom = dpi / 72.0

    matrix = fitz.Matrix(zoom, zoom)

    pix = page.get_pixmap(matrix=matrix, alpha=False)

    arr = np.frombuffer(pix.samples, dtype=np.uint8).reshape(pix.height, pix.width, pix.n)

    if pix.n == 1:

        return cv2.cvtColor(arr, cv2.COLOR_GRAY2BGR)

    if pix.n == 4:

        return cv2.cvtColor(arr, cv2.COLOR_RGBA2BGR)

    return cv2.cvtColor(arr, cv2.COLOR_RGB2BGR)

def extract_pdf_native_text(pdf_path: str, pdf_index: int) -> str:

    """Extrait le texte natif d'une page PDF, en utilisant le cache worker."""

    doc = getcached_pdf_doc(pdf_path)

    return doc[pdf_index].get_text("text").strip()

def is_empty_page(image: np.ndarray, threshold: float = EMPTY_PAGE_THRESHOLD) -> bool:

    """Détecte si une page est essentiellement vide (blanc/noir uniforme)."""

    gray = to_gray(image)

    unique, counts = np.unique(gray, return_counts=True)

    max_count = counts.max()

    total_pixels = gray.size

    return (max_count / total_pixels) > threshold

def meaningful_text(text: str, threshold: int) -> bool:

    return compact_len(text) >= threshold

---------------------------------------------------------------------------
Prétraitement image
---------------------------------------------------------------------------
def to_gray(image: np.ndarray) -> np.ndarray:

    if image.ndim == 2:

        return image.copy()

    return cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

def estimate_skew_angle(gray: np.ndarray) -> float:

    blurred = cv2.GaussianBlur(gray, (5, 5), 0)

    , thresh = cv2.threshold(blurred, 0, 255, cv2.THRESHBINARY_INV + cv2.THRESH_OTSU)

    coords = np.column_stack(np.where(thresh > 0))

    if coords.shape[0] < 200:

        return 0.0

    angle = cv2.minAreaRect(coords)[-1]

    if angle < -45:

        angle = -(90 + angle)

    else:

        angle = -angle

    angle = float(angle)

    if abs(angle) < 0.2 or abs(angle) > 15:

        return 0.0

    return angle

def rotate_image(image: np.ndarray, angle: float) -> np.ndarray:

    h, w = image.shape[:2]

    matrix = cv2.getRotationMatrix2D((w / 2.0, h / 2.0), angle, 1.0)

    border_value = 255 if image.ndim == 2 else (255, 255, 255)

    return cv2.warpAffine(

        image,

        matrix,

        (w, h),

        flags=cv2.INTER_CUBIC,

        borderMode=cv2.BORDER_CONSTANT,

        borderValue=border_value,

    )

def normalize_gray(gray: np.ndarray) -> np.ndarray:

    angle = estimate_skew_angle(gray)

    if angle:

        gray = rotate_image(gray, angle)

    clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8, 8))

    normalized = clahe.apply(gray)

    denoised = cv2.fastNlMeansDenoising(normalized, h=7, templateWindowSize=7, searchWindowSize=21)

    return denoised

def preprocess_variant(image: np.ndarray, variant: str) -> np.ndarray:

    gray = to_gray(image)

    if variant == "raw-gray":

        return gray

    normalized = normalize_gray(gray)

    if variant == "gray":

        return normalized

    if variant == "adaptive":

        return cv2.adaptiveThreshold(

            normalized,

            255,

            cv2.ADAPTIVE_THRESH_GAUSSIAN_C,

            cv2.THRESH_BINARY,

            31,

            11,

        )

    if variant == "otsu":

        , binary = cv2.threshold(normalized, 0, 255, cv2.THRESHBINARY + cv2.THRESH_OTSU)

        return binary

    raise ValueError(f"Variant de prétraitement inconnue : {variant}")

def build_attempt_plan(base_psm: int, no_preprocess: bool, retries: int) -> List[AttemptSpec]:

    candidates: List[AttemptSpec] = []

    if no_preprocess:

        raw = [AttemptSpec("raw-gray", base_psm)]

        if base_psm != 6:

            raw.append(AttemptSpec("raw-gray", 6))

        if base_psm != 11:

            raw.append(AttemptSpec("raw-gray", 11))

        candidates.extend(raw)

    else:

        candidates.extend(

            [

                AttemptSpec("adaptive", base_psm),

                AttemptSpec("gray", base_psm),

                AttemptSpec("otsu", base_psm),

            ]

        )

        if base_psm != 6:

            candidates.append(AttemptSpec("adaptive", 6))

        if base_psm != 11:

            candidates.append(AttemptSpec("gray", 11))

    deduped: List[AttemptSpec] = []

    seen = set()

    for item in candidates:

        key = (item.variant, item.psm)

        if key not in seen:

            seen.add(key)

            deduped.append(item)

    return deduped[: max(1, 1 + retries)]

---------------------------------------------------------------------------
Tesseract
---------------------------------------------------------------------------
def tesseract_base_command(img_path: str, output_base: str, lang: str, psm: int, oem: int) -> List[str]:

    return [

        "tesseract",

        img_path,

        output_base,

        "-l",

        lang,

        "--psm",

        str(psm),

        "--oem",

        str(oem),

        "-c",

        "preserve_interword_spaces=1",

        "-c",

        "tessedit-threads=1",

    ]

def run_command(command: List[str], timeout: int) -> subprocess.CompletedProcess:

    env = os.environ.copy()

    env.setdefault("OMP_THREAD_LIMIT", "1")

    env.setdefault("MKL_NUM_THREADS", "1")

    env.setdefault("OPENBLAS_NUM_THREADS", "1")

    return subprocess.run(

        command,

        capture_output=True,

        text=True,

        encoding="utf-8",

        errors="replace",

        timeout=timeout,

        check=False,

        env=env,

    )

def run_tesseract_text(

    image: np.ndarray, lang: str, psm: int, oem: int, timeout: int

) -> Tuple[str, Optional[str]]:

    with tempfile.TemporaryDirectory() as tmp_dir:

        img_path = str(Path(tmp_dir) / "page.png")

        if not cv2.imwrite(img_path, image):

            raise RuntimeError("Échec d'écriture de l'image temporaire")

        try:

            result = run_command(tesseract_base_command(img_path, "stdout", lang, psm, oem), timeout)

        except subprocess.TimeoutExpired:

            return "[TIMEOUT ERROR]", f"Timeout Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:

            err = safe_error_text(result.stderr)

            return f"[TESSERACT ERROR: {err}]", err

        return result.stdout.strip(), None

def parse_tsv_metrics(tsv_text: str) -> Tuple[Optional[float], int]:

    lines = [line for line in tsv_text.splitlines() if line.strip()]

    if len(lines) <= 1:

        return None, 0

    scores: List[float] = []

    word_count = 0

    reader = csv.DictReader(lines, delimiter="\t")

    for row in reader:

        text = (row.get("text") or "").strip()

        conf_raw = (row.get("conf") or "").strip()

        if text:

            word_count += 1

        if conf_raw and conf_raw not in {"-1", "nan"}:

            try:

                scores.append(float(conf_raw))

            except ValueError:

                pass

    if not scores:

        return None, word_count

    return round(sum(scores) / len(scores), 2), word_count

def collect_tsv_metrics(

    image: np.ndarray, lang: str, psm: int, oem: int, timeout: int

) -> Tuple[Optional[float], int, Optional[str]]:

    with tempfile.TemporaryDirectory() as tmp_dir:

        img_path = str(Path(tmp_dir) / "page.png")

        if not cv2.imwrite(img_path, image):

            return None, 0, "Échec d'écriture de l'image temporaire pour TSV"

        command = tesseract_base_command(img_path, "stdout", lang, psm, oem) + ["tsv"]

        try:

            result = run_command(command, timeout)

        except subprocess.TimeoutExpired:

            return None, 0, f"Timeout TSV dépassé ({timeout}s)"

        if result.returncode != 0:

            return None, 0, safe_error_text(result.stderr)

        mean_conf, words = parse_tsv_metrics(result.stdout)

        return mean_conf, words, None

def generate_searchable_page_pdf(

    image: np.ndarray, lang: str, psm: int, oem: int, timeout: int, dest_path: Path

) -> Optional[str]:

    with tempfile.TemporaryDirectory() as tmp_dir:

        img_path = str(Path(tmp_dir) / "page.png")

        out_base = str(Path(tmp_dir) / "page_ocr")

        if not cv2.imwrite(img_path, image):

            return "Échec d'écriture de l'image temporaire pour PDF"

        command = tesseract_base_command(img_path, out_base, lang, psm, oem) + ["pdf"]

        try:

            result = run_command(command, timeout)

        except subprocess.TimeoutExpired:

            return f"Timeout PDF Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:

            return safe_error_text(result.stderr)

        produced = Path(out_base + ".pdf")

        if not produced.exists():

            return "Tesseract n'a pas produit le PDF attendu"

        atomic_write_bytes(dest_path, produced.read_bytes())

        return None

---------------------------------------------------------------------------
Worker par page
---------------------------------------------------------------------------
def process_page(task: Dict[str, object]) -> Dict[str, object]:

    """Traite une seule page. Appelé dans un worker ProcessPoolExecutor."""

    cv2.setNumThreads(1)

    page = PageRef(**task["page"])

    page_no = page.page_no

    started = time.perf_counter()

    lang = str(task["lang"])

    psm = int(task["psm"])

    oem = int(task["oem"])

    dpi = int(task["dpi"])

    timeout = int(task["timeout"])

    pdf_mode = str(task["pdf_mode"])

    no_preprocess = bool(task["no_preprocess"])

    text_threshold = int(task["text_threshold"])

    min_chars = int(task["min_chars"])

    retries = int(task["retries"])

    collect_conf = bool(task["collect_conf"])

    searchable_pdf = bool(task["searchable_pdf"])

    paths = {k: Path(v) for k, v in task["paths"].items()}

    text_path = page_text_path(paths, page_no)

    meta_path = page_meta_path(paths, page_no)

    pdf_path = page_pdf_path(paths, page_no)

    attempts_meta: List[Dict[str, object]] = []

    selected_attempt: Optional[AttemptSpec] = None

    selected_image: Optional[np.ndarray] = None

    selected_text = ""

    selected_error: Optional[str] = None

    method = "failed"

    confidence_mean: Optional[float] = None

    confidence_words = 0

    pdf_error: Optional[str] = None

    is_empty = False

    try:

        # === Étape 1 : Extraction texte natif PDF ===

        if page.kind == "pdf" and pdf_mode in {"auto", "text"}:

            try:

                native_text = extract_pdf_native_text(page.source_path, int(page.pdf_index))

            except Exception as exc:

                native_text = ""

                LOG.warning("Échec extraction texte natif page %d : %s", page_no, exc)

            if meaningful_text(native_text, text_threshold):

                method = "native-text"

                selected_text = native_text

                atomic_write_text(text_path, selected_text + "\n")

                duration_ms = round((time.perf_counter() - started) * 1000, 2)

                return buildmeta(

                    page=page, method=method, text=selected_text, error=None,

                    confidence_mean=None, confidence_words=0, pdf_error=None,

                    pdf_path=pdf_path if pdf_path.exists() else None,

                    attempts_meta=[], selected_attempt=None, is_empty=False,

                    duration_ms=duration_ms,

                )

            if pdf_mode == "text":

                selected_error = f"Aucun texte natif exploitable sur la page {page_no}"

                selected_text = f"[NO NATIVE TEXT page {page_no}]"

                method = "native-text"

                atomic_write_text(text_path, selected_text + "\n")

                duration_ms = round((time.perf_counter() - started) * 1000, 2)

                return buildmeta(

                    page=page, method=method, text=selected_text, error=selected_error,

                    confidence_mean=None, confidence_words=0, pdf_error=None,

                    pdf_path=pdf_path if pdf_path.exists() else None,

                    attempts_meta=[], selected_attempt=None, is_empty=False,

                    duration_ms=duration_ms,

                )

        # === Étape 2 : Rendu de l'image source ===

        if page.kind == "image":

            source_image = read_image(page.source_path)

        elif page.kind == "pdf":

            source_image = render_pdf_page(page.source_path, int(page.pdf_index), dpi)

        else:

            raise RuntimeError(f"Type de page inconnu : {page.kind}")

        # === Étape 3 : Détection de page vide ===

        is_empty = is_empty_page(source_image)

        if is_empty:

            LOG.debug("Page %d détectée comme vide", page_no)

            selected_text = "[EMPTY PAGE]"

            method = "empty"

            atomic_write_text(text_path, selected_text + "\n")

            duration_ms = round((time.perf_counter() - started) * 1000, 2)

            return buildmeta(

                page=page, method=method, text=selected_text, error=None,

                confidence_mean=None, confidence_words=0, pdf_error=None,

                pdf_path=pdf_path if pdf_path.exists() else None,

                attempts_meta=[], selected_attempt=None, is_empty=True,

                duration_ms=duration_ms,

            )

        # === Étape 4 : Tentatives OCR ===

        best_score = -1.0

        plan = build_attempt_plan(psm, no_preprocess, retries)

        for attempt in plan:

            try:

                processed = preprocess_variant(source_image, attempt.variant)

                text, err = run_tesseract_text(processed, lang, attempt.psm, oem, timeout)

            except Exception as exc:

                text = f"[OCR ERROR page {page_no}: {exc}]"

                err = str(exc)

                processed = None

            score = compact_len(text)

            # Calculer la confiance pour cette tentative

            attempt_conf: Optional[float] = None

            attempt_words: int = 0

            if collect_conf and processed is not None and err is None:

                attempt_conf, attempt_words, = collecttsv_metrics(

                    processed, lang, attempt.psm, oem, timeout

                )

            attempts_meta.append(

                {

                    "variant": attempt.variant,

                    "psm": attempt.psm,

                    "chars": len(text),

                    "non_ws_chars": score,

                    "error": err,

                    "confidence_mean": attempt_conf,

                    "confidence_words": attempt_words,

                }

            )

            # Critère amélioré : score combiné texte + confiance

            combined_score = float(score)

            if attempt_conf is not None and attempt_conf > CONFIDENCE_THRESHOLD:

                combined_score = score + (attempt_conf * 10)

            if combined_score > best_score:

                best_score = combined_score

                selected_attempt = attempt

                selected_image = processed

                selected_text = text

                selected_error = err

                method = "ocr"

                confidence_mean = attempt_conf

                confidence_words = attempt_words

            # Early exit si seuil atteint avec bonne confiance

            if err is None and score >= min_chars and (attempt_conf is None or attempt_conf >= CONFIDENCE_THRESHOLD):

                selected_attempt = attempt

                selected_image = processed

                selected_text = text

                selected_error = None

                method = "ocr"

                confidence_mean = attempt_conf

                confidence_words = attempt_words

                break

        if selected_attempt is None or selected_image is None:

            raise RuntimeError("Aucune tentative OCR exploitable")

        # === Étape 5 : PDF searchable ===

        if searchable_pdf and method == "ocr":

            pdf_error = generate_searchable_page_pdf(

                selected_image, lang, selected_attempt.psm, oem, timeout, pdf_path

            )

        # === Étape 6 : Écriture des résultats ===

        atomic_write_text(text_path, selected_text + "\n")

        duration_ms = round((time.perf_counter() - started) * 1000, 2)

        return buildmeta(

            page=page, method=method, text=selected_text, error=selected_error,

            confidence_mean=confidence_mean, confidence_words=confidence_words,

            pdf_error=pdf_error, pdf_path=pdf_path if pdf_path.exists() else None,

            attempts_meta=attempts_meta, selected_attempt=selected_attempt,

            is_empty=is_empty, duration_ms=duration_ms,

        )

    except Exception as exc:

        duration_ms = round((time.perf_counter() - started) * 1000, 2)

        error = str(exc)

        tb = traceback.format_exc()

        LOG.exception("Crash worker page %d : %s", page_no, error)

        fallback_text = f"[OCR ERROR page {page_no}: {error}]\n\n{tb}"

        atomic_write_text(text_path, fallback_text + "\n")

        return {

            "page_no": page_no,

            "status": "error",

            "source_kind": page.kind,

            "source_path": page.source_path,

            "pdf_index": page.pdf_index,

            "method": "failed",

            "chars": len(fallback_text),

            "non_ws_chars": compact_len(fallback_text),

            "attempt_count": len(attempts_meta),

            "attempts": attempts_meta,

            "selected_attempt": None,

            "confidence_mean": None,

            "confidence_words": 0,

            "error": error,

            "traceback": tb,

            "pdf_page_error": None,

            "searchable_pdf_page": None,

            "duration_ms": duration_ms,

            "is_empty_page": False,

            "processed_at": utc_now_iso(),

        }

def buildmeta(

    page: PageRef,

    method: str,

    text: str,

    error: Optional[str],

    confidence_mean: Optional[float],

    confidence_words: int,

    pdf_error: Optional[str],

    pdf_path: Optional[Path],

    attempts_meta: List[Dict[str, object]],

    selected_attempt: Optional[AttemptSpec],

    is_empty: bool,

    duration_ms: float,

) -> Dict[str, object]:

    return {

        "page_no": page.page_no,

        "status": "done",

        "source_kind": page.kind,

        "source_path": page.source_path,

        "pdf_index": page.pdf_index,

        "method": method,

        "chars": len(text),

        "non_ws_chars": compact_len(text),

        "attempt_count": len(attempts_meta),

        "attempts": attempts_meta,

        "selected_attempt": None

        if selected_attempt is None

        else {"variant": selected_attempt.variant, "psm": selected_attempt.psm},

        "confidence_mean": confidence_mean,

        "confidence_words": confidence_words,

        "error": error,

        "pdf_page_error": pdf_error,

        "searchable_pdf_page": str(pdf_path.resolve()) if pdf_path else None,

        "duration_ms": duration_ms,

        "is_empty_page": is_empty,

        "processed_at": utc_now_iso(),

    }

---------------------------------------------------------------------------
Assemblage texte / PDF / rapports
---------------------------------------------------------------------------
def assemble_text_output(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:

    blocks: List[str] = []

    for page in pages:

        part = page_text_path(paths, page.page_no)

        if part.exists():

            text = part.read_text(encoding="utf-8", errors="replace").rstrip()

        else:

            text = f"[MISSING PAGE {page.page_no}]"

        blocks.append(f"{SEPARATOR}\nPAGE {page.page_no}\n{SEPARATOR}\n{text}")

    atomic_write_text(paths["txt_out"], "\n\n".join(blocks) + "\n")

def append_image_as_pdf_page(doc, image_path: str) -> None:

    import fitz

    rect = fitz.Rect(0, 0, 595, 842)

    try:

        pix = fitz.Pixmap(image_path)

        rect = fitz.Rect(0, 0, pix.width, pix.height)

    except Exception:

        pass

    page = doc.new_page(width=rect.width, height=rect.height)

    page.insert_image(rect, filename=image_path)

def assemble_searchable_pdf(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:

    import fitz

    out_doc = fitz.open()

    source_pdf_doc = None

    try:

        if pages and pages[0].kind == "pdf":

            source_pdf_doc = fitz.open(pages[0].source_path)

        for page in pages:

            meta_file = page_meta_path(paths, page.page_no)

            meta = {}

            if meta_file.exists():

                try:

                    meta = json.loads(meta_file.read_text(encoding="utf-8"))

                except Exception:

                    meta = {}

            method = meta.get("method")

            page_pdf = page_pdf_path(paths, page.page_no)

            if method == "native-text" and source_pdf_doc is not None and page.pdf_index is not None:

                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)

                continue

            if page_pdf.exists():

                try:

                    with fitz.open(str(page_pdf)) as tmp_doc:

                        out_doc.insert_pdf(tmp_doc)

                except Exception:

                    append_image_as_pdf_page(out_doc, str(page_pdf))

                continue

            if page.kind == "pdf" and source_pdf_doc is not None and page.pdf_index is not None:

                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)

            elif page.kind == "image":

                append_image_as_pdf_page(out_doc, page.source_path)

        out_doc.save(str(paths["searchable_pdf"]), garbage=3, deflate=True)

    finally:

        if source_pdf_doc is not None:

            source_pdf_doc.close()

        out_doc.close()

def generate_reports(

    paths: Dict[str, Path], pages: Sequence[PageRef], state: Dict, args: argparse.Namespace

) -> None:

    rows: List[Dict[str, object]] = []

    method_counts: Dict[str, int] = {}

    total_chars = 0

    error_count = 0

    total_duration_ms = 0.0

    confidence_values: List[float] = []

    for page in pages:

        meta_file = page_meta_path(paths, page.page_no)

        if not meta_file.exists():

            rows.append(

                {

                    "page_no": page.page_no,

                    "status": "missing",

                    "method": None,

                    "chars": 0,

                    "non_ws_chars": 0,

                    "duration_ms": 0,

                    "confidence_mean": None,

                    "confidence_words": 0,

                    "attempt_count": 0,

                    "error": "missing meta",

                    "searchable_pdf_page": None,

                    "source_kind": page.kind,

                    "source_path": page.source_path,

                    "pdf_index": page.pdf_index,

                    "is_empty_page": False,

                }

            )

            error_count += 1

            continue

        try:

            meta = json.loads(meta_file.read_text(encoding="utf-8"))

        except Exception:

            rows.append(

                {

                    "page_no": page.page_no,

                    "status": "missing",

                    "method": None,

                    "chars": 0,

                    "non_ws_chars": 0,

                    "duration_ms": 0,

                    "confidence_mean": None,

                    "confidence_words": 0,

                    "attempt_count": 0,

                    "error": "corrupted meta",

                    "searchable_pdf_page": None,

                    "source_kind": page.kind,

                    "source_path": page.source_path,

                    "pdf_index": page.pdf_index,

                    "is_empty_page": False,

                }

            )

            error_count += 1

            continue

        rows.append(

            {

                "page_no": meta.get("page_no"),

                "status": meta.get("status"),

                "method": meta.get("method"),

                "chars": meta.get("chars"),

                "non_ws_chars": meta.get("non_ws_chars"),

                "duration_ms": meta.get("duration_ms"),

                "confidence_mean": meta.get("confidence_mean"),

                "confidence_words": meta.get("confidence_words"),

                "attempt_count": meta.get("attempt_count"),

                "error": meta.get("error"),

                "searchable_pdf_page": meta.get("searchable_pdf_page"),

                "source_kind": meta.get("source_kind"),

                "source_path": meta.get("source_path"),

                "pdf_index": meta.get("pdf_index"),

                "is_empty_page": meta.get("is_empty_page", False),

            }

        )

        method = str(meta.get("method"))

        method_counts[method] = method_counts.get(method, 0) + 1

        total_chars += int(meta.get("chars") or 0)

        total_duration_ms += float(meta.get("duration_ms") or 0.0)

        if meta.get("error"):

            error_count += 1

        if meta.get("confidence_mean") is not None:

            try:

                confidence_values.append(float(meta["confidence_mean"]))

            except Exception:

                pass

    # CSV

    fieldnames = [

        "page_no",

        "status",

        "method",

        "chars",

        "non_ws_chars",

        "duration_ms",

        "confidence_mean",

        "confidence_words",

        "attempt_count",

        "error",

        "searchable_pdf_page",

        "source_kind",

        "source_path",

        "pdf_index",

        "is_empty_page",

    ]

    paths["pages_csv"].parent.mkdir(parents=True, exist_ok=True)

    with paths["pages_csv"].open("w", encoding="utf-8", newline="") as f:

        writer = csv.DictWriter(f, fieldnames=fieldnames)

        writer.writeheader()

        writer.writerows(rows)

    # Summary JSON

    avg_confidence = None

    if confidence_values:

        avg_confidence = round(sum(confidence_values) / len(confidence_values), 2)

    # Alerte qualité

    quality_warning = None

    low_conf_pages = sum(1 for v in confidence_values if v < CONFIDENCE_THRESHOLD)

    if confidence_values and low_conf_pages / len(confidence_values) > 0.3:

        quality_warning = (

            f"ATTENTION: {low_conf_pages}/{len(confidence_values)} pages "

            f"ont une confiance moyenne < {CONFIDENCE_THRESHOLD}"

        )

    summary = {

        "generated_at": utc_now_iso(),

        "source": str(Path(args.source).resolve()),

        "text_output": str(paths["txt_out"].resolve()),

        "searchable_pdf_output": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,

        "page_count": len(pages),

        "method_counts": method_counts,

        "error_pages": error_count,

        "total_chars": total_chars,

        "avg_confidence_mean": avg_confidence,

        "total_duration_ms": round(total_duration_ms, 2),

        "quality_warning": quality_warning,

        "bundle": str(paths["bundle"].resolve()),

        "csv_report": str(paths["pages_csv"].resolve()),

        "state_file": str(paths["state"].resolve()),

        "config": {

            "dpi": args.dpi,

            "lang": args.lang,

            "psm": args.psm,

            "oem": args.oem,

            "workers": args.workers,

            "timeout": args.timeout,

            "pdf_mode": args.pdf_mode,

            "text_threshold": args.text_threshold,

            "no_preprocess": args.no_preprocess,

            "min_chars": args.min_chars,

            "retries": args.retries,

            "searchable_pdf": args.searchable_pdf,

            "tsv_metrics": not args.no_tsv_metrics,

            "max_source_mb": args.max_source_mb,

            "dedup": args.dedup,

        },

        "state_snapshot": state,

    }

    atomic_write_json(paths["summary"], summary)

---------------------------------------------------------------------------
Pipeline principal
---------------------------------------------------------------------------
def ocr_pipeline(args: argparse.Namespace, paths: Dict[str, Path]) -> str:

    source = Path(args.source)

    pages = list_pages(source, args.start, args.end, dedup=args.dedup)

    valid_page_nos = {p.page_no for p in pages}

    # Charger ou initialiser l'état

    state = load_state(paths["state"]) if args.resume else {}

    if state:

        ensure_state_compatible(state, args, paths)

    else:

        state = init_state(args, paths, pages)

        save_state(paths["state"], state)

    # Identifier les pages déjà traitées

    done_pages = completed_pages_from_state(state, paths, valid_page_nos)

    pages_to_do = [p for p in pages if p.page_no not in done_pages]

    if done_pages:

        LOG.info("Mode reprise — %d page(s) déjà finalisées", len(done_pages))

    if not pages_to_do:

        LOG.info("Toutes les pages demandées sont déjà traitées.")

        assemble_text_output(paths, pages)

        if args.searchable_pdf:

            assemble_searchable_pdf(paths, pages)

        generate_reports(paths, pages, state, args)

        return str(paths["txt_out"])

    worker_count = min(args.workers, len(pages_to_do))

    LOG.info(

        "%d page(s) à traiter (workers=%d, dpi=%d, psm=%d, oem=%d, pdf_mode=%s, "

        "preprocess=%s, retries=%d, dedup=%s)",

        len(pages_to_do),

        worker_count,

        args.dpi,

        args.psm,

        args.oem,

        args.pdf_mode,

        not args.no_preprocess,

        args.retries,

        args.dedup,

    )

    # Construire les tâches

    base_task = {

        "lang": args.lang,

        "psm": args.psm,

        "oem": args.oem,

        "dpi": args.dpi,

        "timeout": args.timeout,

        "pdf_mode": args.pdf_mode,

        "no_preprocess": args.no_preprocess,

        "text_threshold": args.text_threshold,

        "min_chars": args.min_chars,

        "retries": args.retries,

        "collect_conf": not args.no_tsv_metrics,

        "searchable_pdf": args.searchable_pdf,

        "paths": {k: str(v) for k, v in paths.items()},

    }

    tasks: List[Dict[str, object]] = []

    for page in pages_to_do:

        task = dict(base_task)

        task["page"] = {

            "page_no": page.page_no,

            "kind": page.kind,

            "source_path": page.source_path,

            "pdf_index": page.pdf_index,

        }

        tasks.append(task)

    total = len(tasks)

    completed = 0

    with ProcessPoolExecutor(max_workers=worker_count) as executor:

        future_map = {executor.submit(process_page, task): task["page"]["page_no"] for task in tasks}

        for future in as_completed(future_map):

            page_no = future_map[future]

            try:

                meta = future.result()

            except Exception as exc:

                error = f"Crash worker: {exc}"

                tb = traceback.format_exc()

                LOG.exception("Exception non capturée page %d", page_no)

                fallback_text = f"[OCR ERROR page {page_no}: {error}]\n\n{tb}"

                atomic_write_text(page_text_path(paths, page_no), fallback_text + "\n")

                meta = {

                    "page_no": page_no,

                    "status": "error",

                    "source_kind": None,

                    "source_path": None,

                    "pdf_index": None,

                    "method": "failed",

                    "chars": len(fallback_text),

                    "non_ws_chars": compact_len(fallback_text),

                    "attempt_count": 0,

                    "attempts": [],

                    "selected_attempt": None,

                    "confidence_mean": None,

                    "confidence_words": 0,

                    "error": error,

                    "traceback": tb,

                    "pdf_page_error": None,

                    "searchable_pdf_page": None,

                    "duration_ms": 0.0,

                    "is_empty_page": False,

                    "processed_at": utc_now_iso(),

                }

                atomic_write_json(page_meta_path(paths, page_no), meta)

            state["pages"][str(page_no)] = {

                "status": meta.get("status"),

                "method": meta.get("method"),

                "chars": meta.get("chars"),

                "error": meta.get("error"),

                "duration_ms": meta.get("duration_ms"),

                "processed_at": meta.get("processed_at"),

            }

            save_state(paths["state"], state)

            completed += 1

            error_mark = " ⚠️ ERREUR" if meta.get("error") or meta.get("pdf_page_error") else ""

            LOG.info(

                "Page %d traitée [%d/%d] — %s — %d chars%s",

                page_no,

                completed,

                total,

                meta.get("method"),

                int(meta.get("chars") or 0),

                error_mark,

            )

    # Assemblage final

    assemble_text_output(paths, pages)

    if args.searchable_pdf:

        LOG.info("Assemblage du PDF searchable…")

        assemble_searchable_pdf(paths, pages)

    state["assembled_at"] = utc_now_iso()

    save_state(paths["state"], state)

    generate_reports(paths, pages, state, args)

    total_chars = sum(

        len(page_text_path(paths, p.page_no).read_text(encoding="utf-8", errors="replace"))

        for p in pages

        if page_text_path(paths, p.page_no).exists()

    )

    LOG.info("OCR terminé → %s (%d chars)", paths["txt_out"], total_chars)

    if args.searchable_pdf:

        LOG.info("PDF searchable → %s", paths["searchable_pdf"])

    LOG.info("Bundle de run → %s", paths["bundle"])

    return str(paths["txt_out"])

---------------------------------------------------------------------------
CLI
---------------------------------------------------------------------------
def build_arg_parser() -> argparse.ArgumentParser:

    ap = argparse.ArgumentParser(

        description="OCR orienté production — PDF, images, dossiers"

    )

    ap.add_argument("source", help="PDF, image ou dossier d'images")

    ap.add_argument("--dpi", type=int, default=300, help="Résolution de rendu PDF (défaut: 300)")

    ap.add_argument("--start", type=int, default=1, help="Première page PDF (défaut: 1)")

    ap.add_argument("--end", type=int, default=None, help="Dernière page PDF (défaut: fin document)")

    ap.add_argument("--timeout", type=int, default=120, help="Timeout Tesseract par page (s)")

    ap.add_argument("--psm", type=int, default=3, help="Page Segmentation Mode Tesseract")

    ap.add_argument("--oem", type=int, default=1, help="OCR Engine Mode Tesseract")

    ap.add_argument(

        "--workers",

        type=int,

        default=max(1, (os.cpu_count() or 2) // 2),

        help="Nombre de workers parallèles",

    )

    ap.add_argument("--resume", action="store_true", help="Reprendre une run interrompue")

    ap.add_argument("--out-dir", default=None, help="Dossier de sortie")

    ap.add_argument("--suffix", default=".ocr.txt", help="Suffixe de la sortie texte")

    ap.add_argument("--lang", default="ara+fra", help="Langues Tesseract (défaut: ara+fra)")

    ap.add_argument(

        "--pdf-mode",

        choices=["auto", "ocr", "text"],

        default="auto",

        help="PDF: auto=texte natif si exploitable sinon OCR ; ocr=OCR forcé ; text=texte natif seul",

    )

    ap.add_argument(

        "--text-threshold",

        type=int,

        default=25,

        help="Seuil minimal de caractères non blancs pour accepter le texte natif PDF",

    )

    ap.add_argument("--no-preprocess", action="store_true", help="Désactive le prétraitement image")

    ap.add_argument(

        "--min-chars",

        type=int,

        default=10,

        help="Seuil minimal de caractères non blancs pour accepter une tentative OCR",

    )

    ap.add_argument(

        "--retries",

        type=int,

        default=2,

        help="Nombre de tentatives supplémentaires avec variantes de prétraitement / PSM",

    )

    ap.add_argument(

        "--no-tsv-metrics",

        action="store_true",

        help="Désactive la collecte de métriques TSV (confiance moyenne, nombre de mots)",

    )

    ap.add_argument(

        "--searchable-pdf",

        action="store_true",

        help="Produit un PDF searchable final quand possible",

    )

    ap.add_argument(

        "--max-source-mb",

        type=int,

        default=MAX_SOURCE_MB,

        help=f"Taille max du fichier source en Mo (défaut: {MAX_SOURCE_MB})",

    )

    ap.add_argument(

        "--dedup",

        action="store_true",

        help="Active la dé-duplication des pages (hash perceptuel)",

    )

    ap.add_argument("--verbose", action="store_true", help="Logs verbeux")

    return ap

def main() -> None:

    parser = build_arg_parser()

    args = parser.parse_args()

    validate_args(args)

    paths = compute_paths(Path(args.source), args.out_dir, args.suffix)

    reset_outputs(paths, args.resume)

    configure_logging(paths["run_log"], verbose=args.verbose)

    # Check déjà fait dans validate_args, mais on re-vérifie pour les imports

    check_tesseract_languages(args.lang)

    check_pdf_support_if_needed(Path(args.source))

    try:

        ocr_pipeline(args, paths)

    except KeyboardInterrupt:

        die("Interruption utilisateur.", code=130)

    finally:

        closeworker_pdf_cache()

if name == "main":

    main()


Write
downloads/annales_svt/scripts/ocr_pipeline_production.py
1532 lines
open

Ran commands
2

Edit
downloads/annales_svt/scripts/ocr_pipeline_production.py
ocr_pipeline_production.py
PYTHON
#!/usr/bin/env python3
"""
Pipeline OCR orienté production — arabe (principal) + français.

Traite :
- PDF (extraction native + OCR)
- Image unique (PNG, JPG, JPEG, TIFF, TIF, BMP, WEBP)
- Dossier d'images

Fonctionnalités :
- Reprise robuste par page via état JSON + artefacts intermédiaires
- PDF ouvert une seule fois par worker (cache par processus)
- Parallélisation par page sans sérialiser les images entre processus
- Extraction native du texte PDF avant OCR si demandé
- Détection de pages vides et de doublons perceptuels (images; PDF sans dédup rendu par défaut)
- Métriques d'exécution par page et globales (confidence, qualité)
- Sortie texte finale réassemblée dans l'ordre
- CSV et JSON de reporting
- PDF searchable optionnel
- Journal d'exécution sur disque
- Limitation mémoire configurable

Version nettoyée/corrigée depuis le script fourni dans le chat : suppression des
artefacts Markdown, correction des imports, écriture effective des métadonnées
par page, compatibilité avec l'environnement Agent Mode.
"""

from __future__ import annotations

import argparse
import csv
import json
import logging
import os
import re
import shutil
import subprocess
import sys
import tempfile
import time
import traceback
from concurrent.futures import ProcessPoolExecutor, as_completed
from dataclasses import dataclass
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Dict, List, Optional, Sequence, Set, Tuple

import cv2
import numpy as np

# ---------------------------------------------------------------------------
# Constants
# ---------------------------------------------------------------------------

IMAGE_EXTENSIONS: Set[str] = {".png", ".jpg", ".jpeg", ".tiff", ".tif", ".bmp", ".webp"}
PDF_EXTENSIONS: Set[str] = {".pdf"}
SEPARATOR: str = "=" * 70
STATE_VERSION: int = 3
MAX_SOURCE_MB: int = 2000
CONFIDENCE_THRESHOLD: float = 45.0
EMPTY_PAGE_THRESHOLD: float = 0.98
DEDUP_HASH_SIZE: Tuple[int, int] = (16, 16)

LOG = logging.getLogger("ocr_pipeline_production")

# ---------------------------------------------------------------------------
# Dataclasses
# ---------------------------------------------------------------------------


@dataclass(frozen=True)
class PageRef:
    page_no: int
    kind: str  # 'pdf' | 'image'
    source_path: str
    pdf_index: Optional[int] = None
    perceptual_hash: Optional[str] = None


@dataclass(frozen=True)
class AttemptSpec:
    variant: str
    psm: int


# ---------------------------------------------------------------------------
# Worker-level PDF cache (un cache par processus worker)
# ---------------------------------------------------------------------------

_worker_pdf_cache: Dict[str, Any] = {}


def _get_cached_pdf_doc(pdf_path: str) -> Any:
    """Retourne un document PyMuPDF ouvert, avec cache par processus worker."""
    if pdf_path not in _worker_pdf_cache:
        import fitz
        _worker_pdf_cache[pdf_path] = fitz.open(pdf_path)
    return _worker_pdf_cache[pdf_path]


def _close_worker_pdf_cache() -> None:
    """Ferme tous les documents PDF ouverts dans le worker courant."""
    for doc in _worker_pdf_cache.values():
        try:
            doc.close()
        except Exception:
            pass
    _worker_pdf_cache.clear()


# ---------------------------------------------------------------------------
# Logging / utilitaires de base
# ---------------------------------------------------------------------------


def configure_logging(log_file: Optional[Path] = None, verbose: bool = False) -> None:
    LOG.setLevel(logging.DEBUG if verbose else logging.INFO)
    LOG.handlers.clear()
    LOG.propagate = False

    formatter = logging.Formatter(
        fmt="%(asctime)s [%(levelname)s] %(message)s",
        datefmt="%H:%M:%S",
    )

    console = logging.StreamHandler(sys.stdout)
    console.setLevel(logging.DEBUG if verbose else logging.INFO)
    console.setFormatter(formatter)
    LOG.addHandler(console)

    if log_file is not None:
        log_file.parent.mkdir(parents=True, exist_ok=True)
        file_handler = logging.FileHandler(log_file, encoding="utf-8")
        file_handler.setLevel(logging.DEBUG)
        file_handler.setFormatter(formatter)
        LOG.addHandler(file_handler)


def die(message: str, code: int = 1) -> None:
    LOG.error(message)
    raise SystemExit(code)


def utc_now_iso() -> str:
    """Retourne l'heure UTC courante en ISO 8601."""
    return datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z")


def natural_key(value: str) -> List[Any]:
    """Tri naturel pour les noms de fichiers (page_2 avant page_10)."""
    return [int(part) if part.isdigit() else part.lower() for part in re.split(r"(\d+)", value)]


def atomic_write_text(path: Path, content: str, encoding: str = "utf-8") -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with tempfile.NamedTemporaryFile("w", delete=False, dir=str(path.parent), encoding=encoding) as tmp:
        tmp.write(content)
        tmp_path = Path(tmp.name)
    os.replace(tmp_path, path)


def _json_sanitize(obj: Any) -> Any:
    """Convertit les scalaires NumPy et objets Path en types JSON natifs."""
    if isinstance(obj, dict):
        return {str(k): _json_sanitize(v) for k, v in obj.items()}
    if isinstance(obj, list):
        return [_json_sanitize(v) for v in obj]
    if isinstance(obj, tuple):
        return [_json_sanitize(v) for v in obj]
    if isinstance(obj, Path):
        return str(obj)
    if isinstance(obj, np.generic):
        return obj.item()
    return obj


def atomic_write_json(path: Path, data: Dict) -> None:
    atomic_write_text(path, json.dumps(_json_sanitize(data), ensure_ascii=False, indent=2) + "\n")


def atomic_write_bytes(path: Path, payload: bytes) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with tempfile.NamedTemporaryFile("wb", delete=False, dir=str(path.parent)) as tmp:
        tmp.write(payload)
        tmp_path = Path(tmp.name)
    os.replace(tmp_path, path)


def compact_len(text: str) -> int:
    return len(re.sub(r"\s+", "", text or ""))


def safe_error_text(text: str, limit: int = 500) -> str:
    return (text or "").strip().replace("\n", " ")[:limit] or "Erreur sans détail"


# ---------------------------------------------------------------------------
# Chemins d'artefacts
# ---------------------------------------------------------------------------


def compute_paths(source: Path, out_dir: Optional[str], suffix: str) -> Dict[str, Path]:
    if out_dir:
        out_root = Path(out_dir)
        out_root.mkdir(parents=True, exist_ok=True)
        txt_out = out_root / f"{source.stem}{suffix}"
        searchable_pdf = out_root / f"{source.stem}.searchable.pdf"
    elif source.is_dir():
        txt_out = source.parent / f"{source.name}{suffix}"
        searchable_pdf = source.parent / f"{source.name}.searchable.pdf"
    else:
        txt_out = source.with_suffix(suffix)
        searchable_pdf = txt_out.parent / f"{source.stem}.searchable.pdf"

    bundle = txt_out.with_name(txt_out.name + ".bundle")
    return {
        "txt_out": txt_out,
        "searchable_pdf": searchable_pdf,
        "bundle": bundle,
        "pages_txt": bundle / "pages_txt",
        "pages_meta": bundle / "pages_meta",
        "pages_pdf": bundle / "pages_pdf",
        "state": bundle / "run.state.json",
        "summary": bundle / "summary.json",
        "pages_csv": bundle / "pages.csv",
        "run_log": bundle / "run.log",
    }


def page_text_path(paths: Dict[str, Path], page_no: int) -> Path:
    return paths["pages_txt"] / f"page_{page_no:06d}.txt"


def page_meta_path(paths: Dict[str, Path], page_no: int) -> Path:
    return paths["pages_meta"] / f"page_{page_no:06d}.json"


def page_pdf_path(paths: Dict[str, Path], page_no: int) -> Path:
    return paths["pages_pdf"] / f"page_{page_no:06d}.pdf"


def reset_outputs(paths: Dict[str, Path], resume: bool) -> None:
    if resume:
        for key in ("bundle", "pages_txt", "pages_meta", "pages_pdf"):
            paths[key].mkdir(parents=True, exist_ok=True)
        return

    if paths["bundle"].exists():
        shutil.rmtree(paths["bundle"])
    if paths["txt_out"].exists():
        paths["txt_out"].unlink()
    if paths["searchable_pdf"].exists():
        paths["searchable_pdf"].unlink()

    for key in ("bundle", "pages_txt", "pages_meta", "pages_pdf"):
        paths[key].mkdir(parents=True, exist_ok=True)


# ---------------------------------------------------------------------------
# Validation / prérequis
# ---------------------------------------------------------------------------


def parse_langs(lang_spec: str) -> List[str]:
    langs = [chunk.strip() for chunk in lang_spec.split("+") if chunk.strip()]
    if not langs:
        raise ValueError("Aucune langue Tesseract valide dans --lang")
    return langs


def check_tesseract_languages(lang_spec: str) -> None:
    if not shutil.which("tesseract"):
        die("Tesseract introuvable dans le PATH.")

    result = subprocess.run(
        ["tesseract", "--list-langs"],
        capture_output=True,
        text=True,
        check=False,
    )
    if result.returncode != 0:
        die(f"Échec de tesseract --list-langs : {safe_error_text(result.stderr)}")

    installed: Set[str] = set()
    for line in (result.stdout + "\n" + result.stderr).splitlines():
        line = line.strip()
        if not line:
            continue
        if "language" in line.lower() and "list" in line.lower():
            continue
        parts = re.split(r"[\s,\n]+", line)
        for part in parts:
            part = part.strip()
            if part and re.match(r"^[a-z]{2,4}(_[A-Z]{2,4})?$", part):
                installed.add(part)

    missing = [lang for lang in parse_langs(lang_spec) if lang not in installed]
    if missing:
        die(
            "Langues Tesseract manquantes : "
            f"{', '.join(missing)}\n"
            f"Ubuntu/Debian : apt install {' '.join(f'tesseract-ocr-{x}' for x in missing)}"
        )


def check_pdf_support_if_needed(source: Path) -> None:
    need_pdf = source.is_file() and source.suffix.lower() in PDF_EXTENSIONS
    if not need_pdf:
        return
    try:
        import fitz  # noqa: F401
    except Exception as exc:
        die(f"Support PDF indisponible. Installez PyMuPDF (pymupdf). Détail : {exc}")


def check_source_file(source: Path, max_source_mb: int) -> None:
    """Validation approfondie du fichier source."""
    if not source.exists():
        die(f"Source introuvable : {source}")

    if not os.access(source, os.R_OK):
        die(f"Source non lisible (permissions) : {source}")

    if source.is_file():
        size_mb = source.stat().st_size / (1024 * 1024)
        if size_mb > max_source_mb:
            die(
                f"Source trop volumineuse : {size_mb:.1f} Mo "
                f"(limite : {max_source_mb} Mo). Ajustez avec --max-source-mb."
            )

    if source.is_file() and source.suffix.lower() in PDF_EXTENSIONS:
        try:
            import fitz
            with fitz.open(str(source)) as doc:
                if doc.page_count == 0:
                    die(f"Le PDF ne contient aucune page : {source}")
        except Exception as exc:
            die(f"Le PDF semble corrompu ou invalide : {source} — {exc}")


def validate_args(args: argparse.Namespace) -> None:
    source = Path(args.source)

    if args.dpi <= 0:
        die("--dpi doit être > 0")
    if args.workers <= 0:
        die("--workers doit être >= 1")
    if args.timeout <= 0:
        die("--timeout doit être > 0")
    if args.start <= 0:
        die("--start doit être >= 1")
    if args.end is not None and args.end < args.start:
        die("--end doit être >= --start")
    if args.psm < 0 or args.psm > 13:
        die("--psm doit être entre 0 et 13")
    if args.oem not in (0, 1, 2, 3):
        die("--oem doit être dans {0,1,2,3}")
    if args.pdf_mode not in {"auto", "ocr", "text"}:
        die("--pdf-mode doit être dans {auto, ocr, text}")
    if args.text_threshold < 0:
        die("--text-threshold doit être >= 0")
    if args.min_chars < 0:
        die("--min-chars doit être >= 0")
    if args.retries < 0:
        die("--retries doit être >= 0")
    if args.max_source_mb <= 0:
        die("--max-source-mb doit être > 0")

    if source.is_file() and source.suffix.lower() not in (IMAGE_EXTENSIONS | PDF_EXTENSIONS):
        die(f"Extension non supportée : {source.suffix}")

    if source.is_dir():
        files = [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS]
        if not files:
            die(f"Aucune image supportée trouvée dans : {source}")

    check_source_file(source, args.max_source_mb)
    check_tesseract_languages(args.lang)
    check_pdf_support_if_needed(source)


# ---------------------------------------------------------------------------
# État de reprise
# ---------------------------------------------------------------------------


def runtime_params() -> Set[str]:
    """Paramètres qui n'affectent PAS la compatibilité de l'état."""
    return {"workers", "timeout", "verbose", "resume", "out_dir", "suffix", "max_source_mb", "dedup"}


def resume_identity(args: argparse.Namespace) -> Dict[str, object]:
    return {
        "source": str(Path(args.source).resolve()),
        "dpi": args.dpi,
        "lang": args.lang,
        "psm": args.psm,
        "oem": args.oem,
        "pdf_mode": args.pdf_mode,
        "text_threshold": args.text_threshold,
        "no_preprocess": args.no_preprocess,
        "min_chars": args.min_chars,
        "retries": args.retries,
        "tsv_metrics": not args.no_tsv_metrics,
        "searchable_pdf": args.searchable_pdf,
        "start": args.start,
        "end": args.end,
    }


def load_state(path: Path) -> Dict:
    if not path.exists():
        return {}
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except json.JSONDecodeError as exc:
        LOG.warning("Fichier d'état corrompu, reprise ignorée : %s", exc)
        return {}
    except Exception as exc:
        die(f"Fichier d'état illisible : {path} ({exc})")


def save_state(path: Path, state: Dict) -> None:
    state["updated_at"] = utc_now_iso()
    atomic_write_json(path, state)


def ensure_state_compatible(state: Dict, args: argparse.Namespace, paths: Dict[str, Path]) -> None:
    if not state:
        return
    if state.get("version") != STATE_VERSION:
        die(
            f"Version d'état incompatible (attendu v{STATE_VERSION}, obtenu v{state.get('version')}). "
            "Relancez sans --resume ou supprimez le bundle."
        )
    current_identity = resume_identity(args)
    saved_identity = state.get("identity", {})
    functional_keys = set(current_identity.keys()) - runtime_params()

    for key in functional_keys:
        if current_identity.get(key) != saved_identity.get(key):
            die(
                f"Le bundle existant ne correspond pas à la même configuration fonctionnelle. "
                f"Différence sur : {key} (bundle={saved_identity.get(key)!r}, "
                f"actuel={current_identity.get(key)!r}). Relancez sans --resume."
            )

    if state.get("output_txt") != str(paths["txt_out"].resolve()):
        die("Le bundle existant ne correspond pas à cette destination texte.")


def init_state(args: argparse.Namespace, paths: Dict[str, Path], pages: Sequence[PageRef]) -> Dict:
    return {
        "version": STATE_VERSION,
        "created_at": utc_now_iso(),
        "updated_at": utc_now_iso(),
        "identity": resume_identity(args),
        "output_txt": str(paths["txt_out"].resolve()),
        "output_searchable_pdf": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,
        "page_count": len(pages),
        "pages": {},
        "run_config": {
            "workers": args.workers,
            "timeout": args.timeout,
            "verbose": args.verbose,
        },
    }


def completed_pages_from_state(state: Dict, paths: Dict[str, Path], valid_page_nos: Set[int]) -> Set[int]:
    done: Set[int] = set()
    for key, meta in state.get("pages", {}).items():
        try:
            pno = int(key)
        except (ValueError, TypeError):
            continue
        if pno not in valid_page_nos:
            continue
        try:
            if (
                meta.get("status") == "done"
                and page_text_path(paths, pno).exists()
                and page_meta_path(paths, pno).exists()
            ):
                done.add(pno)
        except Exception:
            continue
    return done


# ---------------------------------------------------------------------------
# Pages source
# ---------------------------------------------------------------------------


def to_gray(image: np.ndarray) -> np.ndarray:
    if image.ndim == 2:
        return image.copy()
    return cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)


def compute_perceptual_hash(image: np.ndarray) -> str:
    """Hash perceptuel simple (dHash) pour la dé-duplication."""
    resized = cv2.resize(image, (DEDUP_HASH_SIZE[0] + 1, DEDUP_HASH_SIZE[1]), interpolation=cv2.INTER_AREA)
    gray = to_gray(resized)
    diff = gray[:, 1:] > gray[:, :-1]
    return "".join("1" if val else "0" for row in diff for val in row)


def list_pages(source: Path, start: int, end: Optional[int], dedup: bool = False) -> List[PageRef]:
    if source.is_dir():
        files = sorted(
            [p for p in source.iterdir() if p.is_file() and p.suffix.lower() in IMAGE_EXTENSIONS],
            key=lambda p: natural_key(p.name),
        )
        first = max(1, start)
        last = len(files) if end is None else min(len(files), end)
        if first > last:
            die(f"Intervalle vide : start={first}, end={last}, total={len(files)}")
        selected_files = files[first - 1:last]
        pages: List[PageRef] = []
        seen_hashes: Set[str] = set()
        for i, path in enumerate(selected_files, start=first):
            phash = None
            if dedup:
                img = read_image(str(path))
                phash = compute_perceptual_hash(img)
                if phash in seen_hashes:
                    LOG.debug("Page doublon détectée (hash=%s), ignorée : %s", phash[:16], path.name)
                    continue
                seen_hashes.add(phash)
            pages.append(PageRef(page_no=i, kind="image", source_path=str(path.resolve()), perceptual_hash=phash))
        return pages

    if source.suffix.lower() in IMAGE_EXTENSIONS:
        phash = None
        if dedup:
            img = read_image(str(source))
            phash = compute_perceptual_hash(img)
        return [PageRef(page_no=1, kind="image", source_path=str(source.resolve()), perceptual_hash=phash)]

    if source.suffix.lower() in PDF_EXTENSIONS:
        import fitz
        with fitz.open(str(source)) as doc:
            total = doc.page_count

        first = max(1, start)
        last = total if end is None else min(total, end)
        if first > last:
            die(f"Intervalle vide : start={first}, end={last}, total={total}")

        pages = []
        for pno in range(first, last + 1):
            pages.append(
                PageRef(
                    page_no=pno,
                    kind="pdf",
                    source_path=str(source.resolve()),
                    pdf_index=pno - 1,
                    perceptual_hash=None,
                )
            )
        return pages

    die(f"Type de source non supporté : {source}")
    return []


# ---------------------------------------------------------------------------
# Lecture / rendu source
# ---------------------------------------------------------------------------


def read_image(path: str) -> np.ndarray:
    img = cv2.imread(path, cv2.IMREAD_COLOR)
    if img is None:
        raise RuntimeError(f"Impossible de lire l'image : {path}")
    return img


def render_pdf_page(pdf_path: str, pdf_index: int, dpi: int) -> np.ndarray:
    """Rend une page PDF en image, en utilisant le cache worker."""
    import fitz
    doc = _get_cached_pdf_doc(pdf_path)
    page = doc[pdf_index]
    zoom = dpi / 72.0
    matrix = fitz.Matrix(zoom, zoom)
    pix = page.get_pixmap(matrix=matrix, alpha=False)

    arr = np.frombuffer(pix.samples, dtype=np.uint8).reshape(pix.height, pix.width, pix.n).copy()
    if pix.n == 1:
        return cv2.cvtColor(arr, cv2.COLOR_GRAY2BGR)
    if pix.n == 4:
        return cv2.cvtColor(arr, cv2.COLOR_RGBA2BGR)
    return cv2.cvtColor(arr, cv2.COLOR_RGB2BGR)


def extract_pdf_native_text(pdf_path: str, pdf_index: int) -> str:
    """Extrait le texte natif d'une page PDF, en utilisant le cache worker."""
    doc = _get_cached_pdf_doc(pdf_path)
    return doc[pdf_index].get_text("text").strip()


def is_empty_page(image: np.ndarray, threshold: float = EMPTY_PAGE_THRESHOLD) -> bool:
    """Détecte si une page est essentiellement vide (blanc/noir uniforme)."""
    gray = to_gray(image)
    unique, counts = np.unique(gray, return_counts=True)
    max_count = counts.max()
    total_pixels = gray.size
    return (max_count / total_pixels) > threshold


def meaningful_text(text: str, threshold: int) -> bool:
    return compact_len(text) >= threshold


# ---------------------------------------------------------------------------
# Prétraitement image
# ---------------------------------------------------------------------------


def estimate_skew_angle(gray: np.ndarray) -> float:
    blurred = cv2.GaussianBlur(gray, (5, 5), 0)
    _ret, thresh = cv2.threshold(blurred, 0, 255, cv2.THRESH_BINARY_INV + cv2.THRESH_OTSU)
    coords = np.column_stack(np.where(thresh > 0))
    if coords.shape[0] < 200:
        return 0.0

    angle = cv2.minAreaRect(coords)[-1]
    if angle < -45:
        angle = -(90 + angle)
    else:
        angle = -angle

    angle = float(angle)
    if abs(angle) < 0.2 or abs(angle) > 15:
        return 0.0
    return angle


def rotate_image(image: np.ndarray, angle: float) -> np.ndarray:
    h, w = image.shape[:2]
    matrix = cv2.getRotationMatrix2D((w / 2.0, h / 2.0), angle, 1.0)
    border_value = 255 if image.ndim == 2 else (255, 255, 255)
    return cv2.warpAffine(
        image,
        matrix,
        (w, h),
        flags=cv2.INTER_CUBIC,
        borderMode=cv2.BORDER_CONSTANT,
        borderValue=border_value,
    )


def normalize_gray(gray: np.ndarray) -> np.ndarray:
    angle = estimate_skew_angle(gray)
    if angle:
        gray = rotate_image(gray, angle)
    clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8, 8))
    normalized = clahe.apply(gray)
    denoised = cv2.fastNlMeansDenoising(normalized, h=7, templateWindowSize=7, searchWindowSize=21)
    return denoised


def preprocess_variant(image: np.ndarray, variant: str) -> np.ndarray:
    gray = to_gray(image)

    if variant == "raw-gray":
        return gray

    normalized = normalize_gray(gray)

    if variant == "gray":
        return normalized

    if variant == "adaptive":
        return cv2.adaptiveThreshold(
            normalized,
            255,
            cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
            cv2.THRESH_BINARY,
            31,
            11,
        )

    if variant == "otsu":
        _ret, binary = cv2.threshold(normalized, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
        return binary

    raise ValueError(f"Variant de prétraitement inconnue : {variant}")


def build_attempt_plan(base_psm: int, no_preprocess: bool, retries: int) -> List[AttemptSpec]:
    candidates: List[AttemptSpec] = []

    if no_preprocess:
        raw = [AttemptSpec("raw-gray", base_psm)]
        if base_psm != 6:
            raw.append(AttemptSpec("raw-gray", 6))
        if base_psm != 11:
            raw.append(AttemptSpec("raw-gray", 11))
        candidates.extend(raw)
    else:
        candidates.extend([
            AttemptSpec("adaptive", base_psm),
            AttemptSpec("gray", base_psm),
            AttemptSpec("otsu", base_psm),
        ])
        if base_psm != 6:
            candidates.append(AttemptSpec("adaptive", 6))
        if base_psm != 11:
            candidates.append(AttemptSpec("gray", 11))

    deduped: List[AttemptSpec] = []
    seen = set()
    for item in candidates:
        key = (item.variant, item.psm)
        if key not in seen:
            seen.add(key)
            deduped.append(item)

    return deduped[: max(1, 1 + retries)]


# ---------------------------------------------------------------------------
# Tesseract
# ---------------------------------------------------------------------------


def tesseract_base_command(img_path: str, output_base: str, lang: str, psm: int, oem: int) -> List[str]:
    # NB: certains builds Tesseract ignorent tessedit-threads; pour compatibilité
    # maximale on limite plutôt via variables d'environnement dans run_command.
    return [
        "tesseract",
        img_path,
        output_base,
        "-l",
        lang,
        "--psm",
        str(psm),
        "--oem",
        str(oem),
        "-c",
        "preserve_interword_spaces=1",
    ]


def run_command(command: List[str], timeout: int) -> subprocess.CompletedProcess:
    env = os.environ.copy()
    env.setdefault("OMP_THREAD_LIMIT", "1")
    env.setdefault("MKL_NUM_THREADS", "1")
    env.setdefault("OPENBLAS_NUM_THREADS", "1")
    return subprocess.run(
        command,
        capture_output=True,
        text=True,
        encoding="utf-8",
        errors="replace",
        timeout=timeout,
        check=False,
        env=env,
    )


def run_tesseract_text(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int) -> Tuple[str, Optional[str]]:
    with tempfile.TemporaryDirectory() as tmp_dir:
        img_path = str(Path(tmp_dir) / "page.png")
        if not cv2.imwrite(img_path, image):
            raise RuntimeError("Échec d'écriture de l'image temporaire")

        try:
            result = run_command(tesseract_base_command(img_path, "stdout", lang, psm, oem), timeout)
        except subprocess.TimeoutExpired:
            return "[TIMEOUT ERROR]", f"Timeout Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:
            err = safe_error_text(result.stderr)
            return f"[TESSERACT ERROR: {err}]", err

        return result.stdout.strip(), None


def parse_tsv_metrics(tsv_text: str) -> Tuple[Optional[float], int]:
    lines = [line for line in tsv_text.splitlines() if line.strip()]
    if len(lines) <= 1:
        return None, 0

    scores: List[float] = []
    word_count = 0
    reader = csv.DictReader(lines, delimiter="\t")
    for row in reader:
        text = (row.get("text") or "").strip()
        conf_raw = (row.get("conf") or "").strip()
        if text:
            word_count += 1
        if conf_raw and conf_raw not in {"-1", "nan"}:
            try:
                scores.append(float(conf_raw))
            except ValueError:
                pass

    if not scores:
        return None, word_count
    return round(sum(scores) / len(scores), 2), word_count


def collect_tsv_metrics(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int) -> Tuple[Optional[float], int, Optional[str]]:
    with tempfile.TemporaryDirectory() as tmp_dir:
        img_path = str(Path(tmp_dir) / "page.png")
        if not cv2.imwrite(img_path, image):
            return None, 0, "Échec d'écriture de l'image temporaire pour TSV"

        command = tesseract_base_command(img_path, "stdout", lang, psm, oem) + ["tsv"]
        try:
            result = run_command(command, timeout)
        except subprocess.TimeoutExpired:
            return None, 0, f"Timeout TSV dépassé ({timeout}s)"

        if result.returncode != 0:
            return None, 0, safe_error_text(result.stderr)

        mean_conf, words = parse_tsv_metrics(result.stdout)
        return mean_conf, words, None


def generate_searchable_page_pdf(image: np.ndarray, lang: str, psm: int, oem: int, timeout: int, dest_path: Path) -> Optional[str]:
    with tempfile.TemporaryDirectory() as tmp_dir:
        img_path = str(Path(tmp_dir) / "page.png")
        out_base = str(Path(tmp_dir) / "page_ocr")

        if not cv2.imwrite(img_path, image):
            return "Échec d'écriture de l'image temporaire pour PDF"

        command = tesseract_base_command(img_path, out_base, lang, psm, oem) + ["pdf"]
        try:
            result = run_command(command, timeout)
        except subprocess.TimeoutExpired:
            return f"Timeout PDF Tesseract dépassé ({timeout}s)"

        if result.returncode != 0:
            return safe_error_text(result.stderr)

        produced = Path(out_base + ".pdf")
        if not produced.exists():
            return "Tesseract n'a pas produit le PDF attendu"

        atomic_write_bytes(dest_path, produced.read_bytes())
        return None


# ---------------------------------------------------------------------------
# Worker par page
# ---------------------------------------------------------------------------


def _build_meta(
    page: PageRef,
    method: str,
    text: str,
    error: Optional[str],
    confidence_mean: Optional[float],
    confidence_words: int,
    pdf_error: Optional[str],
    pdf_path: Optional[Path],
    attempts_meta: List[Dict[str, object]],
    selected_attempt: Optional[AttemptSpec],
    is_empty: bool,
    duration_ms: float,
) -> Dict[str, object]:
    return {
        "page_no": page.page_no,
        "status": "done",
        "source_kind": page.kind,
        "source_path": page.source_path,
        "pdf_index": page.pdf_index,
        "method": method,
        "chars": len(text),
        "non_ws_chars": compact_len(text),
        "attempt_count": len(attempts_meta),
        "attempts": attempts_meta,
        "selected_attempt": None if selected_attempt is None else {"variant": selected_attempt.variant, "psm": selected_attempt.psm},
        "confidence_mean": confidence_mean,
        "confidence_words": confidence_words,
        "error": error,
        "pdf_page_error": pdf_error,
        "searchable_pdf_page": str(pdf_path.resolve()) if pdf_path else None,
        "duration_ms": duration_ms,
        "is_empty_page": is_empty,
        "processed_at": utc_now_iso(),
    }


def process_page(task: Dict[str, object]) -> Dict[str, object]:
    """Traite une seule page. Appelé dans un worker ProcessPoolExecutor."""
    cv2.setNumThreads(1)

    page = PageRef(**task["page"])
    page_no = page.page_no
    started = time.perf_counter()

    lang = str(task["lang"])
    psm = int(task["psm"])
    oem = int(task["oem"])
    dpi = int(task["dpi"])
    timeout = int(task["timeout"])
    pdf_mode = str(task["pdf_mode"])
    no_preprocess = bool(task["no_preprocess"])
    text_threshold = int(task["text_threshold"])
    min_chars = int(task["min_chars"])
    retries = int(task["retries"])
    collect_conf = bool(task["collect_conf"])
    searchable_pdf = bool(task["searchable_pdf"])
    paths = {k: Path(v) for k, v in task["paths"].items()}

    text_path = page_text_path(paths, page_no)
    meta_path = page_meta_path(paths, page_no)
    pdf_path = page_pdf_path(paths, page_no)

    attempts_meta: List[Dict[str, object]] = []
    selected_attempt: Optional[AttemptSpec] = None
    selected_image: Optional[np.ndarray] = None
    selected_text = ""
    selected_error: Optional[str] = None
    method = "failed"
    confidence_mean: Optional[float] = None
    confidence_words = 0
    pdf_error: Optional[str] = None
    is_empty = False

    def finish(meta: Dict[str, object]) -> Dict[str, object]:
        atomic_write_json(meta_path, meta)
        return meta

    try:
        # === Étape 1 : Extraction texte natif PDF ===
        if page.kind == "pdf" and pdf_mode in {"auto", "text"}:
            try:
                native_text = extract_pdf_native_text(page.source_path, int(page.pdf_index))
            except Exception as exc:
                native_text = ""
                LOG.warning("Échec extraction texte natif page %d : %s", page_no, exc)

            if meaningful_text(native_text, text_threshold):
                method = "native-text"
                selected_text = native_text
                atomic_write_text(text_path, selected_text + "\n")
                duration_ms = round((time.perf_counter() - started) * 1000, 2)
                return finish(_build_meta(page, method, selected_text, None, None, 0, None, None,
                                          [], None, False, duration_ms))

            if pdf_mode == "text":
                selected_error = f"Aucun texte natif exploitable sur la page {page_no}"
                selected_text = f"[NO NATIVE TEXT page {page_no}]"
                method = "native-text"
                atomic_write_text(text_path, selected_text + "\n")
                duration_ms = round((time.perf_counter() - started) * 1000, 2)
                return finish(_build_meta(page, method, selected_text, selected_error, None, 0, None, None,
                                          [], None, False, duration_ms))

        # === Étape 2 : Rendu de l'image source ===
        if page.kind == "image":
            source_image = read_image(page.source_path)
        elif page.kind == "pdf":
            source_image = render_pdf_page(page.source_path, int(page.pdf_index), dpi)
        else:
            raise RuntimeError(f"Type de page inconnu : {page.kind}")

        # === Étape 3 : Détection de page vide ===
        is_empty = is_empty_page(source_image)
        if is_empty:
            selected_text = "[EMPTY PAGE]"
            method = "empty"
            atomic_write_text(text_path, selected_text + "\n")
            duration_ms = round((time.perf_counter() - started) * 1000, 2)
            return finish(_build_meta(page, method, selected_text, None, None, 0, None, None,
                                      [], None, True, duration_ms))

        # === Étape 4 : Tentatives OCR ===
        best_score = -1.0
        plan = build_attempt_plan(psm, no_preprocess, retries)

        for attempt in plan:
            try:
                processed = preprocess_variant(source_image, attempt.variant)
                text, err = run_tesseract_text(processed, lang, attempt.psm, oem, timeout)
            except Exception as exc:
                text = f"[OCR ERROR page {page_no}: {exc}]"
                err = str(exc)
                processed = None

            score = compact_len(text)

            attempt_conf: Optional[float] = None
            attempt_words: int = 0
            if collect_conf and processed is not None and err is None:
                attempt_conf, attempt_words, _conf_err = collect_tsv_metrics(processed, lang, attempt.psm, oem, timeout)

            attempts_meta.append({
                "variant": attempt.variant,
                "psm": attempt.psm,
                "chars": len(text),
                "non_ws_chars": score,
                "error": err,
                "confidence_mean": attempt_conf,
                "confidence_words": attempt_words,
            })

            combined_score = float(score)
            if attempt_conf is not None and attempt_conf > CONFIDENCE_THRESHOLD:
                combined_score = score + (attempt_conf * 10)

            if combined_score > best_score:
                best_score = combined_score
                selected_attempt = attempt
                selected_image = processed
                selected_text = text
                selected_error = err
                method = "ocr"
                confidence_mean = attempt_conf
                confidence_words = attempt_words

            if err is None and score >= min_chars and (attempt_conf is None or attempt_conf >= CONFIDENCE_THRESHOLD):
                selected_attempt = attempt
                selected_image = processed
                selected_text = text
                selected_error = None
                method = "ocr"
                confidence_mean = attempt_conf
                confidence_words = attempt_words
                break

        if selected_attempt is None or selected_image is None:
            raise RuntimeError("Aucune tentative OCR exploitable")

        # === Étape 5 : PDF searchable ===
        if searchable_pdf and method == "ocr":
            pdf_error = generate_searchable_page_pdf(selected_image, lang, selected_attempt.psm, oem, timeout, pdf_path)

        # === Étape 6 : Écriture des résultats ===
        atomic_write_text(text_path, selected_text + "\n")
        duration_ms = round((time.perf_counter() - started) * 1000, 2)
        return finish(_build_meta(
            page=page,
            method=method,
            text=selected_text,
            error=selected_error,
            confidence_mean=confidence_mean,
            confidence_words=confidence_words,
            pdf_error=pdf_error,
            pdf_path=pdf_path if pdf_path.exists() else None,
            attempts_meta=attempts_meta,
            selected_attempt=selected_attempt,
            is_empty=is_empty,
            duration_ms=duration_ms,
        ))

    except Exception as exc:
        duration_ms = round((time.perf_counter() - started) * 1000, 2)
        error = str(exc)
        tb = traceback.format_exc()
        fallback_text = f"[OCR ERROR page {page_no}: {error}]\n\n{tb}"
        atomic_write_text(text_path, fallback_text + "\n")
        meta = {
            "page_no": page_no,
            "status": "error",
            "source_kind": page.kind,
            "source_path": page.source_path,
            "pdf_index": page.pdf_index,
            "method": "failed",
            "chars": len(fallback_text),
            "non_ws_chars": compact_len(fallback_text),
            "attempt_count": len(attempts_meta),
            "attempts": attempts_meta,
            "selected_attempt": None,
            "confidence_mean": None,
            "confidence_words": 0,
            "error": error,
            "traceback": tb,
            "pdf_page_error": None,
            "searchable_pdf_page": None,
            "duration_ms": duration_ms,
            "is_empty_page": False,
            "processed_at": utc_now_iso(),
        }
        atomic_write_json(meta_path, meta)
        return meta


# ---------------------------------------------------------------------------
# Assemblage texte / PDF / rapports
# ---------------------------------------------------------------------------


def assemble_text_output(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:
    blocks: List[str] = []
    for page in pages:
        part = page_text_path(paths, page.page_no)
        if part.exists():
            text = part.read_text(encoding="utf-8", errors="replace").rstrip()
        else:
            text = f"[MISSING PAGE {page.page_no}]"
        blocks.append(f"{SEPARATOR}\nPAGE {page.page_no}\n{SEPARATOR}\n{text}")
    atomic_write_text(paths["txt_out"], "\n\n".join(blocks) + "\n")


def append_image_as_pdf_page(doc, image_path: str) -> None:
    import fitz
    rect = fitz.Rect(0, 0, 595, 842)
    try:
        pix = fitz.Pixmap(image_path)
        rect = fitz.Rect(0, 0, pix.width, pix.height)
    except Exception:
        pass

    page = doc.new_page(width=rect.width, height=rect.height)
    page.insert_image(rect, filename=image_path)


def assemble_searchable_pdf(paths: Dict[str, Path], pages: Sequence[PageRef]) -> None:
    import fitz
    out_doc = fitz.open()
    source_pdf_doc = None

    try:
        if pages and pages[0].kind == "pdf":
            source_pdf_doc = fitz.open(pages[0].source_path)

        for page in pages:
            meta_file = page_meta_path(paths, page.page_no)
            meta = {}
            if meta_file.exists():
                try:
                    meta = json.loads(meta_file.read_text(encoding="utf-8"))
                except Exception:
                    meta = {}

            method = meta.get("method")
            page_pdf = page_pdf_path(paths, page.page_no)

            if method == "native-text" and source_pdf_doc is not None and page.pdf_index is not None:
                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)
                continue

            if page_pdf.exists():
                try:
                    with fitz.open(str(page_pdf)) as tmp_doc:
                        out_doc.insert_pdf(tmp_doc)
                except Exception:
                    append_image_as_pdf_page(out_doc, str(page_pdf))
                continue

            if page.kind == "pdf" and source_pdf_doc is not None and page.pdf_index is not None:
                out_doc.insert_pdf(source_pdf_doc, from_page=page.pdf_index, to_page=page.pdf_index)
            elif page.kind == "image":
                append_image_as_pdf_page(out_doc, page.source_path)

        out_doc.save(str(paths["searchable_pdf"]), garbage=3, deflate=True)
    finally:
        if source_pdf_doc is not None:
            source_pdf_doc.close()
        out_doc.close()


def generate_reports(paths: Dict[str, Path], pages: Sequence[PageRef], state: Dict, args: argparse.Namespace) -> None:
    rows: List[Dict[str, object]] = []
    method_counts: Dict[str, int] = {}
    total_chars = 0
    error_count = 0
    total_duration_ms = 0.0
    confidence_values: List[float] = []

    for page in pages:
        meta_file = page_meta_path(paths, page.page_no)
        if not meta_file.exists():
            rows.append({
                "page_no": page.page_no,
                "status": "missing",
                "method": None,
                "chars": 0,
                "non_ws_chars": 0,
                "duration_ms": 0,
                "confidence_mean": None,
                "confidence_words": 0,
                "attempt_count": 0,
                "error": "missing meta",
                "searchable_pdf_page": None,
                "source_kind": page.kind,
                "source_path": page.source_path,
                "pdf_index": page.pdf_index,
                "is_empty_page": False,
            })
            error_count += 1
            continue

        try:
            meta = json.loads(meta_file.read_text(encoding="utf-8"))
        except Exception:
            rows.append({
                "page_no": page.page_no,
                "status": "missing",
                "method": None,
                "chars": 0,
                "non_ws_chars": 0,
                "duration_ms": 0,
                "confidence_mean": None,
                "confidence_words": 0,
                "attempt_count": 0,
                "error": "corrupted meta",
                "searchable_pdf_page": None,
                "source_kind": page.kind,
                "source_path": page.source_path,
                "pdf_index": page.pdf_index,
                "is_empty_page": False,
            })
            error_count += 1
            continue

        rows.append({
            "page_no": meta.get("page_no"),
            "status": meta.get("status"),
            "method": meta.get("method"),
            "chars": meta.get("chars"),
            "non_ws_chars": meta.get("non_ws_chars"),
            "duration_ms": meta.get("duration_ms"),
            "confidence_mean": meta.get("confidence_mean"),
            "confidence_words": meta.get("confidence_words"),
            "attempt_count": meta.get("attempt_count"),
            "error": meta.get("error"),
            "searchable_pdf_page": meta.get("searchable_pdf_page"),
            "source_kind": meta.get("source_kind"),
            "source_path": meta.get("source_path"),
            "pdf_index": meta.get("pdf_index"),
            "is_empty_page": meta.get("is_empty_page", False),
        })

        method = str(meta.get("method"))
        method_counts[method] = method_counts.get(method, 0) + 1
        total_chars += int(meta.get("chars") or 0)
        total_duration_ms += float(meta.get("duration_ms") or 0.0)
        if meta.get("error"):
            error_count += 1
        if meta.get("confidence_mean") is not None:
            try:
                confidence_values.append(float(meta["confidence_mean"]))
            except Exception:
                pass

    fieldnames = [
        "page_no", "status", "method", "chars", "non_ws_chars", "duration_ms",
        "confidence_mean", "confidence_words", "attempt_count", "error",
        "searchable_pdf_page", "source_kind", "source_path", "pdf_index", "is_empty_page",
    ]
    paths["pages_csv"].parent.mkdir(parents=True, exist_ok=True)
    with paths["pages_csv"].open("w", encoding="utf-8", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=fieldnames)
        writer.writeheader()
        writer.writerows(rows)

    avg_confidence = None
    if confidence_values:
        avg_confidence = round(sum(confidence_values) / len(confidence_values), 2)

    quality_warning = None
    low_conf_pages = sum(1 for v in confidence_values if v < CONFIDENCE_THRESHOLD)
    if confidence_values and low_conf_pages / len(confidence_values) > 0.3:
        quality_warning = (
            f"ATTENTION: {low_conf_pages}/{len(confidence_values)} pages "
            f"ont une confiance moyenne < {CONFIDENCE_THRESHOLD}"
        )

    summary = {
        "generated_at": utc_now_iso(),
        "source": str(Path(args.source).resolve()),
        "text_output": str(paths["txt_out"].resolve()),
        "searchable_pdf_output": str(paths["searchable_pdf"].resolve()) if args.searchable_pdf else None,
        "page_count": len(pages),
        "method_counts": method_counts,
        "error_pages": error_count,
        "total_chars": total_chars,
        "avg_confidence_mean": avg_confidence,
        "total_duration_ms": round(total_duration_ms, 2),
        "quality_warning": quality_warning,
        "bundle": str(paths["bundle"].resolve()),
        "csv_report": str(paths["pages_csv"].resolve()),
        "state_file": str(paths["state"].resolve()),
        "config": {
            "dpi": args.dpi,
            "lang": args.lang,
            "psm": args.psm,
            "oem": args.oem,
            "workers": args.workers,
            "timeout": args.timeout,
            "pdf_mode": args.pdf_mode,
            "text_threshold": args.text_threshold,
            "no_preprocess": args.no_preprocess,
            "min_chars": args.min_chars,
            "retries": args.retries,
            "searchable_pdf": args.searchable_pdf,
            "tsv_metrics": not args.no_tsv_metrics,
            "max_source_mb": args.max_source_mb,
            "dedup": args.dedup,
        },
        "state_snapshot": state,
    }
    atomic_write_json(paths["summary"], summary)


# ---------------------------------------------------------------------------
# Pipeline principal
# ---------------------------------------------------------------------------


def ocr_pipeline(args: argparse.Namespace, paths: Dict[str, Path]) -> str:
    source = Path(args.source)
    pages = list_pages(source, args.start, args.end, dedup=args.dedup)
    valid_page_nos = {p.page_no for p in pages}

    state = load_state(paths["state"]) if args.resume else {}
    if state:
        ensure_state_compatible(state, args, paths)
    else:
        state = init_state(args, paths, pages)
        save_state(paths["state"], state)

    done_pages = completed_pages_from_state(state, paths, valid_page_nos)
    pages_to_do = [p for p in pages if p.page_no not in done_pages]

    if done_pages:
        LOG.info("Mode reprise — %d page(s) déjà finalisées", len(done_pages))

    if not pages_to_do:
        LOG.info("Toutes les pages demandées sont déjà traitées.")
        assemble_text_output(paths, pages)
        if args.searchable_pdf:
            assemble_searchable_pdf(paths, pages)
        generate_reports(paths, pages, state, args)
        return str(paths["txt_out"])

    worker_count = min(args.workers, len(pages_to_do))
    LOG.info(
        "%d page(s) à traiter (workers=%d, dpi=%d, psm=%d, oem=%d, pdf_mode=%s, "
        "preprocess=%s, retries=%d, dedup=%s)",
        len(pages_to_do), worker_count, args.dpi, args.psm, args.oem, args.pdf_mode,
        not args.no_preprocess, args.retries, args.dedup,
    )

    base_task = {
        "lang": args.lang,
        "psm": args.psm,
        "oem": args.oem,
        "dpi": args.dpi,
        "timeout": args.timeout,
        "pdf_mode": args.pdf_mode,
        "no_preprocess": args.no_preprocess,
        "text_threshold": args.text_threshold,
        "min_chars": args.min_chars,
        "retries": args.retries,
        "collect_conf": not args.no_tsv_metrics,
        "searchable_pdf": args.searchable_pdf,
        "paths": {k: str(v) for k, v in paths.items()},
    }

    tasks: List[Dict[str, object]] = []
    for page in pages_to_do:
        task = dict(base_task)
        task["page"] = {
            "page_no": page.page_no,
            "kind": page.kind,
            "source_path": page.source_path,
            "pdf_index": page.pdf_index,
        }
        tasks.append(task)

    total = len(tasks)
    completed = 0

    with ProcessPoolExecutor(max_workers=worker_count) as executor:
        future_map = {executor.submit(process_page, task): task["page"]["page_no"] for task in tasks}

        for future in as_completed(future_map):
            page_no = future_map[future]
            try:
                meta = future.result()
            except Exception as exc:
                error = f"Crash worker: {exc}"
                tb = traceback.format_exc()
                fallback_text = f"[OCR ERROR page {page_no}: {error}]\n\n{tb}"
                atomic_write_text(page_text_path(paths, page_no), fallback_text + "\n")
                meta = {
                    "page_no": page_no,
                    "status": "error",
                    "source_kind": None,
                    "source_path": None,
                    "pdf_index": None,
                    "method": "failed",
                    "chars": len(fallback_text),
                    "non_ws_chars": compact_len(fallback_text),
                    "attempt_count": 0,
                    "attempts": [],
                    "selected_attempt": None,
                    "confidence_mean": None,
                    "confidence_words": 0,
                    "error": error,
                    "traceback": tb,
                    "pdf_page_error": None,
                    "searchable_pdf_page": None,
                    "duration_ms": 0.0,
                    "is_empty_page": False,
                    "processed_at": utc_now_iso(),
                }
                atomic_write_json(page_meta_path(paths, page_no), meta)

            state["pages"][str(page_no)] = {
                "status": meta.get("status"),
                "method": meta.get("method"),
                "chars": meta.get("chars"),
                "error": meta.get("error"),
                "duration_ms": meta.get("duration_ms"),
                "processed_at": meta.get("processed_at"),
            }
            save_state(paths["state"], state)

            completed += 1
            error_mark = " ⚠️ ERREUR" if meta.get("error") or meta.get("pdf_page_error") else ""
            LOG.info(
                "Page %d traitée [%d/%d] — %s — %d chars%s",
                page_no, completed, total, meta.get("method"), int(meta.get("chars") or 0), error_mark,
            )

    assemble_text_output(paths, pages)
    if args.searchable_pdf:
        LOG.info("Assemblage du PDF searchable…")
        assemble_searchable_pdf(paths, pages)

    state["assembled_at"] = utc_now_iso()
    save_state(paths["state"], state)
    generate_reports(paths, pages, state, args)

    total_chars = sum(
        len(page_text_path(paths, p.page_no).read_text(encoding="utf-8", errors="replace"))
        for p in pages
        if page_text_path(paths, p.page_no).exists()
    )
    LOG.info("OCR terminé → %s (%d chars)", paths["txt_out"], total_chars)
    if args.searchable_pdf:
        LOG.info("PDF searchable → %s", paths["searchable_pdf"])
    LOG.info("Bundle de run → %s", paths["bundle"])

    return str(paths["txt_out"])


# ---------------------------------------------------------------------------
# CLI
# ---------------------------------------------------------------------------


def build_arg_parser() -> argparse.ArgumentParser:
    ap = argparse.ArgumentParser(description="OCR orienté production — PDF, images, dossiers")
    ap.add_argument("source", help="PDF, image ou dossier d'images")
    ap.add_argument("--dpi", type=int, default=300, help="Résolution de rendu PDF (défaut: 300)")
    ap.add_argument("--start", type=int, default=1, help="Première page PDF (défaut: 1)")
    ap.add_argument("--end", type=int, default=None, help="Dernière page PDF (défaut: fin document)")
    ap.add_argument("--timeout", type=int, default=120, help="Timeout Tesseract par page (s)")
    ap.add_argument("--psm", type=int, default=3, help="Page Segmentation Mode Tesseract")
    ap.add_argument("--oem", type=int, default=1, help="OCR Engine Mode Tesseract")
    ap.add_argument(
        "--workers",
        type=int,
        default=max(1, (os.cpu_count() or 2) // 2),
        help="Nombre de workers parallèles",
    )
    ap.add_argument("--resume", action="store_true", help="Reprendre une run interrompue")
    ap.add_argument("--out-dir", default=None, help="Dossier de sortie")
    ap.add_argument("--suffix", default=".ocr.txt", help="Suffixe de la sortie texte")
    ap.add_argument("--lang", default="ara+fra", help="Langues Tesseract (défaut: ara+fra)")
    ap.add_argument(
        "--pdf-mode",
        choices=["auto", "ocr", "text"],
        default="auto",
        help="PDF: auto=texte natif si exploitable sinon OCR ; ocr=OCR forcé ; text=texte natif seul",
    )
    ap.add_argument(
        "--text-threshold",
        type=int,
        default=25,
        help="Seuil minimal de caractères non blancs pour accepter le texte natif PDF",
    )
    ap.add_argument("--no-preprocess", action="store_true", help="Désactive le prétraitement image")
    ap.add_argument(
        "--min-chars",
        type=int,
        default=10,
        help="Seuil minimal de caractères non blancs pour accepter une tentative OCR",
    )
    ap.add_argument(
        "--retries",
        type=int,
        default=2,
        help="Nombre de tentatives supplémentaires avec variantes de prétraitement / PSM",
    )
    ap.add_argument(
        "--no-tsv-metrics",
        action="store_true",
        help="Désactive la collecte de métriques TSV (confiance moyenne, nombre de mots)",
    )
    ap.add_argument(
        "--searchable-pdf",
        action="store_true",
        help="Produit un PDF searchable final quand possible",
    )
    ap.add_argument(
        "--max-source-mb",
        type=int,
        default=MAX_SOURCE_MB,
        help=f"Taille max du fichier source en Mo (défaut: {MAX_SOURCE_MB})",
    )
    ap.add_argument("--dedup", action="store_true", help="Active la dé-duplication des pages images")
    ap.add_argument("--verbose", action="store_true", help="Logs verbeux")
    return ap


def main() -> None:
    parser = build_arg_parser()
    args = parser.parse_args()

    configure_logging(None, verbose=args.verbose)
    validate_args(args)
    paths = compute_paths(Path(args.source), args.out_dir, args.suffix)
    reset_outputs(paths, args.resume)
    configure_logging(paths["run_log"], verbose=args.verbose)

    # Déjà vérifié dans validate_args; on re-vérifie après installation des handlers fichier.
    check_tesseract_languages(args.lang)
    check_pdf_support_if_needed(Path(args.source))

    try:
        ocr_pipeline(args, paths)
    except KeyboardInterrupt:
        die("Interruption utilisateur.", code=130)
    finally:
        _close_worker_pdf_cache()


if __name__ == "__main__":
    main()
Open

Ran commands
2

Edit
downloads/annales_svt/MANIFESTE.md
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02	28	Exercices	✅ OCR production complet + ✅ extraction/intégration (khelifa1_volume02_extraction_phase2.json)	29 questions
KHELIFA 1	Vol 03	28	Exercices	✅ OCR production v3 complet (KHELIFA1_VOLUME_03.ocr_prod_full.txt)	À extraire
KHELIFA 1	Vol 04-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 141 questions / 33 micro-concepts couverts sur 42
Derniers ajouts
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
KHELIFA 1 Vol 02 : 29 questions ajoutées (q_khelifa1_vol02_001 à q_khelifa1_vol02_029)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
Oui ✅ J’ai appliqué ton nouveau script OCR pipeline.

1. Script remplacé par la nouvelle version
J’ai sauvegardé la version corrigée ici :

text

downloads/annales_svt/scripts/ocr_pipeline_production.py
Comme avant, le script copié contenait des artefacts Markdown, par exemple :

Python

from **future** import annotations
*worker*pdf_cache
[getattr](http://...)
if **name** == "__main__":
Je les ai corrigés.

J’ai aussi corrigé quelques problèmes techniques dans la version fournie :

import fitz manquant dans render_pdf_page
écriture effective des fichiers pages_meta/page_xxxxxx.json
conversion des types NumPy (np.bool_, np.float64, etc.) pour éviter les erreurs JSON
logging initial avant validation
compatibilité avec le workspace actuel
maintien du cache PDF par worker
2. Test du script
Test effectué sur :

text

KHELIFA1_VOLUME_03.pdf
pages 1-2
Résultat :

text

2 pages traitées
method_counts: {'ocr': 2}
error_pages: 0
avg_confidence_mean: 60.65
✅ Le script fonctionne.

3. OCR complet appliqué à KHELIFA1_VOLUME_03
J’ai ensuite lancé le nouveau pipeline sur tout le volume :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_03.pdf
Commande utilisée :

Bash

python scripts/ocr_pipeline_production.py input/KHELIFA1_VOLUME_03.pdf \
  --start 1 --end 28 \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt
4. Résultat OCR Volume 03
Sortie texte :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_03.ocr_prod_full.txt
Bundle :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_03.ocr_prod_full.txt.bundle/
Rapport :

text

Pages traitées : 28 / 28
Méthode : OCR sur 28 pages
Erreurs : 0
Total caractères OCR : 58,629
Taille fichier final : 95,740 bytes
Confiance moyenne : 65.29
Quality warning : aucun
Durée : ~13 min 52 s
5. Amélioration constatée
Le nouveau script est meilleur que la version précédente sur le reporting :

STATE_VERSION = 3
détection pages vides
meilleur calcul de confiance
rapport qualité quality_warning
cache PDF par worker
métriques plus propres dans le bundle
6. Manifeste mis à jour
J’ai mis à jour :

text

downloads/annales_svt/MANIFESTE.md
État :

text

KHELIFA 1 Vol 03 — OCR production v3 complet — À extraire
Prochaine étape : extraction phase 2 de KHELIFA1_VOLUME_03.ocr_prod_full.txt, puis tagging/intégration JSON.


mettre le nouveau script ocr pipeline corrige sur workspace


C’est fait ✅

Le nouveau script OCR pipeline corrigé est maintenant disponible dans le workspace à deux endroits :

Version principale du projet
text

downloads/annales_svt/scripts/ocr_pipeline_production.py
Copie facile d’accès à la racine du workspace
text

ocr_pipeline_production.py
J’ai aussi rendu la copie exécutable :

Bash

chmod +x ocr_pipeline_production.py
Et j’ai vérifié qu’il compile correctement :

text

OK compiled
Tu peux donc l’utiliser directement comme ça :

Bash

python ocr_pipeline_production.py downloads/annales_svt/input/KHELIFA1_VOLUME_04.pdf \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt
Ou depuis le dossier projet :

Bash

cd downloads/annales_svt

python scripts/ocr_pipeline_production.py input/KHELIFA1_VOLUME_04.pdf \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt

extraction phase 2 de KHELIFA1_VOLUME_03.ocr_prod_full.txt



Ran commands
2

Edited files
3
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02	28	Exercices	✅ OCR production complet + ✅ extraction/intégration (khelifa1_volume02_extraction_phase2.json)	29 questions
KHELIFA 1	Vol 03	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction phase 2 (khelifa1_volume03_extraction_phase2.json)	28 blocs candidats
KHELIFA 1	Vol 04-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 141 questions / 33 micro-concepts couverts sur 42
Derniers ajouts
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
KHELIFA 1 Vol 02 : 29 questions ajoutées (q_khelifa1_vol02_001 à q_khelifa1_vol02_029)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
Phase 2 — Extraction des questions candidates
MD
Phase 2 — Extraction des questions candidates
Source : KHELIFA1_VOLUME_03.pdf
OCR de référence : input/KHELIFA1_VOLUME_03.ocr_prod_full.txt
Pipeline utilisé : scripts/ocr_pipeline_production.py v3
Fichier JSON détaillé : output/khelifa1_volume03_extraction_phase2.json

Résultat
28 blocs de questions/exercices candidats extraits.
Le volume 03 ressemble fortement à un volume de corrigés / reprises d’exercices autour de l’expression de l’information génétique.
Domaine dominant : synthèse des protéines, transcription, traduction, code génétique, mutations.
Qualité OCR
Rapport OCR production v3 :

text

Pages traitées : 28 / 28
Méthode : OCR sur 28 pages
Erreurs OCR : 0
Total caractères OCR : 58,629
Confiance moyenne : 65.29
Quality warning : aucun
Micro-concepts probables dominants
mc_prot_01 — Transcription de l'ADN
mc_prot_02 — Traduction de l'ARNm
mc_prot_03 — Code génétique
mc_prot_04 — ARNm
mc_prot_05 — ARNt
mc_prot_06 — Ribosome
mc_prot_07 — Initiation de la traduction
mc_prot_08 — Élongation et terminaison
mc_struc_01 — Structure primaire
mc_struc_05 — Relation structure-fonction
Liste courte des blocs extraits
ID	Pages	Thème	Qualité OCR
khelifa1_vol03_c001	1	Expression génétique : transcription/traduction	bonne
khelifa1_vol03_c002	2	α-amanitine et ARN polymérase	bonne
khelifa1_vol03_c003	2-3	Mucoviscidose / CFTR	bonne
khelifa1_vol03_c004	3-4	Organites, ARNm et synthèse protéique	moyenne
khelifa1_vol03_c005	4-5	Mécanismes de synthèse protéique chez eucaryotes	bonne
khelifa1_vol03_c006	5	Initiation, élongation, terminaison	bonne
khelifa1_vol03_c007	6	Albinisme, codon stop et mélanine	bonne
khelifa1_vol03_c008	7	HbA/HbS et drépanocytose	bonne
khelifa1_vol03_c009	8-9	RNase, puromycine, ARNt	moyenne
khelifa1_vol03_c010	10	Procaryotes vs eucaryotes	bonne
khelifa1_vol03_c011	11	ARNm, codons, polysomes	bonne
khelifa1_vol03_c012	11-12	Insuline humain/rat	bonne
khelifa1_vol03_c013	12	Mutation silencieuse / mutation changeante	bonne
khelifa1_vol03_c014	12	ARNm mélanine et synthèse in vitro	bonne
khelifa1_vol03_c015	13	CFTR : ARNm et protéine	moyenne
khelifa1_vol03_c016	14	Mutations Hb et relation gène-protéine	moyenne
khelifa1_vol03_c017	14-15	Cellule sécrétrice, noyau et cytoplasme	bonne
khelifa1_vol03_c018	15	Calcul longueur gène / codons	bonne
khelifa1_vol03_c019	16	Globules rouges sans noyau	moyenne
khelifa1_vol03_c020	17-18	ADN / ARN / protéine	bonne
khelifa1_vol03_c021	19	Voie sécrétoire : REG → Golgi → vésicules	bonne
khelifa1_vol03_c022	20	Amibe : ARN du noyau vers cytoplasme	bonne
khelifa1_vol03_c023	21	Nucléotide / nucléoside / ADN	moyenne
khelifa1_vol03_c024	22	Courbes de sécrétion protéique	bonne
khelifa1_vol03_c025	23-24	Noyau, ADN, ARN et synthèse protéique	bonne
khelifa1_vol03_c026	25	Caséine et glandes mammaires	moyenne
khelifa1_vol03_c027	26-27	Mutation Ser → Arg	moyenne
khelifa1_vol03_c028	28	Noyau, ARN préexistant et effet des rayons	moyenne
Remarques
Plusieurs passages sont des corrigés ; les questions ont donc été reconstruites à partir des réponses et des thèmes visibles.
Les séquences nucléotidiques exactes restent à vérifier visuellement avant une correction scientifique ultra-fidèle.
Cette phase ne modifie pas encore questions_taggees.json.
Prochaine phase
Phase 3 possible : transformer ces 28 blocs en entrées finales puis intégrer dans :

text

output/questions_taggees.json
Validation ensuite avec :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Open
khelifa1_volume03_extraction_phase2.json
JSON
[
  {
    "candidate_id": "khelifa1_vol03_c001",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [1],
    "theme": "مرحلتان من التعبير المورثي عند حقيقيات النواة",
    "texte_corrige": "تمثل الوثيقة صورتين بالمجهر الإلكتروني لمرحلتين من ظاهرة مهمة تحدث عند حقيقيات النواة. سم الظاهرة، وحدد المرحلة الخاصة بكل شكل، واكتب البيانات المرقمة. انطلاقا من جزء من إحدى سلسلتي ADN وجزء من ARNm المستنسخ، سم السلسلتين، أكمل السلسلة الناقصة، واستخرج السلسلة الببتيدية المركبة، ثم مثل برسم تخطيطي نهاية مرحلة الترجمة.",
    "sous_questions": ["تعرف النسخ والترجمة", "تسمية البيانات", "إكمال ARNm أو شريط ADN", "استخراج السلسلة الببتيدية", "رسم نهاية الترجمة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03"],
    "qualite_ocr": "bonne",
    "notes": "الصفحة تجمع سؤالا عن النسخ والترجمة وجدول الشفرة الوراثية."
  },
  {
    "candidate_id": "khelifa1_vol03_c002",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [2],
    "theme": "تأثير الأمانيتين على ARN بوليميراز ودور الإنزيم في النسخ",
    "texte_corrige": "لإظهار العناصر المتدخلة خلال مرحلتي الظاهرة المدروسة، نضع في أنبوب اختبار مستخلصا خلويا يحتوي على ADN ونيوكليوتيدات حرة وإنزيم ARN بوليميراز، ثم نقيس كمية ARNm المركبة في وجود تراكيز متزايدة من المركب α-amanitine المعروف بارتباطه بإنزيم ARN بوليميراز. حلل منحنى الوثيقة واستخرج دور إنزيم ARN بوليميراز في مرحلة النسخ.",
    "sous_questions": ["تحليل تأثير الأمانيتين", "استنتاج دور ARN بوليميراز", "ربط تركيب ARNm بمرحلة النسخ"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "سؤال واضح حول النسخ ومثبط ARN بوليميراز."
  },
  {
    "candidate_id": "khelifa1_vol03_c003",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [2,3],
    "theme": "مرض mucoviscidose وطفرة CFTR",
    "texte_corrige": "مرض mucoviscidose وراثي يسبب اضطرابات هضمية وتنفسية، ويرجع إلى طفرة في مورثة موجودة على الذراع الطويل للصبغي 7 مسؤولة عن صنع بروتين CFTR. قارن تتابع النيوكليوتيدات في جزء من المورثة عند الشخص العادي والمريض، وحدد تتابع الأحماض الأمينية باستعمال جدول الشفرة الوراثية. هل كل تغير في ADN يؤدي إلى تغير في البروتين؟ فسر سبب ظهور المرض، وحدد نوع الطفرة المدروسة واذكر أنواعا أخرى من الطفرات، ثم بين العلاقة بين المورثة والبروتين.",
    "sous_questions": ["مقارنة المورثة العادية والطافرة", "ترجمة ARNm إلى أحماض أمينية", "تحديد نوع الطفرة", "تفسير علاقة CFTR بالمرض", "بيان العلاقة مورثة-بروتين"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "المتتاليات موجودة لكنها تحتاج تدقيقا بصريا."
  },
  {
    "candidate_id": "khelifa1_vol03_c004",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [3,4],
    "theme": "العضيات الخلوية مقر تركيب البروتين وخصائص ARNm",
    "texte_corrige": "بهدف التعرف على البنيات الخلوية والعضيات مقر تركيب البروتين، تستعمل خلايا كبدية موضوعة في وسط يحافظ على بنية ونشاط العضيات ثم تفصل مكوناتها بالطرد المركزي حسب الحجم والوزن. يضاف إلى مستخلص بكتيري خال من ADN أحماض أمينية مشعة وARNm وتقاس كمية الأحماض الأمينية المدمجة والبروتينات المركبة. علل نزع ADN من المستخلص، واستخرج إحدى خصائص ARNm، وفسر وجود أنواع أخرى من ARN في الهيولى بكميات ثابتة أثناء وخارج فترة تركيب البروتين.",
    "sous_questions": ["تحديد العضيات مقر تركيب البروتين", "تعليل نزع ADN", "استنتاج خصائص ARNm", "تمييز أنواع ARN الأخرى"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "moyenne",
    "notes": "بعض المنحنيات غير واضحة نصيا."
  },
  {
    "candidate_id": "khelifa1_vol03_c005",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [4,5],
    "theme": "مقر وآليات صنع البروتين عند حقيقيات النواة",
    "texte_corrige": "تتنوع البروتينات وتتخصص حسب المعلومات الوراثية المخزنة. تلخص الوثيقة آليات ومقر صنع البروتين عند حقيقيات النواة. صف مقر مرحلة النسخ بدقة، ووضح برسم تخطيطي نشاط النسخ، ثم استخرج مراحل الترجمة والعناصر الكيميائية الحيوية المتدخلة فيها: ARNm، الأحماض الأمينية، ARNt، الريبوزومات، النظام الإنزيمي والطاقة ATP.",
    "sous_questions": ["تحديد مقر النسخ", "رسم النسخ", "تعداد عناصر الترجمة", "شرح البداية والاستطالة والنهاية"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "الصفحة 5 تحتوي شرحا نظريا واضحا لمراحل النسخ والترجمة."
  },
  {
    "candidate_id": "khelifa1_vol03_c006",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [5],
    "theme": "مراحل الترجمة: البداية، الاستطالة، النهاية",
    "texte_corrige": "اشرح مراحل الترجمة انطلاقا من الرسالة الوراثية المحمولة على ARNm. بين دور رامزة البدء AUG في بداية القراءة، وكيف يحدث توضع ARNt والحمض الأميني الموافق على الريبوزوم، ثم وضح في مرحلة الاستطالة ارتباط الرامزة بالرامزة المضادة وتشكل الرابطة الببتيدية واستهلاك الطاقة، وأخيرا اشرح توقف القراءة عند رامزات التوقف UAA وUAG وUGA وتحرر السلسلة الببتيدية.",
    "sous_questions": ["شرح بداية الترجمة", "شرح الاستطالة والرابطة الببتيدية", "تحديد رامزات التوقف", "ربط السلسلة الببتيدية بالبنية الوظيفية"],
    "micro_concepts_probables": ["mc_prot_07", "mc_prot_08", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "مستخرج كتمرين/ملخص حول آلية الترجمة."
  },
  {
    "candidate_id": "khelifa1_vol03_c007",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [6],
    "theme": "المهق: طفرة رامزة توقف وتأثيرها على تركيب إنزيم الميلانين",
    "texte_corrige": "عند الشخص الأمهق حدث استبدال في قاعدة آزوتية على مستوى ADN أدى إلى استبدال الرامزة UGG بالرامزة UAG على مستوى ARNm، وهي رامزة توقف لا تعبر عن أي حمض أميني، فتوقف صنع البروتين أو الإنزيم المسؤول عن تركيب الميلانين. فسر أثر هذه الطفرة على طول السلسلة الببتيدية ووظيفة الإنزيم، وبيّن لماذا يؤدي نقص الميلانين إلى حساسية أكبر للأشعة فوق البنفسجية وارتفاع احتمال حدوث طفرات وسرطان جلدي.",
    "sous_questions": ["تحديد طفرة استبدال", "تفسير ظهور رامزة توقف", "ربط توقف البروتين بفقدان وظيفة الإنزيم", "تفسير علاقة الميلانين بالأشعة UV"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_05", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "الصفحة تبدو تصحيحا لسؤال سابق حول المهق والطفرة."
  },
  {
    "candidate_id": "khelifa1_vol03_c008",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [7],
    "theme": "فقر الدم المنجلي: استبدال حمض أميني في HbA/HbS",
    "texte_corrige": "قارن بين خضاب الدم العادي HbA وخضاب الدم HbS. يظهر الاختلاف في استبدال الحمض الأميني السادس في HbA بحمض أميني آخر في HbS نتيجة استبدال نيوكليوتيدة على مستوى ADN. استخرج ARNm والسلسلة الببتيدية الموافقة، وحدد أثر الاستبدال على بنية الخضاب ووظيفته وعلى ظهور فقر الدم المنجلي.",
    "sous_questions": ["مقارنة HbA وHbS", "تحديد الاستبدال النيوكليوتيدي", "ترجمة ARNm", "ربط البنية البروتينية بالصفة المرضية"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "يتداخل مع سؤال Vol01 لكنه هنا بصيغة تصحيحية."
  },
  {
    "candidate_id": "khelifa1_vol03_c009",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [8,9],
    "theme": "دور RNase وpuromycine وARNt في تركيب البروتين",
    "texte_corrige": "تستعمل تجارب على أوساط خلوية لبيان دور ARNase ومواد مثبطة مثل البيروميسين في تركيب البروتين. فسر لماذا تبقى كمية الأحماض الأمينية ثابتة عند تثبيط ARNt أو تعطيل مرحلة من الترجمة، ولماذا تتناقص عند استمرار تركيب البروتين. استنتج دور ARNt وARNm والريبوزومات في إدماج الأحماض الأمينية ضمن السلسلة الببتيدية.",
    "sous_questions": ["تحليل أثر RNase", "تحليل أثر البيروميسين أو تعطيل ARNt", "استنتاج دور ARNt", "ربط استهلاك الأحماض الأمينية بالترجمة"],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "moyenne",
    "notes": "النص في الصفحتين يحتوي تصحيح نتائج تجريبية."
  },
  {
    "candidate_id": "khelifa1_vol03_c010",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [10],
    "theme": "مقارنة صنع البروتين في بدائيات وحقيقيات النواة",
    "texte_corrige": "يمثل شكلان آلية صنع البروتين في بدائيات النواة وحقيقيات النواة. ضع عنوانا مناسبا، واكتب البيانات: ADN، الاستنساخ، ARNm، الريبوزوم، السلسلة الببتيدية والغلاف النووي. ثم قارن أوجه الاختلاف بين بدائيات وحقيقيات النواة من حيث مقر النسخ والترجمة، وجود الغلاف النووي، وإمكانية تزامن النسخ والترجمة.",
    "sous_questions": ["تسمية البيانات", "تحديد مقر النسخ والترجمة", "المقارنة بين بدائيات وحقيقيات النواة", "تفسير تزامن أو عدم تزامن المرحلتين"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "صفحة واضحة نسبيا حول المقارنة."
  },
  {
    "candidate_id": "khelifa1_vol03_c011",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [11],
    "theme": "مكونات ARNm وتعدد الرامزات والبوليزوم",
    "texte_corrige": "حدد المكونات الكيميائية لنيوكليوتيدة ARNm: سكر الريبوز، حمض الفوسفور وقاعدة آزوتية. استنتج أن الرامزة تتكون من ثلاث قواعد آزوتية، وبين أن عدة رامزات قد تعبر عن نفس الحمض الأميني. عرف البوليزوم، ووضح لماذا يصنع نوعا واحدا من البروتينات عندما ترتبط عدة ريبوزومات بنفس ARNm.",
    "sous_questions": ["تحديد مكونات ARNm", "تعريف الرامزة الثلاثية", "توضيح ترادف الشفرة الوراثية", "تعريف البوليزوم ودوره"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_03", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "نص تصحيحي واضح."
  },
  {
    "candidate_id": "khelifa1_vol03_c012",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [11,12],
    "theme": "مقارنة سلسلة B للأنسولين عند الإنسان والجرذ",
    "texte_corrige": "انطلاقا من قطعتي ARNm المسؤولتين عن بناء السلسلة B للأنسولين عند الإنسان والجرذ، استعمل جدول الشفرة الوراثية لتحديد الأحماض الأمينية الموافقة، ثم حدد أي شكل يمثل أنسولين الإنسان وأي شكل يمثل أنسولين الجرذ. بين الفرق بين السلسلتين ومصدره المورثي المتمثل في اختلاف تتابع النيوكليوتيدات على مستوى المورثتين.",
    "sous_questions": ["ترجمة قطع ARNm", "تمييز سلسلة الإنسان والجرذ", "تحديد الأحماض المختلفة", "ربط الاختلاف بالمورثة"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "المتتاليات ظاهرة جزئيا في الصفحة 11."
  },
  {
    "candidate_id": "khelifa1_vol03_c013",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [12],
    "theme": "طفرة صامتة وطفرة مغيرة في سلسلة ADN قصيرة",
    "texte_corrige": "لدينا سلسلة ناسخة من ADN: TAC ACG CGA TTT TAT GTA. مثل الشريط المكمل وARNm الموافق ثم استخرج السلسلة الببتيدية. إذا تحولت الثلاثية الثانية بحيث بقيت تعبر عن نفس الحمض الأميني، استنتج مفهوم الطفرة الصامتة وترادف الشفرة الوراثية. وإذا أدى استبدال آخر إلى ظهور رامزة UGG التي تعبر عن التريبتوفان بدلا من السيستئين، فسر تغير البنية الأولية للبروتين وخاصة إذا حدث ذلك في الموقع الفعال.",
    "sous_questions": ["إكمال الشريط المكمل", "استخراج ARNm", "ترجمة السلسلة", "تفسير الطفرة الصامتة", "تفسير الطفرة المغيرة"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "السلسلة واضحة نسبيا في OCR."
  },
  {
    "candidate_id": "khelifa1_vol03_c014",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [12],
    "theme": "حقن ARNm الميلانين وشروط تركيب بروتين نوعي في وسط اصطناعي",
    "texte_corrige": "يبين حقن ARNm في بيضة منزوعة النواة أن ARNm يحمل معلومات تسمح بصنع الميلانين، وأن هذه المعلومات مستنسخة من المورثة المسؤولة عنه. كما تبين تجارب أوساط اصطناعية أن تركيب بروتين نوعي يتطلب وجود الريبوزومات وATP وARNm وإنزيمات التنشيط. حلل النتائج وحدد دور كل عنصر من هذه العناصر في تركيب البروتين.",
    "sous_questions": ["تفسير دور ARNm في صنع الميلانين", "تعليل نزع النواة", "تحديد شروط تركيب البروتين", "تحديد دور الريبوزومات وATP وARNm"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "مرتبط بسؤال Vol01 حول الميلانين."
  },
  {
    "candidate_id": "khelifa1_vol03_c015",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [13],
    "theme": "تغير مورثة CFTR وتغير ARNm والبروتين",
    "texte_corrige": "يترتب عن تغير في المورثة تشكل ARNm جديد، ثم تغير في تتابع الأحماض الأمينية لبروتين CFTR. قارن ARNm والبروتين عند الشخص العادي والمريض، واستنتج كيف يمكن لطفرة أن تغير أو لا تغير البروتين، مع التمييز بين الطفرة الصامتة والطفرة التي تغير البنية الأولية.",
    "sous_questions": ["مقارنة ARNm العادي والطافر", "ترجمة البروتين", "تحديد أثر الطفرة", "تمييز الطفرة الصامتة وغير الصامتة"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة فيها أجزاء مشوشة لكن الخلاصة العلمية واضحة."
  },
  {
    "candidate_id": "khelifa1_vol03_c016",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [14],
    "theme": "أصل الطفرات على مستوى Hb وعلاقة المورثة بالبروتين",
    "texte_corrige": "بالاعتماد على جدول الشفرات الوراثية يمكن تحديد أصل الطفرات التي ظهرت على مستوى ADN المسؤول عن تركيب خضاب الدم Hb. ادرس أمثلة استبدال قواعد تؤدي إلى أحماض أمينية مختلفة، ثم بين أن أي تغيير في ترتيب القواعد أو أنواعها على مستوى المورثة قد يؤدي إلى تغير في التعبير على مستوى البروتين وفق الشفرة الوراثية.",
    "sous_questions": ["استعمال جدول الشفرة لتحديد أصل الطفرة", "تفسير استبدال حمض أميني بآخر", "توضيح العلاقة بين المورثة والبروتين", "ربط تغير القواعد بتغير الصفة"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "يتضمن عدة أمثلة طفرية."
  },
  {
    "candidate_id": "khelifa1_vol03_c017",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [14,15],
    "theme": "خلية إفرازية: بنية خلوية ودور الهيولى والنواة في صنع البروتين",
    "texte_corrige": "تعرض وثيقة خلية إفرازية ذات شبكة هيولية محببة وجهاز غولجي وحويصلات إفرازية وميتوكندريات. تعرف على العناصر المرقمة، وحدد خصائص الخلية المتخصصة في الإفراز. ثم ناقش إشكالية: من المسؤول عن صنع البروتين النوعي، النواة أم الهيولى؟ بين أن النواة تحمل المعلومة الوراثية، وأن الهيولى تحتوي على العناصر الضرورية لتنفيذ الترجمة مثل الريبوزومات وARNt والطاقة.",
    "sous_questions": ["تسمية العضيات", "تحديد خصائص الخلية الإفرازية", "مناقشة دور النواة", "مناقشة دور الهيولى في الترجمة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "الصفحة تحتوي أيضا دور ARNm ونظام توافق مورثة-بروتين."
  },
  {
    "candidate_id": "khelifa1_vol03_c018",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [15],
    "theme": "حساب طول المورثة وعدد النيوكليوتيدات انطلاقا من عدد الأحماض الأمينية",
    "texte_corrige": "إذا كان البروتين يتكون من عدد معين من الأحماض الأمينية، احسب عدد النيوكليوتيدات اللازمة لترميزه على ARNm، مع إضافة رامزتي البدء والتوقف عند الحاجة، ثم استنتج الحد الأدنى لطول المورثة في شريط واحد أو في الشريطين. بين العلاقة بين الرامزات الثلاثية وعدد الأحماض الأمينية في البروتين.",
    "sous_questions": ["حساب عدد الرامزات", "حساب عدد النيوكليوتيدات في ARNm", "حساب طول المورثة", "تطبيق قاعدة ثلاثية الرامزات"],
    "micro_concepts_probables": ["mc_prot_03", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "الصفحة تتضمن مثالا عدديا حول 120 حمض أميني."
  },
  {
    "candidate_id": "khelifa1_vol03_c019",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [16],
    "theme": "وجود البروتينات في الكريات الحمراء رغم غياب النواة",
    "texte_corrige": "تطرح الوثيقة إشكالية وجود البروتينات في الكرية الحمراء رغم غياب النواة والذخيرة الوراثية ADN. ضع فرضية تفسر ذلك، ووضح أن الكرية الحمراء قد تكون صنعت البروتينات قبل فقدان النواة، أو استعملت رسائل ARNm موجودة سابقا في الهيولى لفترة قصيرة. بين علاقة هذه الظاهرة بمرحلة النسخ في النواة والترجمة في الهيولى.",
    "sous_questions": ["صياغة الإشكالية", "اقتراح فرضية", "ربط غياب النواة بغياب ADN", "تفسير استمرار مؤقت لتركيب البروتين"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "السؤال يظهر كتصحيح في الصفحة 16."
  },
  {
    "candidate_id": "khelifa1_vol03_c020",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [17,18],
    "theme": "تعرف ADN وARN والبروتين وعلاقة النسخ بالترجمة",
    "texte_corrige": "تعرف على البنية س بوصفها ADN، والبنية ص بوصفها ARN، والبنية ع بوصفها متعدد ببتيد. حدد المكونات الكيميائية لكل بنية، ووضح البنية الفراغية للـ ADN، ثم استنتج أن ARNm وسيط بين ADN والبروتين وحامل للرسالة الوراثية. سم ظاهرة تحويل ADN إلى ARNm بالاستنساخ، وحدد بداية ونهاية الاستنساخ.",
    "sous_questions": ["تمييز ADN وARN والبروتين", "تحديد المكونات الكيميائية", "شرح دور ARNm كوسيط", "تحديد ظاهرة النسخ"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "الصفحتان تقدمان تصحيحا لمفاهيم أساسية."
  },
  {
    "candidate_id": "khelifa1_vol03_c021",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [19],
    "theme": "مسار البروتينات الإفرازية من الشبكة الهيولية إلى جهاز غولجي والحويصلات",
    "texte_corrige": "بعد وسم الأحماض الأمينية يظهر الإشعاع أولا على مستوى الشبكة الهيولية المحببة، ثم ينتقل إلى جهاز غولجي، وبعد مدة أطول يظهر في الحويصلات الإفرازية. حلل تطور موضع الإشعاع واستنتج أن تركيب البروتينات الإفرازية يبدأ على الريبوزومات المرتبطة بالشبكة الهيولية المحببة، ثم تنتقل البروتينات إلى جهاز غولجي ومنها إلى الحويصلات الإفرازية لتطرح إلى الوسط الخارجي.",
    "sous_questions": ["تحليل انتقال الإشعاع", "تحديد مقر بداية تركيب البروتين", "تفسير دور جهاز غولجي", "تحديد مسار الإفراز"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "يتعلق بالبروتينات الإفرازية ومسارها الخلوي."
  },
  {
    "candidate_id": "khelifa1_vol03_c022",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [20],
    "theme": "تجارب الأميبا: انتقال ARN من النواة إلى الهيولى",
    "texte_corrige": "تدل تجربة نقل نواة الأميبا أو تتبع اليوريدين المشع على أن ARN ينتقل من النواة إلى الهيولى. استنتج أن مرحلة النسخ تحدث في النواة، ثم تنتقل الرسالة الوراثية إلى الهيولى حيث تتم الترجمة إلى بروتين محدد. قارن باختصار خطوات تركيب البروتين عند بدائيات النواة وحقيقيات النواة.",
    "sous_questions": ["تفسير انتقال ARN", "تحديد مقر النسخ", "تحديد مقر الترجمة", "مقارنة بدائيات وحقيقيات النواة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "الصفحة تحتوي خلاصة تجارب الأميبا."
  },
  {
    "candidate_id": "khelifa1_vol03_c023",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [21],
    "theme": "النيوكليوتيدة والنيوكليوزيدة وبنية ADN",
    "texte_corrige": "عرف النيوكليوتيدة بوصفها وحدة بنائية للحموض النووية تتكون من قاعدة آزوتية وسكر خماسي وحمض فوسفوريك، وميزها عن النيوكليوزيدة التي تتكون من السكر والقاعدة فقط. استعمل هذه المعطيات لتمثيل قطعة من ADN وبيان ارتباط النيوكليوتيدات فيما بينها.",
    "sous_questions": ["تعريف النيوكليوتيدة", "تعريف النيوكليوزيدة", "تمثيل قطعة ADN", "توضيح الروابط في الحمض النووي"],
    "micro_concepts_probables": ["mc_prot_01"],
    "qualite_ocr": "moyenne",
    "notes": "لا يوجد مفهوم مستقل لبنية ADN في المرجع، فتم ربطه بالنسخ/المورثة."
  },
  {
    "candidate_id": "khelifa1_vol03_c024",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [22],
    "theme": "تحليل منحنيات مسار بروتين إفرازي باللوسين المشع",
    "texte_corrige": "حلل منحنيات شدة الإشعاع في الشبكة الهيولية المحببة وجهاز غولجي والحويصلات الإفرازية بعد وسم الأحماض الأمينية. فسر ارتفاع الإشعاع أولا في الشبكة الهيولية المحببة ثم تناقصه، وارتفاعه لاحقا في جهاز غولجي ثم الحويصلات، واستنتج المراحل الأساسية لتصنيع ونقل وإفراز البروتين.",
    "sous_questions": ["تحليل منحنى الشبكة الهيولية", "تحليل منحنى جهاز غولجي", "تحليل منحنى الحويصلات", "استخراج مسار البروتين الإفرازي"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "يشبه سؤال تتبع مسار البروتينات الإفرازية."
  },
  {
    "candidate_id": "khelifa1_vol03_c025",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [23,24],
    "theme": "نزع النواة ودور ADN/ARN في استمرار تركيب البروتين",
    "texte_corrige": "أظهرت تجارب نزع النواة أن غياب النواة لا يمنع الخلية من الاستمرار في الحياة لبضع ساعات، لأن تركيب البروتين يتم في الهيولى وقد تبقى فيها رسائل وراثية ARNm سبق تركيبها. فسر لماذا يصبح بناء البروتين منعدما في الجزء عديم النواة بعد مدة، بينما يزداد نشاط البناء البروتيني في الجزء الحاوي للنواة. استنتج دور النواة وADN وARN في تركيب البروتين.",
    "sous_questions": ["تفسير أثر نزع النواة", "تفسير استمرار مؤقت لتركيب البروتين", "تحليل الجزء الحاوي للنواة والعديم النواة", "استنتاج دور ADN وARN"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "يرتبط بتجارب الأميبا/الأستبلاريا."
  },
  {
    "candidate_id": "khelifa1_vol03_c026",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [25],
    "theme": "تركيب وإفراز الكازيين في الغدد اللبنية",
    "texte_corrige": "لدراسة تركيب وإفراز الكازيين، حلل تغير شدة الإشعاع داخل الخلية بعد وسم اللوسين. بين أن الإشعاع يرتفع داخل الخلية خلال مرحلة تركيب البروتين ثم يتناقص عند انتقال البروتين نحو الإفراز. قارن بين نوعي الكازيين أو بين الحيوانين المدروسين، واستنتج علاقة اختلاف المورثة أو ARNm باختلاف البروتين المصطنع.",
    "sous_questions": ["تحليل تغير الإشعاع داخل الخلية", "تفسير تركيب وإفراز الكازيين", "مقارنة نوعي الكازيين", "ربط اختلاف المورثة باختلاف البروتين"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة تحتوي تصحيحا جزئيا لمنحنى الكازيين."
  },
  {
    "candidate_id": "khelifa1_vol03_c027",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [26,27],
    "theme": "طفرة تغير الرامزة وتتابع الأحماض الأمينية",
    "texte_corrige": "حدد موقع طفرة على مستوى رامزة في المورثة حيث استبدلت رامزة كانت تعبر عن السيرين برامزة أخرى تعبر عن الأرجنين. استعمل جدول الشفرة الوراثية لكتابة ARNm وتتابع الأحماض الأمينية، ثم فسر أثر هذا التغير على البنية الأولية للبروتين وعلى وظيفته المحتملة.",
    "sous_questions": ["تحديد موقع الطفرة", "استعمال جدول الشفرة", "ترجمة ARNm", "تفسير أثر تغير حمض أميني"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "تتابعات الصفحة 27 مشوشة وتحتاج تدقيقا بصريا."
  },
  {
    "candidate_id": "khelifa1_vol03_c028",
    "source": "KHELIFA1_VOLUME_03.pdf",
    "pages": [28],
    "theme": "الأشعة والطفرات وغياب النواة وتأثيرهما على تركيب البروتين",
    "texte_corrige": "فسر لماذا يؤدي عدم وجود النواة إلى غياب ADN وبالتالي غياب المعلومات الوراثية وعدم إمكانية تركيب البروتين إلا لفترة قصيرة اعتمادا على ARNm موجود سابقا. كما قارن بين مورثة عادية قبل التعرض للأشعة ومورثة بعد التعرض، واستنتج أثر الأشعة في إحداث طفرة قد تغير الرسالة الوراثية والبروتين الناتج.",
    "sous_questions": ["ربط غياب النواة بغياب ADN", "تفسير استمرار مؤقت بوجود ARNm", "مقارنة مورثة عادية وطافرة", "استنتاج أثر الأشعة على البروتين"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_03"],
    "qualite_ocr": "moyenne",
    "notes": "خلاصة نهائية لعدة أسئلة حول النواة والطفرات."
  }
]
Open
Phase 2 terminée pour :

text

KHELIFA1_VOLUME_03.ocr_prod_full.txt
Fichiers créés
JSON détaillé
text

downloads/annales_svt/output/khelifa1_volume03_extraction_phase2.json
Contient :

text

28 blocs candidats
Résumé Markdown
text

downloads/annales_svt/output/khelifa1_volume03_extraction_phase2.md
Résultat
Le volume 03 semble être surtout un volume de corrigés / reprises d’exercices sur :

transcription,
traduction,
ARNm,
ARNt,
ribosomes,
code génétique,
mutations,
maladies génétiques,
relation gène → protéine → caractère.
Comme plusieurs pages sont des corrigés, les questions ont été reconstruites à partir des réponses visibles.

Qualité OCR utilisée
OCR de référence :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_03.ocr_prod_full.txt
Rapport OCR :

text

Pages traitées : 28 / 28
Erreurs OCR : 0
Total caractères OCR : 58,629
Confiance moyenne : 65.29
Quality warning : aucun
Micro-concepts probables dominants
text

mc_prot_01  Transcription de l'ADN
mc_prot_02  Traduction de l'ARNm
mc_prot_03  Code génétique
mc_prot_04  ARNm
mc_prot_05  ARNt
mc_prot_06  Ribosome
mc_prot_07  Initiation de la traduction
mc_prot_08  Élongation et terminaison
mc_struc_01 Structure primaire
mc_struc_05 Relation structure-fonction
Exemples de blocs extraits
expression génétique : transcription / traduction
α-amanitine et ARN polymérase
mucoviscidose / CFTR
albinisme, codon stop et mélanine
HbA / HbS et drépanocytose
RNase, puromycine, ARNt
procaryotes vs eucaryotes
ARNm, codons, polysomes
insuline humain / rat
mutation silencieuse / mutation changeante
voie sécrétoire REG → Golgi → vésicules
noyau, ADN, ARN et synthèse protéique
caséine et glandes mammaires
Manifeste mis à jour
J’ai mis à jour :

text

downloads/annales_svt/MANIFESTE.md
État :

text

KHELIFA 1 Vol 03 — OCR production v3 complet + extraction phase 2 — 28 blocs candidats
Prochaine étape
Phase 3 possible :

convertir ces 28 blocs en entrées finales,
les ajouter à :
text

downloads/annales_svt/output/questions_taggees.json
puis valider avec :
Bash

python scripts/validate_tags.py output/questions_taggees.json

etat des annales



Explored
1 read
Read
MANIFESTE.md
Voici l’état actuel des annales SVT dans le workspace.

État global
text

Total intégré dans questions_taggees.json : 141 questions
Micro-concepts couverts : 33 / 42
Validation : 0 erreur, 0 avertissement
Le fichier principal est :

text

downloads/annales_svt/output/questions_taggees.json
Il est actuellement valide.

Répartition des questions intégrées
Source	Questions intégrées
FINALBAC Vol 3	50
FINALBAC Vol 4	30
KHELIFA 1 Vol 01	32
KHELIFA 1 Vol 02	29
KHELIFA 1 Vol 03	0 intégré pour l’instant
Total	141
Détail par volume
Source	Volume	État	Questions
FINALBAC	Vol 1	OCR fait, cours théorique	N/A
FINALBAC	Vol 2	OCR partiel, cours théorique	N/A
FINALBAC	Vol 3	traité + intégré	50
FINALBAC	Vol 4	traité + intégré	30
FINALBAC	Vol 5	OCR partiel, à extraire	—
FINALBAC	Vol 6	OCR partiel, à extraire	—
KHELIFA 1	Vol 01	OCR + extraction + intégration	32
KHELIFA 1	Vol 02	OCR + extraction + intégration	29
KHELIFA 1	Vol 03	OCR + extraction phase 2 seulement	28 blocs candidats
KHELIFA 1	Vol 04-15	à traiter	—
KHELIFA 2	Vol 01-15	à traiter	—
MORAFIK	Vol 1-3	à traiter	—
KHELIFA 1 Vol 03
Le volume 03 est OCRisé et extrait, mais pas encore intégré dans le JSON final.

Fichiers disponibles :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_03.ocr_prod_full.txt
downloads/annales_svt/output/khelifa1_volume03_extraction_phase2.json
downloads/annales_svt/output/khelifa1_volume03_extraction_phase2.md
Résultat phase 2 :

text

28 blocs candidats extraits
Prochaine action logique :

text

Phase 3 : intégrer KHELIFA1 Vol 03 dans questions_taggees.json
Après intégration, le total devrait passer de :

text

141 questions
à environ :

text

169 questions
si les 28 blocs sont tous ajoutés.

Pipeline OCR actif
Le nouveau script corrigé est disponible ici :

text

ocr_pipeline_production.py
et aussi ici :

text

downloads/annales_svt/scripts/ocr_pipeline_production.py
Il est validé et utilisé pour les derniers OCR.

Prochaine étape recommandée
Continuer avec :

text

KHELIFA1_VOLUME_03 — Phase 3 tagging + intégration JSON
Puis ensuite :

text

KHELIFA1_VOLUME_04 — OCR production + extraction + intégration

ok



Edited files
2
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02	28	Exercices	✅ OCR production complet + ✅ extraction/intégration (khelifa1_volume02_extraction_phase2.json)	29 questions
KHELIFA 1	Vol 03	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume03_extraction_phase2.json)	28 questions
KHELIFA 1	Vol 04-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 169 questions / 34 micro-concepts couverts sur 42
Derniers ajouts
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
KHELIFA 1 Vol 02 : 29 questions ajoutées (q_khelifa1_vol02_001 à q_khelifa1_vol02_029)
KHELIFA 1 Vol 03 : 28 questions ajoutées (q_khelifa1_vol03_001 à q_khelifa1_vol03_028)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
KHELIFA 1 Volume 03 — Phase 3 intégration
MD
KHELIFA 1 Volume 03 — Phase 3 intégration
Source : input/KHELIFA1_VOLUME_03.pdf
OCR : input/KHELIFA1_VOLUME_03.ocr_prod_full.txt
Extraction : output/khelifa1_volume03_extraction_phase2.json
Sortie consolidée : output/questions_taggees.json

Actions réalisées
Sauvegarde du JSON avant intégration :
text

output/questions_taggees.before_khelifa1_vol03.json
Transformation des 28 blocs candidats en entrées normalisées.

Ajout dans :

text

output/questions_taggees.json
Validation automatique :
Bash

python scripts/validate_tags.py output/questions_taggees.json
Résultat validation globale
text

Total questions : 169
Concepts distincts utilisés : 34
Erreurs : 0
Avertissements : 0
Ajout KHELIFA1 Vol 03
Questions ajoutées : 28
IDs : q_khelifa1_vol03_001 à q_khelifa1_vol03_028
Distribution des concepts principaux ajoutés
Micro-concept	Questions
mc_prot_01 — Transcription de l'ADN	11
mc_prot_02 — Traduction de l'ARNm	4
mc_prot_03 — Code génétique	8
mc_prot_04 — ARNm	3
mc_prot_05 — ARNt	1
mc_prot_07 — Initiation de la traduction	1
Distribution des types
Type	Questions
analyse_document	12
raisonnement	5
schema_a_completer	3
definition	3
application	3
comparaison	2
Distribution des difficultés
Difficulté	Questions
facile	4
moyenne	21
difficile	3
Remarques
Le volume 03 contient beaucoup de corrigés/reformulations ; certaines questions ont été reconstruites à partir des réponses.
Les tags sont validés structurellement et restent dans les 42 micro-concepts autorisés.
Les séquences nucléotidiques exactes restent à vérifier visuellement si besoin d'une correction scientifique exhaustive.
Open
C’est fait ✅

KHELIFA1_VOLUME_03 intégré
J’ai ajouté les 28 blocs candidats de KHELIFA1_VOLUME_03 dans :

text

downloads/annales_svt/output/questions_taggees.json
Sauvegarde créée
Avant modification :

text

downloads/annales_svt/output/questions_taggees.before_khelifa1_vol03.json
IDs ajoutés
text

q_khelifa1_vol03_001
→
q_khelifa1_vol03_028
Validation globale
Commande lancée :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Résultat :

text

Total questions : 169
Concepts distincts utilisés : 34
Erreurs : 0
Avertissements : 0
✅ JSON valide.

Distribution des 28 questions ajoutées
Concepts principaux
text

mc_prot_01  Transcription de l'ADN       11
mc_prot_02  Traduction de l'ARNm          4
mc_prot_03  Code génétique                8
mc_prot_04  ARNm                          3
mc_prot_05  ARNt                          1
mc_prot_07  Initiation de la traduction   1
Types
text

analyse_document       12
raisonnement            5
schema_a_completer      3
definition              3
application             3
comparaison             2
Difficulté
text

facile       4
moyenne     21
difficile    3
Rapport créé
text

downloads/annales_svt/output/khelifa1_volume03_phase3_integration.md
Manifeste mis à jour
text

downloads/annales_svt/MANIFESTE.md
Nouvel état :

text

169 questions traitées
34 micro-concepts couverts sur 42
0 erreur
0 avertissement
Prochaine étape logique :

text

KHELIFA1_VOLUME_04 — OCR production v3 + extraction + intégration

oui



Ran commands
2
Write
downloads/annales_svt/output/khelifa1_volume04_extraction_phase2.json
256 lines
open
khelifa1_volume04_extraction_phase2.json
JSON
[
  {
    "candidate_id": "khelifa1_vol04_c001",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [1],
    "theme": "ترابط النسخ والترجمة عند بدائيات النواة والبوليزوم",
    "texte_corrige": "استنتج من الوثيقة أن الاستنساخ والترجمة يمكن أن يحدثا في الوقت نفسه عند بدائيات النواة. بين كيف يمكن لنفس قطعة ARNm أن تستعمل لتركيب عدة جزيئات من نفس البروتين في الوقت نفسه، واحسب عدد جزيئات البروتين الممكن تركيبها انطلاقا من عدد الريبوزومات المثبتة على ARNm في الجزء المؤطر.",
    "sous_questions": ["تفسير تزامن النسخ والترجمة عند بدائيات النواة", "تحديد دور البوليزوم", "حساب عدد جزيئات البروتين المركبة"],
    "micro_concepts_probables": ["mc_prot_06", "mc_prot_02", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "الصفحة تظهر تصحيحا حول البوليزوم وتزامن النسخ والترجمة."
  },
  {
    "candidate_id": "khelifa1_vol04_c002",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [3,4],
    "theme": "الأحماض النووية والروابط الهيدروجينية والتهجين الجزيئي",
    "texte_corrige": "استعمل معطيات نسب القواعد الآزوتية والروابط الهيدروجينية لتحديد تركيب قطعة ADN ورسمها. ثم فسر تجربة التهجين الجزيئي بين سلسلة ADN مورثية وARNm، واستنتج وجود قطع دالة وغير دالة في مورثات حقيقيات النواة من خلال اختلاف طول ADN عن طول ARNm الناضج.",
    "sous_questions": ["حساب القواعد الآزوتية والروابط الهيدروجينية", "رسم قطعة ADN", "تحليل التهجين الجزيئي", "استنتاج وجود الإكسونات والإنترونات"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "moyenne",
    "notes": "يجمع بين بنية ADN والنسخ/نضج ARNm."
  },
  {
    "candidate_id": "khelifa1_vol04_c003",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [5],
    "theme": "دور ARNt في تركيب البروتين وتثبيط الترجمة",
    "texte_corrige": "فسر تناقص الأحماض الأمينية في وسط خلايا يحدث فيه تركيب البروتين، واستنتج أن ARNt ضروري لتركيب البروتين. عند استعمال مادة تعطل عمل ARNt لا يلاحظ تركيب البروتين، فسر ذلك وحدد المرحلة من التعبير المورثي التي تتأثر بهذا التعطيل.",
    "sous_questions": ["تحليل تغير كمية الأحماض الأمينية", "استنتاج دور ARNt", "تفسير أثر تعطيل ARNt", "تحديد مرحلة الترجمة"],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "السؤال مرتبط بنتائج تجريبية حول ARNt."
  },
  {
    "candidate_id": "khelifa1_vol04_c004",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [7],
    "theme": "حساب قواعد قطعة ADN وتطبيق قاعدة Chargaff",
    "texte_corrige": "اعتمادا على عدد القواعد الآزوتية في قطعة ADN وقاعدة التكامل بين القواعد، احسب عدد كل من A وT وC وG، ثم استخرج مجموع الروابط الهيدروجينية وفسر العلاقة بين بنية ADN وتكامل القواعد.",
    "sous_questions": ["حساب القواعد الآزوتية", "تطبيق قاعدة التكامل", "حساب الروابط الهيدروجينية", "تفسير البنية المزدوجة للـ ADN"],
    "micro_concepts_probables": ["mc_prot_01"],
    "qualite_ocr": "moyenne",
    "notes": "لا يوجد مفهوم مستقل لبنية ADN، أُسنِد إلى النسخ/المورثة."
  },
  {
    "candidate_id": "khelifa1_vol04_c005",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [8],
    "theme": "آلية النسخ: ARN بوليميراز وتكامل القواعد",
    "texte_corrige": "صف آلية الاستنساخ على المستوى الجزيئي: توضع إنزيم ARN بوليميراز على الشريط القالب من ADN، توضع النيوكليوتيدات الريبية الجديدة مقابل نيوكليوتيدات شريط ADN وفق تكامل القواعد، ثم تشكل جزيئة ARNm. سم الظاهرة وحدد مقرها عند حقيقيات النواة.",
    "sous_questions": ["تحديد دور ARN بوليميراز", "شرح تكامل القواعد أثناء النسخ", "تسمية الظاهرة", "تحديد مقر النسخ"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "السؤال واضح حول النسخ."
  },
  {
    "candidate_id": "khelifa1_vol04_c006",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [9],
    "theme": "البنية الأولية والثالثية للأنسولين وعلاقة البنية بالوظيفة",
    "texte_corrige": "تبين الوثيقة أن البنية الأولية تتمثل في ارتباط مجموعة من الأحماض الأمينية بروابط ببتيدية مشكلة سلسلة خطية. تأخذ جزيئة الأنسولين بعد ذلك بنية فراغية نتيجة وجود بنى ثانوية ومناطق انعطاف وروابط بين السلاسل. فسر كيف تنتقل السلسلة من بنية أولية إلى بنية وظيفية، وما أهمية البنية الفراغية في وظيفة البروتين.",
    "sous_questions": ["تعريف البنية الأولية", "تحديد الروابط الببتيدية", "تفسير تشكل البنية الفراغية", "ربط البنية بالوظيفة"],
    "micro_concepts_probables": ["mc_struc_01", "mc_struc_03", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "ينتقل من مفاهيم التعبير المورثي إلى بنية البروتين."
  },
  {
    "candidate_id": "khelifa1_vol04_c007",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [10],
    "theme": "تأثير الأشعة فوق البنفسجية على ADN والطفرات",
    "texte_corrige": "فسر تأثير الأشعة فوق البنفسجية على ADN الخلايا الجلدية بتكوين روابط بين قاعدتين متجاورتين، ثم عرف الطفرة بأنها تغير مفاجئ يصيب ADN وقد يتمثل في استبدال أو حذف أو إضافة نيوكليوتيدة أو أكثر. بين أثر هذه التغيرات على الرسالة الوراثية والبروتين الناتج.",
    "sous_questions": ["تفسير أثر UV على ADN", "تعريف الطفرة", "تمييز الاستبدال والحذف والإضافة", "ربط الطفرة بالبروتين"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "سؤال طفرات مرتبط بمرض جلدي/UV."
  },
  {
    "candidate_id": "khelifa1_vol04_c008",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [11],
    "theme": "حقن ARNm في بيض الضفدع ودور الريبوزومات",
    "texte_corrige": "استخلص من تجربة حقن ARNm في بيض الضفدع أن ARNm ينقل المعلومة الوراثية المشفرة لتركيب الهيموغلوبين، ويحدد عدد وتسلسل ونوع الأحماض الأمينية، أي يلعب دور الوسيط بين الرسالة النووية والرسالة البروتينية. اقترح فرضية تبين دور الريبوزومات في هذا النشاط الحيوي.",
    "sous_questions": ["تفسير دور ARNm المحقون", "بيان علاقة ARNm بتسلسل الأحماض الأمينية", "اقتراح دور الريبوزومات", "ربط الرسالة النووية بالبروتين"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "تجربة كلاسيكية حول ARNm."
  },
  {
    "candidate_id": "khelifa1_vol04_c009",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [12],
    "theme": "أثر إضافة وحذف نيوكليوتيدة على متعدد الببتيد",
    "texte_corrige": "بين كيف تؤدي إضافة نيوكليوتيدة أو حذف أخرى في المورثة إلى تغير إطار القراءة، ومن ثم تغير متعدد الببتيد المتشكل. استعمل جدول الشفرة الوراثية لتفسير تغير تتابع الأحماض الأمينية، وناقش متى يمكن أن يكون الأثر كبيرا على وظيفة البروتين.",
    "sous_questions": ["تفسير تغير إطار القراءة", "استعمال جدول الشفرة", "تحديد تغير متعدد الببتيد", "ربط التغير بالوظيفة"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة مشوشة جزئيا لكنها تدور حول الإدماج والحذف."
  },
  {
    "candidate_id": "khelifa1_vol04_c010",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [13],
    "theme": "اليوريدين المشع وتتبع تركيب ARN وانتقاله إلى الهيولى",
    "texte_corrige": "بعد 10 دقائق من إضافة اليوريدين المشع يظهر الإشعاع في النواة فقط، ويفسر ذلك بإدماج اليوريدين مع النيوكليوتيدات الريبية لتصنيع ARN. بعد 30 دقيقة يظهر الإشعاع في الهيولى نتيجة انتقال ARN من النواة إلى الهيولى. فسر هذه النتائج واستنتج مقر النسخ ومسار ARNm.",
    "sous_questions": ["تفسير استعمال اليوريدين المشع", "تحليل ظهور الإشعاع في النواة", "تحليل انتقال الإشعاع إلى الهيولى", "استنتاج مسار ARNm"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "تجربة واضحة حول النسخ وانتقال ARNm."
  },
  {
    "candidate_id": "khelifa1_vol04_c011",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [14],
    "theme": "تعرف ADN وARNm والبروتين ومرحلتي النسخ والترجمة",
    "texte_corrige": "تعرف على البنية س بوصفها ADN، وعلى البنية الأخرى بوصفها ARNm أو متعدد ببتيد حسب الوثيقة. سم مرحلتي الاستنساخ والترجمة، وحدد مقر كل مرحلة، ثم استخرج البيانات الأساسية للرسم التخطيطي الذي يوضح انتقال المعلومة من ADN إلى ARNm ثم إلى بروتين.",
    "sous_questions": ["تسمية ADN وARNm والبروتين", "تعرف النسخ", "تعرف الترجمة", "تحديد مقر كل مرحلة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_04"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة طويلة وفيها ضجيج OCR لكن المفهوم واضح."
  },
  {
    "candidate_id": "khelifa1_vol04_c012",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [15],
    "theme": "أنواع ARN أثناء تركيب البروتين: ARNr وARNm وARNt",
    "texte_corrige": "حلل الشوكات الناتجة عن الطرد المركزي أثناء تركيب البروتين وخارجه. استنتج أن بعض الشوكات تمثل ARNr الريبوزومي لأنها ذات وزن جزيئي كبير وتترسب أولا، وأن شوكة أخرى تمثل ARNm لأنها تظهر أثناء تركيب البروتين فقط وتختفي خارجه. ميز بين ARNr وARNm وARNt ودور كل منها في الترجمة.",
    "sous_questions": ["تحليل شوكات الطرد المركزي", "تعرف ARNr", "تعرف ARNm", "تمييز أدوار أنواع ARN"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_05", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "يركز على أنواع ARN."
  },
  {
    "candidate_id": "khelifa1_vol04_c013",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [16],
    "theme": "قاعدة التكامل وتحديد السلسلة الناسخة ورامزة البدء",
    "texte_corrige": "اعتمادا على قاعدة التقابل والتكامل بين القواعد الآزوتية، حيث C يقابل G وA يقابل T في ADN، حدد السلسلة الناسخة في قطعة ADN. استعمل وجود رامزة البدء AUG على ARNm لتحديد رامزة ADN الموافقة TAC، ثم استنتج اتجاه النسخ وبداية القراءة.",
    "sous_questions": ["تطبيق قاعدة التكامل", "تحديد السلسلة الناسخة", "تحديد رامزة البدء", "استنتاج اتجاه القراءة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_03", "mc_prot_07"],
    "qualite_ocr": "bonne",
    "notes": "السؤال حول تحديد الشريط المستنسخ."
  },
  {
    "candidate_id": "khelifa1_vol04_c014",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [18],
    "theme": "الرامزة والرامزة المضادة ورامزة الانطلاق",
    "texte_corrige": "عرف الرامزة بأنها وحدة الشفرة الوراثية المكونة من تتابع ثلاث نيكليوتيدات على ARNm وتشفر لحمض أميني واحد. عرف الرامزة المضادة بأنها تتابع متمم تحمله ARNt. حدد رامزة الانطلاق AUG التي تشفر للميثيونين، ثم استعمل جدول الشفرة الوراثية لتحديد الأحماض الأمينية الموافقة للرامزات المعطاة.",
    "sous_questions": ["تعريف الرامزة", "تعريف الرامزة المضادة", "تحديد رامزة الانطلاق", "استعمال جدول الشفرة"],
    "micro_concepts_probables": ["mc_prot_03", "mc_prot_05", "mc_prot_07"],
    "qualite_ocr": "bonne",
    "notes": "تعريفات أساسية للشفرة والترجمة."
  },
  {
    "candidate_id": "khelifa1_vol04_c015",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [19],
    "theme": "مرحلة النسخ باليوريدين المشع ورسمها التخطيطي",
    "texte_corrige": "قبل زمن معين يلاحظ الإشعاع في النواة لأن اليوريدين المشع يدمج أثناء تركيب ARNm في مرحلة النسخ، ثم ينتقل بعد ذلك إلى الهيولى للترجمة. سم الظاهرة البيوكيميائية، وحدد البيانات المرقمة مثل ADN وARN بوليميراز وARNm، ومثل الظاهرة برسم تخطيطي.",
    "sous_questions": ["تحليل موضع الإشعاع", "تسمية النسخ", "تحديد بيانات الرسم", "رسم النسخ"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04"],
    "qualite_ocr": "bonne",
    "notes": "يشبه سؤال الصفحة 13 لكنه بصيغة بيانات ورسم."
  },
  {
    "candidate_id": "khelifa1_vol04_c016",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [20],
    "theme": "نهاية الترجمة وتفكك الريبوزوم وأهمية ARNm",
    "texte_corrige": "عندما يصل الريبوزوم إلى رامزة توقف تنفصل السلسلة الببتيدية وتنفصل تحت الوحدة الكبرى عن تحت الوحدة الصغرى، ثم يتفكك المركب ويحرر ARNm. فسر أحداث نهاية الترجمة، وبين أهمية ARNm الناتج عن النسخ في نقل المعلومات الوراثية من النواة إلى الهيولى.",
    "sous_questions": ["تعرف رامزة التوقف", "شرح نهاية الترجمة", "تفسير تفكك الريبوزوم", "بيان دور ARNm"],
    "micro_concepts_probables": ["mc_prot_08", "mc_prot_06", "mc_prot_04"],
    "qualite_ocr": "moyenne",
    "notes": "السؤال يدعم مفهوم نهاية الترجمة الذي كان قليل التغطية."
  },
  {
    "candidate_id": "khelifa1_vol04_c017",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [21],
    "theme": "تنشيط الأحماض الأمينية ودور ATP وARNt",
    "texte_corrige": "تعرف العملية التي تسبق إدماج الحمض الأميني في السلسلة الببتيدية بوصفها تنشيط الأحماض الأمينية. اذكر العناصر المشاركة في هذه العملية: إنزيم التنشيط النوعي، ATP، الحمض الأميني وARNt. استنتج أن ARNt المشحون ينقل الحمض الأميني إلى الريبوزوم حسب الرامزة الموافقة على ARNm.",
    "sous_questions": ["تسمية تنشيط الأحماض الأمينية", "تحديد دور ATP", "تحديد دور ARNt", "ربط ARNt بالترجمة"],
    "micro_concepts_probables": ["mc_prot_05", "mc_prot_02", "mc_prot_07"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة فيها ضجيج لكنها تشير بوضوح إلى تنشيط الأحماض الأمينية."
  },
  {
    "candidate_id": "khelifa1_vol04_c018",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [23],
    "theme": "إثبات ضرورة الريبوزومات في تركيب البروتين",
    "texte_corrige": "قورنت نسبة الإشعاع في وسط يحتوي على الريبوزومات ووسط يخلو منها. في وجود الريبوزومات تكون نسبة الإشعاع كبيرة وتدل على إدماج الأحماض الأمينية في البروتين، أما في غياب الريبوزومات فالإشعاع منعدم ويدل على عدم تركيب البروتين. استنتج أن تركيب البروتين يتطلب وجود الريبوزومات.",
    "sous_questions": ["تحليل نتائج وجود الريبوزومات", "تحليل نتائج غياب الريبوزومات", "استنتاج دور الريبوزوم", "ربط الريبوزوم بالترجمة"],
    "micro_concepts_probables": ["mc_prot_06", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "تجربة واضحة لإثبات دور الريبوزومات."
  },
  {
    "candidate_id": "khelifa1_vol04_c019",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [24],
    "theme": "استنساخ ARNm من ADN ثم ترجمته إلى سلسلة ببتيدية",
    "texte_corrige": "سم الظاهرة المدروسة بوصفها استنساخ ARNm من ADN، ثم باستعمال الشفرة الوراثية أنجز ترتيب الأحماض الأمينية في السلسلة الببتيدية الناتجة. بين أن الميثيونين الأولي قد ينفصل لاحقا من السلسلة، واستنتج العلاقة بين المورثة وARNm والبروتين.",
    "sous_questions": ["تسمية النسخ", "استعمال جدول الشفرة", "تحديد السلسلة الببتيدية", "توضيح مصير الميثيونين الأولي"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03"],
    "qualite_ocr": "bonne",
    "notes": "سؤال تطبيقي حول النسخ والترجمة."
  },
  {
    "candidate_id": "khelifa1_vol04_c020",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [25],
    "theme": "استعمال برنامج Anagène لمحاكاة النسخ والترجمة",
    "texte_corrige": "حدد الهدف من استعمال برنامج المحاكاة Anagène: عرض أو إنشاء متتاليات ADN وARN ومقارنتها، مقارنة الأحماض الأمينية في بروتين، وإجراء عملية استنساخ ARNm من ADN ثم ترجمة ARNm إلى سلسلة ببتيدية. تعرف على البنيات المعروضة وعلل إجابتك.",
    "sous_questions": ["تحديد هدف Anagène", "تعرف ADN وARN والبروتين", "محاكاة النسخ", "محاكاة الترجمة"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_02", "mc_prot_03"],
    "qualite_ocr": "bonne",
    "notes": "يدعم أسئلة البرنامج والمحاكاة."
  },
  {
    "candidate_id": "khelifa1_vol04_c021",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [26],
    "theme": "رامزات التوقف ونهاية تركيب البروتين",
    "texte_corrige": "عندما يصل الريبوزوم إلى وحدة رمزية من نوع UGA أو UAA أو UAG لا يقابلها أي حمض أميني، يتوقف تركيب البروتين وتنفصل وحدتا الريبوزوم عن بعضهما. اشرح دور رامزات التوقف في إنهاء الترجمة، وبيّن أثرها على طول السلسلة الببتيدية الناتجة.",
    "sous_questions": ["تحديد رامزات التوقف", "شرح توقف الترجمة", "تفسير انفصال وحدتي الريبوزوم", "ربط التوقف بطول البروتين"],
    "micro_concepts_probables": ["mc_prot_08", "mc_prot_06", "mc_prot_03"],
    "qualite_ocr": "moyenne",
    "notes": "صفحة طويلة ومشوشة لكن تحتوي بوضوح فكرة رامزات التوقف."
  },
  {
    "candidate_id": "khelifa1_vol04_c022",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [27],
    "theme": "ترجمة جزء من مورثة فيروسية إلى ببتيد",
    "texte_corrige": "يعطى جزء من مورثة فيروسية أو ARNm وتتابع ببتيدي يحتوي على أحماض أمينية مثل Tyr وSer وIle وThr وPro وPhe. باستعمال جدول الشفرة الوراثية، حدد الرامزات الموافقة وترجم السلسلة، ثم ناقش أثر أي تغير في الرامزات على الببتيد الناتج.",
    "sous_questions": ["قراءة الرامزات", "ترجمة ARNm", "تحديد تتابع الأحماض الأمينية", "تفسير أثر تغير الرامزات"],
    "micro_concepts_probables": ["mc_prot_02", "mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "المتتالية مشوشة وتحتاج مراجعة بصرية."
  },
  {
    "candidate_id": "khelifa1_vol04_c023",
    "source": "KHELIFA1_VOLUME_04.pdf",
    "pages": [28],
    "theme": "قراءة الريبوزومات للـ ARNm وتركيب السلسلة البروتينية",
    "texte_corrige": "تظهر الوثيقة وجود عدة ريبوزومات على ARNm، حيث تتم قراءة الرسالة الوراثية وتترجم إلى سلسلة ببتيدية. فسر كيف يقرأ الريبوزوم ARNm رامزة بعد أخرى، وكيف يسمح وجود عدة ريبوزومات على نفس ARNm بزيادة كمية البروتين المصطنع.",
    "sous_questions": ["تفسير قراءة ARNm", "تحديد دور الريبوزومات", "شرح البوليزوم", "ربط عدد الريبوزومات بكمية البروتين"],
    "micro_concepts_probables": ["mc_prot_06", "mc_prot_02", "mc_prot_04"],
    "qualite_ocr": "moyenne",
    "notes": "خلاصة مرتبطة بالبوليزومات والترجمة."
  }
]
Open

Edited files
2
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02	28	Exercices	✅ OCR production complet + ✅ extraction/intégration (khelifa1_volume02_extraction_phase2.json)	29 questions
KHELIFA 1	Vol 03	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume03_extraction_phase2.json)	28 questions
KHELIFA 1	Vol 04	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume04_extraction_phase2.json)	23 questions
KHELIFA 1	Vol 05-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 192 questions / 35 micro-concepts couverts sur 42
Derniers ajouts
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
KHELIFA 1 Vol 02 : 29 questions ajoutées (q_khelifa1_vol02_001 à q_khelifa1_vol02_029)
KHELIFA 1 Vol 03 : 28 questions ajoutées (q_khelifa1_vol03_001 à q_khelifa1_vol03_028)
KHELIFA 1 Vol 04 : 23 questions ajoutées (q_khelifa1_vol04_001 à q_khelifa1_vol04_023)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
KHELIFA 1 Volume 04 — OCR production v3 + extraction/intégration
MD
KHELIFA 1 Volume 04 — OCR production v3 + extraction/intégration
Source : input/KHELIFA1_VOLUME_04.pdf

OCR production v3
Sortie :

text

input/KHELIFA1_VOLUME_04.ocr_prod_full.txt
input/KHELIFA1_VOLUME_04.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Méthode : OCR sur 28 pages
Erreurs : 0
Total caractères OCR : 61,362
Taille fichier texte final : 98,082 bytes
Confiance moyenne : 65.05
Quality warning : aucun
Durée : ~13 min 51 s
Phase 2 — Extraction
Fichier créé :

text

output/khelifa1_volume04_extraction_phase2.json
Résultat : 23 blocs candidats extraits.

Phase 3 — Intégration
Sauvegarde avant intégration :

text

output/questions_taggees.before_khelifa1_vol04.json
Questions ajoutées :

text

q_khelifa1_vol04_001 → q_khelifa1_vol04_023
Validation globale
text

Total questions : 192
Concepts distincts utilisés : 35
Erreurs : 0
Avertissements : 0
Distribution des 23 questions ajoutées
Concepts principaux
Micro-concept	Questions
mc_prot_01 — Transcription de l'ADN	9
mc_prot_02 — Traduction de l'ARNm	1
mc_prot_03 — Code génétique	3
mc_prot_04 — ARNm	2
mc_prot_05 — ARNt	2
mc_prot_06 — Ribosome	3
mc_prot_08 — Élongation et terminaison	2
mc_struc_01 — Structure primaire	1
Types
Type	Questions
analyse_document	6
raisonnement	6
application	5
schema_a_completer	3
definition	3
Difficulté
Difficulté	Questions
facile	5
moyenne	17
difficile	1
Thèmes principaux
Polysomes et traduction simultanée chez les bactéries
Transcription par ARN polymérase
ARNm comme intermédiaire noyau → cytoplasme
ARNt et activation des acides aminés
Ribosomes et nécessité de leur présence pour la synthèse protéique
Codons, anticodons, codons stop
Mutations : substitution, insertion, délétion
Structure primaire/tertiaire de l'insuline
Open
Oui ✅ J’ai traité KHELIFA1_VOLUME_04 entièrement.

1. OCR production v3 terminé
PDF traité :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_04.pdf
Sortie OCR :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_04.ocr_prod_full.txt
Bundle :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_04.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Erreurs OCR : 0
Total caractères OCR : 61,362
Taille fichier final : 98,082 bytes
Confiance moyenne : 65.05
Quality warning : aucun
Durée : ~13 min 51 s
2. Phase 2 extraction terminée
Fichier créé :

text

downloads/annales_svt/output/khelifa1_volume04_extraction_phase2.json
Résultat :

text

23 blocs candidats extraits
3. Phase 3 intégration terminée
Sauvegarde créée :

text

downloads/annales_svt/output/questions_taggees.before_khelifa1_vol04.json
Questions ajoutées dans :

text

downloads/annales_svt/output/questions_taggees.json
IDs ajoutés :

text

q_khelifa1_vol04_001
→
q_khelifa1_vol04_023
4. Validation globale
Validation lancée avec :

Bash

python scripts/validate_tags.py output/questions_taggees.json
Résultat :

text

Total questions : 192
Concepts distincts utilisés : 35
Erreurs : 0
Avertissements : 0
✅ JSON valide.

5. Distribution des 23 questions ajoutées
Concepts principaux
text

mc_prot_01  Transcription de l'ADN          9
mc_prot_02  Traduction de l'ARNm             1
mc_prot_03  Code génétique                   3
mc_prot_04  ARNm                             2
mc_prot_05  ARNt                             2
mc_prot_06  Ribosome                         3
mc_prot_08  Élongation et terminaison        2
mc_struc_01 Structure primaire               1
Types
text

analyse_document       6
raisonnement           6
application            5
schema_a_completer     3
definition             3
Difficulté
text

facile       5
moyenne     17
difficile    1
6. Rapport créé
text

downloads/annales_svt/output/khelifa1_volume04_phase3_integration.md
7. Manifeste mis à jour
text

downloads/annales_svt/MANIFESTE.md
Nouvel état global :

text

192 questions traitées
35 micro-concepts couverts sur 42
0 erreur
0 avertissement
Prochaine étape
Continuer avec :

text

KHELIFA1_VOLUME_05
Mais ce PDF n’est pas encore dans le workspace actuel. Il faudra le télécharger depuis Dropbox, puis appliquer :

OCR production v3
extraction phase 2
intégration JSON
validation.

bilan et vérification du ocr production



Explored
2 reads
Read
khelifa1_volume04_phase3_integration.md
Read
MANIFESTE.md
Voici le bilan + vérification du pipeline OCR production.

1. Vérification du script OCR production
Script principal :

text

downloads/annales_svt/scripts/ocr_pipeline_production.py
Copie à la racine du workspace :

text

ocr_pipeline_production.py
Vérification compilation :

text

OK — le script compile sans erreur
Taille du script :

text

55 KB
Tesseract est disponible :

text

/usr/bin/tesseract
Langues installées :

text

ara
eng
fra
osd
Donc le pipeline ara+fra est opérationnel ✅

2. Fonctionnalités validées du script OCR production
Le script actuel gère bien :

PDF scannés
OCR ara+fra
PyMuPDF pour rendu PDF
OpenCV pour prétraitement
reprise par page via run.state.json
sortie texte réassemblée
bundle par volume
logs disque
rapport CSV
rapport JSON summary.json
métriques de confiance OCR
détection d’erreurs OCR
validation par page via pages_meta
version corrigée du cache PDF worker
Structure produite pour chaque volume :

text

KHELIFA1_VOLUME_XX.ocr_prod_full.txt
KHELIFA1_VOLUME_XX.ocr_prod_full.txt.bundle/
├── pages_txt/
├── pages_meta/
├── pages_pdf/
├── run.state.json
├── summary.json
├── pages.csv
└── run.log
3. Bilan OCR KHELIFA 1 volumes 01 à 04
Volume	Pages	Erreurs OCR	Caractères OCR	Confiance moyenne	Quality warning	Bundle
KHELIFA1 Vol 01	28	0	133,913	57.59	Aucun	✅
KHELIFA1 Vol 02	28	0	53,838	60.45	Aucun	✅
KHELIFA1 Vol 03	28	0	58,629	65.29	Aucun	✅
KHELIFA1 Vol 04	28	0	61,362	65.05	Aucun	✅
Total OCR KHELIFA 1 Vol 01-04 :

text

112 pages OCRisées
0 erreur OCR
307,742 caractères OCR
Tous les volumes ont :

text

state.json : OK
pages.csv : OK
run.log : OK
pages_meta : 28 fichiers par volume
4. Bilan extraction + intégration JSON
Fichier principal :

text

downloads/annales_svt/output/questions_taggees.json
Validation actuelle :

text

Total questions : 192
Concepts distincts utilisés : 35
Erreurs : 0
Avertissements : 0
Donc le JSON final est propre ✅

5. Questions intégrées par source
Source	Questions intégrées
FINALBAC Vol 3	50
FINALBAC Vol 4	30
KHELIFA1 Vol 01	32
KHELIFA1 Vol 02	29
KHELIFA1 Vol 03	28
KHELIFA1 Vol 04	23
Total	192
6. État actuel du traitement
Source	Volume	État
FINALBAC Vol 1	cours	OCR fait, non taggé
FINALBAC Vol 2	cours	OCR partiel
FINALBAC Vol 3	sujets	✅ intégré
FINALBAC Vol 4	sujets/corrigés	✅ intégré
FINALBAC Vol 5	corrigés	OCR partiel, à extraire
FINALBAC Vol 6	corrigés	OCR partiel, à extraire
KHELIFA1 Vol 01	exercices	✅ OCR + extraction + intégration
KHELIFA1 Vol 02	exercices	✅ OCR + extraction + intégration
KHELIFA1 Vol 03	exercices/corrigés	✅ OCR + extraction + intégration
KHELIFA1 Vol 04	exercices/corrigés	✅ OCR + extraction + intégration
KHELIFA1 Vol 05-15	exercices	⬜ à télécharger/traiter
KHELIFA2 Vol 01-15	exercices	⬜ à traiter
MORAFIK Vol 1-3	cours/sujets	⬜ à traiter
7. Points forts du pipeline actuel
✅ Très stable sur les 4 volumes KHELIFA traités
✅ Aucun crash OCR sur les runs complets
✅ Aucun avertissement qualité global
✅ Rapports exploitables volume par volume
✅ Reprise possible via --resume
✅ Sorties traçables et auditables
✅ Compatible avec les scans arabes/français

8. Point d’attention
Même si l’OCR est techniquement propre, les séquences nucléotidiques exactes restent parfois à vérifier visuellement, car une seule lettre mal lue peut changer :

une ramزة,
un acide aminé,
une mutation,
le tag final.
Donc pour un usage scientifique très strict, il faut relire les séquences ADN/ARN dans les images originales.

9. Prochaine étape recommandée
Continuer avec :

text

KHELIFA1_VOLUME_05
Mais il faut d’abord le télécharger depuis Dropbox, car dans le workspace actuel les PDF présents sont surtout :

text

KHELIFA1_VOLUME_01.pdf
KHELIFA1_VOLUME_02.pdf
KHELIFA1_VOLUME_03.pdf
KHELIFA1_VOLUME_04.pdf
Ensuite même pipeline :

Bash

python scripts/ocr_pipeline_production.py input/KHELIFA1_VOLUME_05.pdf \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt

CONTINUE



Ran commands
2
Write
downloads/annales_svt/output/khelifa1_volume05_extraction_phase2.json
245 lines
open
khelifa1_volume05_extraction_phase2.json
JSON
[
  {
    "candidate_id": "khelifa1_vol05_c001",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [2],
    "theme": "الطفرات وتغير بنية الإنزيم ووظيفته",
    "texte_corrige": "أدت طفرات مختلفة إلى ظهور أليلات تشفر لإنزيمات تختلف عن الإنزيم العادي بحمض أميني واحد. فسر كيف يؤدي تغير ترتيب الأحماض الأمينية إلى تغير البنية الفراغية للإنزيم، ومنه فقدان الوظيفة وظهور المرض.",
    "sous_questions": ["تعريف الطفرة المغيرة للمعنى", "ربط تغير الحمض الأميني بالبنية الفراغية", "تفسير فقدان وظيفة الإنزيم"],
    "micro_concepts_probables": ["mc_struc_05", "mc_struc_03", "mc_prot_03"],
    "qualite_ocr": "bonne",
    "notes": "مدخل يربط الطفرة ببنية/وظيفة الإنزيم."
  },
  {
    "candidate_id": "khelifa1_vol05_c002",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [3],
    "theme": "ADN وARNm والريبوزومات ومتعدد الببتيد",
    "texte_corrige": "تعرف على البيانات المرقمة في مخطط التعبير المورثي: ADN، ARNm، اتجاه الاستنساخ، الريبوزومات ومتعدد الببتيد. حدد السلسلة غير المستنسخة وسلسلة ARNm، ثم بين علاقة المورثة بالرسالة والبروتين الناتج.",
    "sous_questions": ["تسمية ADN وARNm والريبوزومات", "تحديد اتجاه النسخ", "ربط ARNm بمتعدد الببتيد"],
    "micro_concepts_probables": ["mc_prot_01", "mc_prot_04", "mc_prot_06"],
    "qualite_ocr": "bonne",
    "notes": "انتقال من مجال البروتينات إلى التعبير المورثي."
  },
  {
    "candidate_id": "khelifa1_vol05_c003",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [4],
    "theme": "حذف ثلاث قواعد وأثره على البروتين",
    "texte_corrige": "إذا حذفت ثلاث قواعد من الأليل غير العادي في المورثة، فسر أثر هذا الحذف على قراءة ARNm وعلى السلسلة الببتيدية. قارن بين الحذف الذي يحافظ على إطار القراءة والحذف الذي يغير إطار القراءة، واستنتج أثر ذلك على البروتين.",
    "sous_questions": ["تحليل حذف ثلاث نيوكليوتيدات", "تحديد أثر الحذف على إطار القراءة", "ربط الحذف بالبروتين"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "مستخرج من تصحيح حول أليل غير عادي."
  },
  {
    "candidate_id": "khelifa1_vol05_c004",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [5],
    "theme": "تجربة متعدد اليوراسيل واكتشاف الشفرة الوراثية",
    "texte_corrige": "في وجود ARNm مكون من متعدد اليوراسيل يتشكل راسب ببتيدي عند إضافة الفينيل ألانين فقط، بينما لا يتشكل الراسب عند إضافة أحماض أمينية أخرى مثل الألانين أو الفالين. استنتج أن UUU يرمز للحمض الأميني Phe، وأن كل ثلاثية نيوكليوتيدات تعبر عن حمض أميني واحد.",
    "sous_questions": ["تحليل تجربة متعدد اليوراسيل", "استنتاج الرامزة UUU", "استخلاص مبدأ الشفرة الوراثية"],
    "micro_concepts_probables": ["mc_prot_03", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "تجربة كلاسيكية لفك الشفرة الوراثية."
  },
  {
    "candidate_id": "khelifa1_vol05_c005",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [6],
    "theme": "متطلبات تركيب البروتين في أنابيب اختبار",
    "texte_corrige": "قورنت أوساط تحتوي على مستخلص خلوي كامل أو على ميتوكوندريات وميكروزومات. فسر لماذا يكون تركيب البروتين كبيرا في المستخلص الكامل أو في وجود الميكروزومات، واستنتج العناصر الضرورية لتركيب البروتين داخل الخلية.",
    "sous_questions": ["تحليل نتائج الأوساط المختلفة", "تحديد دور الميكروزومات/الريبوزومات", "استنتاج شروط تركيب البروتين"],
    "micro_concepts_probables": ["mc_prot_06", "mc_prot_02"],
    "qualite_ocr": "bonne",
    "notes": "الميكروزومات تشير غالبا إلى أجزاء من الشبكة الهيولية المحببة والريبوزومات."
  },
  {
    "candidate_id": "khelifa1_vol05_c006",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [7],
    "theme": "خصائص ARNm من حقنه وتتبع كمية البروتين",
    "texte_corrige": "عند حقن ARNm معين يتشكل بروتين نوعي ثم تختفي فعاليته تدريجيا وتتوقف كمية البروتين عن الزيادة. استنتج أن ARNm يحمل معلومة وراثية تحدد نوع البروتين المراد تصنيعه، وأنه جزيء مؤقت العمر داخل الخلية.",
    "sous_questions": ["تحليل أثر حقن ARNm", "استنتاج أن ARNm يحمل معلومة نوعية", "تفسير زوال فعالية ARNm"],
    "micro_concepts_probables": ["mc_prot_04", "mc_prot_02"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة ضعيفة جزئيا لكنها واضحة حول ARNm."
  },
  {
    "candidate_id": "khelifa1_vol05_c007",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [10],
    "theme": "إماهة الببتيدات بإنزيم التريبسين",
    "texte_corrige": "يدرس تأثير إنزيم التريبسين الذي يكسر الروابط الببتيدية بعد أحماض أمينية محددة مثل الأرجينين Arg والليزين Lys. حدد نتائج إماهة ببتيدات معطاة، واستنتج نوعية عمل الإنزيم تجاه مواضع معينة في السلسلة الببتيدية.",
    "sous_questions": ["تحديد مواضع قطع التريبسين", "استنتاج نوعية الإنزيم", "تفسير الإماهة الإنزيمية للببتيدات"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_01", "mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "السؤال حول نوعية إنزيمات البروتياز."
  },
  {
    "candidate_id": "khelifa1_vol05_c008",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [11],
    "theme": "تشكيل ببتيدات من أحماض أمينية واحتمالات الصيغ",
    "texte_corrige": "نرغب في تشكيل مركب ناتج عن اتحاد عدة أحماض أمينية بروابط ببتيدية. سم المركب الناتج، واحسب عدد احتمالات صيغه الكيميائية مع التعليل الرياضي، ثم اكتب صيغة واحدة وحدد المجاميع الوظيفية الموجودة فيها وشحنة المركب في وسط حامضي قوي ووسط قاعدي قوي.",
    "sous_questions": ["تعريف الببتيد", "حساب احتمالات ترتيب الأحماض الأمينية", "كتابة صيغة ببتيدية", "تحديد المجاميع الوظيفية والشحنة"],
    "micro_concepts_probables": ["mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "كيمياء البروتينات والأحماض الأمينية."
  },
  {
    "candidate_id": "khelifa1_vol05_c009",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [12,13],
    "theme": "تأثير مادة غذائية سكرية على نمو الخميرة وتجارب إنزيمية",
    "texte_corrige": "تحلل تجارب تقيس تغير تركيز مادة غذائية سكرية أو نمو الخميرة مع الزمن في أوساط مختلفة. حدد العلاقة بين توفر المادة الغذائية ونشاط الخميرة، واستنتج وجود نشاط إنزيمي يسمح باستعمال السكريات وتحويلها.",
    "sous_questions": ["تحليل المنحنيات", "ربط المادة الغذائية بنشاط الخميرة", "استنتاج تدخل إنزيمات في استعمال السكر"],
    "micro_concepts_probables": ["mc_enz_03", "mc_enz_04"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحات تحتوي منحنيات وجداول يصعب استخراجها نصيا."
  },
  {
    "candidate_id": "khelifa1_vol05_c010",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [14],
    "theme": "حركية تفاعل إنزيمي وتأثير الحرارة",
    "texte_corrige": "تمثل الوثيقة حركية تفاعل إنزيمي وتفاعل كيميائي. قارن السرعتين واستنتج مميزات التفاعل الإنزيمي. حدد ما تمثله درجات الحرارة على المنحنى، وفسر تأثير الحرارة على النشاط الإنزيمي في بضعة أسطر.",
    "sous_questions": ["مقارنة التفاعل الإنزيمي والكيميائي", "تحديد سرعة التفاعل", "تفسير تأثير الحرارة", "استخراج درجة الحرارة المثلى"],
    "micro_concepts_probables": ["mc_enz_03", "mc_enz_04"],
    "qualite_ocr": "bonne",
    "notes": "حركية إنزيمية وعامل الحرارة."
  },
  {
    "candidate_id": "khelifa1_vol05_c011",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [15],
    "theme": "نقطة التعادل الكهربائي للأحماض الأمينية والهجرة الكهربائية",
    "texte_corrige": "تعرض وثيقة هجرة كهربائية لأحماض أمينية عند قيم مختلفة من pH. استنتج قيم نقطة التعادل الكهربائي pHi للأحماض الأمينية الثلاثة، وقارن بينها، وفسر اختلاف مسافة الهجرة بين الأحماض الأمينية حسب شحنتها في الوسط.",
    "sous_questions": ["تعريف pHi", "استنتاج قيم pHi", "تفسير الهجرة الكهربائية", "ربط الشحنة بقيمة pH"],
    "micro_concepts_probables": ["mc_struc_01"],
    "qualite_ocr": "bonne",
    "notes": "كيمياء أحماض أمينية؛ أقرب مفهوم هو البنية الأولية/الأحماض الأمينية."
  },
  {
    "candidate_id": "khelifa1_vol05_c012",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [16],
    "theme": "إنزيم اللاكتاز وتأثير ثيولاكتوز على سرعة التفاعل",
    "texte_corrige": "يدرس إنزيم اللاكتاز إماهة سكر اللاكتوز وتقاس سرعة التفاعل في وجود أو غياب الثيولاكتوز. حلل النتائج، وفسر تأثير إضافة الثيولاكتوز على نشاط اللاكتاز، واستنتج إن كان يؤثر كمثبط أو كمادة مشابهة للركيزة.",
    "sous_questions": ["تحليل سرعة إماهة اللاكتوز", "تفسير أثر الثيولاكتوز", "استنتاج طبيعة التأثير على الإنزيم"],
    "micro_concepts_probables": ["mc_enz_05", "mc_enz_02", "mc_enz_03"],
    "qualite_ocr": "moyenne",
    "notes": "يبدو سؤال تثبيط/تشابه ركيزة."
  },
  {
    "candidate_id": "khelifa1_vol05_c013",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [18],
    "theme": "pHi للأحماض الأمينية وجفاف الجلد المصطبغ بالأشعة UV",
    "texte_corrige": "استنتج pHi لأحماض أمينية من نتائج الهجرة الكهربائية، ثم فسر في جزء آخر كيف يؤدي التعرض المطول للأشعة فوق البنفسجية إلى ظهور مرض جلدي نتيجة تأثير مباشر على ADN مسبب لطفرات.",
    "sous_questions": ["استنتاج نقطة التعادل الكهربائي", "تفسير أثر UV على ADN", "ربط الطفرة بظهور المرض"],
    "micro_concepts_probables": ["mc_prot_03", "mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة تجمع موضوعين؛ اعتُمد المحور الوراثي/البروتيني."
  },
  {
    "candidate_id": "khelifa1_vol05_c014",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [19],
    "theme": "إنزيم الكيموتريبسين وبنية الموقع الفعال",
    "texte_corrige": "إن إنزيم الكيموتريبسين يحفز إماهة الروابط الببتيدية بجانب أحماض أمينية معينة. اعتمادا على طبيعة الأحماض الأمينية المكونة للموقع الفعال مثل السيرين والهستيدين وأحماض كارهة أو محبة للماء، فسر نوعية الإنزيم وقدرته على التعرف على الركيزة.",
    "sous_questions": ["تحديد دور أحماض أمينية في الموقع الفعال", "تفسير نوعية الكيموتريبسين", "ربط بنية الموقع الفعال بالتعرف على الركيزة"],
    "micro_concepts_probables": ["mc_enz_01", "mc_enz_02", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "مفهوم الموقع الفعال والنوعية."
  },
  {
    "candidate_id": "khelifa1_vol05_c015",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [20],
    "theme": "تصنيف مواد التفاعل في معادلة إنزيمية",
    "texte_corrige": "اعتمادا على تفاعلات إنزيمية تتضمن الغلوكوز، غلوكوز-6-فوسفات، ATP وADP، صنف المواد حسب موقعها في معادلة التفاعل: مادة تفاعل، ناتج، عامل مساعد أو ناقل للطاقة. ثم حدد مادة التفاعل التي يعمل عليها الإنزيم المدروس.",
    "sous_questions": ["تصنيف مواد التفاعل", "تحديد الركيزة", "تحديد الناتج", "ربط ATP/ADP بالطاقة"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_01"],
    "qualite_ocr": "bonne",
    "notes": "نوعية إنزيمية وتصنيف مكونات التفاعل."
  },
  {
    "candidate_id": "khelifa1_vol05_c016",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [21],
    "theme": "دور الإنزيم والنوعية تجاه مادة التفاعل",
    "texte_corrige": "من خلال تحليل النتائج التجريبية، حدد دور الإنزيم في التفاعل، واشرح كيف تبرهن النتائج على النوعية الإنزيمية بالنسبة لمادة التفاعل. توقع النتائج عند رفع درجة الحرارة إلى قيمة مرتفعة، وعلل ذلك اعتمادا على تأثير الحرارة في بنية الإنزيم.",
    "sous_questions": ["تحديد دور الإنزيم", "إثبات النوعية تجاه الركيزة", "توقع أثر الحرارة العالية", "ربط الحرارة بالبنية الوظيفية"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_04", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "نوعية وتأثير حرارة."
  },
  {
    "candidate_id": "khelifa1_vol05_c017",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [22],
    "theme": "النشاط الإنزيمي وعلاقته ببنية الإنزيم",
    "texte_corrige": "لدراسة النشاط الإنزيمي وعلاقته ببنية الإنزيم، نقيس سرعة التفاعل في ظروف ثابتة من درجة الحرارة وpH مع تغيير عامل محدد. حلل النتائج، واستنتج كيف تتحدد فعالية الإنزيم ببنية موقعه الفعال وبالظروف الفيزيائية الكيميائية للوسط.",
    "sous_questions": ["تحليل نشاط إنزيمي", "ربط النشاط ببنية الموقع الفعال", "تحديد أثر pH والحرارة", "استنتاج شروط النشاط الأمثل"],
    "micro_concepts_probables": ["mc_enz_01", "mc_enz_04", "mc_enz_03"],
    "qualite_ocr": "moyenne",
    "notes": "موضوع عام حول بنية/نشاط الإنزيم."
  },
  {
    "candidate_id": "khelifa1_vol05_c018",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [23],
    "theme": "أكسدة الغلوكوز والنوعية بين الغلوكوز والفركتوز",
    "texte_corrige": "في تجربتين نقيس تغيرات تركيز الأكسجين في وسط التفاعل بوجود نفس تركيز الإنزيم، مع إضافة سكر الغلوكوز أو الفركتوز في درجة حرارة وpH ثابتين. حلل النتائج، واستنتج نوعية الإنزيم بالنسبة للركيزة وقدرته على أكسدة الغلوكوز دون الفركتوز.",
    "sous_questions": ["تحليل تغير تركيز O2", "مقارنة الغلوكوز والفركتوز", "استنتاج النوعية الإنزيمية"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_03"],
    "qualite_ocr": "bonne",
    "notes": "تجربة نوعية إنزيمية مع غلوكوز أوكسيداز غالبا."
  },
  {
    "candidate_id": "khelifa1_vol05_c019",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [24],
    "theme": "تأثير الحرارة على سرعة التفاعل الإنزيمي",
    "texte_corrige": "أجريت تجارب بنفس تركيز الإنزيم ونفس تركيز مادة التفاعل مع تغيير درجة الحرارة. حلل جدول النتائج، وارسم أو استنتج منحنى تغير سرعة التفاعل بدلالة الحرارة، ثم فسر وجود درجة حرارة مثلى وانخفاض النشاط عند درجات الحرارة العالية.",
    "sous_questions": ["تحليل جدول الحرارة", "استنتاج سرعة التفاعل", "تحديد الحرارة المثلى", "تفسير التخرب الحراري للإنزيم"],
    "micro_concepts_probables": ["mc_enz_04", "mc_enz_03", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "عامل الحرارة في النشاط الإنزيمي."
  },
  {
    "candidate_id": "khelifa1_vol05_c020",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [25,26],
    "theme": "استعمال مدخرات النشاء أثناء إنبات بذور القمح",
    "texte_corrige": "تحتوي بذور القمح على مدخرات نشوية في السويداء تستعمل من قبل الرشيم أثناء الإنبات. باستغلال المعطيات التجريبية، حدد الآليات التي تسمح باستعمال مدخرات النشاء، وفسر دور الإنزيمات المحللة للنشاء في تحرير مواد قابلة للاستعمال من طرف الرشيم.",
    "sous_questions": ["تحليل تجربة بذور القمح", "تحديد دور النشاء", "استنتاج تدخل إنزيمات التحلل", "ربط الإنبات باستعمال المدخرات"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_03"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحتان فيهما ضجيج لكن الموضوع واضح حول إنزيمات النشاء."
  },
  {
    "candidate_id": "khelifa1_vol05_c021",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [27],
    "theme": "الأرابينوز كمثبط لإنزيم غلوكوز أوكسيداز",
    "texte_corrige": "يجري التفاعل بتركيز ثابت للغلوكوز وبوجود سكر آخر هو الأرابينوز بتراكيز مختلفة، ثم تقاس سرعة أكسدة الغلوكوز. اشرح لماذا يمكن اعتبار الأرابينوز مثبطا لإنزيم غلوكوز أوكسيداز، واقترح فرضية لتفسير تأثيره على نشاط الإنزيم.",
    "sous_questions": ["تحليل تغير السرعة مع تركيز الأرابينوز", "تحديد التثبيط", "اقتراح فرضية حول التنافس مع الغلوكوز"],
    "micro_concepts_probables": ["mc_enz_05", "mc_enz_02", "mc_enz_03"],
    "qualite_ocr": "bonne",
    "notes": "تثبيط إنزيمي محتمل تنافسي."
  },
  {
    "candidate_id": "khelifa1_vol05_c022",
    "source": "KHELIFA1_VOLUME_05.pdf",
    "pages": [28],
    "theme": "ثنائي وثلاثي الببتيد ونقطة التعادل الكهربائي",
    "texte_corrige": "شكل ثنائي الببتيد ألانين-ليسين ثم ثلاثي الببتيد ألانين-ليسين-حمض الأسبارتيك. عرف نقطة التعادل الكهربائي pHi، ومثل توضع الأحماض الأمينية أو الببتيدات على ورقة جهاز الرحلان الكهربائي حسب شحنتها في الوسط، ثم فسر اختلاف اتجاه الهجرة.",
    "sous_questions": ["تشكيل ببتيد", "تعريف pHi", "تفسير الرحلان الكهربائي", "ربط الشحنة بالهجرة"],
    "micro_concepts_probables": ["mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "كيمياء الأحماض الأمينية والببتيدات."
  }
]
Open

Edited files
2
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02	28	Exercices	✅ OCR production complet + ✅ extraction/intégration (khelifa1_volume02_extraction_phase2.json)	29 questions
KHELIFA 1	Vol 03	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume03_extraction_phase2.json)	28 questions
KHELIFA 1	Vol 04	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume04_extraction_phase2.json)	23 questions
KHELIFA 1	Vol 05	28	Exercices/corrigés	✅ téléchargé + ✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume05_extraction_phase2.json)	22 questions
KHELIFA 1	Vol 06-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 214 questions / 35 micro-concepts couverts sur 42
Derniers ajouts
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
KHELIFA 1 Vol 02 : 29 questions ajoutées (q_khelifa1_vol02_001 à q_khelifa1_vol02_029)
KHELIFA 1 Vol 03 : 28 questions ajoutées (q_khelifa1_vol03_001 à q_khelifa1_vol03_028)
KHELIFA 1 Vol 04 : 23 questions ajoutées (q_khelifa1_vol04_001 à q_khelifa1_vol04_023)
KHELIFA 1 Vol 05 : 22 questions ajoutées (q_khelifa1_vol05_001 à q_khelifa1_vol05_022)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
KHELIFA 1 Volume 05 — OCR production v3 + extraction/intégration
MD
KHELIFA 1 Volume 05 — OCR production v3 + extraction/intégration
Source : input/KHELIFA1_VOLUME_05.pdf

Téléchargement
PDF téléchargé depuis Dropbox :

text

input/KHELIFA1_VOLUME_05.pdf
Taille : ~22 MB
Pages : 28

OCR production v3
Sortie :

text

input/KHELIFA1_VOLUME_05.ocr_prod_full.txt
input/KHELIFA1_VOLUME_05.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Méthode : OCR sur 28 pages
Erreurs : 0
Total caractères OCR : 52,817
Taille fichier texte final : 83,883 bytes
Confiance moyenne : 63.35
Quality warning : aucun
Durée : ~15 min 50 s
Phase 2 — Extraction
Fichier créé :

text

output/khelifa1_volume05_extraction_phase2.json
Résultat : 22 blocs candidats extraits.

Phase 3 — Intégration
Sauvegarde avant intégration :

text

output/questions_taggees.before_khelifa1_vol05.json
Questions ajoutées :

text

q_khelifa1_vol05_001 → q_khelifa1_vol05_022
Validation globale
text

Total questions : 214
Concepts distincts utilisés : 35
Erreurs : 0
Avertissements : 0
Distribution des 22 questions ajoutées
Concepts principaux
Micro-concept	Questions
mc_enz_01 — Site actif de l'enzyme	2
mc_enz_02 — Spécificité enzymatique	5
mc_enz_03 — Vitesse de réaction enzymatique	2
mc_enz_04 — Facteurs influençant l'activité	1
mc_enz_05 — Inhibition enzymatique	2
mc_prot_01 — Transcription de l'ADN	1
mc_prot_03 — Code génétique	3
mc_prot_04 — ARNm	1
mc_prot_06 — Ribosome	1
mc_struc_01 — Structure primaire	3
mc_struc_05 — Relation structure-fonction	1
Types
Type	Questions
analyse_document	13
raisonnement	4
application	4
schema_a_completer	1
Difficulté
Difficulté	Questions
moyenne	19
difficile	3
Thèmes principaux
Mutations et enzymes non fonctionnelles
Code génétique et expérience poly-U
Conditions de synthèse protéique in vitro
Protéases : trypsine, chymotrypsine
Structure des peptides et acides aminés
pHi et électrophorèse
Cinétique enzymatique
Température, pH et activité enzymatique
Spécificité enzymatique
Inhibition : arabinose / glucose oxydase
Enzymes de mobilisation des réserves d'amidon pendant la germination
Open
J’ai continué avec KHELIFA1_VOLUME_05 ✅

1. Téléchargement
Le PDF n’était pas encore dans le workspace, donc je l’ai téléchargé depuis Dropbox.

Fichier :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_05.pdf
Taille :

text

~22 MB
Pages :

text

28
2. OCR production v3 terminé
Sortie OCR :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_05.ocr_prod_full.txt
Bundle :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_05.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Erreurs OCR : 0
Total caractères OCR : 52,817
Taille fichier final : 83,883 bytes
Confiance moyenne : 63.35
Quality warning : aucun
Durée : ~15 min 50 s
3. Extraction phase 2 terminée
Fichier créé :

text

downloads/annales_svt/output/khelifa1_volume05_extraction_phase2.json
Résultat :

text

22 blocs candidats extraits
Thèmes principaux :

enzymes non fonctionnelles après mutation,
code génétique / poly-U,
synthèse protéique in vitro,
trypsine / chymotrypsine,
structure des peptides,
pHi et électrophorèse,
cinétique enzymatique,
température / pH / activité enzymatique,
spécificité enzymatique,
inhibition enzymatique,
enzymes et germination du blé.
4. Intégration phase 3 terminée
Sauvegarde créée :

text

downloads/annales_svt/output/questions_taggees.before_khelifa1_vol05.json
Questions ajoutées dans :

text

downloads/annales_svt/output/questions_taggees.json
IDs ajoutés :

text

q_khelifa1_vol05_001
→
q_khelifa1_vol05_022
5. Validation globale
Résultat :

text

Total questions : 214
Concepts distincts utilisés : 35
Erreurs : 0
Avertissements : 0
✅ JSON toujours valide.

6. Distribution des 22 questions ajoutées
Concepts principaux
text

mc_enz_01    Site actif de l'enzyme              2
mc_enz_02    Spécificité enzymatique             5
mc_enz_03    Vitesse de réaction enzymatique     2
mc_enz_04    Facteurs influençant l'activité     1
mc_enz_05    Inhibition enzymatique              2
mc_prot_01   Transcription de l'ADN              1
mc_prot_03   Code génétique                      3
mc_prot_04   ARNm                                1
mc_prot_06   Ribosome                            1
mc_struc_01  Structure primaire                  3
mc_struc_05  Relation structure-fonction         1
Types
text

analyse_document       13
raisonnement            4
application             4
schema_a_completer      1
7. Rapport créé
text

downloads/annales_svt/output/khelifa1_volume05_phase3_integration.md
8. Manifeste mis à jour
Nouvel état global :

text

214 questions traitées
35 micro-concepts couverts sur 42
0 erreur
0 avertissement
Prochaine étape :

text

KHELIFA1_VOLUME_06
Je peux continuer : télécharger le volume 06, OCR production v3, extraction, intégration, validation.


continue



Ran commands
2
Write
downloads/annales_svt/output/khelifa1_volume06_extraction_phase2.json
223 lines
open
khelifa1_volume06_extraction_phase2.json
JSON
[
  {
    "candidate_id": "khelifa1_vol06_c001",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [1, 2],
    "theme": "بنية الهيموغلوبين HbA/HbS وعلاقة البنية بالوظيفة",
    "texte_corrige": "في إطار دراسة العلاقة بين بنية البروتين ووظيفته، تعرض الوثائق البنية الفراغية لمادة موجودة داخل الكريات الدموية الحمراء وعلاقتها بنقل الغازات. تعرف على هذه المادة وبنيتها، ثم حدد أصل مرض فقر الدم المنجلي من خلال تغير بنية الخضاب HbA/HbS وعلاقة ذلك بتغير الشحنة والهجرة الكهربائية.",
    "sous_questions": ["تعرف الهيموغلوبين", "ربط البنية الفراغية بالوظيفة", "تفسير أصل HbS", "تحليل الهجرة الكهربائية"],
    "micro_concepts_probables": ["mc_struc_05", "mc_struc_03", "mc_prot_03"],
    "qualite_ocr": "moyenne",
    "notes": "بداية volume مرتبطة بالخضاب وبنية البروتين؛ OCR جزئي."
  },
  {
    "candidate_id": "khelifa1_vol06_c002",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [3],
    "theme": "الموقع الفعال لإنزيم كاربوكسي ببتيداز",
    "texte_corrige": "تمثل الوثيقة الأحماض الأمينية المشكلة للموقع الفعال لإنزيم كاربوكسي ببتيداز. قارن بين وضعية الأحماض الأمينية في غياب مادة التفاعل وفي وجودها، واستنتج علاقة بنية الموقع الفعال بالتعرف على الركيزة وتحفيز التفاعل.",
    "sous_questions": ["تعرف الموقع الفعال", "مقارنة البنية قبل وبعد ارتباط الركيزة", "استنتاج التكامل البنيوي", "تفسير عمل الإنزيم"],
    "micro_concepts_probables": ["mc_enz_01", "mc_enz_02", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "ذكر واضح لإنزيم Carboxypeptidase والموقع الفعال."
  },
  {
    "candidate_id": "khelifa1_vol06_c003",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [4,5],
    "theme": "سلوك بروتين زلال البيض في الرحلان الكهربائي",
    "texte_corrige": "لدراسة سلوك بروتين زلال البيض في جهاز الفصل الكهربائي، توضع قطرة من محلول الزلال على ورقة ترشيح ضمن مجال كهربائي وبقيم مختلفة من pH. حلل اتجاه هجرة البروتين، واستنتج شحنته حسب pH الوسط وعلاقة ذلك بنقطة التعادل الكهربائي وببنية البروتين.",
    "sous_questions": ["تحليل الرحلان الكهربائي", "تحديد شحنة البروتين", "ربط pH بنقطة التعادل الكهربائي", "استنتاج خصائص البروتين"],
    "micro_concepts_probables": ["mc_struc_01", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "الوثائق والجداول غير مستخرجة بالكامل."
  },
  {
    "candidate_id": "khelifa1_vol06_c004",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [6],
    "theme": "حلمأة البروتين وتصنيف البنيات البروتينية",
    "texte_corrige": "انطلاقا من نتيجة حلمأة البروتين، استنتج أن البروتين مكون من أحماض أمينية مرتبطة بروابط ببتيدية. صنف البنيات الممثلة في الوثيقة حسب مستويات تنظيم البروتين، ووضح المعايير المعتمدة في التصنيف.",
    "sous_questions": ["استنتاج نواتج حلمأة البروتين", "تعريف الرابطة الببتيدية", "تصنيف مستويات البنية", "ربط البنية بالوظيفة"],
    "micro_concepts_probables": ["mc_struc_01", "mc_struc_03", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "سؤال بنية البروتين."
  },
  {
    "candidate_id": "khelifa1_vol06_c005",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [7],
    "theme": "بنية إنزيم الريبونوكلياز وأثر تكسير الجسور الكبريتية",
    "texte_corrige": "تعرض الوثيقة البنية الفراغية لإنزيم الريبونوكلياز في شكله الوظيفي، ثم البنية بعد معالجته ببيتا-ميركابتوإيثانول الذي يكسر الجسور الكبريتية وباليوريا التي تعيق الانطواء. قارن بين البنيتين، واستنتج العلاقة بين البنية الفراغية للإنزيم ونشاطه الوظيفي.",
    "sous_questions": ["مقارنة البنية الوظيفية وغير الوظيفية", "تفسير دور الجسور الكبريتية", "تفسير أثر اليوريا", "ربط البنية بالنشاط الإنزيمي"],
    "micro_concepts_probables": ["mc_struc_03", "mc_struc_05", "mc_enz_01"],
    "qualite_ocr": "bonne",
    "notes": "سؤال كلاسيكي عن RNase والطي البروتيني."
  },
  {
    "candidate_id": "khelifa1_vol06_c006",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [8],
    "theme": "تفاعل بين حمضين أمينيين وصيغة الرابطة الببتيدية",
    "texte_corrige": "تعرف على العناصر المرقمة في بنية حمضين أمينيين، واشرح كيف يتم الارتباط بينهما. اكتب الصيغة الكيميائية الناتجة عن اتحاد الحمضين، وحدد الرابطة المتشكلة ونوع التفاعل المصاحب لها.",
    "sous_questions": ["تعرف المجاميع الوظيفية", "كتابة تفاعل اتحاد حمضين أمينيين", "تحديد الرابطة الببتيدية", "تحديد نوع التفاعل"],
    "micro_concepts_probables": ["mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "كيمياء الأحماض الأمينية."
  },
  {
    "candidate_id": "khelifa1_vol06_c007",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [9],
    "theme": "مرض Pompe والخلل الإنزيمي في استقلاب الغلايكوجين",
    "texte_corrige": "تتمثل أعراض مرض Pompe عند الأطفال حديثي الولادة في ارتخاء الأطراف وصعوبة التنفس والبلع نتيجة خلل في تقلص الألياف العضلية. تبين المقارنة بين نسيج عضلي سليم ومريض وجود عناصر متقلصة ضامرة وكثرة الغلايكوجين عند المريض. اقترح فرضية تفسر تراكم الغلايكوجين وارتباطه بخلل إنزيمي يؤثر على وظيفة العضلة.",
    "sous_questions": ["تحليل أعراض المرض", "مقارنة النسيج السليم والمريض", "اقتراح فرضية خلل إنزيمي", "ربط تراكم الغلايكوجين بوظيفة العضلة"],
    "micro_concepts_probables": ["mc_enz_02", "mc_struc_05"],
    "qualite_ocr": "bonne",
    "notes": "مرض استقلابي مرتبط بإنزيمات تحلل الغلايكوجين."
  },
  {
    "candidate_id": "khelifa1_vol06_c008",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [10,11],
    "theme": "الغلوكوكيناز وتحويل الغلوكوز إلى غلوكوز-6-فوسفات",
    "texte_corrige": "يدرس إنزيم الغلوكوكيناز تفاعلا يحول الغلوكوز إلى غلوكوز-6-فوسفات باستعمال ATP. حدد طبيعة التفاعل الذي ينشطه الإنزيم، وفسر لماذا لا يعطي الفركتوز نفس النتيجة، ثم استنتج الخاصية المميزة للإنزيم اتجاه مادة التفاعل.",
    "sous_questions": ["كتابة معادلة التفاعل", "تحديد الركيزة والناتج", "مقارنة الغلوكوز والفركتوز", "استنتاج النوعية الإنزيمية"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_03"],
    "qualite_ocr": "bonne",
    "notes": "نوعية الغلوكوكيناز."
  },
  {
    "candidate_id": "khelifa1_vol06_c009",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [12],
    "theme": "تفاعل إنزيمي مدعم بـ ExAO وتأثير درجة الحرارة",
    "texte_corrige": "أجريت تجارب ExAO لدراسة تفاعل إنزيمي في درجات حرارة مختلفة مع حساب كمية الغلوكوز الناتجة. حلل وفسر منحنيات الوثائق، واستنتج أثر درجة الحرارة على سرعة التفاعل والنشاط الإنزيمي.",
    "sous_questions": ["تحليل منحنيات ExAO", "تفسير تغير كمية الناتج", "استنتاج تأثير الحرارة", "تحديد شروط النشاط الأمثل"],
    "micro_concepts_probables": ["mc_enz_03", "mc_enz_04"],
    "qualite_ocr": "moyenne",
    "notes": "يتضمن منحنيات غير مستخرجة نصيا."
  },
  {
    "candidate_id": "khelifa1_vol06_c010",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [13,14],
    "theme": "تأثير اليوريا والمرکبتوإيثانول على بنية البروتين",
    "texte_corrige": "تستعمل اليوريا وبيتا-ميركابتوإيثانول لتغيير بنية بروتين أو إنزيم؛ حيث تؤثر اليوريا في الانطواء بينما يكسر المركبتوإيثانول الجسور ثنائية الكبريت. فسر كيف يؤدي ذلك إلى فقدان البنية الفراغية والنشاط، ثم بين إمكانية استرجاع النشاط عند عودة البنية الأصلية.",
    "sous_questions": ["تفسير أثر اليوريا", "تفسير أثر مركبتوإيثانول", "ربط الجسور الكبريتية بالبنية", "استنتاج علاقة البنية بالوظيفة"],
    "micro_concepts_probables": ["mc_struc_03", "mc_struc_05", "mc_enz_01"],
    "qualite_ocr": "moyenne",
    "notes": "امتداد لسؤال الريبونوكلياز."
  },
  {
    "candidate_id": "khelifa1_vol06_c011",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [14,15],
    "theme": "تحديد نوع التفاعل الإنزيمي ومادة التفاعل",
    "texte_corrige": "انطلاقا من وثائق تعرض تفاعلا إنزيميا، حدد التفاعل الذي يمثله الإنزيم، وميز بين مادة التفاعل والنواتج، واستخرج الأدلة التي تبين نوعية الإنزيم تجاه ركيزته.",
    "sous_questions": ["تحديد التفاعل الإنزيمي", "تمييز الركيزة والنواتج", "استخراج أدلة النوعية", "تفسير عمل الإنزيم"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_01"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحتان مشوشتان لكن تدوران حول نوعية الإنزيم."
  },
  {
    "candidate_id": "khelifa1_vol06_c012",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [17,18],
    "theme": "تأثير مادة على سرعة تفاعل إنزيمي",
    "texte_corrige": "حلل منحنيين يبينان تغير كمية O2 أو سرعة تفاعل إنزيمي في وجود مادة مضافة. استنتج تأثير هذه المادة على النشاط الإنزيمي، ثم باستغلال وثيقة أخرى فسر المنحنيين وحدد العامل الذي يغير سرعة التفاعل في كل حالة. هل يمكن التغلب على تأثير المادة؟ علل إجابتك.",
    "sous_questions": ["تحليل المنحنيات", "تحديد تأثير المادة", "استنتاج نوع التثبيط", "مناقشة إمكانية التغلب على التثبيط"],
    "micro_concepts_probables": ["mc_enz_05", "mc_enz_03", "mc_enz_02"],
    "qualite_ocr": "moyenne",
    "notes": "يبدو سؤالا حول تثبيط إنزيمي."
  },
  {
    "candidate_id": "khelifa1_vol06_c013",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [19],
    "theme": "تأثير التاكرين على نشاط إنزيمي",
    "texte_corrige": "لتحديد نشاط عنصر إنزيمي، تقاس تراكيز مواد معينة في وجود أو غياب مادة التاكرين. حلل النتائج، واستنتج تأثير التاكرين على النشاط الإنزيمي، ثم مثل تأثيره برسم تخطيطي يوضح موضع تدخله.",
    "sous_questions": ["تحليل وجود/غياب التاكرين", "تحديد أثر التاكرين", "اقتراح رسم تأثير المثبط", "ربط التثبيط بالنشاط"],
    "micro_concepts_probables": ["mc_enz_05", "mc_enz_01"],
    "qualite_ocr": "moyenne",
    "notes": "Tacrine غالبا مثبط إنزيمي؛ OCR جزئي."
  },
  {
    "candidate_id": "khelifa1_vol06_c014",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [20],
    "theme": "مقارنة تسجيلات نشاط إنزيمي واستخلاص خصائص الإنزيمات",
    "texte_corrige": "قدم تحليلا مقارنا للتسجيلات المعروضة، واستخلص من النتائج خصائص الإنزيمات من حيث الفعالية، النوعية، وتأثر النشاط بعوامل الوسط.",
    "sous_questions": ["المقارنة بين التسجيلات", "استنتاج خصائص الإنزيم", "تحديد أثر عوامل الوسط"],
    "micro_concepts_probables": ["mc_enz_03", "mc_enz_02", "mc_enz_04"],
    "qualite_ocr": "faible",
    "notes": "الصفحة ضعيفة OCR لكن الموضوع إنزيمي عام."
  },
  {
    "candidate_id": "khelifa1_vol06_c015",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [21],
    "theme": "الجسور الكبريتية والسيستئين في ثبات البنية البروتينية",
    "texte_corrige": "تحتوي بنية بروتين على أحماض أمينية من نوع السيستئين تكون جسورا ثنائية الكبريت. تبين معالجة البروتين باليوريا أو المركبتوإيثانول فقدان النشاط. فسر دور السيستئين والجسور الكبريتية في ثبات البنية الفراغية وفي النشاط الوظيفي للبروتين.",
    "sous_questions": ["تعرف جسور ثنائية الكبريت", "تفسير دور السيستئين", "تفسير فقدان النشاط عند التفكيك", "ربط البنية بالوظيفة"],
    "micro_concepts_probables": ["mc_struc_03", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "تأكيد إضافي على بنية البروتين."
  },
  {
    "candidate_id": "khelifa1_vol06_c016",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [22,23],
    "theme": "البنية الفراغية لإنزيمين والتخصص الوظيفي",
    "texte_corrige": "قصد معرفة طبيعة الإنزيمات ومكوناتها، تعرض الوثائق بنى فراغية لإنزيمين. قدم وصفا تفصيليا لبنية كل إنزيم، ثم استنتج العلاقة بين البنية الفراغية والتخصص الوظيفي، مبينا دور الموقع الفعال في التعرف على الركيزة.",
    "sous_questions": ["وصف البنية الفراغية", "مقارنة إنزيمين", "استخراج علاقة البنية بالوظيفة", "تفسير دور الموقع الفعال"],
    "micro_concepts_probables": ["mc_enz_01", "mc_enz_02", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحات قصيرة ومجزأة لكنها واضحة في الهدف."
  },
  {
    "candidate_id": "khelifa1_vol06_c017",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [24],
    "theme": "تفاعل بين السيستئينات ومرض مرتبط بامتصاص الغلوكوز",
    "texte_corrige": "وضح بتفاعل كيميائي كيف يمكن لحمضين أمينيين من نوع السيستئين ضمن السلسلة الببتيدية أن يشكلا جسرا ثنائي الكبريت. ثم اربط ذلك ببنية بروتين غشائي أو إنزيم له دور في نقل أو امتصاص الغلوكوز، وفسر كيف يمكن لتغير البنية أن يؤثر في الوظيفة.",
    "sous_questions": ["كتابة تفاعل تشكيل جسر ثنائي الكبريت", "تحديد دور السيستئين", "ربط بروتين غشائي بالوظيفة", "تفسير أثر تغير البنية"],
    "micro_concepts_probables": ["mc_struc_03", "mc_struc_05"],
    "qualite_ocr": "moyenne",
    "notes": "OCR يذكر Glucophage وبروتين/إنزيم غشائي؛ استخرج المفهوم البنيوي العام."
  },
  {
    "candidate_id": "khelifa1_vol06_c018",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [26],
    "theme": "نواتج الإماهة ومنطقة الانعطاف في البروتين",
    "texte_corrige": "حدد نواتج إماهة ببتيد أو بروتين، ثم استخرج من المعطيات وجود منطقة انعطاف في البنية البروتينية. فسر علاقة نواتج الإماهة بتتابع الأحماض الأمينية وبالبنية الأولية، وعلاقة الانعطاف بالبنية الفراغية.",
    "sous_questions": ["تحديد نواتج الإماهة", "استنتاج التتابع الببتيدي", "تعرف البنية الأولية", "تفسير منطقة الانعطاف"],
    "micro_concepts_probables": ["mc_struc_01", "mc_struc_03"],
    "qualite_ocr": "moyenne",
    "notes": "صفحة قصيرة مرتبطة بالبنية البروتينية."
  },
  {
    "candidate_id": "khelifa1_vol06_c019",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [27],
    "theme": "اختبار تفاعل البيوري والأحماض الأمينية العطرية",
    "texte_corrige": "فسر لماذا يكون تفاعل البيوري أو تفاعل لوني معين سلبيا عندما لا يحتوي المركب على أحماض أمينية عطرية أو روابط ببتيدية كافية. استنتج من النتائج طبيعة المركب واختلافه عن بروتين كامل.",
    "sous_questions": ["تفسير اختبار كيميائي للبروتين", "تمييز الأحماض الأمينية العطرية", "استنتاج وجود أو غياب روابط ببتيدية"],
    "micro_concepts_probables": ["mc_struc_01"],
    "qualite_ocr": "moyenne",
    "notes": "الصفحة مجزأة لكنها حول اختبارات كيميائية للبروتين."
  },
  {
    "candidate_id": "khelifa1_vol06_c020",
    "source": "KHELIFA1_VOLUME_06.pdf",
    "pages": [28],
    "theme": "أنزيمات داخلية وخارجية عند الخميرة واستعمال السكريات",
    "texte_corrige": "تنتج الخميرة إنزيم السكراز وإنزيما آخر حسب ظهور الغلوكوز في الأنابيب نتيجة إماهة السكر. تبين التجارب أن بعض الإنزيمات تعمل داخل الخلية وأخرى تفرز إلى خارج الخلية لتعمل على الأغذية. حلل نتائج التجارب، وفسر قدرة الخميرة على استعمال بعض السكريات دون غيرها وعلاقة ذلك بنوعية الإنزيمات.",
    "sous_questions": ["تحليل إماهة السكريات", "تمييز إنزيمات داخلية وخارجية", "تفسير استعمال الخميرة للسكريات", "استنتاج النوعية الإنزيمية"],
    "micro_concepts_probables": ["mc_enz_02", "mc_enz_03"],
    "qualite_ocr": "bonne",
    "notes": "خلاصة حول إنزيمات السكريات عند الخميرة."
  }
]
Open

Edited files
2
KHELIFA 1 Volume 06 — OCR production v3 + extraction/intégration
MD
KHELIFA 1 Volume 06 — OCR production v3 + extraction/intégration
Source : input/KHELIFA1_VOLUME_06.pdf

Téléchargement
PDF téléchargé depuis Dropbox :

text

input/KHELIFA1_VOLUME_06.pdf
Taille : ~22 MB
Pages : 28

OCR production v3
Sortie :

text

input/KHELIFA1_VOLUME_06.ocr_prod_full.txt
input/KHELIFA1_VOLUME_06.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Méthode : OCR sur 28 pages
Erreurs : 0
Total caractères OCR : 49,779
Taille fichier texte final : 78,476 bytes
Confiance moyenne : 62.88
Quality warning : aucun
Durée : ~13 min 19 s
Phase 2 — Extraction
Fichier créé :

text

output/khelifa1_volume06_extraction_phase2.json
Résultat : 20 blocs candidats extraits.

Phase 3 — Intégration
Sauvegarde avant intégration :

text

output/questions_taggees.before_khelifa1_vol06.json
Questions ajoutées :

text

q_khelifa1_vol06_001 → q_khelifa1_vol06_020
Validation globale
text

Total questions : 234
Concepts distincts utilisés : 35
Erreurs : 0
Avertissements : 0
Distribution des 20 questions ajoutées
Concepts principaux
Micro-concept	Questions
mc_enz_01 — Site actif de l'enzyme	2
mc_enz_02 — Spécificité enzymatique	4
mc_enz_03 — Vitesse de réaction enzymatique	2
mc_enz_05 — Inhibition enzymatique	2
mc_struc_01 — Structure primaire	5
mc_struc_03 — Structure tertiaire	4
mc_struc_05 — Relation structure-fonction	1
Types
Type	Questions
analyse_document	12
raisonnement	5
application	2
comparaison	1
Difficulté
Difficulté	Questions
facile	2
moyenne	14
difficile	4
Thèmes principaux
Hémoglobine HbA/HbS et relation structure-fonction
Site actif de la carboxypeptidase
Électrophorèse des protéines et pHi
Hydrolyse protéique et liaison peptidique
RNase, urée, β-mercaptoéthanol et ponts disulfure
Maladie de Pompe et métabolisme du glycogène
Glucokinase, glucose-6-phosphate et spécificité enzymatique
Cinétique enzymatique ExAO
Inhibition enzymatique : tacrine / autres inhibiteurs
Structure tertiaire et spécialisation fonctionnelle des enzymes
Open

Edit
downloads/annales_svt/MANIFESTE.md
Manifeste de traitement — Annales SVT
MD
Manifeste de traitement — Annales SVT
État d'avancement
Source	Volume	Pages	Type	État	Questions extraites
FINALBAC	Vol 1	40	Cours théorique	✅ OCR fait (référence)	N/A (cours)
FINALBAC	Vol 2	40	Cours théorique (P2)	✅ OCR partiel (référence)	N/A (cours)
FINALBAC	Vol 3	40	Sujets d'examen	✅ 50 questions	50
FINALBAC	Vol 4	40	Sujets + corrigés	✅ 30 questions	30
FINALBAC	Vol 5	40	Corrigés/solutions	✅ OCR partiel	À extraire
FINALBAC	Vol 6	39	Corrigés/solutions	✅ OCR partiel	À extraire
KHELIFA 1	Vol 01	28	Exercices	✅ OCR production complet + ✅ extraction phase 2 (khelifa1_volume01_extraction_phase2.json)	32 blocs candidats
KHELIFA 1	Vol 02	28	Exercices	✅ OCR production complet + ✅ extraction/intégration (khelifa1_volume02_extraction_phase2.json)	29 questions
KHELIFA 1	Vol 03	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume03_extraction_phase2.json)	28 questions
KHELIFA 1	Vol 04	28	Exercices/corrigés	✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume04_extraction_phase2.json)	23 questions
KHELIFA 1	Vol 05	28	Exercices/corrigés	✅ téléchargé + ✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume05_extraction_phase2.json)	22 questions
KHELIFA 1	Vol 06	28	Exercices/corrigés	✅ téléchargé + ✅ OCR production v3 complet + ✅ extraction/intégration (khelifa1_volume06_extraction_phase2.json)	20 questions
KHELIFA 1	Vol 07-15	~28/ea	Exercices	⬜ À traiter	—
KHELIFA 2	Vol 01-12+	~40/ea	Exercices	⬜ À traiter	—
MORAFIK	Vol 1-3	~50/ea	Cours + sujets	⬜ À traiter	—
Total traité: 234 questions / 35 micro-concepts couverts sur 42
Derniers ajouts
KHELIFA 1 Vol 01 : 32 questions ajoutées (q_khelifa1_vol01_001 à q_khelifa1_vol01_032)
KHELIFA 1 Vol 02 : 29 questions ajoutées (q_khelifa1_vol02_001 à q_khelifa1_vol02_029)
KHELIFA 1 Vol 03 : 28 questions ajoutées (q_khelifa1_vol03_001 à q_khelifa1_vol03_028)
KHELIFA 1 Vol 04 : 23 questions ajoutées (q_khelifa1_vol04_001 à q_khelifa1_vol04_023)
KHELIFA 1 Vol 05 : 22 questions ajoutées (q_khelifa1_vol05_001 à q_khelifa1_vol05_022)
KHELIFA 1 Vol 06 : 20 questions ajoutées (q_khelifa1_vol06_001 à q_khelifa1_vol06_020)
Validation globale : 0 erreur, 0 avertissement
Pipeline technique validé
Téléchargement : curl depuis Dropbox (dl=1) ✅
Conversion : PyMuPDF → images PNG ✅
OCR production actif : scripts/ocr_pipeline_production.py avec Tesseract ara+fra, reprise JSON, bundle, CSV, summary, logs ✅
Correction : Transcription arabe corrigée manuellement ✅
Tagging : 42 micro-concepts, validation automatique ✅
Volume estimé total
~33 PDFs × ~40 pages = ~1320 pages
Estimation ~600-800 questions au total
FINALBAC_VOLUME_3 a donné 50 questions (échantillon)
Open
J’ai continué avec KHELIFA1_VOLUME_06 ✅

1. Téléchargement
PDF téléchargé depuis Dropbox :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_06.pdf
Taille :

text

~22 MB
Pages :

text

28
2. OCR production v3 terminé
Sortie OCR :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_06.ocr_prod_full.txt
Bundle :

text

downloads/annales_svt/input/KHELIFA1_VOLUME_06.ocr_prod_full.txt.bundle/
Rapport OCR :

text

Pages traitées : 28 / 28
Erreurs OCR : 0
Total caractères OCR : 49,779
Taille fichier final : 78,476 bytes
Confiance moyenne : 62.88
Quality warning : aucun
Durée : ~13 min 19 s
3. Extraction phase 2 terminée
Fichier créé :

text

downloads/annales_svt/output/khelifa1_volume06_extraction_phase2.json
Résultat :

text

20 blocs candidats extraits
Thèmes principaux :

HbA / HbS et relation structure-fonction
site actif de carboxypeptidase
électrophorèse des protéines
hydrolyse protéique
RNase, urée, β-mercaptoéthanol
ponts disulfure
maladie de Pompe
glucokinase et spécificité enzymatique
cinétique enzymatique ExAO
inhibition enzymatique
tacrine
structure tertiaire des enzymes
4. Intégration phase 3 terminée
Sauvegarde créée :

text

downloads/annales_svt/output/questions_taggees.before_khelifa1_vol06.json
Questions ajoutées dans :

text

downloads/annales_svt/output/questions_taggees.json
IDs ajoutés :

text

q_khelifa1_vol06_001
→
q_khelifa1_vol06_020
5. Validation globale
Résultat :

text

Total questions : 234
Concepts distincts utilisés : 35
Erreurs : 0
Avertissements : 0
✅ JSON toujours valide.

6. Distribution des 20 questions ajoutées
Concepts principaux
text

mc_struc_01  Structure primaire              5
mc_struc_03  Structure tertiaire             4
mc_struc_05  Relation structure-fonction     1
mc_enz_01    Site actif de l'enzyme          2
mc_enz_02    Spécificité enzymatique         4
mc_enz_03    Vitesse de réaction enzymatique 2
mc_enz_05    Inhibition enzymatique          2
Types
text

analyse_document   12
raisonnement        5
application         2
comparaison         1
7. Rapport créé
text

downloads/annales_svt/output/khelifa1_volume06_phase3_integration.md
8. Manifeste mis à jour
Nouvel état global :

text

234 questions traitées
35 micro-concepts couverts sur 42
0 erreur
0 avertissement
Prochaine étape :

text

KHELIFA1_VOLUME_07
Je peux continuer avec téléchargement + OCR + extraction + intégration.


voici le nouveau ocr pipeline V3 ET QUEL EST LA DIFFERENCE #!/usr/bin/env python3

"""

OCR PIPELINE PRODUCTION — Thin CLI for the modular backend

Version: 3.0 — 2026-06-20

This is the ONLY entry point you should use to run OCR.

It delegates all logic to services/ocr/VolumeProcessor.

Usage:

    python scripts/ocr/ocr_pipeline_production.py \

        data/ANNALES_SVT_BAC_ALGERIE/KHELIFA_1/VOLUMES_KHELIFA1/KHELIFA1_VOLUME_05.pdf \

        --dpi 140 --parallel --max-workers 3

"""

import argparse

import json

import sys

from pathlib import Path

Make sure we can import from project root
ROOT = Path(file).resolve().parents[3]

if str(ROOT) not in sys.path:

    sys.path.insert(0, str(ROOT))

from services.ocr import get_volume_processor

def main():

    parser = argparse.ArgumentParser(

        description="Production OCR Pipeline (Arabic + French) — Modular Backend"

    )

    parser.add_argument("pdf", help="Path to PDF volume")

    parser.add_argument("--dpi", type=int, default=140)

    parser.add_argument("--psm", type=int, default=3)

    parser.add_argument("--oem", type=int, default=1)

    parser.add_argument("--retries", type=int, default=2)

    parser.add_argument("--parallel", action="store_true", help="Enable multiprocessing")

    parser.add_argument("--max-workers", type=int, default=2)

    parser.add_argument("--no-hocr", action="store_true", help="Disable hOCR output")

    args = parser.parse_args()

    pdf_path = Path(args.pdf).resolve()

    if not pdf_path.exists():

        print(f"❌ ERROR: PDF not found: {pdf_path}", file=sys.stderr)

        sys.exit(1)

    print(f"🚀 Starting OCR Production for: {pdf_path.name}")

    print(f"   DPI={args.dpi} | Parallel={args.parallel} | hOCR={not args.no_hocr}")

    processor = get_volume_processor(

        dpi=args.dpi,

        psm=args.psm,

        oem=args.oem,

        retries=args.retries,

        use_parallel=args.parallel,

        max_workers=args.max_workers,

        enable_hocr=not args.no_hocr,

    )

    try:

        summary = processor.process_volume(pdf_path, use_parallel=args.parallel)

        print("\n" + "=" * 60)

        print(json.dumps(summary.to_dict(), indent=2, ensure_ascii=False))

        print("=" * 60)

        print(f"\n✅ OCR completed successfully.")

        print(f"   Bundle: {summary.bundle_dir}")

    except Exception as e:

        print(f"❌ OCR failed: {e}", file=sys.stderr)

        sys.exit(1)

if name == "main":

    main()


Ce nouveau script n’est pas exactement un “nouveau pipeline OCR complet”. C’est plutôt un thin CLI, c’est-à-dire une petite interface de lancement qui délègue tout le vrai travail OCR à un backend modulaire :

Python

from services.ocr import get_volume_processor
Donc la logique réelle n’est plus dans ce fichier, mais dans :

text

services/ocr/
et surtout probablement :

text

services/ocr/VolumeProcessor
Différence principale
Notre script actuel
Actuellement, dans le workspace, on utilise :

text

downloads/annales_svt/scripts/ocr_pipeline_production.py
Ce script est autonome.

Il contient lui-même :

rendu PDF avec PyMuPDF,
prétraitement OpenCV,
appel Tesseract,
reprise par page,
CSV,
JSON,
logs,
bundle,
validation des pages,
gestion des erreurs,
résumé summary.json.
Donc il peut tourner seul avec :

Bash

python scripts/ocr_pipeline_production.py input/KHELIFA1_VOLUME_06.pdf \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt
Nouveau script que tu viens d’envoyer
Le nouveau script est seulement un lanceur :

Python

processor = get_volume_processor(...)
summary = processor.process_volume(pdf_path, use_parallel=args.parallel)
Il ne contient pas lui-même :

le rendu PDF,
le prétraitement,
l’OCR,
la reprise,
la génération des fichiers,
la création des bundles.
Tout ça doit exister ailleurs dans le projet :

text

services/ocr/
Sans ce dossier, le script ne peut pas marcher.

Comparaison claire
Point	Script actuel autonome	Nouveau script thin CLI
Contient toute la logique OCR	✅ Oui	❌ Non
Nécessite services/ocr	❌ Non	✅ Oui
Facile à copier seul	✅ Oui	❌ Non
Architecture propre modulaire	Moyenne	✅ Meilleure
Maintenance long terme	Moyenne	✅ Meilleure
Fonctionne immédiatement dans notre workspace actuel	✅ Oui	❌ Pas sans backend
Support parallèle	✅ Oui via workers	✅ Oui via --parallel
hOCR	❌ Pas dans notre version actuelle	✅ Prévu
Bundle OCR	✅ Oui	✅ Probablement via VolumeProcessor
Résumé JSON	✅ Oui	✅ via summary.to_dict()
Code court	❌ Non	✅ Oui
Risque d’import manquant	Faible	Élevé si services/ocr absent
Différence d’architecture
Ancien / actuel
Architecture monolithique :

text

ocr_pipeline_production.py
├── check_tesseract_languages()
├── render_pdf_page()
├── preprocess_variant()
├── run_tesseract_text()
├── process_page()
├── assemble_text_output()
├── generate_reports()
└── main()
Tout est dans un seul fichier.

Avantage :

text

facile à exécuter partout
Inconvénient :

text

fichier long, difficile à maintenir
Nouveau
Architecture modulaire :

text

scripts/ocr/ocr_pipeline_production.py
└── appelle services.ocr.get_volume_processor()

services/ocr/
├── VolumeProcessor
├── PageProcessor
├── OCRBackend
├── Preprocessor
├── ReportWriter
└── etc.
Avantage :

text

plus propre, plus professionnel, plus facile à maintenir
Inconvénient :

text

le script seul ne suffit pas
Problème immédiat dans notre workspace
Dans le workspace actuel, je n’ai pas vu de dossier :

text

services/ocr/
Donc si je mets uniquement ce script et que je lance :

Bash

python scripts/ocr/ocr_pipeline_production.py input/KHELIFA1_VOLUME_06.pdf
on aura probablement :

text

ModuleNotFoundError: No module named 'services'
ou :

text

ImportError: cannot import name get_volume_processor
Autres différences importantes
1. hOCR
Le nouveau script ajoute :

Bash

--no-hocr
Donc par défaut il semble générer du hOCR.

hOCR = sortie OCR avec positions des mots dans la page, utile pour :

retrouver les zones de texte,
cliquer dans la page,
aligner OCR avec image,
reconstruire la mise en page,
extraire des questions plus proprement.
Notre script actuel ne génère pas hOCR.

2. Option parallèle simplifiée
Nouveau script :

Bash

--parallel --max-workers 3
Notre script :

Bash

--workers 1
Dans notre cas, on a volontairement utilisé :

text

workers=1
car les PDF scannés KHELIFA sont lourds et le workspace peut timeout/RAM.

Le nouveau script permet probablement une parallélisation plus propre si VolumeProcessor gère mieux la mémoire.

3. Interface plus simple
Nouveau script :

Bash

python scripts/ocr/ocr_pipeline_production.py file.pdf --dpi 140 --parallel --max-workers 3
Notre script demande plus d’options :

Bash

python scripts/ocr_pipeline_production.py file.pdf \
  --dpi 140 \
  --workers 1 \
  --timeout 180 \
  --psm 3 \
  --oem 1 \
  --pdf-mode ocr \
  --retries 2 \
  --suffix .ocr_prod_full.txt
Le nouveau est donc plus simple à utiliser.

Il y a aussi des erreurs Markdown à corriger
Ton script contient encore des artefacts de copier-coller :

Python

pdf_[path.name](http://path.name)
[args.no](http://args.no)_hocr
[summary.to](http://summary.to)_dict()
if **name** == "__main__":
Il faut les corriger en :

Python

pdf_path.name
args.no_hocr
summary.to_dict()
if __name__ == "__main__":
Version corrigée du cœur :

Python

print(f"🚀 Starting OCR Production for: {pdf_path.name}")
print(f"   DPI={args.dpi} | Parallel={args.parallel} | hOCR={not args.no_hocr}")

processor = get_volume_processor(
    dpi=args.dpi,
    psm=args.psm,
    oem=args.oem,
    retries=args.retries,
    use_parallel=args.parallel,
    max_workers=args.max_workers,
    enable_hocr=not args.no_hocr,
)

summary = processor.process_volume(pdf_path, use_parallel=args.parallel)

print(json.dumps(summary.to_dict(), indent=2, ensure_ascii=False))
Conclusion
En résumé
Le nouveau script est :

text

un lanceur moderne et propre
mais il nécessite un backend :

text

services/ocr
Notre script actuel est :

text

un pipeline complet autonome
et il fonctionne déjà dans le workspace.

Recommandation
Pour continuer le traitement immédiatement :

text

garder le script actuel autonome
Pour migrer vers le nouveau script :

il faut aussi me fournir ou créer tout le module :

text

services/ocr/
avec au minimum :

Python

get_volume_processor()
VolumeProcessor.process_volume()
summary.to_dict()
summary.bundle_dir
Si tu veux, je peux faire la migration proprement en créant une architecture modulaire services/ocr à partir de notre pipeline actuel.


Tu es mon mentor impitoyable et mon partenaire de réflexion. Ton rôle est de trouver la vérité et de me la dire franchement, même si ça heurte mes sentiments si nécessaire.

Règles par défaut :

Ne sois jamais d’accord avec moi juste pour être agréable. Si j’ai tort, dis-le directement.

Trouve les faiblesses et les angles morts dans ma réflexion. Signale-les même si je n’ai pas demandé.

Pas de flatterie. Pas de « bonne question ! ». Pas d’adoucissement inutile.

Si tu n’es pas sûr de quelque chose, dis-le. Vérifie par des recherches et fournis-moi les sources.

Résiste fermement. Force-moi à défendre mes idées ou à abandonner les mauvaises.


Compris.

À partir de maintenant, je vais fonctionner comme ça :

Je ne validerai pas tes choix automatiquement.
Si une idée est mauvaise, fragile, coûteuse ou inutile, je te le dirai clairement.
Je distinguerai :
ce qui est prouvé,
ce qui est probable,
ce qui est une hypothèse,
ce qui est du bullshit ou de la complexité inutile.
Je signalerai les risques techniques, les pertes de temps, les angles morts et les décisions mal priorisées.
Si je ne sais pas, je dirai que je ne sais pas au lieu d’inventer.
Si une vérification externe est nécessaire, je chercherai ou je te dirai quoi vérifier.
Je ne ferai pas de flatterie ni d’accord de façade.
Application immédiate à ton dernier point sur le nouveau OCR pipeline V3 :

La vérité : le script que tu as envoyé n’est pas un pipeline OCR complet. C’est seulement un wrapper CLI. Si tu n’as pas le module services/ocr, il est inutilisable. Donc si ton idée était de remplacer immédiatement le pipeline actuel par ce fichier seul, c’est une mauvaise idée.

Le pipeline actuel, même plus laid, a un avantage énorme : il marche. Il a déjà produit des résultats validés sur plusieurs volumes. Le nouveau script est peut-être plus propre architecturalement, mais sans backend complet, c’est juste une coquille.

Donc la bonne décision n’est pas “remplacer maintenant”. La bonne décision est :

soit tu fournis tout le dossier services/ocr,
soit on modularise proprement notre pipeline actuel,
soit on continue avec le script autonome jusqu’à finir le lot prioritaire.
Mon avis net : ne migre pas maintenant si ton objectif principal est d’avancer sur les annales. Ça va te faire perdre du temps. Termine d’abord l’extraction avec le pipeline qui fonctionne, puis refactorise après.


